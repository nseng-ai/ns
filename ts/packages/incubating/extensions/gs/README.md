# @nseng-ai/gs

Incubating ns extension package for read-only inspection of local gh-stack provider state.

The initial package scaffold does not register an `ns` command. The local inventory command and its
complete usage documentation land in later stack changes.
