# @nseng-ai/pi-ns-herdr

**Herdr Pi adapter**:
The Pi-owned registration, interaction adaptation, launch-profile resolution, and Pi process-command construction over the harness-independent Herdr extension API.
*Avoid*: Herdr destination owner, Herdr Domain Core, forwarding adapter.

**Optional Handoffs adapter composition**:
The guarded `@nseng-ai/pi-ns-herdr` dependency on the curated `@nseng-ai/pi-ns-handoffs/create-flow` subpath that adds only `/ns:herdr:tab:handoff` when installed.
*Avoid*: private deep import, reverse dependency, broad failure suppression.
