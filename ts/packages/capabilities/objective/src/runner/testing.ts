// ADR0024-LEGACY-DELETE(whole file): this testing surface only re-exports the
// legacy fake child-session gateway; delete the file with it.
export { FakeChildSessionGateway, type FakeChildSessionScript } from "./fake-child-session.ts";
