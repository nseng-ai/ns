{
  "name": "asdl-ts-workspace",
  "private": true,
  "type": "module",
  "workspaces": ["packages/*"],
  "scripts": {
    "test": "bun run --workspaces --if-present test",
    "check": "bun run --workspaces --if-present check",
    "asdl-dev": "bun run packages/asdl-dev/src/cli.ts"
  },
  "devDependencies": {
    "@types/bun": "^1.3.0",
    "@types/node": "^24.0.0",
    "typescript": "^5.9.0"
  }
}
