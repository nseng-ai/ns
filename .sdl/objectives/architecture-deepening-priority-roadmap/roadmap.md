# Roadmap

## Work

- [x] **Fix `asdl-slots` checkout planning-time mutation.** Make current-checkout planning pure and move redirect mutation into lifecycle execution after preflight. Evidence should show that failed post-preflight planning cannot leave the caller's worktree redirected.
      Evidence: `slot checkout --current` planning no longer executes checkout/detach redirects before pool/occupancy preflight; targeted checkout planning and checkout CLI scenario tests passed, and the full `just` gate passed.
- [x] **Deepen `asdl-slots` release/free/gc workflow.** Introduce a release-focused module such as `SlotReleaseWorkflow` that owns free/gc target classification, dry-run/execute flow, cleanup policy, dirty/operation checks, PR lookup handling, cleanup result accounting, and partial failure behavior while keeping CLI confirmation/rendering outside the lifecycle interface.
      Evidence: `asdl_slots.lifecycle.release` now owns free/gc release previews and execution orchestration, including cleanup preview/execution accounting and GC execute-time rechecks. `slot free` and `slot gc` now map lifecycle release reports into existing CLI results while retaining selector, confirmation, rendering, JSON, and Clinkr responsibilities. Focused lifecycle tests, affected free/gc CLI scenario tests, full `asdl-slots` tests, and the full `just` gate passed.
- [x] **Localize `asdl-core` production gateway construction.** Added `asdl_core.git.construction` as the shared Git construction surface for repo-root and trunk probing, migrated mandatory-Git consumers (`asdl-objectives`, `brmem`, `asdl-handoff`, `asdl-slots`, and roaster's trunk resolver import), and removed the slots-local duplicate trunk-bound Git builder. Added `asdl_core.git.construction.build_git_gateway` as the shared lazy Git gateway builder for command-specific contexts, then migrated the remaining direct production `RealGitGateway()` constructions in `asdl-slots`, `asdl-pr-address`, and `aretro`. Added `asdl_core.gh.construction.build_pr_gateway` as the shared PRGateway production construction surface and migrated current production PRGateway context builders in `asdl-pr-address`, `asdl-slots`, and `roaster` while preserving lazy `gh` behavior. Added `asdl_core.gt.construction.build_gt_gateway` as the shared Graphite gateway production construction surface and migrated the explicit `slot gt` context while keeping generic contexts Graphite-free. Evidence: fresh direct-construction inventory, focused impacted package tests, and full `just` pass.
- [x] **Deepen `asdl-objectives` checked-in Markdown storage.** Create an internal storage module for Objective roots, record facts, file reads, update listing, active/archive moves, and relative paths used by git-touch checks. Preserve checked-in Markdown storage and avoid reintroducing Branch Memory as the storage model.
      Evidence: `asdl_objectives.objective_storage` now owns checked-in Markdown roots, slug/path helpers, checkout inventory, file/update facts, raw Markdown reads, and active/archive move paths; list, branch attribution, archive, and hidden exec commands consume that storage surface directly. Added focused storage unit coverage, ran affected CLI/list tests and full `asdl-objectives` tests, and the full `just` gate passed.
- [x] **Add domain output converters/readers for `asdl-core` real adapters.** Extract domain-specific conversion locality for Git output, GitHub responses, and Graphite metadata where it reduces raw subprocess/string-shape coupling in tests. Avoid a generic parser dumping ground.
      Evidence: Git subprocess output conversion now lives in `asdl_core.git.output_conversion`, and `RealGitGateway` imports those converters while retaining subprocess execution, failure handling, and worktree admin-state behavior. GitHub response mapping now lives in `asdl_core.gh.response_mapping`, and `real_gateway_helpers` delegates JSON/REST/GraphQL payload-to-domain conversion while retaining subprocess execution, command construction, repo resolution, and gateway failure mapping. Graphite SQLite metadata-store reading and stack graph conversion now live in `asdl_core.gt.metadata_reader`, and `RealGtGateway` retains subprocess/git probing responsibilities while delegating metadata reading. Parser/reader tests live in `packages/asdl-core/tests/unit/test_git_output_conversion.py`, `packages/asdl-core/tests/unit/test_gh_response_mapping.py`, and `packages/asdl-core/tests/unit/test_gt_metadata_reader.py`; real gateway tests retain command/failure and adapter wiring coverage. Focused Graphite reader/gateway tests and full `asdl-core` tests passed for the Graphite disposition, completing the row.
- [x] **Deepen `asdl-pr-address` feedback snapshot and prepare-run policy.** Move package-specific feedback fetching, empty-review filtering, resolved-thread policy, contested-thread normalization, and prepare-run setup behind an in-process interface so scenario tests can shrink to CLI contract coverage.
      Evidence: `feedback_snapshot.py` owns shared PR review/thread/comment fetch and filtering policy for `get-feedback`, `summarize-feedback`, and the prepare-run workflow. `prepare_run_workflow.py` now owns prepare-run branch/PR lookup outcomes, feedback snapshot reuse, contested-thread reopening, thread normalization, restructured-file detection, and warning accumulation over `FakePRGateway` / `FakeGitGateway`, while `prepare_run.py` remains the Clinkr/payload adapter. Focused workflow tests, prepare-run scenario coverage, and full `asdl-pr-address` tests passed; full repository validation reaches an unrelated TypeScript `ccc` failure.

## Parked

- [ ] **Park `roaster` inline findings publication.** The publication module would improve locality, but the package audit found `roaster` mostly healthy with strong existing seams. Defer this in favor of finishing the higher-leverage shared `asdl-core` adapter work and the active `asdl-pr-address` feedback workflow row.
- [ ] **Park `areg` init planning and managed-block locality.** This remains a useful internal locality refactor, but it is bootstrap-package cleanup rather than the selected remaining architecture-deepening focus. Revisit only if `areg init` behavior is changing for product reasons.
- [ ] **Park `vibechk` run-store/Git seam reshape.** The audit's proposed collapse/deepen work is valuable, but `vibechk` is standalone and lower cross-package leverage than the selected `asdl-core` and `asdl-pr-address` rows.
- [ ] **Park `packagechk` claim orchestration.** The duplicated PyPI/npm claim flow is the main package-local opportunity, but existing gateway seams are healthy and the refactor can wait until claim behavior evolves again.
- [ ] **Leave `brmem` broad architecture intact.** The audit treats `brmem` as a negative control: `BranchMemoryGateway` is already a real seam with real and fake adapters. Only small implementation-local cleanup, such as snapshot-tree plumbing extraction, should be considered opportunistically.
- [ ] **Defer `asdl-handoff` cleanup.** Handoff slug/key and branch-resolver cleanup is useful but low-leverage compared with the top ten projects.
- [ ] **Defer `aretro` cleanup.** `gateway_access.py` inlining and compact DTO conversion cleanup are worthwhile but not architecture priorities; preserve the evidence-vs-judgment boundary.
- [ ] **Defer broad `clinkr` authoring-surface redesign.** The current seam is deep and reused; revisit only when a second non-CLI adapter or a clearer interface-width problem justifies it.
