# Stack Execution Policy Rebaseline

## Summary

The Objective was rebaselined to support a direct `objective-stack-impl` run for the `context-profiler` reference extraction. The durable record now includes a `## Definition of Progress`, a `## Runner Policy`, and a more explicit roadmap decomposition for the extraction stack.

The executable default stack is: first neutralize reverse-import helper seams, then extract `context-profiler` into the provisional `ts/packages/pi-tools/context-profiler/` package as `@sdl/pi-context-profiler`, then record the proven package recipe or corrected disposition.

## Objective Impact

This update turns the next roadmap row from a high-level extraction target into bounded execution guidance while preserving the Objective's non-goals: `@sdl/pi` must not import extracted Pi-tool packages, capability mirror work must not be folded into Pi-tool extraction, and PR submission remains outside the runner's default authority.

Key de-risking decisions recorded in durable prose:

- Treat neutral display-width/scroll helpers and LM JSON parsing as the first branch because they are concrete reverse-import blockers and can be validated before moving the feature package.
- Treat `ts/packages/pi-tools/context-profiler/` and `@sdl/pi-context-profiler` as the provisional reference package location/name for the first extraction, with permission to record a safer disposition if implementation evidence disproves it.
- Require an acyclic parity-registration seam: live command registration and parity metadata must remain tested without making `@sdl/pi` import the extracted package.
- Authorize local repository edits, validation, Objective updates, and Graphite branch operations after the normal `objective-stack-impl` preview/confirmation, while excluding PR submission, external write-capable actions, Branch Memory stack ledgers, and capability-mirror thinning from this stack.

## Follow-Ups

- Run `objective-stack-impl` for `pi-host-decomposition` from a clean worktree and preview the three-branch default stack before dispatching runner subagents.
- After each branch, record Objective evidence only when a real seam is removed, the package is extracted/dispositioned, or the reference recipe is proven.
- Stop and update the Objective rather than pushing through if package placement, parity registration, or dependency direction evidence contradicts the provisional plan.
