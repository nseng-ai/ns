# Seed edge live: the motivating drift case is now machine-checked

## Summary

The seed roadmap row is complete on local branch `objective-edges-seed-records` (verified
runner-step commit; exactly the two sanctioned `objective.md` files changed, 13 insertions).
`checkout-free-sdl-distribution` and `ship-objectives-to-customers` now carry mirrored Record
Frontmatter encoding their edge with perspective-correct annotations — the distribution side
notes it was split out (2026-07-01) and is consumed as a hard dependency by its first external
consumer; the shipping side notes checkout-free npm distribution must land before objectives
ship externally — plus a Blocked Sentence on `ship-objectives-to-customers` gating first
external publish on that dependency landing. Both per-slug checks and the 120-record
`sdl objective check --all` sweep pass; `sdl objective list` renders EDGES 1 on both records
and `⊘ open (blocked)` on `ship-objectives-to-customers`.

## Objective Impact

The feature's acceptance test — its own motivating case — is met: the relationship that
previously existed only as two drift-prone prose sentences is now structural, mirrored, and
CI-enforced. This also delivers the two pieces of evidence earlier rows deferred: the sweep
passing on a checkout with live seeded edges (linter row) and live list rendering for the two
named records (list-rendering row). Only the vocabulary row remains open.

## Follow-Ups

- none beyond the open vocabulary row.
