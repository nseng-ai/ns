# Skills own edge mutation and blocked judgment; no exec helper needed

## Summary

The skill-updates roadmap row is complete on local branch `objective-edges-skill-updates`
(verified runner-step commit, stacked on the list-rendering branch). The umbrella `objective`
skill gained a shared Record Frontmatter grounding section; objective-create documents initial
edge declaration as the sanctioned mirrored counterpart edit; objective-update documents edge
mutation as mirrored two-file edits and re-judges the record's own Blocked Sentence on every
update; objective-close re-judges each edge counterpart's Blocked Sentence at close ("you are
closing the thing their annotation says gates them"), normally removes the closing record's own
sentence, and never removes edges. Every flow verifies frontmatter edits with
`sdl objective check` (per-slug or `--all`). Validation: dprint, the 120-record sweep, and
`areg-check` all pass.

## Objective Impact

Fifth roadmap row done. Decision: no hidden `sdl objective exec` helper was added — the
per-slug check already provides the deterministic mirror-lookup assist, so prose plus the
linter suffices; the parked public-mutation-CLI row remains the escalation path if agents
fumble the two-file edit. The "No YAML/frontmatter" non-goal prose in the umbrella skill,
create's do-not rule, update's stop/ask trigger, and two execution-policy reference lines now
carry the exactly-blocked-and-edges exception; every other non-goal is intact. Remaining rows:
seed, vocabulary.

## Follow-Ups

- `skills/objective-next/references/confirmed-execution.md` still carries the unqualified
  "Do not add YAML/frontmatter" wording; objective-next is outside this row's three skills —
  add the same blocked+edges clarifier opportunistically (for example with the vocabulary row).
