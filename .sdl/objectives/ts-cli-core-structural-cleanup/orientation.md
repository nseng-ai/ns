**Direction: repeated CLI/core concepts are consolidated into the right shared layers; god-files are split after ownership is reclassified.**

Getting to: neutral duplicated mechanics live once in `@sdl/core`/`@sdl/graphite`/`@sdl/brmem`, while capability-domain policy stays in its capability package/API. Completed history includes `defineCli`, branch-context BrmemGateway migration, `runBrmem`, and brmem/core GitGateway composition.

What you see now — do not copy: stale original-review paths/counts, remaining neutral tool/kernel cleanup rows, and Flow-owned land-stack maintenance still needing decomposition. Completed neutral slices include the `@sdl/core` root-export deletion, roaster/core GitHub helper sharing, and packagechk claim-command simplification. The backlog has been reclassified: implement only rows marked neutral structural cleanup in this Objective; route capability-owned rows to their owning capability context.

Avoid: copying CLI boilerplate into a new command; promoting SDK/core surface for convenience alone; moving capability-domain logic below the SDK just to dedup (ADR 0009 layering guardrail).

Active slice: see this objective's roadmap.md.
