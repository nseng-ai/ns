# Roadmap

## Work

> **Reclassification note:** remaining rows were rebaselined after the extension-architecture endgame advanced. Rows marked **Classification: neutral structural cleanup** remain eligible for future local cleanup slices in this Objective after a narrow pickup re-check. Rows marked **capability-owned** should be routed to the owning capability/Objective or handled as package-local capability maintenance, not pulled down into neutral infra merely for dedup. Rows marked **obsolete / disposed** are retained as historical findings but are not implementation candidates here.

- [x] Shared `defineCli` helper in `@sdl/core/cli-entry` (runtimeInfo derivation, version reading, IO/cwd/env defaulting, `import.meta.main` entry guard); migrate all current `cli.ts` files onto it. See `references/cli-wiring-layer.md`.
      Evidence: `defineCli` exists in `ts/packages/infra/core/src/cli-entry.ts`; current `ts/packages/**/src/cli.ts` entrypoints consume it; old hand-written `const VERSION`/`runtimeInfo`/`readPackageVersion` implementations are absent from those entrypoints; package-local `sdlcc` `--runtime` coverage exists; prior validation passed.
- [x] Rejected: `clinkr` `execGroup(description?)` factory defaulting `name:"exec"` + `isHidden:true`; replace hand-rolled hidden-exec constructions. See `references/cli-wiring-layer.md`.
      Decision: the implementation was reverted because it added a thin wrapper around already-correct, obvious `ClinkrGroup` construction and did not delete meaningful complexity. Do not reintroduce this helper without new evidence that it prevents plausible drift or removes substantial mental load.
- [x] Unify Branch-Memory access: point `branch-context` at the in-process `@sdl/brmem` `BrmemGateway`; delete the parsing half of its old `brmem-gateway.ts` and its `@sdl/core/brmem-cli` dependency. See `references/branch-memory-access.md`.
      Evidence: `branch-context` uses `RealGitBrmemGateway` / `BrmemGateway`; `ts/packages/branch-context/src/brmem-gateway.ts` is absent; no `@sdl/core/brmem-cli` references remain under `branch-context`; the dry-run preview avoids promising a `brmem put` subprocess; CLI scenario tests cover Branch Memory diagnostics.
- [x] Collapse the `@sdl/core/brmem-cli` multi-candidate framework to one public `runBrmem`; fix the duplicated candidate-loop at worktree status; delete dead exports `graphqlErrorsFromJson`, `readOptionalBrmemBooleanField`. See `references/branch-memory-access.md` and `references/asdl-core.md`.
      Evidence: `@sdl/core/brmem-cli` exports `runBrmem`; the former public candidate-iteration names (`resolveBrmemCommandCandidates`, `runBrmemCandidate`, `runFirstAvailableBrmemCommand`) plus `readOptionalBrmemBooleanField` and `graphqlErrorsFromJson` grep to zero under `ts/packages`; the two-candidate fallback behavior lives privately inside `runBrmem` and is covered by prior tests.
- [x] Compose core `GitGateway` inside `brmem/real-git-gateway.ts`; remove duplicated generic `currentBranch` and Git branch-ref validation primitives while keeping Branch Memory Snapshot object/ref plumbing local to brmem. See `references/cross-package-dedup.md`.
      Evidence: `RealGitBrmemGateway` takes shared `commands` and `git` seams, delegates generic branch-ref checks to `GitGateway`, retains brmem's Snapshot Ref encoding pre-check, and branch-context validates target branches through core Git before plan-file I/O or mutation.
- [x] Disposed as obsolete: add `resolveBranchOrCurrent` to `@sdl/core` and replace per-package copies.
      Classification: obsolete / disposed. Current re-probe found no `resolveBranchOrCurrent` symbol and no clear repeated policy-free helper shape to standardize. `@sdl/brmem` already centralizes its `request.branch ?? current branch` operation path, while handoff, branch-context, address, slot, flow, and Pi presentation code have distinct error/reporting semantics. Do not add this helper unless a future fresh count finds real duplicated neutral Git mechanics.
- [x] Export or otherwise share GitHub PR feedback leaf helpers (`ghAuthorSchema` / author normalization / numeric GitHub identity policy) from the current core feedback boundary; delete roaster's divergent copies if the helper is neutral rather than roaster-domain-specific. See `references/cross-package-dedup.md`.
      Classification: neutral structural cleanup, with a narrow ADR 0016 boundary. Evidence: `@sdl/core/github-pr-feedback` exports the narrow leaf helpers `ghAuthorSchema`, `normalizeAuthor`, `numericGithubIdentity`, and failure-agnostic `parseGithubJson`; `@sdl/roaster` imports them from the curated subpath and no longer defines local `ghAuthorSchema`, `normalizeAuthor`, or `numericId`. Roaster retains its gateway/review/files/marker/mutation policy and maps shared parse results to Roaster failures. Focused tests cover positive numeric string ids, missing-login author compatibility, and `github-response-invalid` for missing, non-numeric, zero, and negative discussion comment ids in list and mutation responses; validation passed (`just ts-deps-check`, `just ts-format-check`, `just ts-lint`, `just ts-check`, `just ts-test`, `just ts-test-integration`).
- [x] Delete the vestigial `@sdl/core` root `.` export if it is still only supporting convenience imports; repoint live bare importers at explicit subpaths. See `references/asdl-core.md`.
      Classification: neutral structural cleanup. Evidence: `ts/packages/infra/core/package.json` no longer exports `"."`; `ts/packages/hosts/pi/src/sessions/harness-session.ts` imports `truncatedSha256Digest` from `@sdl/core/primitives`; `ts/packages/infra/core/src/index.ts` was deleted; style-guard fixture strings now use `@sdl/core/primitives`; `just ts-check` and `just ts-test-integration` passed.
- [ ] Rebaseline and decompose `@sdl/areg`'s real gateway/file-system god-file. See `references/areg.md`.
      Classification: neutral/tool-local structural cleanup. Evidence: `ts/packages/tools/areg/src/real-gateways.ts` is still 1383 lines. `areg` is a standalone tool off the extension axis, so decomposition can stay in this Objective as a tool-local cleanup: split per-gateway files under `src/gateways/*`, extract project filesystem concerns, collapse the `init`/`skill-kind` policy fork to data, and share pure `classifySkillSpecResolution` if still duplicated by fakes.
      2026-06-29 partial evidence: non-project real gateways were split under `ts/packages/tools/areg/src/gateways/`, while `RealAregProjectGateway`, project filesystem/path-safety helpers, and init/skill-kind mutation-policy logic remain in `real-gateways.ts`; see `updates/20260629T173216Z-areg-real-gateway-split.md`.
- [ ] Rebaseline Flow/ccc land-stack decomposition before implementing. See `references/ccc.md`.
      Classification: capability-owned (Flow), not neutral structural cleanup. Evidence: `ts/packages/ccc/src/land-stack/landing-operations.ts` is a 14-line compatibility re-export; the implementation is `ts/packages/capabilities/flow/src/land-stack/landing-operations.ts` (1222 lines) with `performGraphiteMaintenance`. Any decomposition must be a Flow-owned maintenance slice that preserves expected-SHA and merge→verify→cleanup ordering; do not move Flow policy below the SDK for dedup.
- [ ] Rebaseline Graphite topology ownership before deduping. See `references/ccc.md`.
      Classification: split / still design-sensitive. Evidence: active topology reads are `sdl-flow` land-stack files (`graphite-topology.ts`, `stack-facts.ts`), while `@sdl/graphite` is the neutral home for command/metadata mechanics. Future work should extract only policy-free Graphite mechanics to `@sdl/graphite`; landing-path decisions, fork refusal wording, and `/sdl:flow:land` behavior stay Flow-owned.
- [ ] `sdlcc`: replace the hand-rolled validation tower in `ts/packages/hosts/sdlcc/src/stack-map-model-loader.ts` with Zod boundary schemas; delete parsed-but-unread fields if still unused. See `references/per-package-cleanups.md`.
      Classification: neutral package-local structural cleanup. `sdlcc` is a host package, not capability domain; this can stay here as a focused boundary cleanup after pickup re-verifies the current loader shape.
- [ ] `@sdl/aretro`: split generic JSON accessors out of `ts/packages/aretro/src/sessions/pi-jsonl-source.ts`; unify the two divergent `bashExecution` count paths if still present. See `references/per-package-cleanups.md`.
      Classification: capability-owned / Aretro-local cleanup. Evidence: Aretro is now a completed capability with command face and no current `/api`; the file still contains multiple `bashExecution` paths. Keep generic JSON mechanics local unless another consumer proves a neutral helper; route semantic evidence collection policy through Aretro ownership.
- [ ] `@sdl/kernel`: replace the reflective `MANIFEST_COMMAND_FIELDS` engine in `ts/packages/kernel/src/extension-discovery.ts` with a Zod schema; de-dup the `index.ts`/`index.js` dir-index block if still present. See `references/per-package-cleanups.md`.
      Classification: neutral SDK/kernel structural cleanup. Evidence: `MANIFEST_COMMAND_FIELDS` still exists in `ts/packages/kernel/src/extension-discovery.ts`; this is kernel manifest parsing, not capability domain.
- [x] `@sdl/packagechk`: collapse `ClaimPolicy`/`ClaimPlan` over-abstraction in `ts/packages/tools/packagechk/src/claim-command.ts` into two linear claim functions sharing small helpers if the shape remains. See `references/per-package-cleanups.md`.
      Classification: neutral/tool-local structural cleanup. Evidence: `ClaimPolicy` and `ClaimPlan` are removed from `claim-command.ts`; CLI wiring now calls explicit PyPI/npm claim runners with concrete gateways instead of policy-builder objects; focused `@sdl/packagechk` scenario tests plus `just ts-format-check`, `just ts-lint`, and `just ts-check` passed.
- [ ] `@sdl/vibechk`: refactor `workflow.ts:executeRun`; re-verify and delete any remaining dead empty-diff write, duplicate provenance/type debris, redundant casts, and `normalizeRunsFormatArgs`. See `references/per-package-cleanups.md`.
      Classification: neutral/tool-local structural cleanup. Evidence: `normalizeRunsFormatArgs` still exists in `ts/packages/tools/vibechk/src/cli.ts`; re-probe `workflow.ts:executeRun` at pickup before editing.
- [ ] Flow submit / former `asdl-core submit` tidy-ups: rebaseline ownership after the Flow capability migration before attempting prewrite/post-submit PR-description dedup, failure-transcript extraction, duplicate formatter cleanup, no-consumer re-export pruning, or alias-file deletion. See `references/asdl-core.md`.
      Classification: capability-owned (Flow). Evidence: submit/PR-description orchestration now lives under `ts/packages/capabilities/flow/src/submit/`; remaining symbols include `submit-pr-metadata-prewrite`, post-submit verification, PR-description formatting, and raw failure transcript handling. Handle as Flow maintenance unless a leaf helper is clearly neutral.
- [ ] ccc/flow small dedup: re-verify `firstNonEmptyLine`, canonical `isRecord`, gt restack/submit arg builders, and repeated `presentLandStackFailure` call sites against the current split between `@sdl/ccc`, `sdl-flow`, `@sdl/core`, and `@sdl/graphite`. See `references/ccc.md`.
      Classification: capability-owned / split. Evidence: repeated `firstNonEmptyLine` exists across core, Flow, ccc, and local-pi tools, but call sites are tiny and context-specific; `presentLandStackFailure` repetition is Flow land-stack presentation policy. Only extract policy-free helpers if pickup proves meaningful complexity deletion.

### Absorbed from `ts-cli-architecture-deepening` (subsumed)

These deepening candidates migrated from the closed `ts-cli-architecture-deepening` Objective. They use the `improve-codebase-architecture` vocabulary (module / interface / depth / seam / deletion test); the reasoning is carried across intact, not flattened.

- [ ] **Collapse slot-dispatch into one orchestration module** (`ccc`/cmux/slot dispatch) — re-verify current homes first.
      Classification: capability-owned / ccc-slot boundary. Slot is a completed capability with `@sdl/slot/api`; any remaining dispatch orchestration should be handled as Slot or ccc consumer cleanup, not neutral core work.
- [x] Disposed as obsolete: **Hide occupancy reconciliation behind the slot inventory** (`@sdl/slot`) — stop leaking `SlotRecord.branch === null` pattern matching into multiple callers if current code still does so.
      Classification: obsolete / disposed. Current grep found no `SlotRecord.branch === null` leakage under `ts/packages`; slot capability migration closed with inventory/lifecycle ownership recorded. Reopen only with fresh leakage evidence.
- [x] Disposed as obsolete: **Put a stack-navigator adapter over Graphite's discriminants** (`@sdl/slot`/gt) — keep inside the `slot gt` boundary and use Graphite plumbing, never parsed display output.
      Classification: obsolete / absorbed by Slot. Current `@sdl/slot` has `operations/gt/*`, `GraphiteStackGateway`, and `sdl slot gt` command-face dispositions. Future refinements belong to Slot, not this neutral Objective.
- [ ] **Pull objective-markdown rules into one validator** (`@sdl/objective`) — if a live objective capability migration owns this first, cross-reference that ownership instead of duplicating the validator here.
      Classification: capability-owned (Objective). Evidence: Objective capability has closed, and current markdown checks live in `ts/packages/objective/src/operations/check-objective.ts`. Further validator deepening belongs to Objective maintenance.
- [x] Disposed as obsolete / already satisfied: **Deepen Branch Memory behind an entry locator** (`@sdl/brmem` / `@sdl/handoff` / `@sdl/branch-context`) — next layer on the already-shipped gateway migration.
      Classification: obsolete as originally framed. Evidence: `@sdl/brmem` now exports `buildEntryLocator`, `mustEntryLocator`, and `parseEntryLocator`; brmem operations produce `entryLocator`, and handoff/branch-context consume locators through brmem-owned ref-layout mechanics. Future compatibility gaps can be new neutral `@sdl/brmem` slices, but this backlog row is no longer a clear implementation candidate.
- [ ] **Replace the shallow brmem adapter with a plan-attachment module** (`@sdl/branch-context`) — likely a branch-context capability/domain seam that may belong under `sdl-extension-architecture`; classify against ADR 0009 before placing it.
      Classification: capability-owned (Branch Context/Plans). Evidence: branch-context/plans child migration closed; `@sdl/branch-context/api` owns attachment semantics and `attach.ts`/`attached-plan.ts` carry plan-domain policy. Do not move plan-attachment domain into `@sdl/brmem`.

## Parked

- [x] Disposed as obsolete: migrate the two `legacyCommand`-based CLIs (`plans`, `branch-context`) off the deprecated `legacyCommand` path onto the rendered `command(...)` shape. See `references/cli-wiring-layer.md`.
      Classification: obsolete / disposed. Current grep under `ts/packages/plans` and `ts/packages/branch-context` found no `legacyCommand` usage.
