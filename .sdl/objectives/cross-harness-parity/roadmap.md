# Roadmap

## Work

- [x] Seed the living parity table (`parity-table.md`) from the 2026-06-03 cross-harness audit: one row per Pi command with shared CLI backing, driving skill, parity verdict (FULL/PARTIAL/NONE/WAIVED), and owning Objective. Evidence: `parity-table.md` exists and classifies the audited current Pi command surfaces; removed surfaces are deleted during refreshes rather than retained as stale FULL rows. Pi tool calls are now treated as host-native bridges that do not require parity metadata rows.

- [x] Close the `asdl-dev` discoverability gaps: author thin skills surfacing `asdl-dev preview-url`, `cp`, and `submit` to Claude/Codex (describing CLI operations only), and reconcile the `internal-code-checkpoint` vs `asdl-dev cp` duplication (decide whether the skill wraps the CLI or the two are explicitly distinct, and record the decision). Evidence: `dev-preview-url` and `internal-code-submit` were added as thin CLI-driving skills; `internal-code-checkpoint` now delegates to `asdl-dev cp`; the parity table's three `asdl-dev` rows moved to FULL.

- [x] Eliminate the `/handoff:list` duplication: point the Pi command at the dedicated `handoff list --format json` CLI and keep only the card renderer in TypeScript, removing the TS re-derivation of listing/preview over raw `brmem list`. Evidence: `/handoff:list` now calls `handoff list --branch/--all --format json`; the parser accepts the CLI `handoffs` array while preserving legacy `entries` compatibility; branch-state/`--include-deleted` are available in the CLI; targeted `bun test test/handoff.test.ts` passed for the Pi handoff extension.

- [x] Author the durable parity-review skill: diff-scoped mode flags any added/changed Pi command lacking a CLI+skill counterpart (or a recorded waiver); on-demand full-sweep mode checks and refreshes the parity table. Encode the WAIVED rule for Pi-native primitives (acceptable only with a documented agent-neutral fallback when a command depends on them) and a severity rubric for "massive feature gap" vs note. Evidence: `internal-code-parity-review` exists as an installed internal skill for Codex/Claude; it documents default diff-scoped and explicit full-sweep modes, live-evidence-first command inventory over `pi.registerCommand` / `registerCliCommandExtension`, CLI-bridge treatment, advisory finding labels, and Objective table sync with a required Semantic Update; manual smoke on the implementation branch confirmed no new Pi command surface was introduced and the full inventory recipe reaches the expected registration sites. (The skill has since been consolidated as the `parity-review` route of the code-workflows skill — renamed 2026-06-11 from `internal-code-workflows` — with the workflow contract unchanged.)

- [x] Add a typed `@asdl/pi-extensions` parity accounting gate. Evidence: package registration modules now export co-located command `PiSurfaceParity` metadata; `ts/packages/pi-extensions/src/parity-registry.ts` aggregates the package-owned records; `ts/packages/pi-extensions/test/parity.test.ts` registers the package modules against a fake Pi host and fails on missing, stale, or duplicate exact command metadata without parsing Markdown. Scope is intentionally v1/package-local: checked-in ad hoc `.pi/extensions/*.ts` adapters and direct `@asdl/ccc` command surfaces are not enforced unless exposed through `@asdl/pi-extensions`; Pi tool calls are ignored by the metadata gate.

- [ ] Implement parity-native command-output summaries as a shared CLI/helper, building on the `ts-cli-foundation` layer (`@asdl/clinkr` + `@asdl/core`) if implemented in TypeScript. Evidence: the contract names command/cwd/timeout/cancellation semantics, shell/argv invocation modes, outcome taxonomy, payload artifact log paths, bounded human and machine-readable summaries, profile-derived counts/excerpts for generic/test/lint/typecheck output where practical, no-leak guarantees, and skill guidance plus optional Pi adapter that wraps the same helper instead of becoming canonical.

- [ ] Give the stack-landing core a clinkr-based CLI entry point + skill. The orchestration is already extracted and test-backed in `@asdl/ccc` (`ccc/src/land.ts` + `land-stack/`: Graphite SQLite-metadata topology, fork-violation detection, backup refs, pre-delete child guards, sequenced merge loop; covered by `ccc/test/land-command.test.ts` and `land-stack.test.ts`) — but the package ships no bin and no skill, so only Pi can reach it. Add the CLI entry on `ts-cli-foundation`, author the skill (the single-PR path may stay skill-only per the skill-only parity row), and keep Pi `/code:land` importing the same core. Evidence: CLI scenario tests; Claude/Codex can land a stack via the skill; Pi carries no duplicate logic.

- [ ] Give the cmux dispatch cores a CLI entry point + skill: a CLI with explicit inputs (plan-file path, prompt) over the `@asdl/ccc` cmux modules and the shared `launchFocusedCmuxTab` / prompt-file utilities (`ccc/src/cmux/focused-terminal-tab.ts`, `prompt-file.ts`), with an agent-neutral launch path (not a hardcoded `pi @file`); cover `/ccc:workspace:dispatch-plan`, `/ccc:workspace:dispatch-prompt`, and a thin skill for the `/ccc:workspace:open-branch` (`slot checkout` + `cmux new-workspace`) pairing. Pi keeps only session-history "latest plan" resolution over the core. Decide `/ccc:claude-plan-tab` alongside (likely WAIVED: Pi/cmux session-native tab launch) and record the verdict in the parity table. Evidence: a Claude/Codex agent can dispatch a branch/plan into a cmux slot via skill+CLI; the `/ccc:workspace:*` rows move off NONE.

- [x] Give `autobranch` a CLI entry point + skill over `ccc/src/autobranch/` (stash → `gt create` → restore → checkpoint commit, plus branch-name selection; slug derivation already routes through the shared `@asdl/plans` helper), and bridge Pi `/code:autobranch` through `registerCliCommandExtension` to the same CLI. Evidence: `@asdl/ccc` now ships the `ccc` bin with hidden `ccc exec autobranch`; `ts/packages/ccc/test/scenario/autobranch-cli.test.ts` covers help/version/runtime, dirty-worktree success, latest-commit success, requested slugs, and failure exit codes; `code-autobranch` is installed as an internal skill driving the CLI; Pi `/code:autobranch` invokes the CLI in process through the shared bridge.

- [ ] Decide `/code:autoslot`: either add a portable skill/CLI path for the CCC autoslot branch-plus-slot workflow or record a deliberate WAIVED verdict if managed slot movement is accepted as Pi-only convenience. Keep it PARTIAL until that decision is implemented because it mutates branch/worktree/slot state. Evidence: parity table's `autoslot` row reaches FULL or WAIVED.

- [ ] Decide `/code:push`: either add a thin skill documenting the clean-worktree `git push` guard or explicitly treat it as primitive git usage. Keep it PARTIAL until that decision is implemented because it mutates remote branch state. Evidence: parity table's `push` row reaches FULL or WAIVED.

- [ ] Provide skill-only parity for the single-PR path of the unified `/code:land`: a skill documenting the single-PR land contract over `gh` (base-must-be-trunk guard, `--match-head-commit` pinning, PR title/body as commit message). `/code:changes` is resolved as WAIVED because it is a read-only Pi UX helper over ordinary git/worktree evidence. Evidence: parity table's `land` row reaches FULL or WAIVED.

- [x] Consolidate provider-specific model defaults behind the backend-neutral text-generation abstraction/config: deduplicate the three `model-slug.ts` helpers (`pi-extensions`, `ccc/autobranch`, `planned-branch`) that hardcode `openai-codex/gpt-5.4-mini`; route the ccc sidebar default model ref and the per-harness model branches in `pi-extensions/src/fast-text-draft.ts` through the same configurable seam; remove concrete provider model refs from shared `asdl-core` prompt guidance (`prompts/defaults/subagent-launch.md`); and parameterize or document a fallback for the hardcoded haiku model in the `refactor-swarm` skill. Deliberately harness-targeted adapters (e.g. roaster's Claude Code runner) are out of scope. Evidence: `@asdl/plans` `model-defaults.ts` is the single configurable source (`DEFAULT_FAST_MODEL_REF` + `parseModelRef`/`resolveModelRef`); the canonical `model-slug.ts` lives in `@asdl/plans` honoring `ASDL_SLUG_MODEL` and the three copies are deleted; the ccc sidebar imports the shared default; fast-text-draft models are overridable via `PI_DRAFT_MODEL`; `subagent-launch.md` and `refactor-swarm` carry harness-neutral default guidance with explicitly labeled OpenAI/Anthropic examples (examples added on PR review feedback; convention recorded in AGENTS.md "Skill Model Examples"); a lock-in grep sweep over shared code/skills leaves only the single default, the env-mitigated `asdl-dev` checkpoint default, the overridable claude-cli branch default, and the labeled provider examples in prompt/skill guidance; full `just` passed.

## Parked

- [ ] Broaden the machine-checkable parity gate beyond v1 accounting: decide whether to add CI wiring, direct `@asdl/ccc` Pi command surfaces, ad hoc `.pi/extensions/*.ts` adapters, and/or generated human table output from the typed metadata. The current implemented gate covers only `@asdl/pi-extensions` package-owned command registrations and checks accounting, not semantic parity correctness.
- [ ] Broad automatic bash-output interceptor or framework-wide output safety net.
- [ ] Pi-only runner-subagent test-summary tool as the canonical command-summary implementation.
- [ ] LM/subagent semantic interpretation of command logs as the default summarizer.
- [ ] Migration of every validation command or package workflow to command-output summaries.
- [ ] Payload retention, garbage collection, durable archive policy, or strict numeric token-budget tests for command logs.
