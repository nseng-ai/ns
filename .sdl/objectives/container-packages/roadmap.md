# Roadmap

## Work

- [x] Land the pilot rename on branch `core-time-topology-circle-consolidation` (PR #2677): the manifest fields are `sdl.subpackages` and `sdl.remainder`, `@sdl/core` declares `subpackages: ["time"]` with `remainder: true`, `extract-graph.mjs` splits only declared subpackages plus the declared remainder, and the TypeScript style guard reads the same config instead of hardcoding `@sdl/core`.
  - Policy: direct execution; field names, the term "remainder subpackage", and pilot scope are all user-confirmed.
  - Evidence: topology report shows `@sdl/core/time` and no auto-discovered directory circles; guard tests and `just` pass.
  - Disposition: **converted** on branch `manifest-driven-topology-circles`, committed as `b7f4b2409`; submit intentionally skipped by autopilot. Evidence: topology report package count stayed 44, topology circles changed 99 → 45, `@sdl/core/time` remains declared, and undeclared `@sdl/slot/*` source directories no longer auto-discover as circles; `just ts-test-typescript-style-guard`, `just ts-format-check`, `just dprint-check`, and `just ts-check` passed.
- [x] Document the vocabulary: add **Subpackage**, **Container package**, and **Remainder subpackage** headwords to root `CONTEXT.md`, reconcile the **Topology circle** entry's "source component" phrasing, and record the end-state ADR (published packages as containers; `sdl.subpackages` as manifest source of truth; the declared remainder as the explicit transitional state; properly formed = no remainder).
  - Policy: steer first on final wording, including confirming the remainder mechanic and term; this is the deliberate domain-language slice.
  - Also from the review pass: consider a headword for the **local space** (`ts/packages/local/`, `@sdl-local/*` scope, strict admission invariant), and fix known drift — the **Package Tier** entry says the gateways "still carry `neutral-infra`", but all four already declare `capability-gateway-backend` in their manifests.
  - Disposition: **converted** after user approval of the full vocabulary bundle. Evidence: root `CONTEXT.md` now defines **Subpackage**, **Container package**, **Remainder subpackage**, **Standalone package**, and **Local space**; **Published package**, **Topology circle**, **Topology overlay**, and **Package Tier** were reconciled; `CONTEXT-MAP.md` references the package-topology cluster; ADR 0022 records manifest-declared subpackages and the explicit remainder; `just dprint-check` passed.
- [x] Implement the rules-of-the-road guard: every source file in a declaring package must belong to a declared unit (named subpackage or declared remainder); named subpackages must exist as `src/<name>/` directories; a package with no remainder declaration fails on any unassociated code. Keep the check lightweight — declared-state conformance, not new import-graph analysis.
  - Policy: direct execution; the mechanic is user-confirmed in `objective.md`.
  - Note: this guard is also the topology shape-preservation mechanism — once the report's only circle source is the manifest, declared-state conformance prevents auto-discovery reversion and orphan nodes. Per-slice shape deltas (stable package count, remainder only shrinking) are recorded as conversion-row evidence by running `extract-graph.mjs` before/after; no separate shape tool is built.
  - Disposition: **converted** on branch `subpackage-conformance-guard`. Evidence: the TypeScript style guard now enforces manifest-declared `sdl.subpackages` / `sdl.remainder` conformance with synthetic cases for missing `src/<name>/`, unassociated source without remainder, remainder-allowed loose source, and properly formed subpackage-only source; the real repo declaration check passes.
- [x] Build the upfront decision inventory in `references/inventory.md`: one entry per workspace package recording containerize (with a proposed subpackage split), keep standalone (with rationale), or fold into a container (with target and subpackage name), presented for user approval in a single review pass. On approval, append one thin conversion row per containerize/fold decision below, pointing at its inventory entry; keep-standalone entries close at approval with rationale recorded.
  - Policy: propose splits and folds from code evidence (tier homogeneity, no dependency cycles); the user approves the inventory before any conversion row becomes actionable. `references/inventory.md` is durable source material, not current truth — conversion slices re-verify at pickup.
  - Threshold (user-confirmed): propose containerize only when the **end-state** split yields four or more subpackages (counting a core-style unit for loose root files, excluding the remainder); three or fewer stays standalone for now. The threshold judges the final state, not a mid-conversion declaration, and does not apply to fold decisions.
  - Consolidation target (user-confirmed): the end state has fewer top-level packages than the 44 at inventory time; the draft proposes 44 → 21 via folds (five neutral-infra packages into `@sdl/core`; `sdl-sdk` into `@sdl/kernel` as its `sdk` subpackage; the four gateway backends into `@sdl/capability-kit` — the user-ruled tier-crossing exception; `sdl-land` into flow; the five `*-pi` shims into their capabilities as `pi` subpackages with `@sdl/pi` as optional peer; `@sdl/worktree-status` into the `@sdl/pi` host) plus one net-negative consolidation container (local Pi tools) whose name the user approves. Every top-level package carries one of three categories: core infra, standalone tool, or first-party extension/capability.
  - Status: **APPROVED 2026-07-01** in a single review pass (grilling session). Draft completed and revised the same day with user rulings (tools standalone, clinkr standalone, plans/branch-context stay split, gateways fold into capability-kit, three-way categorization, pi-subpackage model, capabilities/ directory placement, `local/` space, worktree-status → pi, sdl-sdk → kernel/sdk); closing rulings at review: aretro standalone, roaster containerize, container named `@sdl-local/pi-tools`, kernel `sdk` unit kept pure, strict `local/` admission invariant, dedicated relocation slice. Bin ownership verified a non-issue (no folded package has a `bin`). End state: 44 → 21 top-level (12 containers + 9 standalone). The rendered tree lives in `references/target-layout.md` (approved with the inventory). Conversion rows appended below.
- [ ] Execute approved conversion slices package by package: move code under subpackage directories, update exports/imports, declare `sdl.subpackages`, opt into container status, one PR per package via Graphite. Record a disposition per row as work lands: **converted** (with evidence) or **re-decided** (code reality contradicted the approved split; human re-confirmed).
  - Policy: direct execution for approved inventory rows after pickup re-verification; steer when code reality contradicts the approved split. Follow the default runner loop in `objective.md`.
  - Evidence: per-package PR; style guard green; `just` passes; topology shape check (before/after `extract-graph.mjs` node counts: package count stable, new circles only for newly declared subpackages, remainder only shrinks, no orphan or auto-discovered nodes).
- [x] Relocation slice (mechanical, early): move the standalone capability packages `plans`, `address`, and `aretro` under `ts/packages/capabilities/`; containers relocate within their own conversion slices — see inventory review resolution.
  - Disposition: **converted** on branch `standalone-capability-relocation`. Evidence: `@sdl/plans`, `@sdl/address`, and `@sdl/aretro` kept their package names and standalone manifests while relocating to `ts/packages/capabilities/`; path references, package scripts, lockfile links, package taxonomy docs, context/ADR references, and kernel command-loader package paths were updated. Topology extraction reports package count 44 and topology circle count 45, with the three package paths under `ts/packages/capabilities/`; focused package tests passed for plans, address, and aretro; `sdl objective exec tracking-gate container-packages --format json`, `just ts-test-typescript-style-guard`, `just ts-format-check`, `just ts-check`, and `just dprint-check` passed.
- [x] Convert `@sdl/core` → container: fold `@sdl/exec`, `@sdl/cli-runtime`, `@sdl/cli-theme`, `@sdl/test-kit`, `@sdl/typescript-analysis`; pin the loose-root unit names (candidates `primitives`, `terminal`, `config`) — see inventory entry. (The pilot slice above already declares `time`.)
  - Disposition: **converted** on branch `core-container-conversion`. Evidence: `@sdl/core` now declares `time`, `exec`, `cli-runtime`, `cli-theme`, `test-kit`, `typescript-analysis`, `primitives`, `terminal`, and `config` with no remainder; the five folded packages were deleted and workspace imports/manifests now use `@sdl/core/<subpackage>` exports. Topology extraction changed package count 44 → 39 and topology circles 45 → 48; folded packages no longer exist as top-level package/circle ids and reappear as `@sdl/core/*` circles. Validation passed: `pnpm --dir ts --filter @sdl/core test`, `just ts-test-typescript-style-guard`, `just ts-format-check`, `just ts-check`, `just dprint-check`, `just ts-test`, `just ts-test-integration`, `just ts-lint`, and `just ts-deps-check`.
- [x] Convert `@sdl/kernel` → container: fold `sdl-sdk` as the pure `sdk` subpackage; move kernel's pre-existing `src/sdk/` internals to another unit; re-point the ~12 sdl-sdk dependents to `@sdl/kernel/sdk` — see inventory entry.
  - Disposition: **converted** on branch `kernel-sdk-container`. Evidence: `@sdl/kernel` now declares `cli`, `extensions`, `operations`, `runtime`, and pure author-facing `sdk` subpackages with no remainder; the former kernel `src/sdk/` runtime internals moved to `src/runtime/`; the standalone `ts/packages/sdl-sdk` package was deleted and live workspace imports/manifests now use `@sdl/kernel/sdk` / `@sdl/kernel`. Topology extraction changed package count 39 → 38 and topology circles 48 → 52; `sdl-sdk` no longer exists as a top-level package/circle and reappears as `@sdl/kernel/sdk`; kernel circles match the declared subpackages; orphan count stayed 19 with no new kernel orphan. Validation passed: `pnpm --dir ts --filter @sdl/kernel test`, targeted extension package tests for capability-kit/slot/address/aretro/flow/handoff/objective/roaster/branch-context/pi, `just ts-test-typescript-style-guard`, `just ts-format-check`, `just ts-check`, `just dprint-check`, `just ts-test`, `just ts-test-integration`, `just ts-lint`, and `just ts-deps-check`.
- [x] Convert `@sdl/capability-kit` → container: fold `@sdl/git`, `@sdl/github`, `@sdl/graphite`, `@sdl/cmux`; retire the `capability-gateway-backend` tier and reconcile guard rules keyed to it — see inventory entry and tier note.
  - Disposition: **converted** on branch `capability-kit-gateway-container`. Evidence: `@sdl/capability-kit` now declares `kit`, `git`, `github`, `graphite`, and `cmux` subpackages with no remainder; the four gateway backend packages were deleted and live imports/manifests now use `@sdl/capability-kit/*` subpaths. Topology extraction changed package count 38 → 34 and topology circles 52 → 53; the folded packages no longer exist as top-level package/circle ids and reappear as `@sdl/capability-kit/git`, `@sdl/capability-kit/github`, `@sdl/capability-kit/graphite`, and `@sdl/capability-kit/cmux`; capability-kit has no orphan source. Validation passed: `pnpm --dir ts --filter @sdl/capability-kit test`, `just ts-test-typescript-style-guard`, `just ts-format-check`, `just ts-check`, `just ts-lint`, `just ts-deps-check`, `just dprint-check`, `just ts-test`, `just ts-test-integration`, and `just`.
- [x] Convert `sdl-flow` → container: fold `sdl-land` (→ `land`) and `@sdl/flow-pi` (→ `pi`, optional-peer model) — see inventory entry.
  - Disposition: **converted** on branch `sdl-flow-container-conversion`. Evidence: `sdl-flow` now declares `autobranch`, `commands`, `core`, `land`, `land-stack`, `pi`, `shared`, and `submit` with no remainder; the standalone `sdl-land` and `@sdl/flow-pi` packages were deleted and their public surfaces are available as `sdl-flow/land/*` and `sdl-flow/pi/*`. Topology extraction changed package count 34 → 32 and topology circles 53 → 59; folded packages no longer exist as top-level package/circle ids and reappear under declared `sdl-flow/*` circles. The TypeScript style guard and topology report now allow the pi-subpackage optional peer edge to `@sdl/pi` while keeping other capability→host edges forbidden. Validation passed: `pnpm --dir ts --filter sdl-flow run check`, `pnpm --dir ts --filter sdl-flow run test`, `pnpm --dir ts --filter @sdl/ccc run check`, `pnpm --dir ts --filter @sdl/pi run check`, `just ts-test-typescript-style-guard`, `just ts-format-check`, `just ts-check`, `just ts-deps-check`, `just dprint-check`, and `just`.
- [x] Convert `@sdl/slot` → container — see inventory entry.
  - Disposition: **converted** on branch `slot-container-conversion`. Evidence: `@sdl/slot` now declares `core`, `gateways`, `lifecycle`, `operations`, and `shell` subpackages with no remainder; its former loose root source files moved into `src/core/`, and public package exports plus SDL extension entries now point at the new core paths. Topology extraction changed package count 32 → 32 and topology circles 59 → 64; slot has no orphan source, and the existing standalone `@sdl/aretro` directories continue to prove undeclared directories are not auto-discovered as circles. Validation passed: `pnpm --dir ts --filter @sdl/slot run check`, `pnpm --dir ts --filter @sdl/slot run test`, `just ts-test-typescript-style-guard`, `just ts-format-check`, `just ts-check`, `just dprint-check`, `just ts-test-integration`, and `just`.
- [x] Convert `@sdl/handoff` → container: fold `@sdl/handoff-pi` (→ `pi`) — see inventory entry.
  - Disposition: **converted** on branch `handoff-container-package-pi-fold`. Evidence: `@sdl/handoff` now declares `core`, `operations`, `pi`, and `sdl` subpackages with no remainder; the standalone `@sdl/handoff-pi` package was deleted and its Pi surface is exported from `@sdl/handoff/pi`. Topology extraction changed package count 32 → 31 and topology circles 64 → 67; `@sdl/handoff-pi` no longer exists as a top-level package/circle id, and handoff has no orphan source. Validation passed: `pnpm --dir ts --filter @sdl/handoff run check`, `pnpm --dir ts --filter @sdl/handoff run test`, `just ts-test-typescript-style-guard`, `just ts-format-check`, `just ts-check`, `just dprint-check`, and `just`.
- [x] Convert `@sdl/objective` → container: fold `@sdl/objective-pi` (→ `pi`) — see inventory entry.
  - Disposition: **converted** on branch `objective-container-pi-fold`. Evidence: `@sdl/objective` now lives at `ts/packages/capabilities/objective`, declares `core`, `operations`, `pi`, and `sdl` subpackages with no remainder, and exports the folded Pi surface as `@sdl/objective/pi`; the standalone `@sdl/objective-pi` package was deleted. Topology extraction changed package count 31 → 30 and topology circles 67 → 70; `@sdl/objective-pi` no longer exists as a top-level package/circle id, and objective has no orphan source. Validation passed: `pnpm --dir ts --filter @sdl/objective run check`, `pnpm --dir ts --filter @sdl/objective run test`, `just ts-test-typescript-style-guard`, `just ts-format-check`, `just ts-check`, `just`, and `just ts-test-integration`.
- [x] Convert `@sdl/ccc` → container: fold `@sdl/ccc-pi` (→ `pi`) — see inventory entry.
  - Disposition: **converted** on branch `ccc-container-pi-fold`. Evidence: `@sdl/ccc` now lives at `ts/packages/capabilities/ccc`, declares `autobranch`, `cmux`, `core`, and `pi` subpackages with no remainder, and exports the folded Pi surface as `@sdl/ccc/pi`; the standalone `@sdl/ccc-pi` package was deleted. Topology extraction changed package count 30 → 29 and topology circles 70 → 73; `@sdl/ccc-pi` no longer exists as a top-level package/circle id and reappears as `@sdl/ccc/pi`, with the other CCC source units represented as declared circles. Validation passed: `pnpm --dir ts --filter @sdl/ccc run check`, `pnpm --dir ts --filter @sdl/ccc run test`, `just ts-test-typescript-style-guard`, `just ts-format-check`, `just ts-check`, `just dprint-check`, and `just`.
- [x] Convert `@sdl/branch-context` → container: fold `@sdl/branch-context-pi` (→ `pi`) — see inventory entry.
  - Disposition: **converted** on branch `branch-context-container-pi-fold`. Evidence: `@sdl/branch-context` now lives at `ts/packages/capabilities/branch-context`, declares `core`, `sdl`, `testing`, and `pi` subpackages with no remainder, and exports the folded Pi surface as `@sdl/branch-context/pi`; the standalone `@sdl/branch-context-pi` package was deleted. Topology extraction changed package count 29 → 28 and topology circles 73 → 76; `@sdl/branch-context-pi` no longer exists as a top-level package/circle id and reappears as `@sdl/branch-context/pi`, with branch-context orphan source cleared. Validation passed: `pnpm --dir ts --filter @sdl/branch-context run check`, `pnpm --dir ts --filter @sdl/branch-context run test`, `just ts-test-typescript-style-guard`, `just ts-format-check`, `just ts-check`, `just dprint-check`, `just`, and `just ts-test-integration`.
- [x] Convert `@sdl/roaster` → container — see inventory entry (user-confirmed at review).
  - Disposition: **converted** on branch `roaster-container-conversion`. Evidence: `@sdl/roaster` now lives at `ts/packages/capabilities/roaster`, declares `commands`, `core`, `gateways`, `operations`, and `sdl` subpackages with no remainder, and keeps its public command/API exports at the existing `@sdl/roaster/*` specifiers. Topology extraction changed package count 28 → 28 and topology circles 76 → 81; Roaster's previous flat package circle now has zero root LOC and the five declared Roaster subpackage circles account for source. Validation passed: `pnpm --dir ts --filter @sdl/roaster run check`, `pnpm --dir ts --filter @sdl/roaster run test`, `just ts-test-typescript-style-guard`, `just ts-format-check`, `just dprint-check`, `just ts-check`, `just`, and `just ts-test-integration`.
- [ ] Convert `@sdl/pi` → container: fold `@sdl/worktree-status` (→ `worktree-status` subpackage; pi-tui becomes a runtime dep; re-point the `.pi/extensions` adapter) — see inventory entry.
- [ ] Create `@sdl-local/pi-tools` at `ts/packages/local/pi-tools/`: fold the seven `@local-pi-tools/*` packages; establish the `local/` space and add the strict admission invariant (under `local/` iff `@sdl-local/*`, private, no outside workspace dependents) to the guard — see inventory entry.

## Parked

- Per-subpackage tier declarations.
- Requiring every subpath export of a properly formed container to resolve into a declared subpackage, repo-wide.
