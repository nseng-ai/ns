# Batch 1 B Duplication Cleanup Complete

## Summary

Batch 1 finding B is now complete. The final code cleanup removed the dead exported `errorInfo` helper from `ts/packages/areg/src/operations/init.ts` and its unused `AregErrorInfo` import. The private `errorInfo` helper in `ts/packages/areg/src/real-gateways.ts` remains the only live helper.

The remaining `inspectGenericReplacement` note was resolved by audit rather than code change: grep shows a single implementation in `real-gateways.ts`, used by `RealAregProjectGateway.inspectPiArtifacts`, with the other `genericReplacement` references carrying or testing the inspection data rather than duplicating file inspection logic.

## Objective Impact

The Batch 1 B roadmap row is marked complete. The completed evidence now covers the shared `.pi/settings.json` parser, shared `rejectTextState` / optional-directory file-state helpers, removal of the dead init `errorInfo` export, and grep verification that `inspectGenericReplacement` is already singular.

## Verification

- `rg -n "errorInfo" ts/packages/areg/src/operations/init.ts ts/packages/areg/test/unit/init-helpers.test.ts` returned no matches.
- `rg -n "async function inspectGenericReplacement|inspectGenericReplacement|genericReplacement|hasAdapter|hasPackageModule" ts/packages/areg/src ts/packages/areg/test` showed one `inspectGenericReplacement` implementation in `ts/packages/areg/src/real-gateways.ts`.
- `pnpm --dir ts run test -- ts/packages/areg/test/unit/init-helpers.test.ts ts/packages/areg/test/gateways/real-gateways.test.ts ts/packages/areg/test/unit/pi-replacement.test.ts` passed; the workspace Vitest configuration ran the full suite (`225` files, `2567` tests).
- `pnpm --dir ts run check` passed.

## Follow-Ups

Continue Batch 1 with the next roadmap rows: F, H, I, and the alias-collapse slice of A remain open.
