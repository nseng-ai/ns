**Direction: Flow internals get deeper while `sdl-flow/api` stays narrow.**

Getting to: Flow is the reference Capability: thin command face, curated Capability API, gateway-injected land/submit/autobranch domain cores, and Flow-owned tests/docs.

What you see now — rebaselined: stack preflight/dry-run planning has a private `sdl-land` domain seam; Flow still intentionally owns land command presentation, mutation-heavy merge orchestration, pre/post-merge maintenance, isolated fast-path landing, and compatibility behind private Flow modules.

Avoid: re-widening `sdl-flow/api`; moving Flow policy into neutral infra, kernel, SDK, or Pi; treating CCC as owner of Flow land internals; reintroducing fallback behavior for retired CCC-era backup-ref namespaces without a new explicit compatibility decision.
