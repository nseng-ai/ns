[project]
name = "roaster"
version = "0.1.0"
description = "Markdown-driven local roaster for asdl"
requires-python = ">=3.11"
dependencies = [
  "click>=8.1.7",
  "pyyaml>=6",
  "asdl-core",
  "brmem",
]

[project.scripts]
roaster = "roaster.cli.main:main"

[project.entry-points."asdl.plugins"]
roaster = "roaster.cli.plugin:build_roaster_plugin"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/roaster"]
