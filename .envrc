# Auto-sync and activate the uv-managed .venv for this worktree.
watch_file pyproject.toml uv.lock
uv sync --quiet
export VIRTUAL_ENV="$PWD/.venv"
PATH_add "$VIRTUAL_ENV/bin"

# Auto-install and expose Bun workspace bins for this worktree.
watch_file ts/package.json ts/bun.lock
bun install --cwd ts --silent
PATH_add "$PWD/ts/node_modules/.bin"
