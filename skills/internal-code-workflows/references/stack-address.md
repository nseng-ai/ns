# stack-address

This workflow is no longer hidden behind `internal-code-workflows`.

Use the standalone internal skill `internal-pr-stack-address` directly. This
compatibility reference remains only for older router prompts and should not be
used as the active playbook.
