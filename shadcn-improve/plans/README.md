# Implementation Plans

Generated by the improve skill on 2026-06-15 (against commit `d637e619b`).
Scope of this run: **TypeScript codebase only** (`ts/`), per the invocation.
Each executor: read the plan fully before starting, honor its STOP conditions,
and update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | sdl: command discovery, validation & runtime module-loading tests | P1 | M | — | TODO |
| 002  | pi-extension-runtime: objective picker & selection-path tests | P1 | M | — | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (one-line reason) | REJECTED (one-line rationale)

Both plans are independent (no ordering constraint) and test-only — they create
new test files and modify no source. Either can be executed first or in parallel.

## Audit summary

The TS codebase is exceptionally disciplined: zero `as any`/`@ts-ignore`,
errors-as-values throughout, no source TODOs, ~2267 passing tests, and the
large architectural threads (cross-package infrastructure duplication, the
shared `asdl-core` TS foundation) are already tracked as live objectives under
`.asdl/objectives/` (e.g. `asdl-core-ts`). Net-new, worth-doing, not-already-
tracked findings were few; the two highest-leverage ones are genuinely untested
critical paths with clean fake-driven test stories — those became plans 001 and 002.

## Findings considered and rejected

- **`ts/packages/planned-branch` "dead package"**: not worth a plan — it is an
  untracked local `node_modules` remnant (`git ls-files` empty, no `package.json`),
  not a workspace member or tracked file. A `pnpm prune`/`rm` matter, not a code change.
- **pi-extensions missing `exports` map**: not worth doing — it is a
  `"private": true` leaf extension host that nothing imports as a library; an
  exports map would add no boundary value.
- **N+1 sequential `unresolveReviewThread`** (`pr-address/src/prepare-run.ts:132-139`):
  real (confirmed) but low-leverage — "contested threads" is normally an empty
  set, and parallelizing GitHub *mutations* risks secondary rate limits. Not planned;
  revisit if contested-thread batches are observed to be large.
- **Disposed-controller `.finally()` mutation**
  (`pi-extensions/.../interrogation-controller.ts:116-119`): real but LOW impact
  (post-dispose TUI transcript update); not planned. A one-line `isDisposed` guard
  if it ever matters.
- **pr-address god modules** (`payload-store.ts` 1251 lines, `classification.ts`
  1054): real refactor candidates, but likely belong to the existing
  `pr-address-session-store` / `landed-architecture-review-pr-address-ts`
  objectives — confirm before planning to avoid duplicating tracked work.
- **Zod `.catch()` fallbacks in `context-profiler/segmentation.ts`**: surfaced as
  a question, not a finding — coercing malformed LLM/analysis output to defaults
  is plausibly intentional resilience. Maintainer decides intended behavior.
- **`gateways.ts --limit 1000` PR list**: latent silent truncation past 1000 open
  PRs; LOW, not planned.
- **`/ts` README / PATCHES.md / DEPENDENCIES.md, per-package `check`-script gate**:
  low value; the repo deliberately routes onboarding through `AGENTS.md` +
  objectives, and dependencies are healthy (zod consistent `^4.4.3`, TS 5.9, no lag).

## Not audited this run

Live GitHub conformance paths; deep internals of `pi-extensions` (84 files);
`clinkr`, `plans`, `asdl-dev`, `branch-context` in depth; the docs-site. Standard
depth, weighted to churn hotspots (`pr-address`, `ccc`, `pi-extension-runtime`, `sdl`).
</content>
