# Roadmap

## Work

Dependency gate lifted (2026-06-13): the predecessor `pr-address-typescript-port` is closed, so these rows are now ready to start. The CLI is TypeScript-only at `ts/packages/pr-address/`; the rows below operate on that surface. Start with the descriptor-taxonomy / resolution-contract row.

- [x] Re-ground the evidence on a fresh TypeScript run.
  - Captured a non-mutating TS CLI pass against PR #1427 on 2026-06-13 and recorded the results in `updates/2026-06-13T143520Z-fresh-typescript-reground-run.md`. The PR had zero feedback, so collection/template/validate/plan/finalize and one-PR stack prep/plan/diff were exercised live; actionable batch helpers were inventoried by help/source inspection rather than by a real batch.
  - Policy: completed under preview-confirmed bounded local evidence gathering. No review threads were resolved or replied to, no PR was pushed/submitted, and no write-capable external action was performed.
  - Evidence: the update enumerates the currently observed wrapper/reference state: `plan-feedback` still needs `{manifest, classification}`, batch-build still needs `{plan, batch_id, commit_sha, decisions}`, checkpoint still needs the eight-field composed payload, `finalize-run` still needs `{feedback, checkpoints}`, while `validate-feedback-classification` already supports split manifest/classification inputs.
- [x] Define the session artifact-kind and resolution contract.
  - Reserved PR-scoped descriptors use scope-first names (`pr-address-pr-<n>-<kind>`, `pr-address-pr-<n>-batch-<batch-id>-<kind>`, `pr-address-stack-<kind>`). Latest-of-kind resolves exact descriptor + role + `.json` by highest payload sequence; no index file. The contract is recorded in `updates/2026-06-13T150400Z-artifact-taxonomy-and-planning-resolution.md` and reinforced by store-owned lookup work in `updates/2026-06-14T195448Z-payload-store-lookup-boundary.md`.
  - Evidence: descriptor builders, `PayloadArtifactStore.findLatestJsonArtifact`, node and in-memory lookup tests, and PR/stack planning scenario tests. Full TS test and check passed for the payload-store lookup boundary branch.
- [x] Gate artifacts into the store through validation and bootstrap the session.
  - `validate-feedback-classification` persists the validated classification as a session artifact on success when a harness session is configured. Bootstrap is now the harness-owned `HARNESS_SESSION_ID` / `--harness-session-id` contract, not `ASDL_PAYLOAD_SESSION_ID` or caller-side auto-minting.
  - Session-only validation is now the canonical gate: validate resolves the manifest by `--pr-number`, requires exactly one agent-authored classification source, includes `resolved_inputs.manifest`, writes the PR-scoped classification artifact on success, and rejects removed wrapper/manifest/persist flags as unknown options. Evidence: local branch diff against Graphite parent `update-duplicate-abstraction-review-guidance`; full TypeScript check and test passed.
- [x] Migrate planning and read helpers to implicit session resolution.
  - `validate-feedback-classification --pr-number` resolves its manifest from the session and echoes `resolved_inputs.manifest`; `classification-template --pr-number`, `plan-feedback --pr-number`, empty-stdin `stack-feedback-plan`, and empty-stdin `stack-feedback-diff-current` now resolve pipeline-produced inputs only from the payload session and echo `resolved_inputs` where applicable.
  - Evidence: removed explicit manifest/wrapper/payload/reference option tests assert raw usage errors; non-empty stdin tests assert machine `invalid_request`; session-positive tests assert exact resolved input artifacts. `pnpm --dir ts/packages/pr-address run test` and `pnpm --dir ts/packages/pr-address run check` passed.
- [x] Migrate the mutation flow to explicit artifact references.
  - Single-PR mutation uses the explicit reference contract: `build-resolve-thread-batch-payload --pr-number --batch-id --commit-sha --decisions-file` resolves the latest plan, writes a PR/batch resolve-build artifact for every valid build result, and `resolve-thread-batch --from-build <payload-path>` refuses implicit resolution with `explicit_artifact_required` when the reference is omitted. `resolve-thread-batch` writes a managed resolution-result artifact after successful or partial mutation attempts and rejects non-ready build artifacts before mutation.
  - Stack mutation uses the same boundary: `build-stack-resolve-thread-payloads --batch-id --commit-sha --decisions-file` resolves the latest stack plan from the session, writes one managed per-PR resolve-build artifact for each ready entry, and each ready payload is then applied through `resolve-thread-batch --from-build <payload-path>`.
  - Evidence: local branch diff against Graphite parent `validate-feedback-classification-session-only`, PR #1563, current source/reference inspection for `stack-resolve-thread-payloads.ts` and `skills/pr-address/references/cli-mutation.md`, and full TypeScript check/test passed.
- [x] Shrink lifecycle helpers to agent-owned inputs.
  - `record-batch-checkpoint` now takes PR number, batch id, commit SHA, and an evidence file containing only agent-authored validation and non-thread outcome evidence. It derives `changed_files` from the commit and pulls plan, build payload, and resolution result from the session.
  - `finalize-run --pr-number` now resolves the latest plan, latest final feedback artifact, and latest checkpoint artifact for every planned batch from the session. Missing planned checkpoints are explicit `missing_checkpoint_evidence` errors instead of disappearing.
  - Evidence: local branch diff against Graphite parent `validate-feedback-classification-session-only`, PR #1563, and full TypeScript check/test passed.
- [x] Make compact stdout the default across all exec helpers.
  - Digest on stdout (counts, errors, warnings, `resolved_inputs`, produced artifact reference); full envelope in the session artifact; `--stdout-mode full` escape hatch; process exit code already mirrors envelope `exit_code`.
  - Branch `compact-default-stdout-pr-address-exec-helpers` adds shared compact-output infrastructure (`ts/packages/pr-address/src/stdout-mode.ts`), extends `--stdout-mode compact|full` across collection, planning, mutation, stack, payload, and summary helpers, flips compact to the default, preserves full output through session artifacts, and updates schemas plus CLI reference docs.
  - Follow-up branch `compact-default-stdout/json-schema-fixtures` updates generated JSON schema fixtures and stack orchestration fixtures for the compact-output shapes that initially broke the TypeScript gate.
  - Evidence: local branch diff against Graphite parent `compact-default-stdout-pr-address-exec-helpers`, PR #1571, commit `6858d4c2a`, `pnpm --dir ts/packages/pr-address run test` passed, and `pnpm --dir ts/packages/pr-address run check` passed.
- [x] Remove composed-payload input styles for pipeline-produced artifacts.
  - `--payload-file`/`--payload-json`/stdin composition is deleted wherever the input is something the pipeline produced; file input remains only for agent-authored content (decisions, classification answers, validation results, checkpoint evidence).
  - This final cutover removed `classification-template` explicit manifest input, `plan-feedback` wrapper input, `stack-feedback-plan` explicit payload/prep-reference input, and `stack-feedback-diff-current` explicit payload/reference input. Removed flags are raw usage errors, and non-empty stdin to these session-only helpers is a machine `invalid_request`.
  - Evidence: operation request schemas and JSON schema fixtures were regenerated for the narrowed surfaces; source/tests no longer contain those compatibility branches except removed-option assertions and unrelated helpers that still own explicit agent-authored input.
- [x] Rewrite the pr-address skill and CLI references for the session-store flow.
  - `skills/pr-address/SKILL.md` and `references/cli-*.md` now describe the session-store flow for single-PR and stack runs; payload-composition guidance for pipeline-produced artifacts is removed; the hybrid state is resolved.
  - Evidence captured: real PR #1567 ran through `get-feedback`, `classification-template --pr-number`, an agent-authored classification file, `validate-feedback-classification --pr-number --classification-file`, `plan-feedback --pr-number`, refreshed `get-feedback --include-resolved`, and `finalize-run --pr-number` without composed pipeline wrappers. Real stack slice #1563/#1567 ran through `stack-feedback-prep`, per-PR templates/classification validation, `stack-feedback-plan`, fresh `stack-feedback-prep --include-resolved`, and `stack-feedback-diff-current` without composed stack plan/diff wrappers.
  - Evidence sufficiency decision: the real feedback used for evidence was automation feedback classified as informational, so no safe actionable mutation/build/checkpoint evidence was produced. Non-mutating real evidence is accepted as sufficient because the mutation tail is covered by the explicit `--from-build` contract, scenario-tested refusal of implicit mutation inputs, and session artifact build/checkpoint flows; forcing a GitHub write solely for proof is out of scope.

## Parked

- Staleness guard: mutation helpers refusing a build payload when a newer plan artifact exists in the session (forces rebuild after replan). Explicitly excluded during the design interview; revisit if a real run applies a stale payload.
- Classification round-trip tightening: an `apply-classification` merge helper, or having the classify subagent emit schema-valid classification JSON directly.
- `read-feedback-details` ergonomics: repeated `--pointer` flags instead of a composed selection payload. Revisit if it recurs in retros.
