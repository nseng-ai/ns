# Cross-Harness Parity Table

Living tracker for the `cross-harness-parity` Objective. One row per Pi extension command or custom tool. Seeded from the 2026-06-03 audit; refresh on every parity-relevant Objective update and via the parity-review workflow's full-sweep mode (the `parity-review` route of the `internal-code-workflows` skill). Last full sweep: 2026-06-10. Shared primitive workstreams with no Pi command/tool yet, such as the subsumed command-output summaries work, stay in `objective.md` and `roadmap.md` until a Pi surface exists.

**Parity legend**

- **FULL** — shared CLI carries the logic, a skill drives it, Claude/Codex reach the workflow standalone; the Pi part is purely additive (picker, rendering, tool).
- **PARTIAL** — a CLI exists but no skill points to it, or a skill exists but key logic is Pi-only.
- **NONE** — no CLI and no skill; logic trapped in TypeScript.
- **WAIVED** — genuinely Pi-native primitive; acceptable Pi-only because its value is the Pi UI/session behavior, provided dependent workflows document an agent-neutral fallback.

All rows are owned by `cross-harness-parity`. Rows formerly deferred to the now-closed `planned-branch-ts-cli` and `asdl-dev-submit-consolidation` Objectives are folded into the sections below with a provenance note.

## ✅ FULL — Pi is purely additive

| Pi surface                                    | Workflow                        | Shared CLI backing                                        | Skill                                 | Parity | Notes                                                                                                                                                                                                                                                                                   |
| --------------------------------------------- | ------------------------------- | --------------------------------------------------------- | ------------------------------------- | ------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `/objective:list`                             | Objective inventory             | `objective list --format md`                              | `objective`                           | FULL   | Markdown rendering owned by the Python CLI; Pi only wraps a chat card.                                                                                                                                                                                                                  |
| `/objective:next`                             | Recommend next work             | `objective list` + `objective exec read-objective`        | `objective-next`                      | FULL   | Picker + git-changed-first ordering are Pi ergonomics; skill accepts explicit slug.                                                                                                                                                                                                     |
| `/objective:current`                          | Summarize one Objective         | `objective exec read-objective <slug> --format md`        | `objective-current`                   | FULL   |                                                                                                                                                                                                                                                                                         |
| `/objective:update`                           | Update tracking                 | `objective list` + `objective exec read-objective`        | `objective-update`                    | FULL   |                                                                                                                                                                                                                                                                                         |
| `/objective:stack-impl`                       | Implement as a stack            | `objective list` + `objective exec runner-subagent-usage` | `objective-stack-impl`                | FULL   | Uses the WAIVED `dispatch_runner_subagent`; skill degrades (stop-and-ask) when absent.                                                                                                                                                                                                  |
| (no Pi command)                               | Create / close Objective        | `objective list` + `objective exec read-objective`        | `objective-create`, `objective-close` | FULL   | Pure skill + CLI, no Pi layer at all.                                                                                                                                                                                                                                                   |
| `/handoff:create`                             | Save a directed handoff         | `brmem put --namespace handoff --file /dev/stdin`         | `handoff-create`                      | FULL   | Pi adds only the focus prompt.                                                                                                                                                                                                                                                          |
| `/handoff:pickup`                             | Resume a handoff                | `brmem get --namespace handoff`                           | `handoff-pickup`                      | FULL   | Pi adds picker + fuzzy match (mirrored in skill prose).                                                                                                                                                                                                                                 |
| `/handoff:list`                               | List handoffs                   | `handoff list --format json`                              | `handoff-pickup`                      | FULL   | Pi now consumes the dedicated CLI and keeps only the card renderer.                                                                                                                                                                                                                     |
| `/ccc:sidebar:pr-summary`                     | Sidebar from current PR work    | `asdl exec cmux-workspace-summary`                        | `ccc-sidebar`                         | FULL   | Mutation CLI is scenario-tested; Pi adds fast-model swap + auto-trigger.                                                                                                                                                                                                                |
| `/ccc:sidebar:objective-summary`              | Sidebar from an Objective       | `asdl exec cmux-workspace-summary`                        | `ccc-sidebar`                         | FULL   |                                                                                                                                                                                                                                                                                         |
| `/just`                                       | Run `just`, fix failures        | `just` + skill injection                                  | `internal-code-just-fix`              | FULL   | Claude/Codex get the same outcome by running `just` with the skill installed.                                                                                                                                                                                                           |
| `/internal-code-workflows` + 6 route commands | Internal workflow router        | _none needed_ (playbooks are skill references)            | `internal-code-workflows`             | FULL   | Router plus `/internal-code-delete-stack`, `-stackify-branch`, `-stacker-agent`, `-parity-review`, `-stack-address`, `-gh-ci-debug` (with legacy-name aliases). Each Pi command only injects the route; Claude/Codex read the same `references/*.md` playbooks directly from the skill. |
| `/dev:preview-url`                            | Vercel preview URL              | `asdl-dev preview-url`                                    | `dev-preview-url`                     | FULL   | Skill delegates to the shared CLI; Pi only mirrors it under the dev namespace.                                                                                                                                                                                                          |
| `/code:cp`                                    | Checkpoint commit               | `asdl-dev cp`                                             | `internal-code-checkpoint`            | FULL   | Skill now wraps the shared CLI instead of reimplementing checkpoint git logic.                                                                                                                                                                                                          |
| `/code:pr-regen`                              | Regenerate PR title/description | `asdl-dev pr-regen`                                       | `internal-code-submit`                | FULL   | New 2026-06-10 sweep. CLI-bridged Pi command; shared text-repair core across checkpoint and PR-description flows; skill documents the operation.                                                                                                                                        |
| `/code:submit`                                | Submit a Graphite stack         | `asdl-dev submit`                                         | `internal-code-submit`                | FULL   | Folded from closed `asdl-dev-submit-consolidation`; `asdl-dev submit` is canonical and the skill drives it.                                                                                                                                                                             |
| `/plans:write`                                | Author a reviewed Saved plan    | `plans exec write`                                        | `plans-write`                         | FULL   | Folded from closed `planned-branch-ts-cli`; Pi adds prompt dispatch and model-derived slugging.                                                                                                                                                                                         |
| `write_saved_plan_file` (tool)                | Write a Saved plan file         | `plans exec write`                                        | `plans-write`                         | FULL   | Folded from closed `planned-branch-ts-cli`; backed by the `@asdl/plans` Saved-plan file primitive; no branch creation or Branch Memory writes.                                                                                                                                          |
| `/planned-branch:create`                      | Branch from a Saved plan        | `plans exec resolve` + `planned-branch exec create`       | `planned-branch-create`               | FULL   | Folded from closed `planned-branch-ts-cli`.                                                                                                                                                                                                                                             |
| `/planned-branch:impl`                        | Implement attached plan         | `planned-branch exec load-plan`                           | `planned-branch-impl`                 | FULL   | Folded from closed `planned-branch-ts-cli`.                                                                                                                                                                                                                                             |
| `brmem` (internal helper)                     | Branch-scoped memory            | `brmem` CLI (put/get/list/check/copy/...)                 | `brmem`                               | FULL   | TS file only resolves the binary path.                                                                                                                                                                                                                                                  |

## 🔴 Orphan orchestration gaps — owned here

| Pi surface                                  | Workflow                                | Shared CLI backing                                | Skill  | Parity  | Notes                                                                                                                                                                                                                                                                                                                  |
| ------------------------------------------- | --------------------------------------- | ------------------------------------------------- | ------ | ------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `/code:land`                                | Land current PR or whole Graphite stack | _none_ (core lives in `@asdl/ccc`, no bin)        | _none_ | NONE    | Orchestration is consolidated and test-backed in `ccc/src/land.ts` + `land-stack/` (Graphite SQLite-metadata topology, fork-violation detection, backup refs, pre-delete child guards), but `@asdl/ccc` ships no bin and no skill. Remaining work: clinkr-based CLI entry + skill; single-PR path may stay skill-only. |
| `/ccc:workspace:dispatch-plan`              | Plan → branch → slot → cmux             | `slot`/`brmem`/`cmux` prims; no orchestration CLI | _none_ | NONE    | TS sequence + Pi-session plan source. Remaining work: CLI entry with explicit inputs over the ccc cmux modules; Pi keeps session-history plan resolution.                                                                                                                                                              |
| `/ccc:workspace:dispatch-prompt`            | Prompt → branch → slot → cmux           | `git`/`gt`/`slot`/`cmux` prims                    | _none_ | NONE    | Hardcodes `pi @file` launch; shared `launchFocusedCmuxTab` / prompt-file utilities are now extracted (`ccc/src/cmux/focused-terminal-tab.ts`, `prompt-file.ts`); needs CLI entry + agent-neutral launch.                                                                                                               |
| `/ccc:workspace:open-branch`                | Branch → slot → cmux                    | `slot checkout` + `cmux new-workspace`            | _none_ | PARTIAL | Explicit-branch case is two runnable CLIs; needs a thin skill. Planned-branch inference is Pi-only.                                                                                                                                                                                                                    |
| `/code:autobranch`                          | Branch from uncommitted changes         | _none_ (core lives in `ccc/src/autobranch/`)      | _none_ | NONE    | Stash/create/restore/commit transaction and branch-name selection are factored and tested in `ccc/src/autobranch/`; slug derivation already routes through the shared `@asdl/plans` helper. Remaining work: CLI entry + skill; Pi mirrors.                                                                             |
| `/cp-preview` (alias `/checkpoint-preview`) | Preview checkpoint commit message       | `asdl-dev cp` (commit path only)                  | _none_ | PARTIAL | Pi-only preview re-assembles git status/diff prompt context in TS instead of a `asdl-dev cp` preview/dry-run mode. Push the preview mode into the CLI or record a waiver.                                                                                                                                              |
| `/code:changes`                             | Summarize pending changes               | _none_ (Pi-only model draft)                      | _none_ | NONE    | Cosmetic; skill-only or WAIVE.                                                                                                                                                                                                                                                                                         |

## ⚪ WAIVED — genuinely Pi-native primitives (fallback required)

| Pi surface                                                                         | Workflow                                 | Agent-neutral fallback                                                                                                      | Parity | Notes                                                                                                                                                                                                                                                                                      |
| ---------------------------------------------------------------------------------- | ---------------------------------------- | --------------------------------------------------------------------------------------------------------------------------- | ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `dispatch_runner_subagent` (tool)                                                  | Delegate a focused subagent              | Host Task/subagent primitive                                                                                                | WAIVED | Spawns a `pi` subprocess + parses Pi JSONL. Dependent skills (`objective-stack-impl`, `proto`) already stop-and-ask when absent. Telemetry CLI assumes Pi JSONL. Dynamic terminal outcome tools registered by the runner-subagent runtime extension are part of this waiver.               |
| `grill_ask` (tool) + `/grill-ui` + `/grill-with-docs-ui`                           | Structured grilling interview            | `grill-me` / `grill-with-docs` skills (prose interview)                                                                     | WAIVED | TUI is a pure UI accelerator with a documented prose fallback chain; `pi-grill-ui` / `pi-grill-with-docs-ui` are the Pi-side backend skills.                                                                                                                                               |
| `/handoff-tab` + `derive_handoff_slug_from_content` / `handoff_tab_launch` (tools) | Handoff into a focused cmux tab          | `handoff-create` skill, then manual pickup in a new session                                                                 | WAIVED | cmux tab launch and slug derivation are Pi/cmux session UI; the handoff artifact itself stays brmem/`handoff` CLI-backed. Registered only when `pi.registerTool` is available.                                                                                                             |
| `/code:pr-feedback-watch`                                                          | Ambient PR feedback watcher              | `pr-address` skill/CLI manual invocation                                                                                    | WAIVED | Pi owns opt-in live polling and prompt injection; `pr-address exec prepare-run`/`get-feedback` own feedback normalization and GitHub mutations.                                                                                                                                            |
| worktree status line                                                               | Ambient repo/Graphite status             | `git`/`gt`/`brmem` queries                                                                                                  | WAIVED | Live TUI widget; no command contract. Underlying facts independently queryable.                                                                                                                                                                                                            |
| `/context-profiler`                                                                | Episodic context profiling overlay       | none — TUI-native interactive diagnostic over live session events (`context`, `before_agent_start`); no dependent workflows | WAIVED | Headless transcript profiling is an explicit Non-Goal of the `context-profiler` Objective.                                                                                                                                                                                                 |
| `/model:fable`, `/model:spud`, `/model:gpt-mini`, `/model:haiku`, `/model:opus`    | Switch the live Pi session model         | each harness's own model selection (e.g. Claude Code `/model`, Codex model config)                                          | WAIVED | New 2026-06-10 sweep. One row for the family (`pi-extensions/src/model-shortcuts.ts`); session model switching is harness-native UI with no workflow logic to share.                                                                                                                       |
| `/ccc:claude-plan-tab`                                                             | Open Claude Code plan mode in a cmux tab | `plans-write` / `planned-branch-create` / `planned-branch-impl` skills, or run `claude` in plan mode manually               | WAIVED | New 2026-06-10 sweep (`ccc/src/cmux/claude-plan-tab.ts`). Seeds from the last Pi assistant message and launches a focused cmux tab — Pi/cmux session-native by construction; the underlying `launchFocusedCmuxTab` / prompt-file utilities are shared and feed the dispatch push-down row. |
| `/plans:grill-and-write`                                                           | Grilled Saved plan authoring             | `grill-me` skill then `plans-write` skill                                                                                   | WAIVED | Folded from closed `planned-branch-ts-cli`. Structured `grill_ask` UI is Pi-native; resulting artifact is the same Saved plan written through `write_saved_plan_file` / `plans exec write`.                                                                                                |
| `/planned-branch:upstack-impl-session`                                             | Create branch and launch implementation  | `planned-branch-create` + `planned-branch-impl` skills                                                                      | WAIVED | Adopted from closed `planned-branch-ts-cli` (renamed from `/planned-branch:up-and-impl`). Value is Pi/cmux session orchestration over the portable create/impl pieces, which stay independently skill-reachable.                                                                           |
