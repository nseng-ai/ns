# Cross-Harness Parity Table

Living tracker for the `cross-harness-parity` Objective. One row per Pi extension command, plus selected non-tool UI primitives when this Objective explicitly tracks them. Seeded from the 2026-06-03 audit; refresh on every parity-relevant Objective update and via the parity-review workflow's full-sweep mode (the `parity-review` route of the `code-workflows` skill, renamed 2026-06-11 from `internal-code-workflows`). Last full sweep: 2026-06-10; last targeted refresh: 2026-06-13. Shared primitive workstreams with no Pi command yet, such as the subsumed command-output summaries work, stay in `objective.md` and `roadmap.md` until a Pi command surface exists.

**Machine-checkable source note:** the v1 accounting contract for command surfaces implemented by `@asdl/pi-extensions` package modules lives in typed TypeScript metadata (`ts/packages/pi-extensions/src/parity.ts`, `parity-registry.ts`) and is enforced by `ts/packages/pi-extensions/test/parity.test.ts`. That Vitest gate registers package modules against a fake Pi host and checks for missing, stale, or duplicate exact command metadata; Pi tool calls are host-native bridges and do not require parity metadata. If a command depends on a tool, the command row owns the fallback. The gate does not parse this Markdown table and does not cover ad hoc `.pi/extensions/*.ts` adapters or direct `@asdl/ccc` surfaces unless they are exposed through `@asdl/pi-extensions`.

**Parity legend**

- **FULL** — shared CLI carries the logic, a skill drives it, Claude/Codex reach the workflow standalone; the Pi part is purely additive (picker, rendering, tool).
- **PARTIAL** — a CLI exists but no skill points to it, or a skill exists but key logic is Pi-only.
- **NONE** — no CLI and no skill; logic trapped in TypeScript.
- **WAIVED** — genuinely Pi-native primitive; acceptable Pi-only because its value is the Pi UI/session behavior, provided dependent workflows document an agent-neutral fallback.

All rows are owned by `cross-harness-parity`. Rows formerly deferred to the now-closed `planned-branch-ts-cli` and `asdl-dev-submit-consolidation` Objectives are folded into the sections below with a provenance note.

## ✅ FULL — Pi is purely additive

| Pi surface                       | Workflow                        | Shared CLI backing                                             | Skill                                 | Parity | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| -------------------------------- | ------------------------------- | -------------------------------------------------------------- | ------------------------------------- | ------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `/objective:list`                | Objective inventory             | `objective list --format md`                                   | `objective`                           | FULL   | Markdown rendering owned by the Python CLI; Pi only wraps a chat card.                                                                                                                                                                                                                                                                                                                                                                                                 |
| `/objective:next`                | Recommend next work             | `objective list` + `objective exec read-objective`             | `objective-next`                      | FULL   | Picker + git-changed-first ordering are Pi ergonomics; skill accepts explicit slug.                                                                                                                                                                                                                                                                                                                                                                                    |
| `/objective:current`             | Summarize one Objective         | `objective exec read-objective <slug> --format md`             | `objective-current`                   | FULL   |                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `/objective:update`              | Update tracking                 | `objective list` + `objective exec read-objective`             | `objective-update`                    | FULL   |                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `/objective:stack-impl`          | Implement as a stack            | `objective list` + `objective exec runner-subagent-usage`      | `objective-stack-impl`                | FULL   | Uses Pi's `dispatch_runner_subagent` tool when available; skill degrades (stop-and-ask) when absent. Tool-call surfaces do not require parity rows.                                                                                                                                                                                                                                                                                                                    |
| (no Pi command)                  | Create / close Objective        | `objective list` + `objective exec read-objective`             | `objective-create`, `objective-close` | FULL   | Pure skill + CLI, no Pi layer at all.                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `/handoff:create`                | Save a directed handoff         | `brmem put --namespace handoff --file /dev/stdin`              | `handoff-create`                      | FULL   | Pi adds only the focus prompt.                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `/handoff:pickup`                | Resume a handoff                | `brmem get --namespace handoff`                                | `handoff-pickup`                      | FULL   | Pi adds picker + fuzzy match (mirrored in skill prose).                                                                                                                                                                                                                                                                                                                                                                                                                |
| `/handoff:list`                  | List handoffs                   | `handoff list --format json`                                   | `handoff-pickup`                      | FULL   | Pi now consumes the dedicated CLI and keeps only the card renderer.                                                                                                                                                                                                                                                                                                                                                                                                    |
| `/ccc:sidebar:pr-summary`        | Sidebar from current PR work    | `asdl exec cmux-workspace-summary`                             | `ccc-sidebar`                         | FULL   | Mutation CLI is scenario-tested; Pi adds fast-model swap + auto-trigger.                                                                                                                                                                                                                                                                                                                                                                                               |
| `/ccc:sidebar:objective-summary` | Sidebar from an Objective       | `asdl exec cmux-workspace-summary`                             | `ccc-sidebar`                         | FULL   |                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `/just`                          | Run `just`, fix failures        | `just` + skill injection                                       | `code-just-fix`                       | FULL   | Claude/Codex get the same outcome by running `just` with the skill installed. Skill renamed 2026-06-11 from `internal-code-just-fix`.                                                                                                                                                                                                                                                                                                                                  |
| `/code-workflows`                | Code workflow route selector    | *none needed* (playbooks are skill references)                 | `code-workflows`                      | FULL   | Renamed 2026-06-11 from `/internal-code-workflows`; the six per-route Pi commands were replaced by one selector command with route autocomplete (`delete-stack`, `stackify-branch`, `stacker-agent`, `parity-review`, `gh-ci-debug`). The Pi command only places the route prompt; Claude/Codex read the same `references/*.md` playbooks from the skill. `stack-address` left the router for the standalone `stack-address` skill (skill-only; no Pi command needed). |
| `/dev:preview-url`               | Vercel preview URL              | `asdl-dev preview-url`                                         | `dev-preview-url`                     | FULL   | Skill delegates to the shared CLI; Pi only mirrors it under the dev namespace.                                                                                                                                                                                                                                                                                                                                                                                         |
| `/code:cp`                       | Checkpoint commit               | `asdl-dev cp`                                                  | `code-checkpoint`                     | FULL   | Skill now wraps the shared CLI instead of reimplementing checkpoint git logic. Skill renamed 2026-06-11 from `internal-code-checkpoint`.                                                                                                                                                                                                                                                                                                                               |
| `/code:pr-regen`                 | Regenerate PR title/description | `asdl-dev pr-regen`                                            | `sdl-submit`                          | FULL   | CLI-bridged Pi command remains under `asdl-dev pr-regen` until its own SDL migration decision; shared text-repair core across checkpoint and PR-description flows.                                                                                                                                                                                                                                                                                                     |
| `/sdl:submit`                    | Submit a Graphite stack         | `sdl submit`                                                   | `sdl-submit`                          | FULL   | Submit hard-cutover moved the canonical workflow to the repo-local SDL command module. `asdl-dev submit` and `/code:submit` are removed rather than retained as compatibility aliases.                                                                                                                                                                                                                                                                                 |
| `/code:autobranch`               | Branch current work             | `ccc exec autobranch`                                          | `code-autobranch`                     | FULL   | Hidden `ccc exec` surface for agents; dirty-worktree and latest-commit paths share the existing CCC autobranch core, while Pi `/code:autobranch` is a CLI-bridged command registered through `registerCliCommandExtension`. Graphite/`gt create` is part of the command contract.                                                                                                                                                                                      |
| `/enriched-plan:save`            | Author a reviewed enriched plan | `enriched-plan exec save`                                      | `enriched-plan-save`                  | FULL   | Folded from closed `planned-branch-ts-cli`; Pi adds prompt dispatch and model-derived slugging.                                                                                                                                                                                                                                                                                                                                                                        |
| `/branch-context:from-plan`      | Branch from an enriched plan    | `enriched-plan exec resolve` + `branch-context exec from-plan` | `from-plan`                           | FULL   | Folded from closed `planned-branch-ts-cli`.                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `/branch-context:impl`           | Implement attached plan         | `branch-context exec load`                                     | `branch-context-impl`                 | FULL   | Folded from closed `planned-branch-ts-cli`.                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `brmem` (internal helper)        | Branch-scoped memory            | `brmem` CLI (put/get/list/check/copy/...)                      | `brmem`                               | FULL   | TS file only resolves the binary path.                                                                                                                                                                                                                                                                                                                                                                                                                                 |

## 🔴 Orphan orchestration gaps — owned here

| Pi surface                       | Workflow                                | Shared CLI backing                                | Skill  | Parity  | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| -------------------------------- | --------------------------------------- | ------------------------------------------------- | ------ | ------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `/code:land`                     | Land current PR or whole Graphite stack | extracted `@asdl/ccc` core; no bin                | *none* | PARTIAL | Orchestration is consolidated and test-backed in `ccc/src/land.ts` + `land-stack/` (Graphite SQLite-metadata topology, fork-violation detection, backup refs, pre-delete child guards), but `@asdl/ccc` ships no bin and no skill. Remaining work: clinkr-based CLI entry + skill; single-PR path may stay skill-only. Core hardened 2026-06-11/12 (cleanup, backup-ref rotation, post-submit validation) but reachability is still incomplete; PARTIAL means shared core exists, not that Claude/Codex can run the workflow yet. |
| `/code:autoslot`                 | Branch current work into managed slot   | extracted `@asdl/ccc` core; no dedicated skill    | *none* | PARTIAL | Pi delegates to CCC autoslot orchestration for source-control plus slot movement. Remaining work: decide whether to add a portable skill/CLI path for the slot-moving variant or record a deliberate WAIVED verdict. PARTIAL is retained because the command mutates branch/worktree/slot state rather than being cosmetic Pi UI.                                                                                                                                                                                                 |
| `/code:push`                     | Push already-committed current branch   | `git status` + `git push` primitives              | *none* | PARTIAL | Pi adds a clean-worktree guard and message rendering around `git push`. Non-Pi agents can run `git status` and `git push` directly, but no installed skill owns the guarded workflow yet. Remaining work: decide whether to add a thin skill or treat this as primitive git usage.                                                                                                                                                                                                                                                |
| `/ccc:workspace:dispatch-plan`   | Plan → branch → slot → cmux             | `slot`/`brmem`/`cmux` prims; no orchestration CLI | *none* | NONE    | TS sequence + Pi-session plan source. Remaining work: CLI entry with explicit inputs over the ccc cmux modules; Pi keeps session-history plan resolution.                                                                                                                                                                                                                                                                                                                                                                         |
| `/ccc:workspace:dispatch-prompt` | Prompt → branch → slot → cmux           | `git`/`gt`/`slot`/`cmux` prims                    | *none* | NONE    | Hardcodes `pi @file` launch; shared `launchFocusedCmuxTab` / prompt-file utilities are now extracted (`ccc/src/cmux/focused-terminal-tab.ts`, `prompt-file.ts`); needs CLI entry + agent-neutral launch.                                                                                                                                                                                                                                                                                                                          |
| `/ccc:workspace:open-branch`     | Branch → slot → cmux                    | `slot checkout` + `cmux new-workspace`            | *none* | PARTIAL | Explicit-branch case is two runnable CLIs; needs a thin skill. Planned-branch inference is Pi-only.                                                                                                                                                                                                                                                                                                                                                                                                                               |

## ⚪ WAIVED — genuinely Pi-native primitives (fallback required)

| Pi surface                                                                      | Workflow                                        | Agent-neutral fallback                                                                                                      | Parity | Notes                                                                                                                                                                                                                                                                                                                                                          |
| ------------------------------------------------------------------------------- | ----------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------- | ------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `/pi:grill-me` + `/pi:grill-with-docs`                                          | Structured grilling interview                   | `grill-me` / `grill-with-docs` skills (prose interview)                                                                     | WAIVED | TUI is a pure UI accelerator with a documented prose fallback chain; `pi-grill-ui` / `pi-grill-with-docs-ui` are the Pi-side backend skills. The underlying `grill_ask` tool is a Pi-native host bridge and does not require a separate parity row.                                                                                                            |
| `/ccc:handoff-tab`                                                              | Handoff into a focused cmux tab                 | `handoff-create` skill, then manual pickup in a new session                                                                 | WAIVED | cmux tab launch and slug derivation are Pi/cmux session UI; the handoff artifact itself stays brmem/`handoff` CLI-backed. The `derive_handoff_slug_from_content` and `handoff_tab_launch` tools are Pi-native host bridges and do not require separate parity rows.                                                                                            |
| `/claude:handoff`                                                               | Handoff into an interactive Claude Code session | `handoff-create` skill, then `handoff-pickup` in a new Claude Code session                                                  | WAIVED | New 2026-06-12 (`pi-extensions/src/claude/handoff-command.ts`). Creates the handoff through the same shared brmem/`handoff`-CLI-backed launch flow as `/ccc:handoff-tab`, then stops the TUI and hands the terminal to an interactive `claude` pickup session — Pi-TUI session-native by construction. The artifact and pickup contract stay CLI/skill-backed. |
| `/code:pr-feedback-watch`                                                       | Ambient PR feedback watcher                     | `pr-address` skill/CLI manual invocation                                                                                    | WAIVED | Pi owns opt-in live polling and prompt injection; `pr-address exec prepare-run`/`get-feedback` own feedback normalization and GitHub mutations.                                                                                                                                                                                                                |
| `/code:changes`                                                                 | Summarize pending changes                       | `git status`/`git diff` plus prose summary in the current harness                                                           | WAIVED | Read-only Pi UX helper. Underlying facts are ordinary git/worktree evidence; the command value is a Pi model-drafted message card, not a shared workflow that needs a CLI/skill.                                                                                                                                                                               |
| worktree status line                                                            | Ambient repo/Graphite status                    | `git`/`gt`/`brmem` queries                                                                                                  | WAIVED | Live TUI widget; no command contract. Underlying facts independently queryable.                                                                                                                                                                                                                                                                                |
| `/context-profiler`                                                             | Episodic context profiling overlay              | none — TUI-native interactive diagnostic over live session events (`context`, `before_agent_start`); no dependent workflows | WAIVED | Headless transcript profiling is an explicit Non-Goal of the `context-profiler` Objective.                                                                                                                                                                                                                                                                     |
| `/model:fable`, `/model:spud`, `/model:gpt-mini`, `/model:haiku`, `/model:opus` | Switch the live Pi session model                | each harness's own model selection (e.g. Claude Code `/model`, Codex model config)                                          | WAIVED | New 2026-06-10 sweep. One row for the family (`pi-extensions/src/model-shortcuts.ts`); session model switching is harness-native UI with no workflow logic to share.                                                                                                                                                                                           |
| `/ccc:claude-plan-tab`                                                          | Open Claude Code plan mode in a cmux tab        | `enriched-plan-save` / `from-plan` / `branch-context-impl` skills, or run `claude` in plan mode manually                    | WAIVED | New 2026-06-10 sweep (`ccc/src/cmux/claude-plan-tab.ts`). Seeds from the last Pi assistant message and launches a focused cmux tab — Pi/cmux session-native by construction; the underlying `launchFocusedCmuxTab` / prompt-file utilities are shared and feed the dispatch push-down row.                                                                     |
| `/enriched-plan:grill-and-save`                                                 | Grilled enriched-plan authoring                 | `grill-me` skill then `enriched-plan-save` skill                                                                            | WAIVED | Folded from closed `planned-branch-ts-cli`. Structured grill UI is Pi-native; resulting artifact is the same enriched plan written through `write_saved_plan_file` / `enriched-plan exec save`.                                                                                                                                                                |
| `/branch-context:upstack-impl-session`                                          | Create branch and launch implementation         | `from-plan` + `branch-context-impl` skills                                                                                  | WAIVED | Adopted from closed `planned-branch-ts-cli` (renamed from `/planned-branch:up-and-impl`). Value is Pi/cmux session orchestration over the portable create/impl pieces, which stay independently skill-reachable.                                                                                                                                               |
