# areg promoted out of sequence

## Summary

`areg` is promoted from parked pending evidence into the active out-of-sequence `areg-typescript-port` subobjective.

This is an explicit exception to the persisted default capability order. The normal sequence after `brmem` remains `handoff`, `objective`, `asdl-dispatcher`, roaster, `slot`, `vibechk`, then `aretro`, but present strategic interest in making `areg` TypeScript-default now is strong enough to advance it before `handoff` and `objective` rather than leaving it parked.

The active `areg-typescript-port` subobjective records the detailed slice scope: preserve the standalone `areg` CLI, inventory `init`, `check`, `update-skills`, `command convert|revert|list`, and hidden `exec skillx parse|list|fetch|cleanup` contracts, decide the TypeScript distribution model, update callers/docs, and retire the Python package only after parity and rollback/reference evidence are recorded.

## Objective Impact

The umbrella migration ledger now marks `areg` as an active out-of-sequence subobjective instead of parked pending evidence. The planned capability order remains the durable default, but now includes an explicit exception note so future agents do not silently treat `handoff` as the only valid next slice while `areg-typescript-port` is active.

The umbrella roadmap now records the `areg` promotion as completed tracking work and keeps the broader repeat-capability pattern open. The parked-entry open question is resolved for `areg`; `packagechk` remains parked pending evidence.

## Follow-Ups

- Execute `areg-typescript-port` from its contract-inventory row before implementation.
- When the `areg` slice completes or parks again, record the outcome back in this umbrella Objective and then resume the remaining default order deliberately.
- Preserve reusable `areg` lessons, especially around skill-lock parsing, managed project-file mutation, external `npx skills` behavior, and distribution, in the umbrella playbook only when they are proven beyond this one package.
