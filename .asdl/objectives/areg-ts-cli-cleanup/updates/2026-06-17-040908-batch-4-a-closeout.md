# Batch 4 A Gateway-Collapse Closeout

## Summary

Batch 4 finding A is complete by decision, with a deliberately deferred follow-up boundary. The landed shared project-inspection core in `ts/packages/areg/src/operations/project-inspection.ts` now owns the common base/facet inspection flow and exposes thin wrappers for `areg check`, `areg skill list/show/apply`, `areg init`, and `areg update-skills`.

The remaining `real-gateways.ts` / `fake-gateways.ts` monolith split is intentionally deferred. Evidence from the landed slice shows the duplication was in operation-layer project-inspection choreography, not in a missing real/fake gateway capability split: the current branch diff against `areg-preflight-partial-state-evidence` changes command operations to import the shared wrappers and does not require real/fake gateway edits for the shared-core behavior.

## Objective Impact

The roadmap now marks the Batch 4 A gateway-collapse row complete. Its completion shape is the lower-risk shared operation-layer core plus thin per-feature wrappers, not a new durable `AregProjectInspectionGateway` interface. The deferred real/fake gateway file split is recorded as intentional because doing it now would be a broad file-organization refactor without additional semantic payoff, and the upcoming `skill-kind.ts` decomposition is a better place to discover any next proven capability seam.

The A open question in `objective.md` is resolved accordingly: stop at the shared `inspectProject` / project-inspection core unless later work proves a narrower gateway split is needed.

## Follow-Ups

- Continue Batch 4 with finding J: split `skill-kind.ts` into inference, apply-plan, and frontmatter-edit responsibilities, and document why frontmatter parse and rewrite cannot share one parser.
- Reconsider a real/fake gateway split only if the J slice or later Batch 5 work exposes a concrete capability seam; do not perform a monolith split merely for file aesthetics.
