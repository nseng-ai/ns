# Roadmap

## Work

- [x] Rephase the Objective from completed Pi resource-surface cleanup into current agent skill and command surface consolidation, keeping the durable slug while replacing stale closure assumptions.
- [x] Run a fresh cross-surface inventory and capture the evidence: first-party `skills/<name>/SKILL.md` metadata and line counts, `.agents/skills/` and `.claude/skills/` provenance, `skills-lock.json` state, Pi RPC command inventory, `.pi/extensions/`, `.pi/prompts/`, relevant CLI `exec` helpers, and agent instruction/catalog docs; evidence and initial cluster/disposition map are recorded in `docs/agent-resource-catalog.md` and the 2026-05-31 inventory update.
- [x] Define and document the consolidation taxonomy and disposition format for agent resources: public portable workflow skills, command skills, internal/dev skills, prototype skills, Pi-only adapters, CLI primitives, remote/vendored skills, and user-local personal resources; current policy lives in `docs/agent-resource-catalog.md`, `docs/pi/README.md`, and `AGENTS.md`, including the `code-`/`dev-` prefix boundary.
- [x] Audit first-party skills cluster-by-cluster and assign dispositions. The final catalog categorizes all 42 first-party skill commands by mode, routes public code/source-control work through `code-*`, routes repo-private code/source-control work through `internal-code-*` plus `metadata.internal: true`, records Pi UI/internal helpers and the prototype runner explicitly, and accepts or parks deeper trigger/body/push-down decisions as follow-up work rather than closure blockers.
- [x] Apply low-risk first-party skill cleanup found by the audit: fixed stale H1 and `Original description` scaffolding findings, removed contradictory public/internal comments from internal skills, verified command-skill frontmatter and installed symlinks, and accepted the remaining local-only `PENDING_REGEN` lock entries as harmless install-time metadata per skill-management guidance. Larger trigger/body/push-down decisions remain part of the broader first-party disposition work rather than this low-risk hygiene row.
- [x] Consolidate, rename, hide/internalize, or remove duplicate and obsolete first-party skills or command wrappers after their dispositions are agreed; update `skills/`, `.agents/skills/`, `.claude/skills/`, and `skills-lock.json` through the repo's skill-management conventions. The code/source-control rename is complete for the local code family: public workflows use `code-*` (`code-gt-restack-resolve`, `code-resolve-merge-conflicts`), repo-private workflows use `internal-code-*` (`internal-code-checkpoint`, `internal-code-gh`, `internal-code-gh-ci-debug`, `internal-code-gt-stackify-branch`, `internal-code-just-fix`, `internal-code-stacker-agent`), and `/dev:cp`/`/dev:submit` moved to `/code:*`. No additional duplicate first-party removal or rename was selected for this Objective; `proto-objective-impl`, PR-address automation, and vendored deep rewrites are explicitly parked.
- [x] Review the remote/vendored installed skill set and decide a repo policy for keeping, removing, or documenting those entries without treating vendored code as first-party audit material; the current policy keeps real-directory external skills live by default as explicit developer aids and excludes them from first-party deep review unless explicitly requested.
- [x] Update `docs/agent-resource-catalog.md`, `docs/pi/README.md`, `AGENTS.md`, `CLAUDE.md`, and any affected skill docs so they route agents to the consolidated surface and remove stale names from earlier Objective phases. The final stale-name pass corrected the stale landing-history row, updated the catalog for live `/grill-ui`, `grill_ask`, `/proto:objective-impl`, TypeScript skill, and internal/prototype surfaces, and confirmed remaining old-name hits are either explicit absence statements, historical design notes, current canonical names, or parked prototype/PR-address references.
- [x] Re-run post-change inventories and relevant validation, then record completion evidence and any accepted residual risks in an Objective update. Final evidence records skill/install counts, Pi RPC command inventory, stale-name search results, accepted parked residual risks, `git diff --check`, and Markdown formatting validation.

## Parked

- [ ] User-local CMUX, `gh-pr`, `stack-latest`, and other personal-machine resources remain advisory unless explicitly requested.
- [ ] Deep rewrites of GitHub-sourced or vendored skills are parked unless a separate explicit decision treats one as first-party work.
- [ ] Dedicated Codex-specific configuration remains parked unless the inventory finds a concrete gap that `AGENTS.md`, first-party skills, and CLI/docs workflows cannot cover.
- [ ] Broader Objective runner or standing-objective product redesign is parked unless this audit finds a direct surface-area cleanup action; deeper product design belongs in its own Objective.
- [ ] Prototype Objective runner lifecycle disposition (`proto-objective-impl` / `/proto:objective-impl`: keep separate, merge into `objective-stack-impl`, promote, or retire) is parked for now; only low-risk labeling/docs hygiene around its prototype/internal status is in scope unless explicitly unparked.
- [ ] PR-address/review automation disposition and CLI push-down work, including possible batch thread-resolution or compact feedback-summary helpers, is parked for next-work selection because the user is already working that slice separately; record landed evidence later instead of selecting it as the next Objective slice.
