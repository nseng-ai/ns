# Objective Refresh Rewritten

## Summary

`skills/objective-refresh/SKILL.md` was rewritten as the next behavior-preserving skill-content remediation slice. The rewrite preserved the existing frontmatter and `normal` invocation kind, kept branch/repo orchestration references as contract sources, and left `skills/objective-refresh/references/branch-scope.md` and `skills/objective-refresh/references/repo-scope.md` unchanged.

The body now co-locates the duplicated no-closure, immutable Semantic Update, slug-directory, baseline-prefix, final-report, and verify rules under one canonical per-slug flow. Line count moved from 205 to 182 while preserving command contracts, safety probes, stop/ask conditions, action labels, and the provenance breadcrumb.

## Objective Impact

This completes the planned `objective-refresh` rewrite in the high-reach objective-family sequence. The contract-diff review retained the required routing pointers, `objective list --minimal --format md`, real-branch check, baseline lookup commands, dirty Objective probe, `[objective-refresh]` commit examples, `[objective-branch-refresh]` read-compatibility rule, no-closure invariant, and final action labels.

Validation evidence for the slice: `git diff --check`, `areg check`, and `just dprint-check` passed after the rewrite and Objective tracking update.

## Follow-Ups

Continue the objective-family remediation sequence with `objective-update`, then `objective-create`. Do not broaden this completed slice into branch/repo reference edits unless a future contract review finds live semantic drift in those reference files.
