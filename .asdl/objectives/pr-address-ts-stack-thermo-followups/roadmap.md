# Roadmap

All paths are relative to `ts/packages/pr-address/` unless prefixed. Line numbers are as of commit `147337e28` (`pr-address-ts/test-scaffolding`); symbols are authoritative when lines have drifted. Execute rows in order (dependencies: 2→3; 2, 4, 6 before 8). One branch per row, stacked on `pr-address-ts/test-scaffolding`. Per-branch validation: `pnpm --dir ts/packages/pr-address run check` and `pnpm --dir ts/packages/pr-address run test`; rows 7 and 9 add `pnpm --dir ts run test`; stack tip adds `pnpm --dir ts run check` and `pnpm --dir ts run test`. Fixtures must be byte-identical except where a row explicitly sanctions an update.

## Work

- [ ] Dead-code and dedup sweep — branch `pr-address-ts/fu-dead-code-dedup`.
      Thesis: delete dead ported API and consolidate verbatim-duplicated helpers onto canonical homes, with zero behavior change.
  - Delete `buildStackFeedbackThreadIndex` and `interface StackFeedbackThreadIndex` (`src/stack-feedback-thread-index.ts:25-46`) and `selectedBatchReviewThreadItems` (`:74-76`) — zero production consumers; only `test/unit/stack-feedback-thread-index.test.ts` imports them. Delete the corresponding test cases. Keep the helpers that production does consume (`threadKey`, `threadKeyString`, `reviewThreadKeys`, `duplicateThreadKeys`, and peers — verify consumers with `rg` before deleting anything else). Note `stack-resolve-thread-payloads.ts:203` inlines the same filter `selectedBatch.items.filter((item) => item.source_kind === "review_thread")`; leave that inline (do not resurrect the helper for one call site).
  - Delete the dead re-export block at `src/resolve-thread-batch-payload.ts:493` (`export type { BuildResolveThreadBatchPayloadResult, FeedbackPlan, FeedbackPlanActionItem, FeedbackPlanBatch }`) and the `FeedbackPlanConsumer as FeedbackPlan` import-rename laundering at line 9 — `rg` confirms zero importers; consumers use `feedback-plan-contracts.ts` directly.
  - Consolidate `pythonRepr`: canonical home is `src/string-values.ts` (created by this stack as the shared string-helper module). Four current copies: `src/payload-store.ts:298` (exported), `src/read-feedback-detail.ts:334`, `src/stack-feedback.ts:941` (named `pythonStringRepr`), `src/classification-validation.ts:504` (nullable variant). First diff the four implementations; keep one `pythonRepr(value: string): string` plus, if the nullable variant differs beyond null handling, a thin `pythonReprOrNull` wrapper. All call sites rethreaded; envelope fixtures must stay byte-identical (these feed error-message bytes).
  - Consolidate duplicate detection into one ordered-first-occurrence helper (suggest `duplicateValues<T>(values: readonly T[]): T[]` in `src/string-values.ts` or a small `src/collections.ts`): replaces `duplicateValues` (`src/stack-feedback.ts:920`, also consumed by `src/map-branch-prs.ts:108`), `duplicateNumbers` (`src/stack-feedback-diff-current.ts:414` — currently O(n²) via `duplicates.includes`), and lets `duplicateThreadKeys` (`src/stack-feedback-thread-index.ts:118`) delegate after serialization. Preserve output ordering semantics exactly (ordered by first duplicate occurrence) — fixtures pin messages built from these.
  - Dedupe single-definition constants/helpers: `VALID_RESOLUTION_MODES` re-declared at `src/resolve-thread-batch-payload.ts:19` (canonical export exists at `src/reply-formatting.ts:1`); derive `isResolutionMode` (`src/resolve-thread-batch-payload.ts:488-490`) from the array instead of re-hardcoding the four literals; `provenanceShapeError` duplicated verbatim (`src/resolve-thread-batch-payload.ts:386`, `src/mutation-operations.ts:353`); `firstDuplicatePayloadThreadId` duplicated verbatim (`src/resolve-thread-batch-payload.ts:479`, `src/stack-resolve-thread-payloads.ts:519`). Pick the canonical home per helper (prefer the contracts/support module both consumers already import) and delete the copies.
  - Fix name collisions: rename the local `type ThreadKey = string` in `src/stack-resolve-thread-payloads.ts:72` to `SerializedThreadKey` (the tuple `ThreadKey` in `stack-feedback-thread-index.ts:3` keeps the name); rename the sequence clock in `test/gateways/payload-store.test.ts:54-65` (currently `fixedClock`, colliding with `test/support/run-scenario.ts:54`'s constant clock) to `sequenceClock`.
  - Remove the unused `type ActionComplexity` import at `src/stack-feedback.ts:11`.
  - Fix stale references to the deleted `operation-schemas.ts`: comment at `src/exec-operation.ts:34` ("from the pinned Python-parity builders in `operation-schemas.ts`") and `README.md:21`.
  - Rename `test/scenario/classification-core.test.ts` → `test/scenario/classification-operations.test.ts` (the module it names was deleted by the decomposition; its describe block is "managed classification/planning CLI operations").
  - Evidence: full package check/test green; `git diff` over `test/` fixture files empty; grep proofs in the commit message for the deleted symbols (zero remaining references).

- [ ] Resolved-schema validation in `loadOperationPayload` — branch `pr-address-ts/fu-payload-resolver-parse`.
      Thesis: the shared payload resolver validates the assembled record once at the end, deleting all four remaining payload casts.
  - In `src/json-input.ts`: extract the duplicated JSON.parse→safeParse→error construction shared by `loadArtifactReference` (`:107-130`) and `loadJsonInput` (`:227-250`) into one private `parseJsonWithSchema(text, schema, describe)` helper — the two paths differ only in message prefixes, which the `describe` argument carries. Error message bytes must be identical before/after (fixtures pin them).
  - Change `loadOperationPayload` to validate the assembled `resolvedPayload` record against `options.payloadSchema` via `safeParse` immediately before returning. On success return the parsed value (typed `TPayload`, no cast). On failure, throw an invariant `Error` (programmer error) — do NOT emit a new user-facing envelope: every part of the record was already individually validated (embedded payloads by `payloadSchema` at load, references by their `referenceSchema`), so post-assembly mismatch means the field specs and the payload schema disagree, which is a bug in the operation's spec, not user input. Document this contract in a comment on the throw.
  - This deletes: `resolvedPayload as TPayload` (`json-input.ts:215`) and `resolvedPayload[field.key] as TPayload[typeof field.key] | undefined` (`:204` — after the final parse exists, the intermediate accumulator can be typed `Record<string, unknown>` honestly and the per-field cast removed).
  - Then delete the two call-site narrowing casts that rode on the unvalidated return: `payloadResult.value as StackFeedbackPlanInput` (`src/stack-feedback.ts:410`) and `payloadResult.value as { stack_plan: unknown; current_prep: unknown }` (`src/stack-feedback-diff-current.ts:136`). The diff-current case is the `payloadOptionalWhenAllFieldsReferenced` path where no schema previously ran at all — confirm its `payloadSchema` accepts the assembled all-references record (its fields are `z.unknown()`, so it does), and that the resolver now runs that schema in this path too.
  - Optional in the same branch if trivial: rewrite the triple-boolean `shouldLoadPayload` condition (`json-input.ts:185`) positively, and fix the decorative `unknown | null` field types (`src/stack-feedback.ts:201,234` → `unknown`).
  - Evidence: package check/test green; zero `as TPayload`/`as StackFeedbackPlanInput` remnants (`rg` proof); all envelope and golden fixtures byte-identical; the new invariant throw covered by one unit test with a deliberately inconsistent spec.

- [ ] Classification onto canonical json-input — branch `pr-address-ts/fu-classification-json-input`. Depends on the previous row.
      Thesis: `classification-operations.ts` consumes the canonical `json-input.ts` loader instead of its private re-implementation.
  - Delete from `src/classification-operations.ts`: `loadJsonObjectSource` (`:192-224` — re-implements `loadJsonInput` with an `isRecord` check in place of a schema), the verbatim-copied `jsonParseMessage` (`:226-229`, copy of `json-input.ts:309-312`), and the third competing result shape `LoadPayloadResult` (`:179-190`, flat `{type,errorType,message}` vs the canonical nested `JsonInputResult`). Adopt `JsonInputResult` and delete the adapter re-wrapping at the call sites (`:91, :109, :134, :212`).
  - Message-byte constraint: the classification operations' invalid-JSON / not-an-object error strings are pinned by envelope fixtures. Before rewiring, capture the exact current messages from the fixtures; drive `loadJsonInput` with a schema that reproduces them (e.g. `z.record(z.string(), z.unknown())` if its failure message matches, otherwise keep a minimal record-shape check layered on the canonical loader). If a message genuinely cannot be reproduced through the canonical path, add a small `loadJsonRecord` helper to `json-input.ts` that owns the wording — do not keep a private copy in classification-operations.
  - Evidence: package check/test green; classification scenario/golden fixtures byte-identical; `rg "jsonParseMessage" src` returns exactly one definition.

- [ ] Classification mirror-type deletion — branch `pr-address-ts/fu-classification-infer-types`.
      Thesis: the classification modules type against their existing Zod contracts instead of hand-written mirrors; construction is compiler-checked instead of parse-laundered.
  - Replace with `z.infer` derivations (or imports of the already-derived types from `src/feedback-plan-contracts.ts`): `interface FeedbackClassificationValidationResult` (`src/classification-validation.ts:21-37`, mirrors `feedbackPlanningValidationResultSchema` + counts schema at `feedback-plan-contracts.ts:120-146`); `interface FeedbackClassificationValidationError` (`src/classification-shared.ts:88-94`, mirrors `feedbackPlanningValidationErrorSchema`); `interface FeedbackPlanningResult` (`src/classification-planning.ts:62-72`, mirrors `feedbackPlanResultSchema` whose inferred `FeedbackPlanResult` already exists at `feedback-plan-contracts.ts:204`); `type ManifestKind` (`classification-shared.ts:69`) and `type ClassificationDisposition` (`:68`) — both duplicate same-file or contracts-file schemas; derive via `z.infer`.
  - In `src/classification-planning.ts`, replace the `.parse()` construction-laundering on internally built literals (item builders at `:199, :209, :219, :229, :238, :247` and the whole-result parse at `:115`) with typed literals — `satisfies z.input<typeof feedbackPlan*ItemSchema>` or direct annotation with the inferred types — and drop the per-item/whole-result parses. Rationale: `parse` takes `unknown`, so the compiler checks nothing, and a `ZodError` would escape a function whose contract is errors-as-values. The golden tests already gate byte-level output parity.
  - Replace the unreachable defensive branch at `classification-planning.ts:97-109` (fabricates a `valid:false` result with a synthetic warning when `validation.valid` is true but `view`/`packet` are null — an impossible state) with a thrown invariant error, consistent with `requiredActionComplexity` at `:307-310`.
  - Eliminate the redundant kind computation: `classification-validation.ts:50` calls `manifestKindForPayload` and `buildFeedbackManifestView` (`classification-shared.ts:97`) immediately recomputes it; make the view builder always return `kind` and delete the caller-side fallback (`view?.kind ?? manifestKind`, `:66`).
  - Type `classificationLocatorRef` (`src/classification-packet.ts:69`) as its real two-field shape `{ json_pointer: string; item_pointer: string | null }` instead of `Record<string, string | null>`.
  - Optional renames if cheap and behavior-free (pure file/symbol renames, no logic): `classification-packet.ts` → `classification-template.ts` (it holds the template builder; the packet schema lives in `classification-shared.ts`); skip if it churns row 5's diff.
  - Evidence: package check/test green; classification golden fixtures byte-identical; grep proof that the listed interfaces no longer exist as hand-written declarations.

- [ ] Fold `classification-schemas.ts` into `operation-schemas/` — branch `pr-address-ts/fu-classification-schema-routes`.
      Thesis: the orphaned sixth classification module dissolves into the directory that claims to own every `--json-schema` route; duplicated schema fragments get one definition each; the template builder's return is typed.
  - Move the three document builders from `src/classification-schemas.ts` into `src/operation-schemas/classification.ts` (or a sibling module in that directory), update `operation-schemas/index.ts:3-8` imports, and delete `classification-schemas.ts`.
  - Delete the local `JsonSchemaDocument` re-declaration (`classification-schemas.ts:97-100`); use the `@asdl/clinkr` export already consumed at `src/exec-operation.ts:3`, including for the registry type in `operation-schemas/index.ts:7`.
  - Dedupe `bodyLocatorSchema`: `classification-schemas.ts:32-37` (2-field, loose, shape-identical to `classificationLocatorSchema` at `classification-shared.ts:15-18`) vs `operation-schemas/manifest-mirrors.ts:30` (4-field, sealed — a genuinely different wire shape). Converge the 2-field copy onto `classificationLocatorSchema`; rename one of the two remaining schemas if the shared name misleads grep.
  - Template-schema convergence decision rule (the bounded risky part): `classificationTemplateResultSchema` (`classification-schemas.ts:46-95`, loose objects, `disposition` required) and `classificationTemplateResultDocSchema` (`operation-schemas/classification.ts:67-88`, sealed objects, `disposition` optional) describe the same runtime artifact divergently; the latter is also embedded in the Python-parity-pinned `stack-feedback-prep` document (`operation-schemas/stack.ts:189`). Steps: (a) verify the classification trio is absent from `PARITY_OPERATIONS` in `test/scenario/json-schema-routes.test.ts` (TS-captured fixtures) — if it has since been added, STOP convergence and take the fallback; (b) while `packages/asdl-pr-address` is in-repo, capture the trio's documents from Python (`uv run --project packages/asdl-pr-address pr-address exec <op> --json-schema`, or the invocation the parity test README documents) and converge both TS schemas to the Python-emitted shape, regenerating the trio's TS-captured fixtures with each diff attributed in the commit message — this is the sanctioned fixture update; (c) fallback if Python capture is impractical or the shapes cannot be reconciled byte-safely: do NOT converge — complete the move/dedupe parts only, keep both schemas with a comment cross-referencing the divergence, and record the residual divergence in the Objective update as an explicit handoff to the python-deletion endgame.
  - Type `buildFeedbackClassificationTemplate` (`src/classification-packet.ts:6`, currently `manifest: unknown → { type: "ok"; value: unknown } | ...`): return `z.infer` of the (possibly converged) template-result schema, or at minimum add a golden-test assertion parsing the builder's output through that schema so the doc and the builder cannot drift silently.
  - Update `operation-schemas/index.ts`'s ownership comment if it still says "every pr-address exec operation" while anything remains outside.
  - Evidence: package check/test green; `--json-schema` route fixtures byte-identical EXCEPT the trio when path (b) was taken (each diff attributed); `src/classification-schemas.ts` deleted; one `JsonSchemaDocument` type in the package.

- [ ] Payload/result contract typing — branch `pr-address-ts/fu-contract-typing`.
      Thesis: builders take and return schema-derived types instead of hand-written mirrors and `unknown`; parity-frozen result schemas get drift-guards instead of risky retyping.
  - `src/payload-manifest.ts`: delete the hand-written `GetFeedbackPayloadManifestInput` and `PrepareRunPayloadManifestInput` interfaces (`:78-103`) in favor of `z.input<typeof getFeedbackPayloadManifestInputSchema>` / `z.input<typeof prepareRunPayloadManifestInputSchema>` (`:43-69`). If the schemas' `z.input` doesn't structurally accept the gateway domain types the builders are called with (`prepare-run.ts:183/191`, `feedback-collection.ts:84`, `stack-feedback.ts:495`), adjust the schemas (e.g. field optionality/readonly-array acceptance) so it does — the schema is the single source of truth, the interface goes away either way. Also delete the re-declared `payloadReferenceSchema` + `PayloadReference` interface (`payload-manifest.ts:4-6, :71-73, :275`) and import the canonical pair from `feedback-manifest-contracts.ts:3-5,83`.
  - Type the manifest builder returns: `buildGetFeedbackPayloadManifest` (`payload-manifest.ts:120`) and its prepare-run sibling currently return `unknown`, forcing `stack-feedback.ts:502` to runtime-parse (`prepManifestViewSchema.parse(manifest)`) a value built two lines earlier, with a throwing failure path at `:504`. Give the builders honest return types and delete that self-parse + throw.
  - Parity-frozen result schemas — drift-guards, not retyping: `CompactReviewSummary` et al. (`src/summarize-feedback.ts:27-92`) vs `compactReviewSummarySchema` et al. (`operation-schemas/collection.ts:93-143`); `FinalizeRunResult` (`src/finalization.ts:96-121`) vs `finalizeRunResultSchema` (`operation-schemas/payload.ts:205-218`); the `resolve-thread-batch-payload.ts:71` and `mutation-operations.ts:69` result shapes vs their doc schemas. These doc schemas are Python-parity-pinned, so do NOT rewrite them to fit runtime types. Instead add unit tests that build a representative runtime result (reuse existing golden/scenario outputs where possible) and `parse` it through the corresponding doc schema. Sealed-object (`strict`) doc schemas make this a real two-direction drift guard. Where a parse cannot pass for a documented, deliberate reason, record that reason in a test comment and the Objective update.
  - Small cast/optionality fixes in the same files: `finalization.ts:176` `item.thread_id as string` after a null-filter → `flatMap` with a guard; `src/resolve-thread-batch-payload.ts:351` `resolveDecisionIssues(mode: string | null)` discards its caller's narrowing (`:304-311`) and forces an unreachable re-check (`:355-357`) → type `mode: ResolutionMode`, delete the branch; `ResolveThreadBatchItem.provenance: unknown | null` (`:62`) → `z.infer<typeof provenanceSchema> | null`. Leave `read-feedback-detail.ts:261` alone (owned by the parked containment slice).
  - Evidence: package check/test green; all fixtures byte-identical; grep proof the two manifest-input interfaces and the duplicated `PayloadReference` are gone; new drift-guard tests in place and green.

- [ ] Parse↔doc schema structural parity — branch `pr-address-ts/fu-exec-schema-parity`.
      Thesis: the dual-schema seam behind the clinkr `schemaDocument` override becomes structurally checkable instead of name-checked, and the one known drift is fixed.
  - Expose the parse schema on `ExecOperation`: extend the existential at `src/exec-operation.ts:17-19` from `{ name, addTo }` to `{ name, schema, addTo }` (the existential `addTo` stays — it is the right encoding for the heterogeneous generics; only the schema needs to escape the closure). `defineExecOperation` populates it from the spec.
  - Add a structural parity test (extend `test/scenario/cli.test.ts:222-227` or a new unit file): for each of the operations in `EXEC_OPERATIONS`, compare the clinkr parse-schema option surface (via `buildSurfacePlan` or the schema's shape keys) against the published document's `input_json_schema.properties` keys. Maintain an explicit `const PARITY_DELTAS: Record<string, string[]>` allowlist with a comment per entry explaining the delta (expected entries: operations whose documents describe embedded payload fields while the CLI exposes `--<key>-reference` derivations — enumerate exactly what the test discovers; do not bulk-allowlist). New, unexplained drift fails the test.
  - Fix the known drift — `map-branch-prs`: doc input schema says `{branches: string[]}` (`operation-schemas/collection.ts:21-23`) but the CLI parses `{branches_json?: string}` (`src/map-branch-prs.ts:13-15`), and sibling operations document their actual option surface. Decision rule: verify the fixture for this route is TS-self-captured (per `json-schema-routes.test.ts:44-45`); if so, align the document to the real surface (`branches_json`) and regenerate that one fixture with the diff attributed in the commit message — this is the second sanctioned fixture update. If verification shows it is Python-pinned after all, allowlist it with a comment and record the divergence in the Objective update instead.
  - Add the sortedness assertion the table comment promises (`src/exec-commands.ts:17-21` claims alphabetical = click help order, enforced by nothing): `expect(names).toEqual([...names].sort())`. Replace the churn-prone `toHaveLength(20)` at `cli.test.ts:226` with the 1:1 name check it duplicates. Relocate `EXEC_OPERATION_NAMES` (`exec-commands.ts:45`, consumed only by tests) to test support, or use it as `cli.ts:53`'s default — either way it stops being dead public surface.
  - Evidence: package check/test green plus `pnpm --dir ts run test` (clinkr-adjacent); only the sanctioned `map-branch-prs` fixture differs (or an allowlist entry + update note exists); the parity test fails when an option is added to a parse schema without its doc (prove by temporary mutation during development, not committed).

- [ ] Split `stack-feedback.ts` — branch `pr-address-ts/fu-stack-feedback-split`. Must come after rows 2, 4, and 6 (they edit lines this row moves).
      Thesis: the 947-line two-operation module splits along its existing prep/plan seam as a pure move, before any future PR pushes it over the 1,000-line ceiling.
  - Current internal section map (line ranges pre-drift): schemas `18-111`; wire-shape interfaces `113-285`; prep operation `287-372`; plan operation `374-453`; prep helpers `455-635`; plan helpers `637-912`; python-parity utils `914-947`. The shared surface between prep and plan is small: `stackPrepResultInputSchema`, `triageSummary`, and the (row-1-consolidated) duplicate/repr helpers.
  - Split into: `src/stack-feedback-contracts.ts` (shared schemas + wire-shape types + `triageSummary`), `src/stack-feedback-prep.ts` (~370 lines: prep operation + prep helpers, including the concurrent-fetch/sequential-writes pipeline), `src/stack-feedback-plan.ts` (~480 lines: plan operation + plan helpers). Any python-parity string helpers still local after row 1 move to the row-1 shared home. Delete `src/stack-feedback.ts`; rethread importers (`src/exec-commands.ts`, tests) to the new modules per the repo's canonical-source import rule (no compat re-export shim).
  - Pure move: no logic edits, no renamed exports beyond the module paths, no signature changes. If a tempting cleanup appears mid-move, note it in the Objective update instead.
  - Split `test/scenario/stack-feedback.test.ts` only if the move makes it natural (prep vs plan describe blocks); otherwise leave it whole and just fix imports.
  - Evidence: package check/test green; every stack-feedback fixture byte-identical; `wc -l` on the three new modules each well under 1,000 (target <500); diff reviewable as pure relocation (commit message includes the section→module mapping).

- [ ] clinkr accuracy + test-scaffolding polish — branch `pr-address-ts/fu-clinkr-test-polish`.
      Thesis: comments and docs stop overclaiming, and the test-support consolidation finishes its own sweep. Behavior unchanged everywhere.
  - `ts/packages/clinkr/src/group.ts:420-426` (`parseIntegerValue`): the comment claims "Click-strict: decimal digits only" but click's `int` ACCEPTS `"+5"`, `" 5 "`, `"1_000"`, which this regex rejects. Keep the stricter behavior (changing acceptance is parity arbitration owned by `pr-address-typescript-port` — see this record's Open Questions); reword the comment to state the deliberate divergence, and add acceptance-side test cases in `ts/packages/clinkr/test/strict-integer.test.ts` documenting that `+5`/`" 5 "` are rejected here though click accepts them.
  - `ts/packages/clinkr/src/group.ts:306-309, :364` (`--format` choices `human|json|markdown|md`): `markdown`/`md` silently alias to `human` with no renderer hook (the Python framework has `markdown_renderer`; the TS port deliberately ported only the choices). Add a doc comment on the option/spec stating markdown renders via the human channel until a `renderMarkdown` hook exists, and prefer switching on the literal value over `=== "json" ? : "human"` so a future hook can't be silently swallowed. No behavior change; existing format tests stay green.
  - Test-support polish in `ts/packages/pr-address/test/`: adopt `useTempDirs()` from `test/support/temp.ts` and the shared `REPO_ROOT` in `test/wrapper/pr-address-shim.test.ts:8,12-23` (the last hand-rolled temp-dir machine); make the six unconsumed exported `FAKE_*` constants + `fakePrLookupMissStderr` in `test/support/in-memory-pr-address-gateways.ts:44-53` module-private (keep the pinning comment) or actually consume them in envelope assertions; throw in `goldenCases` (`test/support/golden.ts:16-28`) when an existing operation dir yields zero cases (silent-empty-corpus hazard); inline the two pass-through wrappers `runWithFakeLegacy` (`test/scenario/json-schema-routes.test.ts:47-49`) and `runWithGithub` (`test/scenario/map-branch-prs.test.ts:19-21`); move the duplicated `asWrapperInput` guard (`test/scenario/classification-core.test.ts:12-17` — post-rename path from row 1 — and `test/unit/classification-golden.test.ts:17-22`) into `test/support/golden.ts`; optionally move `REPO_ROOT` out of `golden.ts` into a small `test/support/paths.ts` since non-golden tests import it.
  - Evidence: `pnpm --dir ts run check` and `pnpm --dir ts run test` green (clinkr + pr-address + format-pinning consumers `plans`/`planned-branch`); zero fixture diffs; `rg "mkdtemp|tmpdir" ts/packages/pr-address/test` shows only `test/support/temp.ts`.

## Parked

- Behavior-adjacent review findings, deferred to `pr-address-typescript-port`'s endgame: surface posted-reply evidence when `applyResolution` fails after posting (`src/mutation-operations.ts:315-318` — blind retry currently double-posts), and parallelize the within-PR sequential gateway fetches (`src/stack-feedback.ts:470-476`, `src/feedback-collection.ts:102-118`) using the same await-all-then-scan pattern that preserves error precedence. Both change observable behavior or parity posture; neither belongs in a byte-preserving cleanup stack.
- clinkr integer acceptance-side widening (`+5`, whitespace, underscores) toward true click parity — parity arbitration owned by `pr-address-typescript-port` before python-deletion.
- `stack.ts` intra-module schema dedup (`stackFeedbackAutomationDiscussionSummarySchema` ≡ `stackDiscussionTriageCompactSummarySchema`; five `{code, message, …}` error schemas; the ~15 shared fields of the two plan-item schemas) — defensible verbatim Pydantic mirrors while fixtures are pinned; consolidate after python-deletion.
- Thin-comparator coverage: `json-schema-parity-comparator` has 2 direct unit cases for ~300 lines (union branch-matching, `$ref` cycles untested directly) — the 16 fixture operations exercise it end-to-end; deepen only if the comparator outlives the Python reference.
- `threadKey` tuple→string round-trip ergonomics (`threadKeyString(key[0], key[1])` at every production call site) — micro-API polish; fold into any future thread-index touch.
