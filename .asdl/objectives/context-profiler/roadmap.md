# Roadmap

## Work

- [x] Deterministic core: `ts/packages/pi-extensions/src/context-profiler/` with `model.ts` (pure derivation: entries/events → base regions + flat per-turn list with labeled best-effort token estimates), `view.ts`/`render.ts` (overlay implementing the "UI design" interaction contract recorded in objective.md Scope: frame-stack navigation, overview row format, health colors, density rules, claim lines, content drill-down), `runtime.ts` (live updates from `context` events, with a defined authoritative-source rule vs fallbacks), top-level wiring, and the `.pi/extensions/context-profiler.ts` shim. Land as a small Graphite stack.
      Episode annotations must already be modeled as optional input to the view, not computed structure. Record the cross-harness parity waiver for `/context-profiler` in the parity table as part of this row.
      Evidence: landed as the `context-profiler-deterministic-core` → `context-profiler-review-fixes` Graphite stack off `master` (tip PR #1207); episode annotations are optional clamped input (`EpisodeAnnotation`, `buildLiveRegions(turns, episodes?)`); `runtime.ts` records the authoritative-source rule (`context` event authoritative once received, session-branch fallback only before the first one, provenance carried as `liveSource`); `/context-profiler` waiver row present in the cross-harness parity table; full pi-extensions Vitest suite passed, including `test/context-profiler-model.test.ts` and `test/context-profiler-render.test.ts`. Review fixes added a typed `NormalizedMessage`/`MessagePart` normalization layer (`normalizeMessage`) feeding verbatim rendering and an `OverlaySession` holder for overlay state.
- [x] LM episode segmentation: gateway interface + in-memory fake for the analysis model (fixed cheap model as a single code-level constant, resolved via the model registry), Zod-validated and repaired responses, on-demand trigger (overlay open + manual refresh), explicit truncation/windowing policy for long sessions, and the hard graceful-degradation state ("segmentation unavailable: <reason>"). Decide the within-session caching question before or during this row.
      Evidence: landed in the segmentation stack (`context-profiler-segmentation-model` → `context-profiler-segmentation-gateway` → `context-profiler-segmentation-wiring` → `context-profiler-segmentation-review-fixes`, with current stack-style feedback on top). `segmentation.ts` owns prompt/payload construction, Zod parsing, repair, seam splitting, payload windowing, and the fixed `openai-codex/gpt-5.4-mini` model constant; `segmentation-gateway.ts` resolves that model through the registry and returns failure values for unavailable/auth/request/invalid/aborted cases; `runtime.ts` triggers segmentation on overlay open/refresh and keeps a within-session fingerprint cache that `r` force-refresh bypasses; `view.ts` keeps deterministic rows visible for loading/error states and applies ready episodes as an annotation layer. Verification: `pnpm --dir ts/packages/pi-extensions run test` passed.
- [x] Per-episode efficiency/relevance analysis: on-demand per-episode LM judgment (`efficient/mixed/wasteful`, `load-bearing/still-useful/stale/rot`) rendered in the overlay, through the same gateway, schemas, and degradation rules as segmentation. Neutral and descriptive, never advisory.
      Evidence: landed on `add-context-profiler-analysis` as commit `23a8225b` against Graphite parent `context-profiler-episode-efficiency-relevance-analysis`. The old segmentation gateway was generalized into `analysis-model-gateway.ts` with `segmentTurns` and `analyzeEpisode`; `analysis.ts` owns the neutral verdict prompt, payload assembly, Zod verdict parsing, and JSON extraction; `runtime.ts` launches per-episode analysis after segmentation and on cached snapshots, caches successful verdicts with the segmentation fingerprint, and leaves analysis failures visible but retryable; `model.ts`, `view.ts`, and `render.ts` carry/render the verdicts without advisory language. Verification: `pnpm --dir ts/packages/pi-extensions run test` passed.
- [ ] Delegation/subagent detection: surface delegation claims (turn, label, confidence, including the deterministic name-pattern fallback) in the overlay and feed them into episode analysis context.

## Parked
