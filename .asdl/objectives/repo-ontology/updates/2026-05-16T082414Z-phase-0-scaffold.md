# Phase 0 — `/CONTEXT-MAP.md` scaffold landed

## Summary

Created `/CONTEXT-MAP.md` at repo root as the navigation index for the asdl ontology sweep. The file:

- States the format-is-the-contract rule (no linter, no generator).
- Lists the 6 in-scope contexts (`asdl-core` plus the 5 plugin packages: `brmem`, `asdl-pr-address`, `asdl-reviewer`, `asdl-slots`, `asdl-objectives`), each with a one-line summary lifted from the roadmap.
- Links `asdl-core` live and names its 5 H2 anchors inline (`#clinkr` present, the other 4 marked *Planned*).
- Records the two out-of-scope packages (`asdl-dispatcher`, `asdl-initiatives`) with revisit triggers.
- Seeds Phase 4 placeholders: a Relationships section with candidate edges (`objectives → brmem`, `pr-address → core.gh`, `reviewer → core.gh + core.gt`, `slots → core.git + core.gh`, `* → core.clinkr`) and a Flagged ambiguities section with four candidate collisions (Review, Comment, State, branch/ref) to resolve or punt deliberately during per-package sessions.
- Documents the provisional resolution of the first Open Question (link `asdl-core` as one context with inline H2 anchor names) and flags it for Phase 4 readback.

`just` passes (`ty` clean, `ts check` clean, 1221 pytest pass). No production code touched.

## Objective Impact

- `roadmap.md`: Phase 0 task marked `[x]` with completion-evidence note describing what the scaffold contains and how the H2-linking choice was made.
- `objective.md`: first item in `## Open Questions` annotated with the Phase 0 provisional answer; original question text preserved so the deferral history is visible.
- No change to Thesis, Scope, Non-Goals, Completion Criteria, or Assumptions and Risks. The scaffold did not exercise a risk hard enough to warrant a status change — it confirmed the partition (6 in-scope + 2 out-of-scope) and the format works in practice, which is what Phase 0 was designed to do.

## Follow-Ups

- Next roadmap item: Phase 1 first task — grill and append `## Git` to `packages/asdl-core/CONTEXT.md`. This is the natural next step in the bottom-up sequencing because `git` is the layer-0 gateway (no upward asdl-core imports per `packages/asdl-core/AGENTS.md`) and its vocabulary (branch, ref, start_point, worktree, `NonIdealState` arms) feeds the later `Gt`, `Gh`, and plugin sessions.
- Defer the H2-linking decision to Phase 4 readback: when an unfamiliar contributor navigates the map, decide whether the inline anchor names are useful or noise.
- During each Phase 1–3 session, revisit the seeded Flagged-ambiguities list and either resolve the entry inside the per-package `CONTEXT.md` or confirm it survives as a map-level note.
