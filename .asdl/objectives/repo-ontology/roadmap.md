# Roadmap

## Work

Phase 0 — scaffold

- [x] Create `/CONTEXT-MAP.md` with planned-contexts list, links (existing or TBD), and explicit initial skips — scaffold committed; `asdl-core` linked live (Clinkr anchor present, 4 H2 anchors named as planned), 5 plugin packages marked _Planned_, `asdl-dispatcher` / `asdl-initiatives` listed out of scope with revisit triggers, Relationships and Flagged ambiguities sections seeded as Phase 4 placeholders. This remains useful as the first scaffold, but Phase 0.5 must update it before further map work because `packagechk` is now a tracked package and `asdl-initiatives` is not.

Phase 0.5 — rebaseline after repo thrash

- [x] Update `/CONTEXT-MAP.md` against the current tracked package inventory — map now links the existing root `CONTEXT.md`, lists `packagechk` as the seventh in-scope package context, keeps `asdl-dispatcher` as the only tracked out-of-scope package while it has no operations, and frames `asdl-initiatives` as an absent/historical name rather than a tracked package skip.
- [x] Update the scaffold's candidate Relationships and Flagged ambiguities — map now removes the stale `asdl-objectives → brmem` storage edge, records `packagechk` as standalone/no-`asdl-core`, and expands the State/status ambiguity candidate to include `packagechk.CheckStatus` / `PackageCheckReport.exit_code` alongside PR state and rendered state badges.

Phase 0.6 — rebaseline after Pi/Objective workflow drift

- [x] Reconcile root Objective-system language — `CONTEXT.md` and `docs/objective-system.md` now distinguish Active Objective Root, Objective Archive Root, Archived Objective, Objective Close, Objective Archive, and Closure Marker; `objective list` / `objective exec read-objective` are documented as active-root reads, and `objective archive` / `objective exec runner-subagent-usage` are included in shipped mechanics.
- [x] Add the repo-local TypeScript/Pi extension context — `ts/packages/pi-extensions/CONTEXT.md` now covers `.pi/extensions/` discovery adapters, the engineered private package, planned-branch workflow, checkpoint/new-branch flow, runner subagents, terminal presentation, and runtime CLI edges.
- [x] Rebaseline `/CONTEXT-MAP.md` after the Pi/Objective changes — map now lists `@asdl/pi-extensions`, marks all asdl-core H2s present, updates Python package planned-term bullets against current source, and expands relationships/ambiguities for Objective active/archive status, runner/planned-branch plan terms, and runtime CLI edges.
- [x] Refresh adjacent Pi docs enough to remove contradictions introduced by the new context — `docs/pi/README.md` now lists the current extension inventory and archived Objective links; `docs/pi/runner-subagent-helper.md` now describes `runner-subagent.ts`, final-text mode, current result statuses, and the `dispatch_runner_subagent` tool; `docs/pi/objective-stack-subagent-rewrite-brief.md` now carries a staleness note and points to runner-subagent vocabulary.

Phase 0.7 — rebaseline after roaster/aretro package inventory drift

- [x] Update `/CONTEXT-MAP.md` against the current tracked package inventory — map now treats `roaster` as the renamed former `asdl-reviewer` context, adds `aretro` as the eighth in-scope Python package context, keeps `asdl-dispatcher` as the tracked out-of-scope stub, marks `asdl-core ## Sessions` present, and mentions `asdl-reviewer` only as rename history rather than a live package slot.
- [x] Update the map's candidate Relationships and Flagged ambiguities — map now includes `aretro → asdl-core.sessions + asdl-core.git + asdl-core.clinkr + asdl-core.plugin`, updates `roaster` edges/names, records `asdl-objectives → asdl-core.gt`, and adds Evidence/finding as a map-level ambiguity candidate.

Phase 1 — finish/refresh `packages/asdl-core/CONTEXT.md` (append H2 sections to existing file)

- [x] Grill and append `## Git` — section now defines `GitGateway`, bound repo vs `cwd`-scoped worktree facts, branch/ref/start-point terminology, worktree inventory, dirty-state and history records, patch IDs, restructured files, and `NonIdealState` arms for `DetachedHead` / `GitCommandFailure`; `/CONTEXT-MAP.md` now links the Git anchor as present.
- [x] Grill and append `## Gt` — section now defines `GtGateway`, `Graphite stack`, `StackInfo` (a focused snapshot that always names the current branch, not a complete graph), `Current stack branch`, `Graphite trunk`, `Graphite parent` and `Graphite children` as immediate edges, `Graphite ancestors` and `Graphite descendants` as recursive directions, `Downstack` and `Upstack` directions, `NoParent` (tracked branch with no parent) vs `UntrackedBranch` and `GtCommandFailure` as `NonIdealState` arms, `GtBranchInfo`, `Restack upstack`, `Graphite sync`, and `Stack warning`. Relationships call out Graphite's documented stack directions, the immediate-vs-recursive split, Graphite trunk vs Git Trunk branch as separate sources of truth, and the `GtGateway` vs `GitGateway` boundary. `/CONTEXT-MAP.md` now links the Gt anchor as present.
- [x] Grill and append `## Gh` — landed on master via the pr-gateway-unification Objective (#555), not a dedicated repo-ontology session, and accepted as meeting the section criteria (Language entries with `Avoid:` aliases + Relationships). Resolved the planned disambiguations: `PRState` (lifecycle `OPEN`/`CLOSED`/`MERGED`) vs `PRStateFilter` (query input `open`/`closed`/`merged`/`all`); the `PRReview` / `PRReviewThread` / `PRReviewComment` / `PRDiscussionComment` family with `IssueComment` demoted to an `Avoid:` alias; and `PRSummary` as the single PR metadata record, with `PRDetails` demoted to an `Avoid:` alias after #580 deleted `PRDetails` and `IssueGateway` and finalized `PRGateway` as the sole gateway. `/CONTEXT-MAP.md` already links the Gh anchor as _Present_.
- [x] Grill/read back and append `## Top-level utilities` — section now defines `AsdlPluginSpec`, `Context factory`, standalone CLI builder/invoker, `asdl console`, `asdl table`, `Relative time string`, `State badge`, and `AliasedGroup`; Relationships record plugin-spec flow, presentation-helper boundaries, state-badge vs domain-state ownership, and legacy Click aliasing. The Git section was also refreshed with commit-graph and path-touch terms (`LocalBranchTipRef`, `CommitGraphNode`, `BranchCommitGraph`, `PathTouch`, `PathChangeTouch`).
- [x] Grill/read back and append `## Sessions` — section now defines session-source adapters, normalized session facts, `SessionSourceInfo`, `SessionSourceRef`, `SessionQuery`, `SessionAssociation`, `ParsedSession`, `SessionEvidenceItem`, evidence kind ordering, the privacy boundary/no raw transcript content, and the relationship between `asdl-core.sessions` aggregation, the `aretro` CLI, and `branch-retro` recommendation judgment.
- [x] Reconcile the temporary `asdl_core.gt` subpackage context against the single-file-H2 Non-Goal — the Graphite metadata-store contract is folded into `packages/asdl-core/CONTEXT.md#gt`, covering `stack()` vs `branch_graph()`, read-only Graphite-owned metadata/config reads, named-column schema requirements, optional restack revision metadata, structured `GtCommandFailure`, and the intentional lack of a `gt ls` human-output fallback; the competing subpackage context file is deleted.

Phase 2 — foundational primitive

- [x] Create `packages/brmem/CONTEXT.md` — landed via focused grilling/readback. The context now defines Branch Memory System, Branch Memory, Namespace / Base Namespace, Entry, Entry Key, Snapshot, Snapshot Ref, Entry Locator, Namespace Copy, Copy Conflict, and Export; Relationships cover branch attachment, branch/ref encoding, key safety, snapshot history, namespace-copy semantics, mutation boundaries, export materialization, small-text limits, and higher-level workflow ownership of named Namespaces. Prompt resolution stayed out of the durable ontology because `exec` commands are skill-facing/internal rather than user-facing Branch Memory behavior. The temporary package-local product/code alignment notes have since been resolved: Base Namespace copyability, Entry Locator wording, skill-facing prompt-plugin framing, workflow-owned Namespace wording, and empty destination Snapshot conflict behavior now align with the context; `packages/brmem/FOLLOWUP.md` is deleted.

Phase 3 — package contexts (plugins plus standalone utility)

- [ ] Create `packages/asdl-pr-address/CONTEXT.md` — package behavior around core `PRReviewThread`, `PRReviewComment`, `PRDiscussionComment`, reactions, feedback, thread resolution/replies; cross-reference (not redefine) `asdl-core.gh` types and treat `IssueComment` as legacy command/API wording where it remains.
- [ ] Create `packages/roaster/CONTEXT.md` — `Roaster`, `ReviewDefinition`, `HarnessRuntime`, `HarnessDefinition`, `HarnessReviewRequest`, `ReviewCatalog`, `ReviewSource`, `ReviewFormat`, `Finding`, `InlineCommentability`, severity (info/warning/error), frontmatter, inline finding posting, and the renamed `roaster` CLI/comment-marker vocabulary; explicitly disambiguate roaster "Review" vocabulary against `gh.PRReview` and `pr-address` thread vocabulary.
- [ ] Create `packages/asdl-slots/CONTEXT.md` — `SlotRecord`, `SlotInventory`, `InventoryStatus`, `RepoContext`, `SlotGcPlan`, `InitPlan`, `ResizePlan`, shell directive files, explicit `slot gt` operations, and `free-stack --downstack` downstack-only release.
- [ ] Create `packages/asdl-objectives/CONTEXT.md` — Objective records, `ObjectiveStatus` (`open`/`closed`/`in-flight`), `ObjectiveRecordStatus`, status sources, branch slices/path-touch attribution, archive/unarchive, the opt-in Graphite command group `objective gt` (a `slot gt`-analogous stack-projection surface with `ObjectiveGtStacksRequest`, `ObjectiveGtStacksResult`, `ObjectiveGtStacksRow`, `ObjectiveGtStacksSegment`, `ObjectiveGtStacksObjective`, `ObjectiveBranchTouch*`, stack projection/scope/render, and `asdl-core.gt` branch-graph vocabulary), `exec` group conventions including `read-objective` and `runner-subagent-usage`, and the fact that Objectives are checked-in Markdown rather than brmem entries.
- [ ] Create `packages/packagechk/CONTEXT.md` — `Registry`, `CheckStatus`, `RegistryCheckResult`, `PackageCheckReport`, PyPI normalization, npm validation/scoped-name caveat, claim project specs, publish gateways, and parked Homebrew support.
- [ ] Create `packages/aretro/CONTEXT.md` — branch retrospective evidence collection, `AretroCliContext`, `collect-evidence`, branch resolution sources, session query/source/warning DTOs, session summaries, aggregate metrics, `EvidenceItemDto`, deterministic evidence item kinds, and the boundary between factual evidence collection and `branch-retro` recommendation judgment.

Phase 4 — map finalization

- [ ] Populate Relationships section of `/CONTEXT-MAP.md` with concrete cross-package and extension edges (e.g. `brmem → asdl-core.git + asdl-core.clinkr`, `asdl-pr-address → asdl-core.gh + asdl-core.git + asdl-core.clinkr/plugin`, `roaster → asdl-core.gh + asdl-core.git + asdl-core.clinkr/plugin`, `asdl-slots → asdl-core.git + asdl-core.gh + asdl-core.gt + asdl-core.clinkr/console/plugin`, `asdl-objectives → asdl-core.git + asdl-core.gt + asdl-core.clinkr + asdl-core.console/format/plugin` (`asdl-core.gt` is the `objective gt` stack-projection edge), `aretro → asdl-core.sessions + asdl-core.git + asdl-core.clinkr/plugin`, `@asdl/pi-extensions → Pi runtime + git/gt/gh/brmem/objective/slot CLIs`, and `packagechk` as standalone/no-`asdl-core` edge).
- [ ] Add/finalize "Flagged ambiguities" section for cross-context naming collisions (Review/Comment overload, State/status usage across `gh`, `format.state_badge`, Objectives, slots, and `packagechk.CheckStatus`, active/root terminology, branch/ref/snapshot-ref usage across `git`/`gt`/`slots`/`brmem`/`aretro`, Graphite stack-projection terms in `asdl-objectives`, evidence/finding language across Objectives / `aretro` / `roaster`, and plan terminology across Objectives / planned branches / Branch Memory).
- [ ] Final readback pass — confirm an unfamiliar contributor can navigate from `/CONTEXT-MAP.md` to any context and explain key terms without opening source

## Parked

- ADRs — write only if the `grill-with-docs` three-criteria bar fires during a session (hard to reverse, surprising without context, real trade-off). Not planned proactively.
- `packages/asdl-dispatcher/CONTEXT.md` — revisit when live operations land; today it is a CLI stub with no operations.
- `packages/asdl-initiatives/CONTEXT.md` — no tracked package exists in the current workspace; revisit only if the package is reintroduced with implementation.
- `asdl-reviewer` context — historical package identity replaced by `roaster`; do not recreate unless the package itself is deliberately reintroduced as a separate tracked package.
- Per-subpackage `CONTEXT.md` split for `asdl-core` (e.g. `src/asdl_core/clinkr/CONTEXT.md`) — revisit when `clinkr` or another labs subpackage graduates to a standalone package.
- Periodic re-grilling cadence — out of scope for this Objective; address as a separate process question after the sweep closes.
