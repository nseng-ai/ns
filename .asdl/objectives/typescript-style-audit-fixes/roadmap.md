# Roadmap

## Work

- [x] Remove hard language-style violations across source and tests.
      Evidence: constructor parameter properties were replaced with explicit fields and assignments in the targeted source and test classes, the test-only `any` in runner-subagent fake process listeners was removed, focused scans found no remaining constructor parameter properties or explicit type-level `any` in `ts/packages/pi-extensions`, and the broad emit-time construct scan reports only ambient declarations or text/identifier false positives. Validation: `bun run --cwd ts/packages/pi-extensions check`, `bun run --cwd ts/packages/pi-extensions test`, `just ts-check`, and `just ts-test` passed.
- [x] Convert existing object-shape and contract aliases to interfaces while preserving union and function aliases as `type`.
      Evidence: the pi-extensions AST inventory moved from 268 direct object-literal aliases to 0 across `src` and `test`; 19 simple object-contract intersections were converted to `interface extends`; the 4 remaining intersections are intentional utility/union compositions (`DevExtensionAPI`, `RunnerSubagentOptions`, and two test `SentMessage` helpers); and the fast scan only reports the intentionally preserved `RunnerSubagentOptions` object-base-plus-union alias. Validation: `bun run --cwd ts/packages/pi-extensions check`, `bun run --cwd ts/packages/pi-extensions test`, `just ts-check`, and `just ts-test` passed. Compliance now also extends to `ts/packages/asdl-dev`: object type aliases were converted to interfaces across its `src` and tests on branch `asdl-dev-stack-omnibus-roaster-fixes` (PR #812), with `just ts-check`/`just ts-test` green — see the 2026-06-02 asdl-dev stack remediation update.
- [x] Harden untyped JSON, tool, and runtime boundaries with `unknown` plus guards or decoders.
      Evidence: the CLI/process JSON parse sites are hardened — `land.ts`, `land-stack/pr-facts.ts`, and `worktree-status.ts` now parse `gh pr view` / `brmem list` output into `unknown` and narrow it with local guards before property use. The deferred runner/grill runtime inputs are now hardened too: `runner-subagent/json-events.ts` narrows parsed JSONL with `isJsonEvent`, `runner-subagent/subagent-runtime.ts` parses schema round-trips as `unknown` and requires a JSON object, and `grill-ui/inline-ui.ts` normalizes dynamic TUI/coding-agent imports and custom UI themes from `unknown`. Focused scans found no targeted broad casts in the completed boundary files. Validation: focused Bun tests, `bun run --cwd ts/packages/pi-extensions check`, `bun run --cwd ts/packages/pi-extensions test`, `just ts-check`, `just ts-test`, and `just dprint-check` passed.
- [x] Rework expected failure APIs toward discriminated returned data where callers branch on failures.
      Evidence: the brmem/planned-branch, submit-gateway, `land-stack`, runner runtime parsing, and handoff/objective parsing slices are done. `runFirstAvailableBrmemCommand` now returns a discriminated `FirstAvailableBrmemCommandRun` union (`CompletedBrmemRun | NoAvailableBrmemCommandRun`) instead of throwing on unavailability, a discriminated `BrmemRun` (`"completed" | "unavailable"`) threads through `planned-branch/attached-plan.ts`, `plan-persistence.ts`, and `planned-branch-creation.ts` where callers branch on `.type`, and the preflight `brmem check` aborts before branch creation when unavailable (commit `82fdc07b`). On the `asdl-dev` submit gateway, presentation-string fields were replaced with typed semantic causes: `SubmitSemanticFailureCause` (`"empty_branch_skipped"`) and `CurrentPrVerificationFailureCause` (`"no_current_pr" | "startup_error" | "timeout" | "command_failed"`), with English prose owned by formatter helpers behind an `assertNever` exhaustiveness guard (commit `62c64246`). In `land-stack`, `LandStackError`/`fail(...)` were removed from source and tests; expected dirty repo, missing branch, PR validation, command, cancellation, worktree, submit/update, slot cleanup, and merge-loop stop conditions now flow through `LandStackFailure` / `LandStackResult<T>` values, `withCommandStreaming` returns failed `ExecResult` data instead of rethrowing `pi.exec` startup failures, and presentation formats structured failure data directly while preserving user-facing command behavior. Runner runtime config/result parse/read helpers return discriminated valid/invalid/loaded/missing/read-error data, `dispatchRunnerSubagentProcess` branches on `RuntimeResultReadResult`, and the runtime extension branches on `RuntimeConfigReadResult` while preserving diagnostics. Handoff/objective parsing now returns typed discriminated result data for malformed machine envelopes, Objective list records, `/objective:list` arguments, handoff command arguments, and handoff `brmem list` JSON; command handlers, Objective pickers, and worktree status branch on those values while preserving user-facing notification and custom-message content. Validation for the latest handoff/objective slice: focused handoff/objective/machine-envelope Bun tests, `bun run --cwd ts/packages/pi-extensions check`, `bun run --cwd ts/packages/pi-extensions test`, `just ts-check`, `just ts-test`, and `just dprint-check` passed, with scans showing no `HandoffUsageError` or `CustomCliUsageError` references and no expected parser-failure `toThrow` assertions in the touched parser tests.
- [x] Clarify dependency-injection and adapter ownership for Node/Pi globals.
      Evidence: the remaining narrow domain-ish filesystem/path seams now accept local injected collaborators: `detectInProgressOperation` takes `pathExists`, `detectWorktreeConflicts` takes `normalizePath`, `derivePlanContentSlug` takes `readTextFile`, and saved-plan fallback loading in `attached-plan.ts` takes `LoadAttachedPlanOptions.readTextFile`. Fake-driven tests cover each seam without relying solely on real filesystem state. Larger direct-runtime sites are documented as adapter-owned for this slice: `asdl-dev/src/checkpoint-flow.ts` owns temporary commit-message file lifecycle for `git commit -F`, and `pi-extensions/src/pr-feedback-watch.ts` owns extension-controller clocks/timers/path probing. Optional scans still report expected CLI/command-runner/gateway adapter modules. Validation: targeted ccc/planned-branch/pi-extensions checks and tests, `just ts-check`, `just ts-test`, and `just dprint-check` passed; see the 2026-06-09 DI/adapter ownership update.
- [x] Add lightweight TypeScript style guardrails and contributor guidance for `typescript-style` compliance.
      Evidence: `reviews/typescript-style.md` defines a `haiku` Roaster reviewer for active Tier A, diff-visible TypeScript style checks including erasable syntax, ordinary `any`, broad and double casts, top-level arrow module logic, parameter mutation, naming hygiene, suppression hygiene, and mega-barrels. It intentionally avoids package-manager, formatter, linter, test-runner, and import-suffix choices because the guide is toolchain-neutral. Repo-root agent guidance now also requires agents doing TypeScript work to load the `typescript-style` skill, core rules, relevant references, and completion checklist through `AGENTS.md`, with concise mirror guidance in `CLAUDE.md`. The real reviewer parse test covers `typescript-style`; targeted Roaster parse tests, `just dprint-check`, and full `just check` passed for the reviewer slice; PR #817 corroborates the root guidance file set.
- [ ] Capture remaining intentional exceptions and close the audit loop.
      Evidence: relevant TypeScript checks and tests pass, deviations are documented, and the Objective records what was fixed versus deliberately accepted.

## Parked

- Redesigning Pi extension product behavior beyond type/style remediation.
- Introducing a heavyweight schema/validation dependency unless local guards become clearly insufficient.
- Broad non-TypeScript architecture cleanup outside the specific seams touched by TypeScript style compliance.
