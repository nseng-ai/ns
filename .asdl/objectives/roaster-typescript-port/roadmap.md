# Roadmap

## Work

- [x] Scaffold `ts/packages/roaster` mirroring `pr-address` (package.json, tsconfig, clinkr CLI entry) and port the pure core: unified-diff parsing + token estimation, review-definition frontmatter parsing/validation, path-applicability globs, and `asdl.toml [roaster.diff]` config parsing with glob → git-pathspec conversion
  - Evidence: `ts/packages/roaster/src` now includes `diff-parsing.ts`, `project-config.ts`, `review-applicability.ts`, `review-definition.ts`, and the package CLI scaffold. Unit tests cover the pure parser/config/review-definition surfaces, and the package check/test suite passed.
  - Progress: stack feedback hardening reused shared `@asdl/core/primitives` helpers in project-config and review-definition parsing, renamed a boolean option to predicate style, and made unified-diff raw-segment pairing parse each segment independently instead of zipping parser metadata by index. Evidence: local branch diff against `voided-stack-feedback-thread-bucket`; `just ts-check`, `just ts-test`, and `just dprint-check` passed.
- [x] Define the TS domain + error model: Zod schemas and discriminated-union failure types replacing the Pydantic models and `RoasterFailure` union, with TS-native markers and CLI JSON envelope
  - Evidence: `models.ts`, `failures.ts`, `findings-publication.ts`, and the CLI scenario tests now cover the TS-native request/response models, failure variants, findings payload parsing, summary/inline markers, and clinkr JSON envelopes used by `review run` and hidden `exec` commands.
- [x] Build the local-diff and review-catalog gateways (real + in-memory fake) on asdl-core's git/exec helpers
  - Evidence: current TS roaster sources include `local-diff.ts` and `review-catalog.ts` with gateway tests; stack feedback hardening changed the fake review-catalog setup to copy input maps rather than mutating a caller-owned map parameter. Evidence: local branch diff against `voided-stack-feedback-thread-bucket`; `just ts-check`, `just ts-test`, and `just dprint-check` passed.
  - Progress: shared asdl-core testing support now covers scripted command execution and in-memory Git behavior used by roaster gateway tests, letting roaster remove bespoke `StaticGitGateway`/scripted exec helpers from its support layer. Evidence: local branch diff against `master`; PR #1637 corroborates the same file set; `just ts-check` passed after restack resolution.
- [x] Port the Claude Code harness with the decided two-layer seam: semantic `HarnessGateway` + fake for the workflow; pure prompt-assembly, diff-cap/coverage, and `structured_output`/JSONL parsing functions with direct tests; real adapter with injected process-runner; stdin pump retained, progress streaming dropped
  - Evidence: `ts/packages/roaster/src/gateways/harness.ts` now provides the real/fake harness gateway, prompt helpers, diff-cap/coverage helper, Claude findings JSON schema builder, and Claude output parser; `@asdl/core/exec` now supports stdin so the prompt stays out of argv; roaster context construction includes the harness seam. Local branch diff against `roaster-shared-github-cli-helpers` and PR #1642 corroborate the file set. Verification: targeted core/roaster checks and tests passed, and full `just` passed.
- [x] Build the roaster-local GitHub PR gateway (changed-files+patch, review comments, create review, discussion summary comment) with real + fake, surfaced against the operations roaster actually uses
  - Evidence: current TS roaster sources include the GitHub gateway and gateway tests; stack feedback hardening made batched review creation clean up its temporary JSON input with `try/finally` and added test coverage for the cleanup behavior. Evidence: local branch diff against `voided-stack-feedback-thread-bucket`; `just ts-check`, `just ts-test`, and `just dprint-check` passed.
  - Progress: GitHub CLI execution and temporary JSON input writing were extracted to shared asdl-core helpers (`runGitHubCli`, `withTemporaryJsonFile`) and roaster's real GitHub gateway now consumes those helpers instead of owning duplicate command/cleanup code. Evidence: local branch diff against `master`; PR #1637 corroborates the same file set; `just ts-check` passed after restack resolution.
  - Progress: review feedback simplified the shared GitHub CLI helper to require `CommandExecApi` only, so roaster passes its injected exec API directly instead of routing through a runner adapter or dual-dispatch wrapper. Evidence: local branch diff against Graphite parent `roaster-review-cleanups`; PR #1658 corroborates the same file set. Verification: `pnpm --dir ts --filter @asdl/core run test`, `pnpm --dir ts --filter @asdl/core run check`, and `pnpm --dir ts --filter @asdl/roaster run check` passed.
- [x] Port findings publication (aggregate comment rendering, marker generation, activity-log merge) and inline-commentability classification (patch right-side line mapping) as pure modules with tests
  - Evidence: default-branch source already contains `ts/packages/roaster/src/findings-publication.ts`, `ts/packages/roaster/src/inline-commentability.ts`, and matching unit tests. The modules cover summary and inline markers, aggregate comment rendering, inline body rendering, activity-log preservation, changed-file patch line classification, and fallback-only reasons. Verification: `pnpm --dir ts exec vitest run --config vitest.config.ts packages/roaster/test/unit/findings-publication.test.ts packages/roaster/test/unit/inline-commentability.test.ts` passed.
- [x] Wire the TS `roaster review list` / `ls` CLI discovery surface, including `--applicable`, `--base-ref`, and the CI JSON envelope fields (`.data.keys`, `.data.count`, `reviews_dir`, `reviews[]`)
  - Evidence: `roaster review list` and `ls` are wired through the clinkr CLI with human and JSON rendering; scenario tests cover human output, JSON keys/count/default-model fields, alias behavior, applicable filtering through the fake local-diff context, and invalid review-definition failure. Verification: `pnpm --dir ts --filter @asdl/roaster run test` and `pnpm --dir ts --filter @asdl/roaster run check` passed.
- [x] Wire the TS `roaster review run <key>` CLI orchestration surface through review-catalog, model resolution, local diff loading, harness execution, and the structured findings run envelope
  - Evidence: `roaster review run` now loads review definitions, resolves model/default-model behavior, loads the local diff, calls the `HarnessGateway`, emits progress to stderr, and returns the structured findings payload in the clinkr JSON envelope. Scenario tests cover success with explicit/default model and missing-model failure. Verification: `pnpm --dir ts --filter @asdl/roaster run test` and `pnpm --dir ts --filter @asdl/roaster run check` passed.
- [x] Wire the hidden TS `exec` subgroup commands (`post-inline-findings`, `format-findings-comment`, `post-findings-comment`) to the roaster-local GitHub gateway and pure publication/inline modules
  - Evidence: the hidden `exec` subgroup is wired through raw clinkr commands; scenario tests cover help visibility, summary rendering, malformed stdin rejection, no-op behavior for empty/failed run envelopes, inline posting with duplicate marker handling, fallback-only/API-error reporting, inline result-file inclusion, summary comment create/update behavior, and marker validation. Verification: `pnpm --dir ts --filter @asdl/roaster run test` and `pnpm --dir ts --filter @asdl/roaster run check` passed.
- [~] Resolve post-cutover publication-contract feedback: hard-break `roaster review run --format json` to a single camelCase findings contract, replace lenient self-parsing with strict schemas, use the canonical record/map helper in `FakeHarnessGateway`, and split `harness.ts` into prompt, diff-cap, output, and gateway modules
  - Progress: an enriched plan was grilled and saved at `/Users/schrockn/.asdl/enriched-plan/gh--dagster-io--asdl-tools/off-master-1/hard-break-review-run-json-contracts.md`. Decisions: hard break the review-run JSON shape, make result data camelCase, remove duplicated findings payloads, strict-parse roaster-owned producer contracts, include the harness decomposition, and include the `FakeHarnessGateway` helper cleanup. Evidence: local branch has no implementation diff yet; this is a planned follow-up required before closeout.
  - Remaining: implement the saved plan, update roaster source/tests/workflow expectations as needed, and record validation evidence for the hard-break contract and harness decomposition.
- [x] Flip `.github/workflows/roaster.yml` to the built TS CLI and prove one green end-to-end run on a real PR (discovery → run → inline + summary comments)
  - Evidence: GitHub Actions run `27610014374` (`https://github.com/dagster-io/asdl-tools/actions/runs/27610014374`) succeeded on PR branch `roaster-ts-review-exec-cutover`; its discover job installed the TS workspace and its review matrix jobs ran `pnpm --dir ts exec roaster review run`, `post-inline-findings`, `format-findings-comment`, and `post-findings-comment` successfully.
- [~] Delete the Python `packages/roaster` and purge references in build config, CI, and docs (gated on the green TS CI run)
  - Progress: the green TS workflow evidence gate passed. This deletion branch removes `packages/roaster`, removes Python workspace/build/test/publish references from `pyproject.toml`, `uv.lock`, and `justfile`, deletes the Python roaster plugin smoke test, and preserves workflow YAML coverage in `tests/scenario/test_roaster_workflow.py`. Verification: `uv lock` succeeded; focused workflow/plugin scenario tests passed; the targeted stale Python package grep found no remaining `packages/roaster`, workspace-source, publish-package, or Python import references outside `ts/**`.
  - Remaining: broader `roaster` grep still finds documentation/domain references that are outside this deletion slice, including `AGENTS.md` examples, `CONTEXT-MAP.md` entries for deleted Python package context, and docs-site installation text that still describes `uv tool install roaster` / `asdl roaster`. Resolve those documentation references before closing this Objective.

## Parked

- [ ] Decide and implement asdl-plugin subgroup mounting for the TS roaster (deferred until standalone-CLI parity and CI cutover land; Python exposed `asdl.plugins`, TS analog TBD)
