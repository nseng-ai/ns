# Roadmap

## Work

- [ ] Grill and re-baseline slot occupancy locality on its own branch.
      Evidence: the Objective roadmap is updated with the selected semantic slices, grounded in current `asdl-core` Git occupancy types/gateways and `asdl-slots` lifecycle commands rather than the archived review alone.
- [ ] Review current occupancy handling across Git gateway facts and slot lifecycle commands.
      Evidence: identify where checked-out, rebase, bisect, dirty-worktree, and recovery-message decisions live; decide whether policy is duplicated, sufficiently local, or missing a shared seam.
- [ ] Implement, document, or park the selected locality outcome.
      Evidence: if implementation is selected, keep the slice narrow and test operation-state behavior; if documentation or parking is selected, record the rationale and current design constraints in this Objective.

## Parked

None yet. Parking decisions belong here only after the branch-local grilling/re-baseline produces evidence.
