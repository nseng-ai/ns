# Roadmap

## Work

Stack shape for `objective-stack-impl`: one Graphite stack of four branches, created in row order on top of `branch-context-key-plumbing/structural-cleanups` (the current top of the vocabulary stack). One row = one branch = one independently reviewable thesis. Rows 2–4 depend on nothing in row 1; row 3 builds on row 2's package cleanup (barrel trim) and row 4 imports row 3's relocated contracts, so the order is fixed. Validation: row 1 is prose-only (`just dprint-check`, autofix via `just dprint-fix`); rows 2–4 run full TS validation (`pnpm --dir ts run check && pnpm --dir ts run test`). Many CLI/extension messages are pinned in scenario tests — expect test-expectation churn on behavior-preserving refactors, and treat any pinned-message change as a prompt to re-verify behavior didn't change.

- [x] Branch `thermo-followups/vocabulary-and-docs` — align every prose surface with ADR 0006 vocabulary and restore lost load-bearing documentation. Evidence: branch-local docs/skill/CONTEXT diff passed `just dprint-check`; targeted greps found no active non-historical planned-branch surfaces after excluding ADR/retrospective history; `skills-lock.json` hashes were refreshed through `npx skills add` and local symlink layout restored.
  - Fix the "branch context = branch type" contradiction: rewrite `skills/branch-context/references/lifecycle.md:21` (Term-distinctions entry — define Branch context as the standing Branch Memory context per ADR 0006, not "an implementation branch created from a saved plan") and the "Create a Branch context" phrasing at `lifecycle.md:9`; `docs/pi/branch-context-workflow.md:17,67,89,93-96`; `docs/pi/README.md:180`; `skills/from-plan/SKILL.md:3,8` — use "create a branch and attach its branch context (`plan.md`)" phrasing.
  - Doc nits in the same pass: ADR 0005:49 ("`planned-branch impl` remains the only execution surface" → `branch-context exec load` / `/branch-context:impl`); load synopsis at `branch-context-workflow.md:115` gains the supported positional key (`branch-context exec load [<key>] …`, matching `cli.ts:122`); `skills/branch-context/SKILL.md:3` "saved branch-context plan" → enriched-plan vocabulary, and move the `/enriched-plan:save` / `/enriched-plan:grill-and-save` trigger phrases to `skills/enriched-plan-save/SKILL.md` only.
  - Restore the three dropped content groups from `git show master:docs/pi/planned-branch-workflow.md` into `branch-context-workflow.md` (as a "Mechanics and invariants" section) or `skills/branch-context/references/`: (a) Graphite creation mechanics — `git branch <target> HEAD` + `gt track <target> --parent <current> --no-interactive`, does not switch the checkout, attachment passes the target branch so storage is checkout-independent; (b) the `upstack-impl-session` resumption contract — creation precondition ordering, candidate selection order (`--branch` → session artifact → current branch), read-only verification guarantee, explicit-ambiguity refusal, recovery examples; (c) plan-path normalization rules (`~`/`~/`, leading `@` stripped, must end `.md`).
  - Document the primitives: add an attach/list/check/delete section to `branch-context-workflow.md` (commands exist at `branch-context/src/cli.ts:127-165`); update `skills/branch-context/references/diagnostics-admin.md:7-12,46-51` to prefer `branch-context exec list/check` over raw `brmem list/get`.
  - CONTEXT rebaseline (explicit domain-language task, absorbed from the closed `additive-plan-vocabulary` parked item with widened scope; reference that record, do not edit it): `ts/packages/pi-extensions/CONTEXT.md:84-120` (store path `~/.asdl/enriched-plan`, CLI `branch-context`, namespace `branch-context`, key `plan.md`, skill family names, term entries); `ts/packages/ccc/CONTEXT.md:3,8,24,72` (planned-branch execution references → branch-context); `CONTEXT-MAP.md:24,25,55,70,79,82` (`@asdl/planned-branch` → `@asdl/branch-context`, routing rows, flagged ambiguities).
  - Evidence: `just dprint-check` green; repo grep shows no active (non-historical) doc defining branch context as a branch type and no active doc naming the dead CLI/namespace/store path. If editing SKILL.md content requires a `skills-lock.json` refresh, use the skill-management flow rather than hand-editing the lock.
- [ ] Branch `thermo-followups/package-cleanup` — delete the plumbing debt inside `@asdl/branch-context`: required context, single validation site, real export surface, focused scenario tests.
  - Required context: make `brmem`/`graphite` required on `BranchContextContext` (`src/context.ts:7-12`); delete the six `?? new Real*Gateway(...)` fallback sites (`attach.ts:169-178`, `branch-context-creation.ts:72-74,162`, `attached-plan.ts:118-119`, `existing-branch-reuse.ts:44,79`) and the `BranchContextPrimitiveContext` shim (`attach.ts:45-49`); remove the gateway optionality from the four `*Options` interfaces; have `cli.ts` pass the resolved context down instead of flattening into per-call options bags (`cli.ts:236-243`).
  - Dead validation layer: type `createBranchContextFromFile`'s params as `CreateBranchContextFromFileParams` (`branch-context-creation.ts:66-69`); delete `parseCreateBranchContextFromFileParams`, `normalizeBranchCreationMethod`, `tryNormalizeBranchCreationMethod` (~70 lines, no external consumers) and their tests; keep the CLI's Zod boundary (`cli.ts:181-183`) as the single slug-validation site — the "Invalid plan slug" message at `creation.ts:122` is unreachable from the CLI and goes with the layer.
  - Barrel trim: reduce `src/index.ts` (101 lines) to the externally consumed surface — `BRANCH_CONTEXT_NAMESPACE`, `BRANCH_CONTEXT_PLAN_KEY`, `BRANCH_CONTEXT_OUTPUT_MESSAGE_TYPE`, `BranchContextEvidence`, `extractBranchContextEvidence(FromSessionEntry)`, `formatBranchContextEvidence`, `BranchContextOutputDetails`, `createBranchContextFromFile`, `buildBranchContextCreateOperation`, `BranchContextCreateOperation`, `BranchCreationMethod`, `formatBranchContextCreatePreview/Failure`, `resolveBranchContextCreatePreviewContext`, `resolveExistingBranchContextReuse`, `ExistingBranchContextReuse`, `formatExistingBranchContextReuse`, `deriveTargetBranch`, `loadBranchContextPlan`, `LoadedAttachedPlan`, `formatLoadedAttachedPlanEvidence`, `buildImplBranchContextPrompt`, `derivePlanContentSlug`, `PlanContentSlugEvidence`, `buildPlanContentSlugPrompt` (verified import inventory across ccc/pi-extensions). `runCli` needs no export — `package.json` bin points directly at `src/cli.ts`.
  - Residue and layering in the same pass: delete the self-aliased import and consumer-less re-export in `session-artifact.ts:7,11`; delete `RealCommandExecApi` identity subclass and the pass-through re-exports in `context.ts:24-26` (use `NodeCommandExecApi` directly); export brmem parser symbols only from `brmem-gateway.ts`, not via `attached-plan.ts:13-14`; move `attachBranchContext`/`assertBrmemEntryAbsent`/`AttachBranchContextError` from `branch-context-creation.ts` into `attach.ts` and reverse the import direction (`attach.ts:3`); remove the unreachable `match === undefined` branch (`attach.ts:156-157`); drop the consumer-less re-exports in `plan-content-slug.ts:14,45-47`.
  - Scenario-test split: extract `test/support/cli-harness.ts` (`runWithFakes`, `CliRun`, `parseJson`, `jsonFailure`, `writeSavedPlan` — currently duplicated verbatim in `attach.test.ts:36-47`); delete the unreachable `FakeCommands`/`ScriptedExec` machinery (`cli.test.ts:93-141`) after mechanically re-confirming all 44 `runWithFakes` calls pass empty scripts; split `cli.test.ts` (1037 lines) into `cli-surface` / `cli-from-plan` / `cli-load` / `cli-entry-ops` files; trim the attach/list/check/delete scenarios (`cli.test.ts:707-886`) to envelope/exit-code pins, leaving message-level assertions to `attach.test.ts`.
  - Evidence: full TS validation green; every resulting test file well under 1k lines; no `??`-gateway-fallback remains in the package.
- [ ] Branch `thermo-followups/canonical-contracts` — one canonical home for the impl command and the output-message contract; ccc consumes instead of duplicating.
  - Impl command: new `ts/packages/branch-context/src/impl-command.ts` exporting `IMPL_BRANCH_CONTEXT_COMMAND_NAME = "branch-context:impl"` and `formatImplBranchContextCommand` (elision against `BRANCH_CONTEXT_PLAN_KEY` from `constants.ts`); ccc (`branch-context-up-and-impl.ts:96`, `slot-dispatch-plan.ts:331`) and pi-extensions (`branch-context-extension.ts:42,955`) import it; delete the ccc copy, the duplicate literal, and the cross-feature import at `slot-dispatch-plan.ts:25`. `formatBranchContextUpAndImplFollowUpFlow` stays in ccc and imports the formatter.
  - Output-message contract: in `session-artifact.ts`, define `BranchContextOutputDetails` as a discriminated union on `status` covering what producers actually emit (audit `slot-dispatch-plan.ts:140-146,235,255-273` and the extension's `presentBranchContextMessage` dialect near `branch-context-extension.ts:1257`), keeping the success variant aligned with `successfulBranchContextOutputDetailsSchema`; export a `buildBranchContextOutputMessage(content, details)` builder beside the parser; type both producers against it. Decision recorded: nothing programmatic consumes the dry-run fields (`extractBranchContextEvidence` parses only `status: "success"` + `evidence`) — keep a typed dry-run variant only where its fields add information beyond the human-readable content string; trim unread fields otherwise.
  - ccc residue: add canonical `findLatestBranchContextEvidence(entries)` to `session-artifact.ts` adopting the stricter namespace filter from `existing-branch-reuse.ts:111-125`; delete `findLatestBranchContextSelection` in `slot-open-branch.ts:106-114` (exported, zero consumers, stale name); `ccc-test-harness.ts:26` imports `BRANCH_CONTEXT_PLAN_KEY` instead of the `"plan.md"` literal and `ccc.test.ts:279` uses `SAVED_PLAN_FILENAME`; delete the no-op `if ("error" in launched) { return; }` tail (`slot-dispatch-plan.ts:248-250`); in `branch-context-up-and-impl.ts` build `parentSessionPart` once (lines 62, 69-71, 89) and use the destructured `key` at line 66; rename `BranchContextUpAndImplLaunchOptions.evidence` → `target`; pass the `BranchContextCreateOperation` (or a `Pick`) to `formatFinalSuccess`/`formatPiLaunchCommand` instead of loose `branch`/`key` strings.
  - Evidence: full TS validation green; repo grep finds exactly one `/branch-context:impl` source-of-truth literal in TS source (docs excluded).
- [ ] Branch `thermo-followups/extension-decomposition` — decompose the Pi extension by command family and put each test in its canonical package.
  - Status-sequencing fix first (the one behavior-affecting change — restores intended behavior): split `resolveCreateBranchContextPreview` at both call sites (`branch-context-extension.ts:707-709` and the twin near 766) into resolve-plan-file → `setStatus("deriving branch slug…")` → derive-slug so each status brackets the work it describes; also delete the immediately-overwritten duplicate progress emission at lines 1028-1034.
  - Decompose `branch-context-extension.ts` (1284 lines) into `src/branch-context/host-types.ts` (the ExtensionAPI/CommandContext/ToolContext/ToolDefinition/CustomMessage declarations, lines 48-216), `src/branch-context/enriched-plan-save.ts` (the `enriched-plan:*` commands, prompt builders, and the `write_saved_plan_file` tool, ~550 lines), `src/branch-context/from-plan-commands.ts` (arg parsing, preview, the three `branch-context:*` handlers, presentation helpers, ~500 lines), leaving `branch-context-extension.ts` as a ~60-line registrar. Safe because the `.pi/extensions/branch-context.ts` adapter imports only the default export — re-verify no other importers before splitting.
  - Preview shape: delete the constants-as-data `namespace`/`key` fields from `CreateBranchContextPreview` (lines 150-151; formatters reference `BRANCH_CONTEXT_NAMESPACE`/`BRANCH_CONTEXT_PLAN_KEY` directly); if churn stays low, discriminate the preview on `mode` so `repoRoot`/`repoKey` are required where they exist instead of flat-optional; prefer carrying the explicit-branch fact from `deriveBranchContextTargetBranch` over the `targetBranch !== slug` equality inference at line 883.
  - Test homes: split the 1319-line single-describe `branch-context-extension-commands.test.ts` by command family (`enriched-plan-commands`, `branch-context-impl`, `branch-context-from-plan`, `branch-context-upstack-impl-session`) — the support module already carries the shared fixtures; move `branch-context-saved-plan-file.test.ts` and the six canonical-primitive describes from `branch-context-extension-helpers.test.ts` into `ts/packages/plans/test/` (this includes the repo's only `writeSavedPlanFile` coverage); drop the `formatBranchContextEvidence` describe as a duplicate of branch-context's own; move `src/branch-context/saved-plan-content-slug.ts` plus `test/plan-content-slug.test.ts` into `@asdl/plans` (its only collaborator is `writeSavedPlanFile` and the `/enriched-plan` path convention is plans domain).
  - Evidence: full TS validation green; registrar and every new module well under 1k lines; `helpers` test reduced to genuinely local exports (`parseCreateBranchContextArgs`, prompt builders, `formatCreateBranchContextPreview`).

## Parked

- Zod-first boundary conversions: `brmem-gateway.ts:205-331` typeof ladders and the extension's `parseResolvePromptJson` / `parseWriteSavedPlanFileToolParams` — pre-existing debt; zod is already a dependency in both packages.
- Cosmetic residue: duplicate `formatCompactNumber` in pi-extensions, roaster test fixtures referencing the deleted `planned-branch-extension.test.ts` path, `planned-branches/` fixture branch names in asdl-core tests.
- `formatBranchContextCreateFailure` positional `.replace()` surgery in `slot-dispatch-plan.ts:313-316` — replace with a composable message API if the message ever changes shape.
- `cli.ts` evidence-mapper boilerplate consolidation (four near-identical snake_case mappers) — low value unless a fifth command appears.
