# Roadmap

## Work

- [x] Write the first failing tests for Git active-root touch semantics: additions, modifications, deletions, renames/moves, bare record paths, invalid slugs, and archive-root ignores at the existing `GitGateway.path_touches_under()` / Objective slug extraction seam. Evidence: slice `objective-gt-stacks-rewrite/git-touch-seam` added parser, real-gateway, and Objective slug extraction coverage; targeted pytest and ruff/format checks passed.
- [x] Decide from those tests whether the existing Git seam is sufficient; if not, deepen the Git interface with real/fake coverage before writing projection logic against it. Evidence: the existing `PathChangeTouch.paths` seam remained sufficient after switching the real command/parser to `git log --name-status -M`, preserving both active-root sides of renames without adding a richer Git API.
- [x] Write failing projection tests for the spec worked example using `FakeGtGateway` and `FakeGitGateway`, asserting JSON-semantic facts rather than rendered glyphs. Evidence: slice `objective-gt-stacks-rewrite/projection-core` added a worked-example JSON semantic test for statuses, counts, latest work, segments, connector rows, also-touches, validation result, and restack facts.
- [x] Add failing projection tests for branch scope filtering: current Graphite trunk only, locally present branches only, broken local parent chains, untracked Git branches excluded, trunk included as graph anchor, and current checkout branch not required to be tracked. Evidence: projection-core coverage filters to trunk-connected local Graphite branches, excludes a local untracked branch, skips a branch severed by a missing local parent, de-duplicates warnings, and uses `GtGateway.branch_graph()` without current-branch tracking assumptions.
- [x] Add failing projection tests for `parent..branch` slice attribution, many-to-many branch/Objectives, `also_touches`, active-root-only touches, archive-root ignores, Objective status projection from trunk, latest-work tie-breaking, deterministic ordering, and warning de-duplication. Evidence: projection-core tests cover parent-slice attribution, many-to-many grouping, `also_touches`, open/closed/in-flight trunk status projection, latest-work tie-breaking, group/row ordering, and warning de-duplication; final-hardening added projection-level coverage proving archive-root paths do not create groups or `also_touches` even when branch change data includes them.
- [x] Implement and refactor the deep semantic projection module until the projection tests pass with locality: Graphite graph and Git/Objective reads in, semantic Objective stack projection out. Evidence: `gt_stack_projection.py` exposes semantic dataclasses and `build_objective_gt_stack_projection(repo_root, git, gt)` over `GtGateway.branch_graph()` and `GitGateway` reads, with JSON-data conversion free of glyphs/rendering annotations; final-hardening confirmed the existing data-read failure paths produce stable CLI envelopes without further production code changes.
- [x] Add failing renderer tests for human and Markdown output, then implement renderers over the semantic projection without putting glyphs or annotation strings in JSON models. Evidence: slice `objective-gt-stacks-rewrite/cli-renderer-shell` added human and Markdown renderers over Pydantic DTOs derived from the semantic projection; glyphs, status labels, latest strings, and Graphite annotations remain renderer-only.
- [x] Add failing CLI scenario tests for `objective gt`, `objective gt stacks`, help, `--json-schema`, all formats, empty output, failure envelopes, and plugin discovery; then wire the command and Graphite-specific context. Evidence: the Python CLI exposes visible `objective gt stacks`, JSON/human/markdown/md/help/schema scenarios pass, empty-state and Graphite metadata/not-in-repo failure envelopes are covered, plugin discovery smokes the new subgroup, and final-hardening added data-read failure envelope coverage for per-branch touch reads and trunk status reads.
- [x] Add failing TypeScript tests for `/objective-gt-stacks` registration, completions, strict argument rejection, idle-first execution, markdown/default command invocation, help invocation, success, command failure, startup failure, timeout, truncation, and display-message details. Evidence: slice `objective-gt-stacks-rewrite/pi-wrapper` added Objective Pi extension tests covering registration, completions, parser rejection, idle-first default/help execution, success fallback behavior, nonzero/killed failure presentation, truncation, startup errors, and structured display-message details; targeted objective test and full Pi extension test suite passed.
- [x] Implement `/objective-gt-stacks` inside the existing Objective Pi extension using the `/objective-list` display-wrapper pattern while keeping it a presentation adapter over the CLI. Evidence: slice `objective-gt-stacks-rewrite/pi-wrapper` registers the command, suggests only help flags, rejects unsupported arguments before execution, runs `objective gt stacks --format markdown` or `objective gt stacks --help` with a 30-second timeout after `waitForIdle()`, and presents success/failure/rejected output through `objective-gt-stacks-output` messages without parsing the projection.
- [x] Run the relevant Python, Markdown, and TypeScript validation commands; record meaningful evidence and any changed assumptions through `objective-update`. Evidence: targeted Python pytest, ruff, format, `ty`, Objective Markdown dprint checks, targeted Pi extension objective tests, full Pi extension `bun test`, Pi extension `bun run check`, and repo-level `just check` passed after the final hardening slice.

## Parked

- [ ] Interactive Objective stack viewer or TUI on top of the JSON contract.
- [ ] Cross-trunk, remote, or network-aware Graphite stack scanning.
- [ ] Graphite behavior in generic Objective commands such as `objective list`.
- [ ] Objective branch attachment, hidden state, registries, UUIDs, or workflow-control semantics.
- [ ] A richer Git path-change interface unless the TDD deletion/rename tests prove the current seam cannot satisfy the spec.
- [ ] A shared Pi display-wrapper helper for `/objective-list` and `/objective-gt-stacks` unless duplication becomes meaningfully shallow.
- [ ] Live local Graphite stack smoke test as optional confidence evidence, not a closure requirement.
- [ ] Automatic PR submission or stack submission.
