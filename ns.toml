[areg]
agents = ["codex", "claude-code"]

[roaster.diff]
exclude = [
  ".agents/skills/**/*.py",
  ".claude/skills/**/*.py",
]

[roaster.model_profiles]
quick = "haiku"
deep = "sonnet"

[flow.hooks]
pre_submit = ["just"]
