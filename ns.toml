[areg]
agents = ["codex", "claude-code"]

[reviews.diff]
exclude = [
  ".agents/skills/**/*.py",
  ".claude/skills/**/*.py",
]

[reviews.model_profiles]
quick = "haiku"
deep = "sonnet"

[points]
"flow.submit.pre" = ["just"]
