# Roadmap

> Path/package names rebaselined 2026-06-26 for the `@asdl`→`@sdl` workspace rename and the `pi-extensions`→`hosts/pi`, `core`/`clinkr`→`infra/*`, and standalone `@sdl/autobranch` relocations. Per-row parity-table names still await a full sweep.

## Work

- [x] Seed the living parity table (`parity-table.md`) from the 2026-06-03 cross-harness audit: one row per Pi command with shared CLI backing, driving skill, parity verdict (FULL/PARTIAL/NONE/WAIVED), and owning Objective. Evidence: `parity-table.md` exists and classifies the audited current Pi command surfaces; removed surfaces are deleted during refreshes rather than retained as stale FULL rows. Pi tool calls are now treated as host-native bridges that do not require parity metadata rows.

- [x] Close the dev-CLI discoverability gaps: author thin skills surfacing the dev CLI's preview-url/checkpoint/submit workflows to Claude/Codex (describing CLI operations only), and reconcile the checkpoint-skill vs checkpoint-CLI duplication. Evidence: `code-checkpoint` now delegates to the shared checkpoint CLI; the dev-CLI rows moved to FULL. Note: with the `@asdl`→`@sdl` rename, the `dev-preview-url` and `sdl-submit` skills are no longer present under `skills/`; re-verify the corresponding rows in the next full sweep.

- [x] Eliminate the `/handoff:list` duplication: point the Pi command at the dedicated `handoff list --format json` CLI and keep only the card renderer in TypeScript, removing the TS re-derivation of listing/preview over raw `brmem list`. Evidence: `/handoff:list` now calls `handoff list --branch/--all --format json`; the parser accepts the CLI `handoffs` array while preserving legacy `entries` compatibility; branch-state/`--include-deleted` are available in the CLI.

- [x] Author the durable parity-review skill: diff-scoped mode flags any added/changed Pi command lacking a CLI+skill counterpart (or a recorded waiver); on-demand full-sweep mode checks and refreshes the parity table. Evidence: the workflow is now the `parity-review` route of the `code-workflows` skill (renamed 2026-06-11 from `internal-code-workflows`), documenting diff-scoped and full-sweep modes, live-evidence-first command inventory, CLI-bridge treatment, advisory finding labels, and Objective table sync with a required Semantic Update. Reference: `skills/code-workflows/references/parity-review.md`.

- [x] Add a typed Pi-host parity accounting gate. Evidence: package registration modules export co-located command `PiSurfaceParity` metadata; `ts/packages/hosts/pi/src/parity/registry.ts` aggregates the package-owned records and `ts/packages/hosts/pi/src/parity/{check,extension}.ts` enforce them; `ts/packages/hosts/pi/test/parity.test.ts` registers the package modules against a fake Pi host and fails on missing, stale, or duplicate exact command metadata without parsing Markdown. Scope is v1/package-local: ad hoc `.pi/extensions/*.ts` adapters and direct `@sdl/ccc` command surfaces are not enforced unless exposed through `@sdl/pi`; Pi tool calls are ignored by the metadata gate.

- [x] Give `autobranch` a CLI entry point + skill, and bridge Pi `/code:autobranch` to the same core. Evidence: the autobranch core is extracted into the standalone `@sdl/autobranch` package (`ts/packages/autobranch/`: branch-name selection, dirty/latest-commit transactions, slug) with the checkpoint/flow in `ts/packages/ccc/src/autobranch/`; `@sdl/ccc` ships the `ccc` bin with a hidden `ccc exec autobranch` (`ts/packages/ccc/src/cli.ts`, `ClinkrGroup` with `isHidden: true`); `ts/packages/ccc/test/scenario/autobranch-cli.test.ts` covers help/version/runtime, dirty-worktree success, latest-commit success, requested slugs, and failure exit codes; `code-autobranch` is installed as a skill driving the CLI; Pi `/code:autobranch` invokes the CLI in process through the shared bridge.

- [x] Consolidate provider-specific model defaults behind the backend-neutral text-generation abstraction/config. Evidence: the model-defaults/model-slug seam is now `@sdl/core` (`ts/packages/infra/core/src/model-slug.ts`, `ts/packages/infra/core/test/model-defaults.test.ts`); the canonical slug helper honors `SDL_SLUG_MODEL` over the shared fast-model default; the ccc sidebar and fast-text-draft model branches resolve through the same seam with `SDL_*` env overrides (`SDL_CCC_SIDEBAR_MODEL`, `SDL_DEV_CHECKPOINT_MODEL`, `PI_DRAFT_MODEL`, etc.); shared prompt/skill guidance carries harness-neutral defaults with labeled OpenAI/Anthropic examples (AGENTS.md "Skill Model Examples" convention).

- [ ] Implement parity-native command-output summaries as a shared CLI/helper, building on the `ts-cli-foundation` layer (`@sdl/clinkr` + `@sdl/core`) if implemented in TypeScript. Evidence: the contract names command/cwd/timeout/cancellation semantics, shell/argv invocation modes, outcome taxonomy, payload artifact log paths, bounded human and machine-readable summaries, profile-derived counts/excerpts for generic/test/lint/typecheck output where practical, no-leak guarantees, and skill guidance plus optional Pi adapter that wraps the same helper instead of becoming canonical.

- [ ] Give the stack-landing core a clinkr-based CLI entry point + skill. The orchestration is already extracted and test-backed in `@sdl/ccc` (`ts/packages/ccc/src/land.ts` + `land-stack/`: Graphite SQLite-metadata topology, fork-violation detection, backup refs, pre-delete child guards, sequenced merge loop; covered by `ts/packages/ccc/test/land-command.test.ts` and `land-stack.test.ts`) — but the `ccc` bin does not expose land and no skill points at it, so only Pi can reach it. Add the CLI entry on `ts-cli-foundation`, author the skill (the single-PR path may stay skill-only per the skill-only parity row), and keep Pi `/code:land` importing the same core. Evidence: CLI scenario tests; Claude/Codex can land a stack via the skill; Pi carries no duplicate logic.

- [ ] Give the cmux dispatch cores a CLI entry point + skill: a CLI with explicit inputs (plan-file path, prompt) over the `@sdl/ccc` cmux modules (`ts/packages/ccc/src/cmux/dispatch-from-trunk.ts`, `dispatch-prompt.ts`, `slot-dispatch-plan.ts`, `slot-open-branch.ts`) and the shared `focused-terminal-tab.ts` / `prompt-file.ts` utilities, with an agent-neutral launch path (not a hardcoded `pi @file`); cover `/ccc:workspace:dispatch-plan`, `/ccc:workspace:dispatch-prompt`, and a thin skill for the `/ccc:workspace:open-branch` (`slot checkout` + `cmux new-workspace`) pairing. Pi keeps only session-history "latest plan" resolution over the core. Decide `/ccc:claude-plan-tab` alongside (likely WAIVED: Pi/cmux session-native tab launch) and record the verdict in the parity table. Evidence: a Claude/Codex agent can dispatch a branch/plan into a cmux slot via skill+CLI; the `/ccc:workspace:*` rows move off NONE.

- [ ] Decide `/code:autoslot`: either add a portable skill/CLI path for the CCC autoslot branch-plus-slot workflow or record a deliberate WAIVED verdict if managed slot movement is accepted as Pi-only convenience. Keep it PARTIAL until that decision is implemented because it mutates branch/worktree/slot state. Evidence: parity table's `autoslot` row reaches FULL or WAIVED.

- [ ] Decide `/code:push`: either add a thin skill documenting the clean-worktree `git push` guard or explicitly treat it as primitive git usage. Keep it PARTIAL until that decision is implemented because it mutates remote branch state. Evidence: parity table's `push` row reaches FULL or WAIVED.

- [ ] Provide skill-only parity for the single-PR path of the unified `/code:land`: a skill documenting the single-PR land contract over `gh` (base-must-be-trunk guard, `--match-head-commit` pinning, PR title/body as commit message). `/code:changes` is resolved as WAIVED because it is a read-only Pi UX helper over ordinary git/worktree evidence. Evidence: parity table's `land` row reaches FULL or WAIVED.

- [ ] Reconcile `parity-table.md` with the 2026-06-26 `@asdl`→`@sdl` rename, package relocations, and Capability Kit migration: run a parity-review full sweep that re-verifies each row's current binary/package/skill name and re-confirms or corrects its verdict, and re-verify the now-missing `dev-preview-url` / `sdl-submit` / `objective-current` skill rows. Evidence: a full-sweep Semantic Update reconciling the table against live `@sdl/*` evidence.

## Parked

- [ ] Broaden the machine-checkable parity gate beyond v1 accounting: decide whether to add CI wiring, direct `@sdl/ccc` Pi command surfaces, ad hoc `.pi/extensions/*.ts` adapters, and/or generated human table output from the typed metadata. The current implemented gate covers only `@sdl/pi` package-owned command registrations and checks accounting, not semantic parity correctness. (The sixth parity-rot materialization has re-fired the trigger to promote this to active work.)
- [ ] Broad automatic bash-output interceptor or framework-wide output safety net.
- [ ] Pi-only runner-subagent test-summary tool as the canonical command-summary implementation.
- [ ] LM/subagent semantic interpretation of command logs as the default summarizer.
- [ ] Migration of every validation command or package workflow to command-output summaries.
- [ ] Payload retention, garbage collection, durable archive policy, or strict numeric token-budget tests for command logs.
