---
name: plan-to-branch
description: "Stamp an existing plan file onto a new Graphite branch. Resolves a plan file (from current conversation context, an explicit path argument, or — as last resort — the most recent file in `~/.claude/plans/`), generates a kebab-case slug that summarizes the plan, creates a new branch with `gt create`, writes the plan to `plan-<slug>.md` at the repo root, and commits it as the first commit on that branch. Use when the user wants to 'branch this plan', 'stamp the plan', 'turn this plan into a branch', or right after exiting plan mode to commit the plan as the branch's spec."
allowed-tools:
  - "Bash(gt *)"
  - "Bash(git status *)"
  - "Bash(git add *)"
  - "Bash(git diff *)"
  - "Bash(git rev-parse *)"
  - "Bash(git branch *)"
  - "Bash(ls *)"
  - "Read"
  - "Write"
---

<!-- PUBLIC SKILL: Project-agnostic. Do not reference twerk-internal module paths or class names. Describe shell operations only. -->

# plan-to-branch

Stamp an existing plan file onto a new Graphite branch as that branch's first
commit. The committed plan becomes the durable, code-tracked spec the rest of
the branch's commits work against.

## Goal

Given a plan file (from conversation context or an explicit path), produce:

1. a new Graphite branch whose name is a kebab-case slug summarizing the plan
2. a `plan-<slug>.md` file at the repo root containing the plan verbatim
3. a single first commit on that branch adding only that file

Responsibility ends at the local commit. No push, no `gt submit`.

## Core rules

- **Copy the plan verbatim.** Do not regenerate, summarize, or rewrite.
- **Slug is generated by the model** from the plan's title + intent. No slug
  utility, no script — the model produces it directly per the rules below.
- **Bare slug branch name.** No `plan/` prefix. Flat feature branches stay
  project-neutral and match common conventions.
- **Hermetic commit.** The working tree must be clean before stamping; the
  commit must touch only `plan-<slug>.md`.
- **Never push.** Never `gt submit`. The skill ends at the local commit.

## Workflow

### 1. Resolve the plan file (context-first)

Try in order; stop at the first that succeeds:

1. **Explicit argument.** If the user passed a path, use it. Error if the
   file doesn't exist or isn't readable.
2. **Conversation context.** Scan recent context for a plan file path.
   Plan-mode system reminders include paths like
   `~/.claude/plans/<adjective-verb-noun>.md`. If exactly one such path
   appears recently, use it. If multiple distinct paths appear, pick the
   most recent reference and call out that choice in the report.
3. **Filesystem fallback.** `ls -t ~/.claude/plans/*.md | head -1`.

If all three fail, abort with a clear error explaining what was tried.

### 2. Generate the slug

Read the full plan content, then produce a kebab-case slug from the title +
intent:

- lowercase ASCII, hyphen-separated, ≤50 characters
- leads with a verb when natural (`add-`, `refactor-`, `migrate-`, `rename-`)
- no `-plan` suffix — the `plan-` filename prefix already signals that
- describes the *change*, not the document (e.g. `add-plan-to-branch-skill`,
  not `plan-for-branch-stamping`)

The slug doubles as both the branch name and the suffix in `plan-<slug>.md`.

### 3. Pre-flight checks

Run these and abort on the first failure:

- `git rev-parse --show-toplevel` → capture repo root. Error if not in a
  git repo.
- `git status --porcelain` → must be empty. If dirty, abort and tell the
  user to stash or commit first.
- `gt --version` → confirm Graphite is installed. If missing, abort and
  suggest installing `gt` or doing the commit manually.
- `gt branch info` → if the current branch is untracked by Graphite, run
  `gt track -p main` (or the repo's trunk if not `main`) before continuing.
- Check whether `<repo-root>/plan-<slug>.md` already exists. If so, stop
  and ask the user for explicit confirmation before overwriting.
- Reject detached HEAD with a clear error.

### 4. Write, stage, guard, and create the branch

Follow the Graphite write → stage → guard → `gt create -m` pattern. The
guard is required: `gt create -m` commits everything currently in the
index, so without it any other files that snuck into the index between
the pre-flight check and now would be silently swept into the plan
commit.

1. Use the `Write` tool to create `<repo-root>/plan-<slug>.md` with the
   plan content verbatim.
2. `git add <repo-root>/plan-<slug>.md` — stage **only** the plan file.
   Never use `git add -A`, `git add .`, or any wildcard.
3. **Final guard.** Run `git status --porcelain` and confirm the output
   is exactly one line of the form:
   ```
   A  plan-<slug>.md
   ```
   If any other line appears (additional staged file, unstaged
   modification, untracked file other than the plan file, etc.) — abort.
   The pre-flight in step 3 establishes a clean tree; this guard
   re-verifies it immediately before the commit and is what actually
   prevents stray files from entering the plan commit. Leave the staged
   plan file in the index on abort so the user can inspect; do not
   auto-`reset`.
4. `gt create <slug> -m "Add plan-<slug>.md: <one-line summary>"`

Commit message:
- First line `Add plan-<slug>.md: <one-line summary>`, ≤72 characters.
- Body only if the plan's title adds genuinely useful information.

### 5. Report

Print a short summary to the user:

- branch name created
- commit SHA (`git rev-parse HEAD`)
- source plan file path (so the user can spot a wrong context-resolution)
- next-step hint: "Now implement against `plan-<slug>.md`. Use `gt create`
  for stacked slices and `gt submit --no-interactive` when ready."

If the current branch was not the trunk (the new branch is being stacked
on top of an existing branch), say so explicitly so the user isn't
surprised.

## Edge cases

- **Not in a git repo** → abort with guidance.
- **`gt` not installed** → abort; suggest installing Graphite or doing the
  commit manually with `git checkout -b` + `git commit`.
- **Detached HEAD** → abort with guidance.
- **Already on a non-trunk branch** → fine; the plan branch stacks on top.
  Mention it in the report.
- **Plan file very small (<20 lines)** → warn but proceed.
- **`plan-<slug>.md` already exists** → confirm before overwriting.
- **Multiple plan paths in context** → pick the most recent reference,
  name the choice in the report so the user can re-run with an explicit
  path if wrong.

## Anti-patterns

- Regenerating, summarizing, or rewriting the plan instead of copying
  verbatim.
- Creating a slug utility anywhere — the model produces the slug.
- Skipping the dirty-tree check, producing a mixed plan + work commit.
- Auto-pushing or `gt submit`ing — the skill ends at the local commit.
- Prefixing the branch with `plan/` — flat branches keep the skill
  project-neutral.
- Including more than `plan-<slug>.md` in the commit.
