# Context Map

This map is the repository entry point for domain-language context. It records which context files exist, which context files are planned, and which relationship or ambiguity notes must be carried into later focused context sessions.

## Inventory Baseline

Current checkout facts:

- Python workspace inventory: no active first-party Python workspace packages remain.
- In-scope Python context targets: none. Former Python capability packages were ported to TypeScript, retired, or deleted as migration reference material.
- TypeScript workspace inventory: 34 repo-local packages under `ts/packages/` (`find ts/packages -path '*/package.json' | wc -l` = 34), named `@sdl/*` with scoped local-Pi-tool exceptions under `@local-pi-tools/*` and three unscoped exceptions — `sdlcc` (`ts/packages/hosts/sdlcc`), `sdl-flow` (`ts/packages/capabilities/flow`), and `sdl-land` (`ts/packages/capabilities/land`). Thirteen have present package context files; the rest are Planned, undecided (awaiting a recorded map decision), or out of scope. This map's TypeScript package-context coverage is intentionally partial pending the remaining focused rebaseline phases.
- Present context files: root `CONTEXT.md`, `ts/packages/handoff/CONTEXT.md`, `ts/packages/infra/brmem/CONTEXT.md`, `ts/packages/hosts/pi/CONTEXT.md`, `ts/packages/ccc/CONTEXT.md`, `ts/packages/kernel/CONTEXT.md`, `ts/packages/roaster/CONTEXT.md`, `ts/packages/sdl-capability-kit/src/graphite/CONTEXT.md`, `ts/packages/capabilities/plans/CONTEXT.md`, `ts/packages/branch-context/CONTEXT.md`, `ts/packages/capabilities/objective/CONTEXT.md`, `ts/packages/capabilities/slot/CONTEXT.md`, `ts/packages/capabilities/flow/CONTEXT.md`, and `ts/packages/capabilities/land/CONTEXT.md`.
- Future drift should be handled by focused rebaseline phases before final readback, not silently folded into unrelated package-context sessions.

## Contexts

### Present

- [SDL Tools](./CONTEXT.md) — Objective-system vocabulary for checked-in durable narrative roadmap records, Active Objective Root, Objective Archive Root, Archived Objective, Objective Update, Objective Close, Objective Archive, Semantic Update, Tracking Gate, and Closure Marker; plus an **Architecture Boundaries** section defining the repo-wide Gateway and Domain logic terms (canonical definitions replicated from the `typescript-fake-driven-testing` skill), an **Extension Layering** cluster (Extension, Capability, First-party extension, Capability Kit, Capability API, Consumer/Provider, CCC, Package Tier) whose layering diagram and domain-placement rule live in `docs/adr/0012-domain-package-layer-above-extension-kit.md` (refining ADR 0009), and package-topology terms for Published package, Standalone package, Container package, Subpackage, Remainder subpackage, Local space, Topology circle, and Topology overlay. Package tiers are declared in `package.json` as `sdl.tier` and enforced by the TypeScript style guard.
- [@sdl/handoff](./ts/packages/handoff/CONTEXT.md) — active directed handoff artifact vocabulary over Branch Memory storage for the Handoff Capability: continuation focus, Create a Handoff, Pick Up a Handoff, List Handoffs, and Delete a Handoff actions, handoff slug/key, `handoff` namespace, Handoff Summary, Branch State, List Scope, all-branches inventory, garbage collection, Handoff Technical Locator, the `sdl handoff ...` Command Face, the `@sdl/handoff/api` Capability API, Handoff Domain Core, and the boundary between durable handoff artifacts and worker-protocol handoffs.
- [@sdl/brmem](./ts/packages/infra/brmem/CONTEXT.md) — Branch Memory primitive vocabulary. Present terms include Branch Memory System, Branch Memory, Namespace, Base Namespace `base`, Entry, Entry Key, Snapshot, Snapshot Ref, Entry Locator, Namespace Copy, Copy Conflict, and Export. Do not describe prompt resolution as ordinary Branch Memory operation, and do not revive stale `Entry Ref` / `Ref locator` wording.
- [@sdl/pi](./ts/packages/hosts/pi/CONTEXT.md) — unified private Pi package vocabulary for neutral helper subpaths, project-local discovery adapters, remaining host-resident engineered Pi extension domains, capability-pi/Local Pi-tool packages stacked above the host, command acknowledgement, thin capability mirrors over Handoff/Branch Context + Plans/Objective Capability APIs, accepted PR feedback presentation residue around `sdl address exec ...` / `@sdl/address/api`, runner-subagent runtime helpers, worktree-status adapters, terminal presentation, and the CCC orchestration boundary (CCC and extracted capability-pi/Local Pi-tool packages consume neutral `@sdl/pi/...` helpers; `@sdl/pi` does not import `@sdl/ccc`, `@sdl/flow-pi`, or `@local-pi-tools/*`).
- [@sdl/ccc](./ts/packages/ccc/CONTEXT.md) — CCC (Cmux Command and Control) vocabulary for the private TypeScript orchestration layer that composes Pi, cmux, Graphite, Objective, handoff, branch-context, autobranch/land, and owns worktree-status observability without lower packages importing it.
- [@sdl/capability-kit/graphite](./ts/packages/sdl-capability-kit/src/graphite/CONTEXT.md) — reusable Graphite support vocabulary for direct `gt` command adapters, Graphite metadata DB parsing, topology/status/stack facts, neutral workflow mechanics, testing fakes, and the direct `gt` invocation boundary.
- [@sdl/kernel](./ts/packages/kernel/CONTEXT.md) — Source Development Lifecycle CLI vocabulary for SDL command surfaces, the SDL kernel, project-local and future bundled SDL extensions, SDL command entries, `@sdl/kernel/sdk` as the public SDL extension API, the command-first SDK promotion rule, internal migration exports, SDL Pi mirrors, hard cutover, and lower orchestration ownership.
- [@sdl/roaster](./ts/packages/roaster/CONTEXT.md) — SDL Capability vocabulary for Roaster, its Domain Core, SDL Command Face, Capability API, review definitions, Tripwires, deep reviews, findings, findings comments, inline findings, Branch Memory review logs, and guarded GitHub publication boundary.
- [@sdl/plans](./ts/packages/capabilities/plans/CONTEXT.md) — saved-plan vocabulary for Saved Plans, the Local Plan Store, Source Branch Plan Files, Saved-Plan Selection, Plan Store Directory Evidence, the Plans Capability API, and the package's command-surface and domain-logic boundaries.
- [@sdl/branch-context](./ts/packages/branch-context/CONTEXT.md) — branch-context vocabulary for Branch Context, Attached Plan, Branch Context Creation, Branch Context Attach, the Branch Context Capability API, and the Branch Context Presentation Boundary that keeps concrete Pi slash-command names and launch formatting in Pi/CCC presentation code.
- [@sdl/objective](./ts/packages/capabilities/objective/CONTEXT.md) — Objective CLI/capability vocabulary for the canonical `sdl objective` command surface (`archive`/`check`/`list`), hidden `sdl objective exec` skill/agent fact helpers, checkout-local `sdl objective list` behavior, checked-in Objective record storage that cites the root SDL Tools system terms, the retired top-level `objective` binary policy, the Objective Capability API (`@sdl/objective/api`) with its Objective Client and gateway-injected Objective Domain Core, and the Objective Capability Dependency Boundary (only the `pi` subpackage may import `@sdl/pi` as an optional peer).
- [@sdl/slot](./ts/packages/capabilities/slot/CONTEXT.md) — worktree slot vocabulary for Slots, Slot Pool, Slot Records/Inventory, Slot Repo Context, Slot Checkout Target, the `sdl slot ...` command surface, the `@sdl/slot/api` Capability API, checkout side-effect policy, SDL-owned parent-shell navigation/shell mounts, and `sdl slot gt` command helpers.
- [sdl-flow](./ts/packages/capabilities/flow/CONTEXT.md) — Flow lifecycle vocabulary for the `sdl flow ...` command face, `sdl-flow/api` compatibility boundary consumed by CCC, Flow-owned land execution still present during migration, internal stack preflight adapter into `sdl-land`, submit/autobranch ownership, and future API narrowing candidates that must not be removed without a compatibility plan.
- [sdl-land](./ts/packages/capabilities/land/CONTEXT.md) — land-domain vocabulary for the `sdl-land` package, its `sdl-land/api` and `sdl-land/testing` surfaces, stack landing targets/plans, gateway-injected preflight and dry-run planning, CLI absence boundary, and the current execution gap where mutation-heavy land behavior remains in Flow.

### Planned TypeScript package contexts

These are active TypeScript package context targets for later focused domain-language sessions. Do not recreate deleted Python package paths when authoring them.

- `@sdl/areg` — agent-resource bootstrap and skill workflow vocabulary: `areg init`, `areg check`, `update-skills`, `skillx`, target agents, managed instruction blocks, installed skill directories, lockfile source types, skill metadata/issues, transient skill fetch/cleanup, and external `gh` / `npx skills` boundaries.
- `@sdl/packagechk` — standalone package-name availability and claimability vocabulary for PyPI/npm checks, registry results, name normalization/validation, claim project specs, publish gateways, and parked Homebrew support.
- `@sdl/aretro` — deterministic branch-retrospective evidence vocabulary: `collect-evidence`, branch resolution sources, session query/source/warning DTOs, session summaries, aggregate metrics, `EvidenceItemDto`, and the boundary between evidence collection and recommendation judgment.
- `@sdl/vibechk` — standalone agent-context evaluation vocabulary: baseline/treatment workdirs, plan source, runner adapter, run id, run bundle/store/status, git provenance, metrics, transcript, diff patch, result branch, run report, comparison report, and local-only publish boundary.
- `@sdl/flow-pi` — Flow/Code workflow Pi presentation vocabulary for the capability-pi package that owns `code-workflows`, `/sdl:flow:*`, `code:gt-restack-resolve`, and `gt:squash-stack` registrations while depending only on public neutral `@sdl/pi/...` helper subpaths and being loaded directly by `.pi/extensions/*` adapters.
- `@local-pi-tools/context-profiler` — extracted Pi-native tool package vocabulary for the context-profiler reference slice: package-owned source/tests/parity metadata, dependency on neutral `@sdl/pi/...` helpers, and direct project-local discovery-adapter registration without a host-to-tool dependency.
- `@local-pi-tools/grill` — extracted Pi-native tool package vocabulary for structured grill UI/tool behavior, stable neutral grill surface constants, package-owned parity metadata, and direct discovery-adapter registration.
- `@local-pi-tools/runner-subagents` — dispatch-focused runner-subagent Pi-tool package vocabulary, including package-owned model-visible dispatch behavior plus runner runtime/process/JSON-event helper subpaths over neutral Pi host types.
- `@local-pi-tools/thermo-council` — extracted Pi-native tool package vocabulary for the multi-runner review council, dependency on `@local-pi-tools/runner-subagents`, package-owned parity metadata, and direct discovery-adapter registration.
- `@local-pi-tools/backing-skill-commands` — generated command-backed skill surface vocabulary for repo-local backing skills, package-owned behavior/parity tests, skill expansion through neutral `@sdl/pi/...` helpers, and direct discovery-adapter registration.
- `@local-pi-tools/pr-previews` — read-only PR feedback/check modal preview vocabulary, package-owned TUI views/command tests/parity metadata, dependency on neutral `@sdl/pi/...` helpers, and direct discovery-adapter registration while PR download/editor-prefill/watch behavior remains host-resident.

### Explicitly out of scope for now

- `packages/kernel-initiatives/CONTEXT.md` — no tracked package exists in the current workspace.
- `packages/kernel-reviewer/CONTEXT.md` — historical package identity replaced by `roaster`; do not recreate unless the package itself returns as a separate tracked package.

## Candidate Relationships

These are current map seeds, not final readback output. Package-context phases should confirm, refine, or reject them before Phase 16 finalizes the relationship list.

- **SDL Tools → @sdl/brmem**: Planning and handoff workflows may use Branch Memory, while Objectives themselves remain checked-in Markdown records.
- **SDL Tools → @sdl/pi**: The unified Pi package exposes repo-local Pi commands/tools and neutral helper subpaths for command acknowledgement, runtime parsing, Objective selection, terminal presentation, shared error/timer helpers, and project-local discovery adapters.
- **SDL Tools → @sdl/ccc**: CCC is the private TypeScript orchestration layer for repo-opinionated Pi/capability-kit/cmux/Graphite/worktree command-and-control workflows and owns the `ccc` Pi command prefix for cmux/workspace orchestration.
- **@sdl/areg → @sdl/core project config + external `gh`/`npx skills`**: `areg` reads shared project config from `@sdl/core`, but its skill-management work is bounded by external GitHub and `npx skills` command surfaces.
- **@sdl/brmem → @sdl/core + @sdl/clinkr**: brmem uses TypeScript Git helpers and Clinkr command vocabulary to expose branch-scoped text storage.
- **@sdl/handoff → @sdl/brmem + @sdl/core.git + @sdl/clinkr + @sdl/kernel/sdk**: handoff artifacts use Branch Memory storage through gateway-injected Handoff Domain Core and expose deterministic lifecycle operations through the `sdl handoff ...` Command Face while presenting a user-facing handoff inventory and garbage-collection model.
- **@sdl/roaster → GitHub + Git + project config + SDL command face**: roaster consumes GitHub PR types, local-diff/capability-kit/git facts, shared project config, and the SDL extension command system while exposing curated in-process behavior through `@sdl/roaster/api`.
- **@sdl/slot → Git + GitHub + @sdl/capability-kit/graphite + SDL shell/worktree boundaries**: slot owns worktree slot lifecycle and the `sdl slot gt` command surface while asking `@sdl/capability-kit/graphite` for Graphite stack/navigation facts; Slot exposes in-process checkout composition through `@sdl/slot/api`, while SDL owns `sdl shell ...` and the compatibility `sdl slot shell ...` mount over neutral `@sdl/core/shell-support` helpers.
- **@sdl/objective → Git + Clinkr-style command presentation**: Objective CLI inventory uses Git path-touch facts for checkout-local list metadata. In-process consumers (`ccc`, `sdlcc`, Pi objective adapters) compose Objective behavior through the `@sdl/objective/api` Capability API rather than the package root or private source. Do not reintroduce Objective → brmem as a storage edge.
- **@sdl/packagechk → external package registries**: packagechk is standalone; it owns package-name availability/claimability checks at registry and publish-gateway boundaries.
- **@sdl/aretro → session evidence + Git + SDL command presentation**: Aretro collects deterministic branch/session/capability-kit/git evidence through the `sdl aretro exec ...` command face and leaves recommendation judgment to the `branch-retro` skill; no `@sdl/aretro/api` Capability API is exposed without a concrete in-process consumer.
- **@sdl/vibechk → git + runner/store boundaries**: vibechk is standalone; it owns local evaluation workdirs, run bundles, result branches, metrics, and reports without folding into aretro evidence collection.
- **@sdl/pi → neutral helper consumers**: Other workspace packages, capability-pi packages such as `@sdl/flow-pi`, and extracted Pi-tool packages may import curated neutral `@sdl/pi/...` helper subpaths for runtime parsing, command acknowledgement, command UI helpers, model/LM-JSON helpers, shared error/timer helpers, parity helpers, cmux/Pi tool/runtime types, skill expansion, and terminal layout/presentation. Runner-subagent runtime/process/JSON-event/presentation helpers are owned by `@local-pi-tools/runner-subagents`, not by `@sdl/pi`. They should not import project-local extension entrypoints as helpers.
- **@sdl/pi → Pi runtime + @sdl/kernel + repo CLIs**: Pi owns discovery adapters, argument restoration/UI behavior, worktree-status lifecycle/footer plumbing, runtime cmux helpers, and runtime CLI bridging over `sdl`, `git`/`gt`/`gh`, `brmem`, `objective`, `slot`, and related repo commands. Flow/Code workflow Pi presentation lives in `@sdl/flow-pi`, which consumes neutral `@sdl/pi/...` helpers and is imported directly by `.pi/extensions/*` adapters. Handoff, Branch Context + Plans, and Objective are thin Pi shells over `@sdl/handoff/api`, `@sdl/branch-context/api` + `@sdl/plans/api`, and `@sdl/objective/api`; PR download/editor-prefill/watch behavior remains Pi presentation/session residue around Address portable collection and mutation behavior (`sdl address exec ...` / `@sdl/address/api`), while PR modal previews live in `@local-pi-tools/pr-previews`. Repo-opinionated orchestration for surfaces such as land and worktree-status lives in CCC and dedicated capability packages, which consume neutral `@sdl/pi/...` helpers; `@sdl/pi` no longer imports or declares `@sdl/ccc`. Pi-native standalone tools can be extracted above the host when their discovery adapters import the tool package directly, as with `@local-pi-tools/context-profiler`, `@local-pi-tools/grill`, `@local-pi-tools/runner-subagents`, `@local-pi-tools/thermo-council`, `@local-pi-tools/backing-skill-commands`, and `@local-pi-tools/pr-previews`.
- **@sdl/kernel → @sdl/pi**: Pi runtime extensions mirror SDL commands as `/sdl:*` through thin adapters; SDL owns command behavior, and Pi owns runtime registration and presentation.
- **@sdl/kernel → @sdl/ccc**: CCC may own project-specific orchestration internals for some workflows, while SDL can still own the public lifecycle command surface.
- **@sdl/ccc → @sdl/pi + @sdl/capability-kit/graphite + lower capabilities**: CCC owns multi-capability command orchestration behind the `ccc` Pi command prefix, selected repo workflow orchestration behind current non-`ccc` public surfaces such as `/objective:stack-impl`, `/sdl:flow:autobranch`, and `/sdl:flow:land`, and worktree-status observability facts/presentation. CCC composes injected GitHub/command execution capabilities, SDL checkpoint primitives, `@sdl/capability-kit/graphite` facts/mutations, GitHub/slot landing orchestration policy, and neutral `@sdl/pi` helpers. CCC consumes neutral `@sdl/pi/...` helper subpaths, but `@sdl/pi` no longer imports or declares `@sdl/ccc`; the former private `@sdl/pi` ↔ `@sdl/ccc` package cycle is now broken. Under the acyclic **Extension Dependency Graph** invariant (ADR 0009), any remaining manifest cycles are tracked as debt by the `sdl-extension-architecture` Objective.
- **@sdl/plans → Git/local plan store**: plans owns saved-plan path/evidence/selection semantics over repository identity and source-branch directory facts, using Git gateways to derive plan-store keys at the command or Capability API edge.
- **@sdl/branch-context → @sdl/plans + @sdl/brmem + @sdl/capability-kit/graphite**: branch-context owns plan attachment, Branch Memory namespace/key policy, and branch-context creation semantics while consuming saved-plan sources from `@sdl/plans`, storing attachments through `@sdl/brmem`, and using `@sdl/capability-kit/graphite` for branch tracking and parent checks.
- **Consumer capability packages → @sdl/branch-context/api + @sdl/plans/api + @sdl/slot/api**: downstream consumer packages should use curated Capability API subpaths rather than package roots or private source imports when composing branch-context, plans, or Slot behavior.
- **CCC → sdl-flow**: CCC consumes Flow-owned autobranch, autoslot, trunk-pull, and land behavior through the curated `sdl-flow/api` Capability API; CCC must not import Flow private `src/...` modules or bypass Flow land compatibility by importing `sdl-land` directly.
- **sdl-flow → sdl-land**: Flow keeps the command face, Pi/CLI presentation, `sdl-flow/api` compatibility, submit/autobranch policy, and mutation-heavy land execution while delegating stack preflight/dry-run planning to the `sdl-land` domain core internally. This is current-state migration language, not a claim that all land behavior has moved.
- **sdl-land → gateway facts**: `sdl-land` owns renderer-independent land-domain types, fakes, and stack preflight planning over injected Git, Graphite, GitHub PR, and worktree-slot gateway facts; it does not export CLI parsing, completions, renderer registration, or presentation helpers.
- **Flow submit → @sdl/capability-kit/graphite + @sdl/capability-kit**: Flow owns submit, PR-regeneration, Graphite-submit, and autobranch workflow policy in `sdl-flow`/Flow-owned internals. `@sdl/capability-kit/graphite` owns neutral Graphite command adapters, facts, and mechanics; `@sdl/capability-kit` owns shared capability substrate such as gateway result/error helpers.

## Flagged Ambiguities

Carry these collisions forward to focused package-context phases. Do not finalize them here.

- **Review / Comment**: distinguish Tripwires, Deep reviews, generic Roaster review definitions/runs, GitHub PR reviews, review threads, review comments, discussion comments, Roaster findings, Roaster findings comments, and inline finding posting.
- **State / status**: separate Clinkr `ExitStatus`, Git worktree/file status, GitHub PR state, Objective statuses, slot inventory status, package-check status, vibechk run status, and handoff branch state.
- **Active / root**: keep Active Objective Root, Objective Archive Root, repository root, Git common dir, Base Namespace, and Graphite trunk distinct.
- **Branch / ref / start-point / snapshot-ref**: preserve Git Branch, Ref, Start point, brmem Snapshot Ref, Entry Locator, current branch, branch context, result branch, and Graphite stack node boundaries.
- **Graphite stack operations**: distinguish `@sdl/capability-kit/graphite` as the support package for Graphite command adapters/facts, `sdl slot gt` as the slot-owned command surface, Flow submit as a public workflow that composes neutral Graphite mechanics, and CCC/Flow-owned landing/autobranch orchestration.
- **Evidence / finding**: distinguish Objective completion evidence, aretro deterministic evidence items, roaster findings, vibechk metrics/reports, and branch-retro recommendation judgment.
- **Plan / attachment / handoff**: distinguish enriched plan, saved plan, source branch plan file, branch context, attached plan, Branch Memory attachment, handoff artifact, continuation focus, and handoff technical locator.
- **Skill / agent / resource**: reconcile areg target-agent/resource language, skill-management/skillx public workflow language, and Pi extension skill-expansion helpers.
- **Extension API**: the bare phrase "extension API" is banned anti-vocabulary — it conflates two distinct surfaces and must always be qualified. Write "SDL extension API" (`@sdl/kernel/sdk`) for the SDL author SDK surface and "Pi runtime extension API" for the Pi surface (TUI, slash commands registered with `pi.registerCommand`, model-visible tools, `.pi/extensions`). Keep both distinct from a capability's **Capability API** (`@sdl/<cap>/api`, imported in-process by a downstream **consumer** extension) and its kernel-loaded commands, defined in the root **Extension Layering** cluster.
- **Domain placement / layer**: capability domain logic lives only in the **Capabilities** (first-party extension packages built on the **Capability Kit**, `@sdl/capability-kit`); the `@sdl/pi` runtime host and `@sdl/kernel` kernel are not domain homes. Capability Kit may host small shared first-party capability-building primitives (for example former transitional checkpoint/worktree/text helpers) when SDK/kernel are the wrong home, but it is not a product capability owner or public SDK by default. Domain formerly stranded in `@sdl/pi/*` (for example, the objective-selection rules once at `@sdl/pi/objectives`) has been relocated into its owning capability — `@sdl/objective` — and is re-consumed via that capability's **Capability API** (`@sdl/objective/api`). The SDK boundary is permeable downward over time but gated on proven generality, and opinionated above-SDK patterns such as gateways are expected to stay above the SDK (ADR 0012). Package Tier is now machine-declared (`sdl.tier`) rather than inferred from report prose; the former `transitional` tier was deleted with `@sdl/domain-primitives-transitional`. Keep Package Tier, domain placement, and Capability API cleanliness distinct when updating context.
- **SDK re-export ownership**: `@sdl/kernel/sdk` re-exports and treats as its own vocabulary a minimal set of lower-package types and helpers, including `ExecResult` and `FormatCommandEvidenceOptions` from `@sdl/core/exec`, `PositionalSpec` from `@sdl/clinkr/raw`, schema builder `z`, and command-evidence helpers. The authoritative export list remains `ts/packages/kernel/docs/sdk-reference.md`; the context term records the ownership boundary, not a duplicate export inventory.
- **Run / evaluation / metric**: reconcile vibechk run/bundle/status/metric vocabulary with aretro session/evidence vocabulary and roaster review/finding output.
- **Changes / checkpoint / autobranch / submit / PR regeneration**: distinguish Flow-owned public lifecycle command ownership for workflows such as `changes`, `cp`, `autobranch`, `submit`, and `regenerate-pr`, CCC-owned repo source-control orchestration where the flow crosses primitives, neutral Graphite/Core mechanics, and Pi discovery/presentation adapters in `@sdl/pi`.
