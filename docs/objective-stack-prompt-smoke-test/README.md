# Objective Stack Prompt Smoke Test

This directory exists only to test Objective-stack prompt orchestration.

It is a disposable docs-only fixture for a smoke-test stack. It is not product documentation and should not be treated as a permanent project artifact.

To revert the fixture, delete `docs/objective-stack-prompt-smoke-test/` and drop the smoke-test branches.
