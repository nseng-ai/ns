## Completion instructions
After you finish the implementation:
1. Create or update the branch commit using the repo's normal workflow.
2. Then run `!ns flow submit`.

## Dispatch context
This branch was created from refreshed Graphite trunk and is intentionally unrelated to the caller's current stack.

On a Mac, use either:

 - Option (⌥) + E (Alt+E)
 - Fn + F2 if your function keys control brightness/media; otherwise just F2.


 option+shift+e?


 No—use Option (⌥) + E, without Shift.

 Alternatively, use F2 (or Fn + F2 if macOS uses F2 as a media key).

i want to dipslay the option instructions in addition to "alt" so the person doesn't have to remember what alt corresponds to on mac. also make casing is correct (lowercase)