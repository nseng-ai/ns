# twerk

## JavaScript / TypeScript tooling

This repo uses Bun for the TypeScript workspace under `ts/`.

Install dependencies:

```bash
bun install --cwd ts
```

Run checks:

```bash
bun run --cwd ts check
bun run --cwd ts test
```
