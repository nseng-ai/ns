// ADR0024-LEGACY-DELETE(whole file, plus this command's entry in ../../package.json):
// repo-local stub for the legacy blocking runner-step command.
export { default } from "@nseng-ai/objective/ns/commands/exec-runner-step";
