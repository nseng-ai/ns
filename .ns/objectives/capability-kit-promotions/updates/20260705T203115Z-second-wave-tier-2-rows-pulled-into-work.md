# Second Wave: Three Tier 2 Rows Pulled into Work

## Summary

With all five Tier 1 rows complete, the user ranked the Parked backlog by
potential impact (duplicate sites eliminated × correctness risk × enabling
effect) and deliberately pulled three Tier 2 rows into `## Work`: kit `git`
operation-in-progress/worktree-admin detection (the backlog's only triplicated
logic — slots, flow, hosts/pi), content-slug derivation beside kit
`model-slug` (plans' generalized shape, handoffs collapsing to a variant), and
GitHub REST comment mechanics extracted from reviews into a kit `github`
subpath (second consumer: pi-tools pr-feedback-watch). The result-typed fs
gateway ranked second on impact but was not pulled; it stays Parked.

## Objective Impact

Scope, Non-Goals, Completion Criteria, and Definition of Progress in
`objective.md` now cover the second wave: completion requires all eight Work
rows `[x]` and the three new kit surfaces tested (github mechanics with
real/fake parity). Each new row carries a pinned design and row-level Policy
mirroring the Tier 1 pattern, with steer-first triggers for taxonomy
divergence, non-additive kit changes, Pi exec-seam adapter needs, and any
Roaster-envelope absorption. Second-wave assumptions were spot-verified
against export surfaces on the delivering branch stack (flow's
`InProgressGitOperation` union vs slots' marker table; plans'
`ContentSlugDerivationVariant`; reviews' `RoasterGitHubGateway` real + fake).

## Follow-Ups

Remaining Tier 2 rows (result-typed fs gateway, JSON-input loader, git output
classification, PR-link parsing, shell-install factory, small-helper grab
bag) and all Tier 3 rows stay Parked. Noted during ranking: the Tier 3 Pi
arg-tokenizing/session-JSONL row spans seven packages — the widest duplication
in the backlog — but remains gated on a placement decision (Pi surface vs
foundation) before any pull.
