# README-Driven Development Ownership Selected

## Summary

The destination-map review placed `readme-driven-development` in the internal
`agent-engineering` family rather than `planning`. Its final proposed canonical path is
`skills/internal/agent-engineering/readme-driven-development/`.

## Objective Impact

README-driven development is treated as an agent-engineering practice alongside CLI
push-down, context analysis, ns CLI design, and refactor orchestration. Its internal
disposition and flat harness-visible identity remain unchanged. The map remains complete
with 58 unique identities and destinations; this decision does not authorize migration or
complete the joint ADR-plus-map approval gate.

## Follow-Ups

- Continue reviewing the remaining map and explicitly approve ADR 0046 and the complete
  destination map before any canonical skill moves.
- During cutover, preserve the `readme-driven-development` invocation identity while moving
  its canonical source to the internal `agent-engineering` family.
