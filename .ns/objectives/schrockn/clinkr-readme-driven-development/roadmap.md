# Roadmap

## Work

- [x] Settle the cold-audience Clinkr product contract: filesystem-first command structure, immediate-child lazy discovery, schema-derived argv plus stdin-only `--input-json`, app-owned execution/completion, explicit outcomes and command renderers, invocation-owned context, narrow raw execution, and runtime discovery without manifests or production codegen.
- [x] Audit implementation, tests, exports, packaging, and representative callers; run the broad steelthread through Clinkr, Foundation, SDK composition, Flow, Objectives, and Brmem; preserve its public-contract changes and implementation lessons while rejecting its parallel transitional architecture as the production design.
- [x] Settle the remaining API and architecture decisions: `requiresContext: true` as the contextful discriminant; one typed `resultSchema` with fixed untyped diagnostic outcomes; exact `success | negative | failure | usage-error` status vocabulary; one narrow scoped composition builder; disjoint source ownership; `defineRawCommand(...)`; specialized subpath-only APIs; one private topology and traversal; and a dependency-ordered rebuild stack.
- [x] Approve `references/README-draft.md` as the cold-audience prose contract, remove provisional promises, fix the Node.js floor at `>=24.12.0`, make `md` the sole Markdown CLI token while retaining `renderMarkdown`, record completion support maturity, and specify structured stdin JSON behavior. The now-merged executable-fixture stack supplies 14 synchronized TypeScript fixtures, primary argv/JSON execution, and a cold-process smoke on trunk.
- [x] Land and review the initial Clinkr implementation stack. PRs #3948, #3951, #3952, #3953, #3949, and #3950 merged dependency-order onto `master`; their substantive CI lanes passed. The review accepted the command/outcome contract, bounded root-command filesystem runtime, honest partial README fixture evidence, structured-runtime hardening, and raw dispatch as foundations—not the final clean cut. Before broader acceptance, reconcile the duplicate builder narrative, remove `src/app/` dependencies on legacy exit/confirmation code, enforce semantic metadata validation in the topology owner, and delete the legacy raw model at the tail cutover.
- [ ] Build the canonical private recursively lazy topology and source-composition owner. Adapt filesystem and scoped-callback sources into immediate-child scopes; validate strict module/metadata shapes, canonical names and aliases, reserved names, absolute directories, and disjoint ownership; reject duplicate commands, command/group collisions, and every shared group path with both source identities; expose no public node, provenance, publication, or cache lifecycle machinery.
- [ ] Complete the single `ClinkrApp` runtime over that topology. One navigator resolves execution, help, schema, and completion; selected definitions load transactionally with shared in-flight work, app-lifetime success caching, retry after failure, and fresh Commander materialization. Clinkr alone owns request/outcome validation, framework usage alternatives, envelopes, rendering, streams, exit codes, raw dispatch, completion setup/resolution, merge/deduplication, provider fallback, formats, and exception propagation.
- [ ] Reconcile Foundation and prove Brmem as the first production consumer. Match or exceed `colocate-brmem-commands-remove-operations`: retain route-local `src/cli/`, hidden `exec`, command-local contracts/presentation, inventory and eager-import guards, and fake-driven scenarios; improve field annotations and context typing; prove packed inventory and execution; delete every Brmem/Foundation temporary adapter before continuing.
- [ ] Rebuild SDK/host composition over the same topology and traversal, then prove Objectives as the real-host consumer. Decode extension modules into an exact recursive contribution union, preserve source topology until mount, attach diagnostics to scope opening/selected loading, and remove flat routing, source precedence, group reconstruction, SDK-owned validation/render synthesis, and completion interception. Match or exceed `colocate-objectives-cli-remove-operations`, including recursive hidden `objective/exec`, context adaptation, malformed-neighbor isolation, source-collision diagnostics, and nested import-laziness evidence.
- [ ] Migrate remaining standalone/Foundation CLIs and extension callers in dependency order, porting behavior tests rather than prototype-internal tests. Move presentation facts into typed outcome data before deleting overrides; preserve raw and progressive-output behavior; do not broaden topology or descriptor contracts to ease migration.
- [~] Delete the quarantined legacy architecture and make the prohibitions structural: mutable `ClinkrGroup`, old raw model, automatic aliases, per-exit rendering, `ClinkrFailure`, duplicate completion plans, legacy Foundation construction, permissive descriptor detection, validation escape hatches, parallel route selection, obsolete tests, and obsolete exports. The qualified exit/envelope, failure, emit/format, and JSON Schema closure is now isolated behind the sole aggregate `/legacy` boundary; the per-symbol ownership, evidence, blockers, importers, and deletion prerequisites are recorded in `references/legacy-api-deletion-inventory.md`. Mutable group, completion, interaction gates, old raw closure, and mixed testing utilities remain explicitly blocked until their modern parity lands. Flip the package root to the new surface only at the final clean cut.
- [ ] Qualify packed Clinkr and representative consumers, intact command directories, public subpath boundaries, and shell-completion instructions.
- [ ] At the tip of the implementation stack, complete README testing to the strongest truthful level each example permits. Preserve exact fence synchronization and compilation; add direct behavioral tests for teaching fragments; execute complete commands through the public app boundary; cover nested routing, group discovery, richer metadata, app-owned completion, and contextful testing only after their owning runtime lands; retain explicit deferrals where an intentionally abbreviated fence cannot itself execute. Use `updates/2026-07-28-readme-executable-fixture-gate-complete.md` as the per-example assessment and do not fabricate root-command substitutes for recursive examples.
- [ ] Promote the approved draft to the canonical package README and replace the Objective draft with a provenance pointer.
- [ ] Return the steelthread/rebuild gate lesson to `foundation-readme-driven-pass`, including the rule to rebaseline when a vertical slice validates the product interface but exposes central parallel architecture, then close this Subobjective.

## Parked

- Opaque Commander subtree mounting until a concrete caller requires a framework-specific adapter.
- Generated manifests, filesystem codegen, and single-file bundle support; use the justified programmatic seam or a later dedicated adapter instead of weakening the filesystem contract.
- Per-command context derivation beyond one homogeneous contextful tree until a concrete caller justifies another model.
- Unrelated Clinkr utilities or downstream CLI redesign discovered during the rebuild; split them after discussion.
