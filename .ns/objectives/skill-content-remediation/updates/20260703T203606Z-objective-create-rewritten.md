# objective-create rewritten (queue position 6)

## Summary

Rewrote `skills/objective-create/SKILL.md` — queue position 6, closing the rewrite
tier. 124 → 85 lines (~31%). Frontmatter verbatim. 63-item contract diff passed.

Collapsed within-skill duplication only, honoring the family self-containment rule
(Required shape was not gutted toward the umbrella): the archive-root check stated
twice → one check-both-roots bullet; the planning-only default stated three times →
one Required-shape bullet; three overlapping validation bullets → one; the
"Conditional references" section dissolved with each reference co-located where it
fires; Workflow step 6's restated edge-mutation mechanics → pointer to the Record
Frontmatter section. Load-bearing strings verified verbatim: the exact
`Stop and create Objective <slug>` stop-option text, all headings/statuses, CLI
invocations, the `not_found` envelope, the orientation.md field list.

Deliberate changes: "never mint a new slug" (stronger than "not create"); Stop/ask
now says "(active or archived)", surfacing the archive collision case previously
only implied; the worked numbered-menu example dropped with its rules kept
normatively — restore one line if menus degrade in practice.

## Objective Impact

- Queue position 6 complete via the rewrite method with the gate passed. The
  confirmed-duplication rewrite tier (positions 3–6) is now fully remediated.
- `roadmap.md`: objective-create target row flips to DONE with evidence.
- Evidence: `areg check` "All skills OK"; `dprint` clean; contract + report in
  session scratchpad.

## Follow-Ups

- Surgical tier next: `code-gt-restack-resolve` (7), `code-resolve-merge-conflicts`
  (8), `objective-close` (9).
