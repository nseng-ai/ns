# code-gt-restack-resolve surgical pass (queue position 7)

## Summary

Surgical remediation of `skills/code-gt-restack-resolve/SKILL.md` — queue
position 7, first of the surgical tier (rigid output contract + restack stakes
barred a from-scratch rewrite). 325 → 314 lines; two collapses, verified by
line-by-line read before editing; no output contract, workflow step, or safety
rule reworded.

1. **TS-toolchain rule 2 sites → 1.** The TEMPORARY "keep + reformat" rule was
   stated in the section body (numbered steps) and again inside the Agent prompt
   template. The template is what conflict subagents receive verbatim, so it is
   now the single operative home; the section keeps the conflict-shape
   explanation and a one-paragraph deferral carrying the full rule meaning
   (base-verbatim, discard-incoming, `just ts-format-fix` not hand-wrapping,
   engine-gate verification, only-this-shape, mixed-logic escalates — union
   checked item by item). The TEMPORARY removal marker stays; the block itself
   remains parked on the external toolchain rollout.
2. **Done-step verification table → pointer.** §5 restated the engine's step-4
   check table (four category rows) verbatim; it now points at
   `code-resolve-merge-conflicts` "Verify before continuing", keeping the
   driver-specific framing (full-scope, from the stack tip, at least when a
   conflict was resolved mid-stack).

## Objective Impact

- Queue position 7 complete via the surgical method (method cap honored).
- `roadmap.md`: code-gt-restack-resolve row gains DONE evidence.
- Evidence: `areg check` "All skills OK"; `dprint` clean.

## Follow-Ups

- Queue position 8 (`code-resolve-merge-conflicts` surgical) next.
