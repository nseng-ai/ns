# ccc-stack-map duplication collapsed — queue complete (queue position 10b)

## Summary

Final queue target. `skills/ccc-stack-map/SKILL.md` 164 → 115 lines under the
only-if-cheap gate: the "Read-only command recipe" section (a restatement of Data
sources 1–4) deleted, with its three recipe-only facts preserved by relocation
into Data sources — `data.branches` as the non-trunk inventory, the
report-the-no-stack-envelope rule, and the no-bundled-script-for-v1 rule (kept as
one sentence; the inline Python sketch died, since future-implementation sketch
ground is owned by `references/display-and-code-sketch.md`, whose pointer is
unchanged). The recipe's mutate-nothing sentence duplicated Default posture and
died with the section. Same documented method adaptation as 10a: targeted
collapse rather than from-scratch rewrite, within the gate's intent.

**With this, every target of the post-ADR-0016 re-ranked queue (update
`20260703T201928Z`) is remediated or recorded clean** — positions 1–6 rewrites
through the extract-contract-then-diff gate, 7–9 surgical with method caps
honored, 10 cheap collapses. Dropped/deferred targets (`enriched-plan-save`,
`python-fake-driven-testing` SKILL.md, `branch-context-impl`, `pr-address`)
retain their recorded reasons.

## Objective Impact

- Queue position 10 complete; the per-skill remediation queue is exhausted.
- `roadmap.md`: pair row fully DONE. The per-skill `[~]` row's remaining open
  work is now only the externally-parked items (TEMPORARY block removal) and any
  deferred re-audits.
- Evidence: `areg check` "All skills OK"; `dprint` clean.

## Follow-Ups

- Assess whether the objective's completion criteria are now met end-to-end
  (deferred targets have recorded reasons) and whether an `objective-close` pass
  is warranted — a deliberate owner decision, not auto-closed here.
