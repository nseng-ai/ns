# objective-close surgical pass (queue position 9)

## Summary

Queue position 9, the near-zero-cost tail: `skills/objective-close/SKILL.md` was
confirmed clean on a full read, as the re-rank predicted (lift 1). One
near-verbatim duplication collapsed: the archive redirect ("if the user
explicitly wants retirement from active discovery, use `ns objective archive
<slug>` after or instead of closure, depending on intent") was stated in both
Workflow step 9 and the Stop/ask list. Step 9 keeps the full rule; the Stop/ask
bullet now points to it. No other change; the skill stays at 79 lines.

This satisfies the completion criteria's remediate-or-record bar for the target:
remediated (minimal) and otherwise recorded clean.

## Objective Impact

- Queue position 9 complete. The surgical tier (7–9) is fully dispositioned.
- `roadmap.md`: objective-close row flips to DONE with evidence.
- Evidence: `areg check` "All skills OK"; `dprint` clean.

## Follow-Ups

- Queue position 10 (ccc pair, only-if-cheap gate) is the last remaining target.
