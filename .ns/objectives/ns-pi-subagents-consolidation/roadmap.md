# Roadmap

## Work

- [~] Land the current consolidation stack under this Objective.
  Evidence to preserve: `extension-registry-shim-loading-coverage-split` (open PR #3052) moves real shim-loading checks to the integration lane; `subagent-fleet-dispatch-runner-subagent` (open PR #3069) consolidates explore, dispatch runner subagents, transcript viewing, and fleet navigation under the unified `@nseng-ai/ns-pi-subagents` extension; `stack-feedback/extension-workspace-helper` (open PR #3071) extracts the shared kernel extension-registry test workspace helper. Update this row with merge evidence when the stack lands.
  - Policy: human-owned. The Objective Runner never submits, pushes, or merges; runner steps for the rows below stack on top of these unlanded branches.
- [x] Rename the consolidated command surface to `ns:agents:*`.
      Completed 2026-07-06: `ns:subagents:fleet` → `ns:agents:fleet`; `ns:explore:transcript` → `ns:agents:transcript`; widget/status keys use `ns.agents.fleet`; shim `.pi/extensions/subagents.ts` moved to `.pi/extensions/agents.ts` while keeping the `.pi/lib/workspace-packages.ts` mapping unchanged. No compatibility aliases were added. Package name `@nseng-ai/ns-pi-subagents` and the `explore` / `dispatch_runner_subagent` tool names are unchanged. Updated `docs/pi/README.md`, `docs/pi/runner-subagent-helper.md`, package README/docs-site path references, and the `CONTEXT-MAP.md` `@nseng-ai/ns-pi-subagents` vocabulary line to record the agents-view vs. subagent-substrate split.
  - Policy: direct execution.
  - Evidence: `pnpm --dir ts/packages/extensions/ns-pi-subagents run test` passed (21 files, 191 tests); `pnpm --dir ts/packages/extensions/ns-pi-subagents run check` passed via tsgo; extension/package tests assert `ns:agents:fleet` and `ns:agents:transcript` are registered and old commands are absent; stale-name grep leaves only negative assertions and Objective history.
- [x] Rebaseline package and shim documentation after the rename.
      Completed 2026-07-06: active docs/catalog/package README now present `.pi/extensions/agents.ts` as the local shim over `@nseng-ai/ns-pi-subagents/extension`, include `/ns:agents:fleet` and `/ns:agents:transcript` where summarizing the registered command surface, keep `explore` and `dispatch_runner_subagent` as model-visible tool names, and keep `subagent` for dispatched child/session substrate vocabulary. Historical records stayed untouched. Stale-reference grep over active surfaces found no retired `ns:subagents:*`, `ns:explore:*`, retired shim paths, or `@internal/pi-tools/runner-subagents` references; positive grep confirms the canonical `ns:agents:*`, `ns.agents.fleet`, shim, and package entrypoint references.
  - Policy: direct execution; doc-wording judgment calls are runner-decidable with rationale in the step report.
- [x] Audit package exports and test-helper ownership.
      Completed 2026-07-06: `@nseng-ai/ns-pi-subagents/explore/testing` was demoted from exported package API to the package-local `test/helpers/explore-testing.ts` fixture; the export-map entry and README public-export bullet were removed and package tests now import the helper by relative test-helper paths. `@nseng-ai/ns-pi-subagents/runner-subagents/testing` remains exported test-support API because it has a cross-package test consumer and stays vitest-free. Kernel `test/helpers/extension-workspace.ts` remains a local kernel test fixture because it is not exported and imports Vitest lifecycle helpers. Follow-up observations about the `@internal/pi-tools` runtime/devDependency mismatch, default-lane kernel jiti imports, and missing `ns.subpackages` declaration were recorded but left out of scope.
  - Policy: direct execution; ownership decisions are runner-decidable, recorded in a Semantic Update.
  - Evidence: `pnpm --dir ts/packages/extensions/ns-pi-subagents run test` passed (21 files, 191 tests); `pnpm --dir ts/packages/extensions/ns-pi-subagents run check` passed; `just ts-format-check` and `just ts-lint` passed; Semantic Update `updates/2026-07-06T204816Z-explore-testing-demoted.md`.
- [x] Decide compatibility posture for renamed fleet/explore surfaces.
      Resolved 2026-07-06: the consolidated surface is `ns:agents:*` only, with no compatibility aliases for older `ns:subagents:*` / `ns:explore:*` names — consistent with the standing no-aliases policy in `docs/pi/README.md`. Implementation is the rename row above.
- [x] Assess and record external distribution readiness for `@nseng-ai/ns-pi-subagents`.
      Completed 2026-07-06: external distribution is blocked, not unblocked. The package is still `private: true`, ships raw `src/`, and exposes raw `.ts` export and Pi extension paths; more importantly, `src/fleet/navigator.ts` imports `@internal/pi-tools/overlay-kit` while `@internal/pi-tools` depends back on `@nseng-ai/ns-pi-subagents` for thermo-council/context-profiler runner-subagent helpers. Promotion requires moving or replacing the overlay-kit dependency, breaking the bidirectional internal package coupling, and deciding the release artifact contract before any human-owned publish/version slice. Publishing remains out of scope.
  - Policy: assess-and-record only. No packaging, bundling, version, or publish changes without an explicit human request.
  - Evidence: `updates/2026-07-06T211949Z-distribution-readiness-blocked.md`; focused audit of `ts/packages/extensions/ns-pi-subagents/package.json`, `src/fleet/navigator.ts`, and `ts/packages/internal/pi-tools` consumers.

## Parked

- Shared higher-level orchestration across explore, dispatch runner subagents, and thermo-council remains parked unless a new caller demonstrates a neutral abstraction that preserves each capability's result contract, progress model, and recovery semantics.
