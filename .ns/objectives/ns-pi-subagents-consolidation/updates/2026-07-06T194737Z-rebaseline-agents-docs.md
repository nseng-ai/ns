# Semantic Update: Agents Docs Rebaselined After Rename

- **Active docs aligned on the post-rename surface.** `docs/pi/README.md`, `docs/pi/runner-subagent-helper.md`, `ts/packages/extensions/ns-pi-subagents/README.md`, and `docs-site/lib/extensions-catalog.ts` now consistently point at `.pi/extensions/agents.ts` and `@nseng-ai/ns-pi-subagents/extension` while naming `/ns:agents:fleet` and `/ns:agents:transcript` as the user-facing commands where the command surface is summarized.
- **Vocabulary split preserved.** The docs keep `explore` and `dispatch_runner_subagent` as model-visible tool names and keep `subagent` for dispatched child sessions / runner substrate language; no package, subpath, or tool rename was introduced.
- **No retired active references.** Focused stale-reference grep over the active docs/catalog/context/shim surfaces found no live `ns:subagents:*`, `ns:explore:*`, retired shim paths, or `@internal/pi-tools/runner-subagents` references. Positive grep confirms the canonical `ns:agents:*`, `ns.agents.fleet`, `.pi/extensions/agents.ts`, and package extension entrypoint references.
