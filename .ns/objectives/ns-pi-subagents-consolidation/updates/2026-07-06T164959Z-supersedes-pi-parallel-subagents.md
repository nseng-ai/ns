# Supersedes Pi Parallel Subagents

## Summary

Created `ns-pi-subagents-consolidation` as the successor Objective for post-delivery `@nseng-ai/ns-pi-subagents` package consolidation. The predecessor `pi-parallel-subagents` is closed rather than repurposed because it already delivered its original fan-out/explore capability and every roadmap row is complete.

## Objective Impact

This Objective now owns the current and follow-on consolidation arc around the unified subagents extension surface, including entrypoint consolidation, extension workspace helper extraction, package/shim documentation, and external-distribution readiness questions.

## Follow-Ups

- Update the in-progress stack evidence row when the current Graphite stack finalizes.
- Rebaseline docs and package exports after the consolidated entrypoint stack lands.
