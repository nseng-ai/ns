# Roadmap

## Work

- [x] Add a single parity assertion helper (shape: `expectPiSurfaceParity(register, metadata)`) beside `@ns/pi/parity/testing` and collapse the six copy-pasted parity test files to one call each.
      Evidence: `expectPiSurfaceParity(register, metadata)` now lives in `@ns/pi/parity/testing`; the six Internal Pi-tool parity tests are one-call consumers; stale-pattern grep found no subpackage-local `collect*Surfaces`/compare/format ritual in the targeted parity tests; focused parity commands, `just ts-format-check`, `just ts-lint`, and `just ts-check` passed on 2026-07-05.
- [x] Narrow `runner-subagents` exports to real consumers: drop zero-importer subpaths (`/json-events`, `/presentation`, `/usage`); decide whether `/process`, `/runtime`, `/testing` earn rank (ADR 0023) or tests bind via relative source imports; delete the `usage.ts` shim; fold the `extension-api.ts` pass-through facade into the barrel. Re-verified importer facts before cutting.
      Evidence: `@internal/pi-tools` now keeps only `./runner-subagents`, `./runner-subagents/extension`, and `./runner-subagents/testing`; `/process` and `/runtime` test-only needs route through `/testing`; `usage.ts` is deleted; `extension-api.ts` is retained as the internal type/API home because the pass-through-facade concern is stale against current code; stale-subpath greps, focused runner-subagents and thermo-council tests, `just ts-format-check`, `just ts-lint`, `just ts-check`, and `just ts-test-typescript-style-guard` passed on 2026-07-05.
- [x] Consolidate the context-profiler interrogation flow behind the controller interface (session, prompt, transcript, render remain source-internal units); relocate `InterrogationScope` to the model; delete the `errors.ts` and `lm-json.ts` shims. Must not grow `view.ts` materially.
      Evidence: `EpisodeScopeSeed` and `InterrogationScope` now live in `context-profiler/model.ts`; callers import the model type while prompt/render/session/transcript remain source-internal implementation/test units; `errors.ts` and `lm-json.ts` are deleted with direct imports to `@ns/pi/shared/errors` and `@ns/pi/models/lm-json`; `view.ts` changed from 1047 to 1048 lines due import shape only; stale shim/type-home greps passed; focused context-profiler interrogation test command passed on 2026-07-05; `just ts-format-check`, `just ts-lint`, `just ts-check`, and `just ts-test-typescript-style-guard` passed on 2026-07-05.
- [x] Flatten thermo-council: one barrel, one type home (resolve the `contract.ts`/`types.ts` split), fold the `outcomes.ts`/`prompt-blocks.ts`/`constants.ts` fragments into the orchestrator; export `reviewerOutcomeFromRunnerResult` deliberately or test it through the command.
      Evidence: `index.ts` is now the deliberate root/test barrel; `extension.ts` is focused on Pi registration/parity and extension host types; `contract.ts` owns public domain/terminal constants and types; adapter/local helper types moved to owning modules, deleting `types.ts`; `outcomes.ts`, `prompt-blocks.ts`, and `constants.ts` are deleted and folded into `report.ts`, `prompt.ts`, `contract.ts`, and `scope.ts`; `reviewerOutcomeFromRunnerResult` is deliberately exported from the root barrel and tested through that route; stale fragment/deep-import greps passed; focused thermo-council test commands passed on 2026-07-05; `just ts-format-check`, `just ts-lint`, `just ts-check`, and `just ts-test-typescript-style-guard` passed on 2026-07-05.

## Parked

- [ ] Merge the pr-previews checks/feedback clones into one deep preview-surface module (bordered list/detail modal view, command skeleton, detail-row union, shared zod target schema), with checks and feedback as thin target adapters.
      Parked by explicit user direction before seam classification or implementation. The candidate remains technically plausible, but reopening should have a concrete reason to pay the abstraction risk and must first classify each drifted difference (`modalRows()` overlay budget, footer border chrome, detail glyphs `│`/`▏` and `Evidence:`/`·`, review-role coloring) as parameter or accident.
