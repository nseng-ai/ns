# pr-previews Merge Parked

## Summary

The pr-previews checks/feedback twins merge is parked by explicit user direction before seam classification or implementation. No pr-previews code changes landed for this candidate.

## Objective Impact

Candidate 1 is no longer active Work. The roadmap now keeps the pr-previews merge under `## Parked` with the rationale that reopening should require a concrete reason to pay the abstraction risk and must first classify the known drifted presentation differences as parameters or accidents.

The completion criteria now treat candidate 2 as the landed strong slice and require candidate 1, plus candidates 3–5, to be resolved by landing, explicit parking with rationale, or dropping with recorded rationale.

## Follow-Ups

- If pr-previews is reopened, classify `modalRows()` overlay budget, footer border chrome, detail glyphs `│`/`▏` and `Evidence:`/`·`, and review-role coloring before implementation.
