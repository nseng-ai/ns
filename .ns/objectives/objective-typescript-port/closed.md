# Closure Marker

Closed after the Objective CLI TypeScript cutover completed: standalone `objective` is TypeScript-backed, `asdl objective` is retired, `packages/asdl-objectives/` is deleted with rollback evidence, and final validation passed.
