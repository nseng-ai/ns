# T3 objective-family SSOT executed

## Summary

The objective-family single-source-of-truth cluster executed via an Objective Runner
step on `skill-audit-t3-objective-ssot` (commit 3e1be66c, stacked on the T2 branch).
8 files, +17/−20 lines. Each meaning now has one authoritative home: Selection rules
and the two sanctioned exceptions (objective-update single-candidate,
objective-critique branch-changed record) live in the umbrella; the picker grouping
spec lives in `docs/objective-system.md`; the "step skills self-contained,
create-facades delta-only" family policy is stated once in the umbrella; create stop
conditions live in objective-create's Stop/ask; the Horizon/Drive axes and
Autoobjective shorthand live in the patterns catalog. The RDD facade gained the
family-standard `## Verify and stop` binding it was missing.

Re-verification showed T1's pointer compression had already completed several
T3-tagged consolidations (composition matrix, facade identity sentences, Record
Frontmatter mechanics, close-side Stop-ask, validation-rows/PR-evidence discipline) —
dispositioned verified-already-addressed. Retro template/maintainer-note disclosure
stays with the disclosure-moves cluster; the enriched-plan-save locator is
branch-context family, not this cluster.

Validation: `just` green (full suite, `ns objective check --all` sweep-ok),
`areg check` OK, `areg skill show` verified for all seven touched skills.

## Objective Impact

Tranche 3: three of its clusters are now done (adversarial-reviews, TS ownership
split, objective-family SSOT). Remaining: shared family material to neutral homes,
disclosure moves, reference TOCs, and vague-completion-criteria sharpening. The only
instructed-behavior changes were the two the audit itself prescribes: critique's
selection exception made explicit-and-sanctioned, and the RDD facade's missing
done-condition added.

## Follow-Ups

- Next T3 cluster: shared family material to `docs/conventions/` neutral homes
  (autobranch-family-boundaries, cmux-read-only-posture, gt plumbing-not-display,
  just-gate map, doc-economics rules).
- Then: disclosure moves, reference TOCs, and completion-criteria sharpening.
