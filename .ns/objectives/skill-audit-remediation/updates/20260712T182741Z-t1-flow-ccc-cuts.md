# T1 flow+ccc cuts executed

## Summary

Fourth Tranche 1 family branch executed via an Objective Runner step on
`skill-audit-t1-flow-ccc` (commit 0fe78a7b, stacked on the code/Graphite ops slice).
All live batch-6 T1 findings applied across 8 SKILL.md files, family total 781 → 731
lines. Notable cuts: ns-flow-submit 76 → 58, and the stale
`NS_SLUG_MODEL`/`NS_CHECKPOINT_MODEL`/`NS_DEV_*` env-var blocks deleted from all four
flow skills (the deleted phrases appear only in CLI source/scenario tests, which pin
CLI behavior, not skill prose). No findings rejected in this family — everything not
applied is tranche-routed (T2 description stubs, T3 consolidation/disclosure, the T4
cmux inventory helper).

Validation: `just` green (full suite, `ns objective check --all` sweep-ok),
`areg check` OK, `areg skill show` verified for all 8 touched skills with invocation
kinds unchanged.

## Objective Impact

Tranche 1: four of ~8 family branches done (objective; branch-context/handoff/brmem;
code/Graphite ops; flow+ccc) with `wc -l` evidence captured for each.

**Naming correction for T4:** the audit findings and the frontload update say
`ccc exec` / `ccc-*`; the live surface is `ns cmux exec` and the skills are
`ns-cmux-*`. The T4 graduate record for the inventory/manifest helper should use the
live `ns cmux exec` naming.

## Follow-Ups

- Remaining T1 family branches: scaffolding; TypeScript/CLI; docs/retro/setup;
  review/meta.
- Tranche-routed batch-6 findings: T2 (`Command:` stubs for ns-flow-cp/ns-flow-submit,
  branch-triage `metadata.internal`), T3 (badge-vocabulary and posture-rule
  consolidation, stack-map palette disclosure, branch-triage completion criterion),
  T4 (cmux inventory/manifest exec helper — under its live `ns cmux exec` name).
