# Absorb the Reviews Outcome and Retain Compatibility Runners

## Summary

`reviews-via-pi-gateway` is now the canonical home for the Reviews outcome of the closed `harness-session-generation` architecture exploration. The configured quick and deep review profiles route through Pi and Vercel AI Gateway; the proposed foundation-wide harness-session substrate and Claude/Codex `reading-agent` migrations were never implemented and are not prerequisites for this Objective.

The existing direct Claude Code and Codex review runners and their qualified provider mappings remain supported compatibility routes. They are intentionally kept rather than deprecated or scheduled for removal.

## Objective Impact

The durable preservation boundary inherited from the earlier exploration is explicit: qualified provider routing, structured findings parsing, input-coverage propagation, cancellation/failure mapping, review log and publication identity, and cleanup behavior must remain intact. This does not widen the remaining roadmap: the local deep Pi proof and real-PR quick-review publication proof remain the only open delivery evidence.

The mirrored Objective Edge now records the completed consolidation rather than a future migration dependency.

## Follow-Ups

- Complete the credentialed local deep review proof through the configured Pi profile.
- Complete the real-PR proof for both CI tripwires and findings publication.
- Keep focused tests around the Claude Code and Codex compatibility routes while they remain supported; create a fresh Objective only if a concrete future consumer justifies shared harness-session infrastructure.
