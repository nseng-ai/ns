# Roadmap

## Work

- [x] Contract amendment: replace the previously accepted cursor-diff, descent/history-gated, initial-`--full`, and event-reconstruction contract with level-triggered complete snapshots and generation-aware cursor/plan/event semantics across the canonical README/SPEC and both Objective records. This explicitly supersedes rather than erases the old contract and PR #4128's shallow-history rationale.
  - Evidence: the published contract boundary is open as PR #4132; source and canonical contract searches show reconciliation has no ancestry, diff, shallow-history, cursor-tree, target-row, or `--full` runtime surface.
- [x] Complete snapshot facts and pure planner: simplify `ArtifactGateway`/Gather to target-commit resolution plus complete raw topology/corpus; add one coherent completed-materialization snapshot read; implement `deriveReconciliationPlan(facts)` over immutable target, stored current/tombstone/lineage, and registrations with no gateways.
  - Evidence: the published planner boundary is open as PR #4133. Source and focused tests verify complete topology/corpus validation before planning, create/restore/revise/move/unchanged/delete, lineage and schema legality, complete deletion detection, canonical order, deterministic Reconciliation Plan equality, and merge/history neutrality.
- [x] Durable generation protocol: generation-bearing cursors and CAS, deterministic generation-aware `gpa_` Attempt IDs, generation/Attempt-ID-aware `gpe_` lifecycle events, Reconciliation Plan persistence, atomic Pending Plan insertion, snapshot reads, exact replay/conflict handling, and cleanup are implemented in the fake and SQLite seams with retry-safe core application.
  - Evidence: the published engine boundary is open as PR #4134. The Reconciliation Plan contains Planned Artifact Materializations that Domain logic converts to Prepared Artifact Materializations for the Materialization Store Gateway; the Resulting Cursor and event IDs cannot be independently authored. Literal identity vectors, shared fake/SQLite conformance, SQLite schema/store probes, and focused engine tests cover generation 0/1, retry stability, later same-target distinction, `A → B → A → B` ABA rejection, Pending Plan precedence, and cleanup-only completed residue.
- [x] Retry-safe engine and CLI: compose Gather → Decide → Apply; persist the Reconciliation Plan as the Pending Plan before writes; apply each Planned Artifact Materialization as revision → lineage → current → classified target → event in canonical artifact-ID order; write the Resulting Cursor last; resolve errors and delete the Pending Plan; expose `gitplane reconcile <commit>` with bounded typed output.
  - Evidence: the published CLI boundary is open as PR #4135. Fault injection covers before/after store-write convergence; focused CLI and real Git/SQLite tests cover help, typed output, initial/update/older/divergent/merge/repeated/equal/unavailable/depth-1 behavior, and close-on-all-paths.
- [x] Closure accounting and replacement disposition: `architecture-accounting.md` accounts current rebased implementation tip `e7fdc08304e956200c29e1662aaa818e55c2aaec` against prototype `09d75c3ae`, superseded implementation `48a07b6bb` (and its historical package-equivalent rebased commit `e3e7f5e41`), and historical PR #4128. It inventories replacement proofs and intentionally deleted runtime/test surfaces. Prototype PR #4076 and superseded single-commit PR #4130 are verified closed unmerged.
  - Evidence: the accounting boundary is published as open PR #4136. GitHub reports #4076 and #4130 closed unmerged on 2026-08-06. The rebase changed local stack SHAs, so the former published accounting SHA remains publication provenance rather than a current local tip.
- [~] External closure: the replacement boundaries are published as open non-draft PRs #4132–#4136 with verified base/head chain from `shallow-ancestry-incomplete-history-classification` through the accounting boundary. Finish remote CI, review-tripwire, and Graphite mergeability verification; land the stack; propagate completion evidence to the parent `gitplane` Objective; then close this Objective.
  - Current evidence: publication, prior remote topology, and prototype dispositions are verified. Required TypeScript, integration, isolated, sanity, style-guard, dprint, Objective, review, and Graphite checks were queued or running on parts of the published stack; landing and closure are not yet established.

## Parked

- Incremental complete-snapshot optimization (tree-OID caching, commit-diff fast paths, or similar); measure first, and never make ancestry a correctness input.
- Working-tree targets including dirty or untracked content.
- Repair mode and operator target-row drift detection; add them only after an operational need and backend-neutral semantics are proven.
- Source leases and broader distributed scheduling beyond one Pending Plan plus generation CAS.
- Migration of old prototype/pre-release stores; incompatible stores are recreated.
- Event dispatch, production persistence, and other capabilities parked by the parent `gitplane` Objective.
