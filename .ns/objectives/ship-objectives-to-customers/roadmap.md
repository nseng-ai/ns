# Roadmap

## Work

Subobjective order and rationale for this umbrella Objective live in `objective.md`'s Thesis (the Objective Edges are in its frontmatter); the list is not restated here to avoid drift.

- [x] Decide the npm-distribution structure: split checkout-free `ns` publishing into its own Objective, or keep it as this Objective's spine. **Resolved 2026-07-01: SPLIT** into `checkout-free-sdl-distribution`; consumed here as a hard dependency.
  - Notes: gates sequencing; the long pole either way. Checkout-free `ns` benefits every capability, so it is its own Objective, not this spine.
- [x] **Dependency (owned by `checkout-free-sdl-distribution`):** ship a checkout-free, npm-installable `ns`: a bundled `@nseng-ai/ns` package with no `ts/node_modules` precondition, published versioned to npm.
  - Evidence: a global/`npx` install of `ns` on a machine with no ns checkout runs `ns objective list` against a foreign repo.
  - Notes: tracked here as a consumed dependency; the owning Objective closed 2026-07-06. The full public `@nseng-ai/*` set is published (initially `0.1.1`; registry latest `0.1.2`, 2026-07-07, which also added `@nseng-ai/harness-artifacts` and a standalone `@nseng-ai/kernel` to the public set); `@nseng-ai/ns` ships `bin.ns` and the expected kernel subpath exports, and a registry-backed checkout-free smoke (`npx @nseng-ai/ns@0.1.1 objective list` from a foreign repo with no ns checkout) passed. Evidence bar met for the batteries-included shape; the bare-core target shape is re-verified by the unbundle/republish row below.
- [~] **Dependency (spans `checkout-free-sdl-distribution` + `ns-skills-steelthread`):** deliver the objective skill into the correct per-harness roots (`.claude/skills/`, `.agents/skills/`) from the shipped package. Copy-not-symlink (areg's symlink model does not fit customers).
  - Notes: consume the platform path table, do not rebuild it. `ns skills` exists (`list`/`path`/`install`, from the closed `ns-skills-steelthread`, implemented in `@nseng-ai/harness-artifacts`) and lists the first-party `objective` skill; extension-bundled artifacts are provisioned during `ns init`/`ns extension` activation through the descriptor-declared artifact machinery (`prepareDeclaredArtifactActivation`/`applyPreparedDeclaredArtifactActivation`). Remaining `[~]`: verify the bundled objective skill dirs provision end-to-end from the published tarball into harness roots in a customer repo.
- [x] Scaffold the `@nseng-ai/ns-init` capability package (name amended 2026-07-05 from `@nseng-ai/init`; permanently `private: true`, ships only folded into the `@nseng-ai/ns` bundle): gateway seams, managed-block/git-posture operation surface, surfaced as top-level `ns init`. Reuse `@nseng-ai/foundation/managed-region`; borrow areg `init` patterns without depending on it.
  - Evidence: `ns init --harness ...` scenario tests passed against fakes (block written, `.ns/objectives/` created, git posture verified, skill step invoked-and-faked); host-level `ns init --help` injected through `@nseng-ai/ns`; focused checks/tests passed 2026-07-06.
  - Notes: historical shape — the slice landed with an `activateObjectives` operation, a managed `ns:objectives:begin/end` block writer, and a `SkillMaterializer` seam, all since superseded in place by the generic activation implementation row below (pointer stanza + `.ns/instructions.md`, descriptor `activation` contributions, declared-artifact provisioning; the `SkillMaterializer` seam no longer exists in `ns-init`).
- [x] Implement `ns init` bundle-independent behavior: explicit `--harness` selection (no sniffed default; persist to `ns.toml` for re-runs), managed `AGENTS.md` write + `CLAUDE.md → @AGENTS.md` import (idempotent/upgradeable/removable), `.ns/objectives/` creation with `.gitkeep`, and verify-and-write-never-commit git posture (require repo, verify trunk detectable).
  - Evidence: first-run missing `--harness`, explicit `--harness` persistence, persisted rerun, explicit replacement, invalid `ns.toml`, TOML parse/replace, and host `ns init --help` tests passed in the focused validation listed above.
  - Notes: the managed-write mechanics were subsequently migrated from the `ns:objectives:*` fat block to the `<!-- ns:begin -->` pointer stanza per the 2026-07-09 activation design; harness selection/persistence and git posture carried forward unchanged.
- [x] Wire real skill provisioning into `ns init` now that the npm bundle exists. Delivered via the descriptor activation surface rather than the originally planned `SkillMaterializer`-to-`ns skills` wiring: `ns init` provisions declared bundled artifacts of installed extensions into selected harness roots through `RealArtifactActivationGateway` → `@nseng-ai/harness-artifacts` prepare/apply declared-artifact activation.
  - Notes: resolves the CLI↔skill bidirectional dependency by shipping them together; the platform path table is consumed, not rebuilt. The interim `RealSkillMaterializer`/`provisionFirstPartySkill` wiring in `ns-init` was removed with the de-objectives-ification. End-to-end idempotent provisioning verification from the published tarball in a customer repo is tracked by the dependency row above and the verification row below.
- [x] Design the `ns install` / `ns remove` / `ns update` surface (Pi parity; decided 2026-07-05). **Resolved 2026-07-09** in a README-driven design session: acquisition verbs live under the `ns extension` group (`install`/`uninstall`/`update <source>`/`list`), explicit `npm:` source-spec grammar shared verbatim with `ns.toml`, repo-level `ns.toml` as the only v1 settings home, top-level `ns install` retired and `ns update` narrowed to reserved self-update. Both reopened Open Questions resolved (see objective.md).
  - Evidence: canonical design in `references/README-draft.md`; decision record in `updates/20260709T165911Z-extension-acquisition-surface-designed.md`. Grounded in the Pi checkout (`pi install`/`remove`/`update`) and the landed `extension-descriptor-contract` machinery (managed `ns install`, `acquisition.ts` npm resolution).
- [x] Design the descriptor activation surface for generic `ns init`: extensions contribute activation content through the descriptor; core `init` keeps only extension-agnostic duties. **Resolved 2026-07-09** in a follow-up activation-surface design session: minimal permanent pointer stanza in `AGENTS.md` → committed, tool-owned, wholly regenerated `.ns/instructions.md`; plain-data descriptor `activation` field (`instructions` markdown section + `consumerDirs`, no hook); trunk detection stays generic core git posture; orientations line ships day-one in the objectives-contributed section; no migration machinery (old fence existed only in `ns-init` source/tests).
  - Evidence: design artifact `references/init-activation-design.md`; decision record `updates/20260709T183829Z-init-activation-surface-designed.md`.
- [x] Implement the activation surface and de-objectives-ify `@nseng-ai/ns-init` per `references/init-activation-design.md`: add the optional `activation` descriptor field (coordinated per `extension-descriptor-contract`'s public-shape policy), move objectives' instruction section + `.ns/objectives/` creation into `@nseng-ai/objectives`' descriptor, generalize `ns init` to the pointer-stanza + `.ns/instructions.md` regeneration + declared-extension provisioning, wire regeneration into the `ns extension` verbs, and remove the old `ns:objectives:*` runtime behavior from `ns-init` code/tests.
  - Evidence: the public descriptor accepts strictly validated plain-data `activation.instructions` and canonical, unique `activation.consumerDirs` (`ts/packages/kernel/src/sdk/descriptor.ts`); `@nseng-ai/objectives` owns its exact contribution (`activation.instructions` + `consumerDirs: [".ns/objectives"]` in `ts/packages/capabilities/objectives/src/ns/extension.ts`); and generic `ns init` performs installed-only ordered descriptor preflight before writes, writes the pointer plus regenerated instructions, creates declared consumer directories without deleting data, and provisions declared bundled artifacts into selected harnesses. The 2026-07-10 review remediation added canonical duplicate-declaration rejection, safe manifest-tracked deprovisioning for removed extensions and deselected harnesses, content-bound optimistic prepare/apply validation, and the `.ns/managed-extensions/` gitignore duty while preserving consumer data. No `ns:objectives:*` markers remain in `ns-init` source/tests (`instruction-block.ts` now renders the `<!-- ns:begin -->` pointer stanza). Kernel, harness-artifacts, and ns-init package checks/tests plus the TypeScript style guard passed.
  - Notes: install, uninstall, and update all carry activation reconciliation, completing this lifecycle row. Per-harness pointer-following reliability on Codex/Pi folds into the end-to-end onboarding verification (its prior owner `cross-harness-parity` closed 2026-07-11, intentionally concluded).
- [x] Make `ns init` and extension install/update/uninstall lifecycle mutations observable across human, JSON, and Markdown output.
  - Evidence: all four success schemas require one shared Zod-discriminated, kebab-case ordered `steps` history; traced failures retain accumulated steps alongside existing recovery evidence; activation emits completed files, consumer directories, and artifacts only after their outcomes are known; install distinguishes installed, unchanged, and local-in-place acquisition; update dry-run explicitly records no writes and prospective-effect availability; uninstall records removed/absent declarations, cleanup, and preservation. Human mode adapts the shared recorder to `NsExtensionApi.commandIo.phase`, while structured modes suppress live trace; all four commands have dedicated Markdown renderers. Focused `@nseng-ai/ns-init` check and 130-test suite plus the full TypeScript workspace check passed 2026-07-14; six focused host integration tests cover redirected human tracing and non-streaming JSON/Markdown histories across all four mutations.
- [x] Implement the `ns extension` acquisition verbs per `references/README-draft.md`: `install` in the group with `npm:` source support (registry install into managed storage, pinned vs floating), identity-matched `uninstall`, single-target `update`, and `list`; retire top-level `ns install`; migrate `ns update --extensions` into `ns extension update`. Apply `ns-cli-design` discipline at build time.
  - Evidence: `ns extension install` accepts explicit `npm:` and unprefixed local specs, resolves local packages in place, ensures npm packages in managed storage, fully validates the prospective descriptor set, rejects canonical identity conflicts, records exact specs idempotently, and runs generic activation using persisted harnesses. Identity-matched `ns extension uninstall` removes one declaration without reformatting unrelated TOML, reconciles instructions and safely owned artifacts before deleting bounded npm storage, preserves local sources/consumer data/edited or untracked artifacts, and treats absent declarations as successful recovery runs that clean orphaned npm bytes. Single-target `ns extension update` landed with explicit preview/applied acquisition-state unions (npm pinned ↔ unchanged/restored, floating ↔ refreshed/restored, local in-place), dry-run planning, and descriptor activation preflight/apply after successful live acquisition. The top-level migration is complete: `ns update` is the reserved self-update stub pointing at `ns extension update <source>`, and the host test suite rejects the retired `--extensions` flags with a usage error. The preinstalled ns-init catalog contributes the nested verbs alongside kernel `point`/`points` inspection; no top-level or `remove` aliases exist. `ns extension list` completes the surface with one deterministic, read-only inventory row per declared source, distinct acquisition and artifact states, canonical resolved facts and structured diagnostics, and explicit partial-count semantics when artifact status is `unavailable`; stale `ns update --extensions` references are removed.
- [~] Unbundle first-party extensions from the published `@nseng-ai/ns` (bare core, reaffirmed 2026-07-09), republish, and re-verify checkout-free through the acquisition path: `npm install -g @nseng-ai/ns` → `ns extension install npm:@nseng-ai/objectives` → `ns objective list` in a foreign repo.
  - Evidence so far: the source-side unbundle landed 2026-07-07 (Objective commands removed from the default host; `ts/packages/hosts/ns/package.json` no longer depends on `@nseng-ai/objectives`; the checkout-free smoke script asserts Objective commands are absent from default help). The repo's own dev CLI keeps loading first-party descriptors via workspace discovery (self-hosting unaffected — `ns objective …` still works in this checkout).
  - Remaining: republish (npm latest `0.1.2`, published 2026-07-07T19:18Z, predates the unbundle commit, so the registry artifact is still batteries-included) and run the foreign-repo re-verify through the acquisition path.
- [x] Author the lean, portable, harness-neutral objective instruction content (decided 2026-07-01: objectives-exist + `ns objective list` before non-trivial work + use the objective skills/CLI).
  - Evidence: the lean block landed 2026-07-05 in `ns-init`'s `instruction-block.ts` with marker/concept/negative-wording tests and focused validation.
  - Notes: superseded in delivery form by the 2026-07-09 pointer-stanza architecture — the objectives instruction content now lives in `@nseng-ai/objectives`' descriptor `activation.instructions` contribution (rendered into `.ns/instructions.md`), and the orientations line ships day-one there, reversing the original `load-orientations` exclusion. `instruction-block.ts` now renders the generic `<!-- ns:begin -->` pointer stanza.
- [~] Write the real happy-path Get Started content in `docs-site` (installation + quickstart around the bare-core + `ns extension install` shape, plus real concepts/objectives and tools/objective pages), replacing the placeholder pages. Keep the npm install gate until the bare-core republish and a real global/`npx` install are verified.
  - Notes: docs-site shell, IA restructure, and launch-gate removal owned by `eve-parity-docs-site`; objective content owned here. All four pages carry real objective content (verified present: `docs-site/docs/get-started/installation.mdx`, `get-started/quickstart.mdx`, `concepts/objectives.mdx`, `tools/objective.mdx`), but installation and quickstart still carry the stale "Coming with the first release" gate copy even though `@nseng-ai/ns` is on npm (latest `0.1.2`). The 2026-07-09 bare-core reaffirmation changes the documented happy path to `npm install -g @nseng-ai/ns` → `ns init` → `ns extension install npm:@nseng-ai/objectives` (init-before-install per the 2026-07-10 decision), so the quickstart/installation rewrite depends on the `ns extension list` and unbundle/republish rows above. Un-gating publication is still blocked on the rest of the docs corpus (still placeholder) owned by `eve-parity-docs-site`.
- [ ] Verify onboarding end-to-end in a throwaway non-ns repo on **Claude Code** (first-slice bar, decided 2026-07-05): follow the published docs verbatim — install `@nseng-ai/ns` from npm, `ns init`, `ns extension install npm:@nseng-ai/objectives`, then create → next → update → close. Every deviation becomes a docs fix or new row. Zero improvisation = verified.
  - Notes: also de-risks the standalone-portability assumption. Codex + Pi verification moved to follow-up (see Parked). Depends on the bare-core republish row (the batteries-included registry shape is superseded).

## Parked

- [ ] Codex and Pi end-to-end onboarding verification (deferred 2026-07-05 by the Claude-Code-first slice decision; the all-three bar remains the eventual target). Includes per-harness pointer-stanza-following reliability, formerly a `cross-harness-parity` item (that Objective closed 2026-07-11, intentionally concluded).
- [ ] Additional harnesses beyond Claude Code / Codex / Pi (Cursor, opencode, Gemini, Windsurf).
- [ ] Customer-facing skill upgrade / drift management (versioned skill sync in a customer repo) beyond first install.
- [ ] Telemetry, feedback channel, licensing / accounts, and released-package release automation / CI.
- [ ] Deferred `ns extension` surface (parked in the 2026-07-09 design): `update --all` fleet mode, `git:`/URL sources, per-extension resource filtering / enable-disable (`pi config` parity), a user/global settings scope, bare-name npm sugar, and `update` moving pinned refs.
