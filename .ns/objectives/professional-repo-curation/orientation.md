**Direction: transfer this repository in place after professional presentation, checkout-free product paths, and an enforceable two-zone curation model are complete. The flat `ts/packages/incubator/` already contains all 11 ns extensions; `@nseng-ai/extension-kit` and the `extension` tier vocabulary have landed.**

Getting to: finish the rename prose sweep; run the foundation README-driven pass; decide placement for hosts and rough tools/internal packages; add the incubator contract and prohibit clean→incubator dependencies; reconcile the shipped checkout-free Objectives path; verify PR Feedback; ship root README + `why-ns.md`; harden and transfer.

What you see now: the extension code cutover and Objectives `0.1.3` foreign-repo smoke are landed, but root `README.md` is still a placeholder, `why-ns.md` is absent, PR Feedback remains checkout-bound in its README, and the stronger zone dependency invariant is not enforced.

Avoid: new clean→incubator dependencies; old ns-domain “capability” naming; polishing incubator packages outside an explicit slice; claiming checkout-free behavior without package/registry evidence; refactoring during graduation without user discussion; committing secrets, tokens, private data, or other history that cannot transfer.

Active slices: close `rename-capability-to-extension` and start the Clinkr child under `foundation-readme-driven-pass`.
