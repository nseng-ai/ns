**Direction: transfer this repository in place after professional presentation, checkout-free product paths, and the approved package and skill disposition ontologies are complete. Packages follow ADR 0045's `public`/`incubating`/`internal` owner tree and dependency closure; skills follow the distinct ADR 0046 support-disposition/family contract in authoritative `skills/README.md`.**

Getting to: finish the rename prose sweep; run the foundation README-driven pass; complete `package-disposition-and-host-ontology`'s deferred Pi extraction; reconcile Objectives; verify checkout-free PR Feedback; ship root README + `why-ns.md`; harden and transfer.

What you see now: the package disposition cutover has landed; deferred Pi extraction remains. The deleted Skill Exposure package and folded Harness Artifacts implementation leave 31 workspace packages, and public `@nseng-ai/ns` no longer depends on either Harness Artifacts or Branch Context. The closed skill Subobjective established the ontology; after ADR 0057 retired one redundant standalone workflow, the current tree contains 58 first-party skills: 1 public (`pr-make-accountable`), 24 incubating, and 33 internal. `npx skills` owns acquisition, installed-state lifecycle, and `skills-lock.json`; the repository directly owns owner-nested canonical sources, flat Harness Overlays, and invocation metadata. ns has no skill-management commands, package, or reconciliation interfaces, while runtime Skill-Backed Commands remain. Root `README.md` is still a placeholder, `why-ns.md` is absent, and PR Feedback remains checkout-bound in its README.

Avoid: conflating package disposition with skill support disposition, or skill family/identity/invocation metadata with one another; assigning skill lifecycle, overlays, or metadata reconciliation to ns; managing first-party skills through old `skills/<identity>` fallback paths instead of explicit nested sources; changing flat runtime overlays or vendored directories; dependency edges that violate package disposition closure; polishing incubating residents outside an explicit slice; claiming checkout-free behavior without package/registry evidence; committing secrets or private history.

Active slices: close `rename-capability-to-extension`, start the Clinkr child under `foundation-readme-driven-pass`, finish the package child's deferred Pi extraction, and advance presentation plus checkout-free PR Feedback.
