**Direction: this repo transfers to the target organization as-is (history intact, names unchanged) after three outcomes land: professional presentation with progressive disclosure, seamless install of ns outside this checkout (single-player objective system first, pr-feedback quickstart second), and in-place curation under a two-zone model — the existing clean tiers plus a flat `ts/packages/incubator/` holding everything not yet warranted. Packages leave the incubator only through a README-driven graduation gate. The domain term "capability" is being renamed to "extension" (with an explicit pi-extension/ns-extension disambiguation).**

Getting to: the capability→extension vocabulary verdict first; a foundation README-driven pass runs in parallel (per-package Readme-Driven-Development Subobjectives, clinkr first); then the demotion commit with an enforced invariant — no package outside `incubator/` depends on a package inside it (`packagechk`/tier machinery) — the single-player objective system ship, root README + why-ns, pre-transfer hardening, transfer. See `professional-repo-curation`'s `objective.md` for the gate definition.

What you see now: "capability" is still the ambient term; the incubator directory may not exist yet; capabilities and hosts still sit in their tier directories, and hosts/pi + hosts/ns still import capability packages. Treat all capabilities, hosts, and rough tools as incubator-destined regardless of current location.

Avoid: adding any new dependency from a clean-tier package onto a capability, host, or other incubator-destined package; coining new "capability"-based names in docs or code while the rename is unsettled; polishing or refactoring incubator packages outside an explicit graduation slice; implementing package refactorings before discussing them with the user; letting graduation slices grow unrelated redesign or feature work — the gate is honest-and-explainable, not ideal; committing anything to git history that cannot survive an organization transfer (secrets, tokens, private data).

Active slice: see this objective's roadmap.md — Subobjectives `rename-capability-to-extension` (primary) and `foundation-readme-driven-pass` (parallel).
