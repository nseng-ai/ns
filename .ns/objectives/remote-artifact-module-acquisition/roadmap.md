# Roadmap

## Work

Design decisions (steer-first; record each as a Semantic Update before implementing):

- [x] Decide the source-spec grammar and first-slice source kinds.
      **Decided 2026-07-07** (see `updates/20260707T182049Z-source-spec-grammar-decision.md`): adopt pi's grammar shape verbatim as the durable grammar (`npm:pkg[@ver]` / `git:host/user/repo@ref` / local path); slice one ships **npm-only**, with git and local-path reserved until the storage and gateway decisions land. Pinning semantics adopted with the grammar: versioned npm specs pinned/skipped, unversioned reconciled, git refs reconciled-not-advanced. Malformed/unknown specs are per-entry diagnostics. The local-path acquisition-vs-pointer question stays open.
- [x] Decide the fetched-module storage location and its explicit, inspectable record, plus how discovery reads it.
      **Decided 2026-07-07** (see `updates/20260707T184938Z-managed-extensions-storage-and-model-decision.md`; refined by `updates/20260707T190752Z-pi-aligned-fetch-mechanics-and-dependency-behavior.md`): consumption-agnostic acquisition owned by a new acquisition-only subpackage (working name `@nseng-ai/extensions`); ns.toml key **`extensions = [...]`** (supersedes the `artifact-packages` working name); managed root **`.ns/managed-extensions/`** (gitignored via repo-root `.gitignore`, written by `ns init` in consumers), with pi-aligned source-kind subdirs. Npm uses a managed package project at `.ns/managed-extensions/npm/` and installed modules under `node_modules/<pkg-name>`; git clones remain under `git/<host>/<path>`. **No lockfile** — the spec is the record, resolved state inspectable via fetched `package.json` + manifest provenance; discovery gains the managed root as a third root reusing the existing walk. Slice one wires only artifact discovery; kernel command loading from fetched content is deferred to the trust re-judgment row.
- [x] Decide fetch mechanics and the acquisition gateway seam (registry access, git transport, fake-driven tests; no real network in tests).
      **Decided 2026-07-07** (see `updates/20260707T190752Z-pi-aligned-fetch-mechanics-and-dependency-behavior.md`): follow pi's package-manager acquisition model rather than npm tarball extraction. Slice-one npm acquisition creates a private managed npm project, installs declared specs with peer/host dependency suppression, permits normal runtime dependencies, and reports per-extension acquisition diagnostics. Git behavior is reserved but follows pi's direct clone layout with dependency install inside the clone. The acquisition package exposes gateway seams for package-manager/npm commands and git commands; tests use fakes and never perform real network fetches.
- [x] Decide how acquisition composes with `ns update` (inside unconditionally, flagged, or a separate composed verb) and the per-source update/pinning semantics command contract (pinned skipped; unpinned reconciled to spec).
      **Direction set 2026-07-07** (`updates/20260707T193019Z-...`); **contract decided 2026-07-07** (see `updates/20260707T200657Z-ns-update-acquisition-command-contract-decision.md`): pi-verbatim modes — bare `ns update`/`--self` = self-update only (errors with a clear use-`--extensions` diagnostic until the self-update mechanism ships), `--extensions` = acquire declared specs + reconcile/provision (today's bare behavior moves here), `--all` = both; per-spec targeting (`ns update <spec>` / `--extension <spec>`, declared specs only) is in the contract. Per-source semantics: pinned npm installed-if-missing then skipped, unpinned npm reconciled to current registry resolution, git reconciled-to-ref, local paths validated pointers. Per-extension failure isolation, idempotent unchanged specs, reconcile-to-spec on change; removed specs are report-only orphan diagnostics (removal verb deferred).
- [x] Re-judge the carried trust-posture risk acceptance with real fetch semantics on the table; record the outcome (continue accepting under the trusted-repo contract, or escalate to the umbrella). **Now also owns:** whether the kernel may load command extensions (executable code) from fetched managed-root modules — per-consumer wiring deferral in the storage decision (`updates/20260707T184938Z-...`).
      **Decided 2026-07-07** (see `updates/20260707T193019Z-trust-deferral-executable-extensions-and-update-composition-direction.md`): trust gate deferred — risk acceptance continues under the explicit trusted-repo assumption, no consent gate in this record; pi's trust-store/`--approve` model is the blueprint for a fresh Objective if ns's audience widens. Kernel loading of executable command/CLI-group extensions from fetched managed-root modules is **allowed** as a later slice (after passive artifact wiring, once the kernel extension contract is explicit).

Implementation (after the decisions above):

- [ ] `ns.toml` declaration list (`extensions = [...]`, per the storage decision): schema, parsing, and diagnostics in the ns-toml layer.
- [ ] Acquisition slice one: fetch + resolve declared specs of the first-slice source kind(s) into the decided storage root behind the gateway, with per-module failure diagnostics that do not block provisioning of present modules.
- [ ] Wire acquired modules into existing static-declaration discovery and the `ns update` reconcile flow; prove idempotence on unchanged specs and reconcile-to-spec on changed specs.
- [ ] Wire the pi-verbatim `ns update` command surface: `--extensions`/`--all`/`--self` modes, bare/`--self` interim error diagnostic, per-spec targeting for declared specs, `--dry-run`/`--force` carried into extensions mode.
- [ ] End-to-end evidence: one real remote module declared, fetched, and provisioned into `pi`/`claude-code`/`codex` roots with manifest hashes; existing arrival paths verified unchanged; full `just` green.
- [ ] Build the ns self-update mechanism behind bare `ns update`/`--self` (added 2026-07-07 by user instruction with the pi-verbatim contract decision; sequenced after the acquisition slices — the interim error contract holds until this lands).

## Parked

- (none)
