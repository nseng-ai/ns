# Created as the Steelthread Subobjective Split from skill-management-subsystem

## Summary

This record was split out of `skill-management-subsystem` on 2026-07-06, applying the Steelthread Objective pattern's child-split shaping: the parent had been reshaped thread-first the same day (parent update `20260706T120909Z-reshaped-as-steelthread-objective.md`), and its parked breadth — extension-carried provisioning, AREG re-platforming, the vocabulary reconciliation sweep, update/uninstall surfaces, remaining artifact kinds — is substantial enough to need its own coordination. The parent is now the Umbrella Objective holding the reusable-subsystem ambition; this Subobjective owns exactly the thread: one real ns-owned skill through `ns skills list/path/install` into the `pi`/`claude-code`/`codex` harness roots, with a per-file-hashed install manifest and zero `npx skills` dependency.

Decision provenance carried over from the parent (its `updates/` remain the immutable records): harness-artifact vocabulary and layering (`20260702T035321Z`), reconcile-primitive design orientation (`20260702T041140Z`), the `@nseng-ai/ns-init` `SkillMaterializer` consumer seam (`20260705T231627Z`), and the seeded `@nseng-ai/harness-artifacts` package (`20260706T100934Z`). The Pup research report stays in the parent's `references/`.

The `ship-objectives-to-customers` edge moved from the parent to this record, since what that Objective consumes is exactly this thread's `ns skills` provisioning surface.

## Objective Impact

- This record starts with the parent's two open thread rows (design the thread's model/plan/manifest; implement and validate `ns skills`) and the thread-validation Closure Gate.
- `## Parked` is deliberately empty here; deferred breadth lives in the umbrella.

## Follow-Ups

- Keep the umbrella's `[~]` coordination row updated as the thread progresses; closure evidence synthesizes back to the umbrella per the Umbrella pattern.
