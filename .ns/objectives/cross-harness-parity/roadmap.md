# Roadmap

> Names rebaselined 2026-07-03 for the `@sdl`→`@ji` scope cutover (`ji` bin owned by `@ji/kernel`, `.sdl`→`.ji`, `SDL_*`→`JI_*` env vars) and the capability-tree relocation (`ts/packages/capabilities/*`, `ts/packages/capability-kit/`). This follows the earlier 2026-06-26 `@asdl`→`@sdl` rebaseline; `parity-table.md` per-row names are now two renames stale and still await a full sweep.

## Work

- [x] Seed the living parity table (`parity-table.md`) from the 2026-06-03 cross-harness audit: one row per Pi command with shared CLI backing, driving skill, parity verdict (FULL/PARTIAL/NONE/WAIVED), and owning Objective. Evidence: `parity-table.md` exists and classifies the audited Pi command surfaces; removed surfaces are deleted during refreshes rather than retained as stale FULL rows. Pi tool calls are treated as host-native bridges that do not require parity metadata rows.

- [x] Close the dev-CLI discoverability gaps: author thin skills surfacing the checkpoint/submit workflows to Claude/Codex (describing CLI operations only), and reconcile the checkpoint-skill vs checkpoint-CLI duplication. Evidence: the wrapper skills now mirror the Flow CLI as `sdl-flow-cp` and `sdl-flow-submit` (renamed from the `code-*` family), delegating to `ji flow cp` / `ji flow submit`, installed for Claude (`.claude/skills`) and Codex (`.agents/skills`). Note: the former `dev-preview-url` and `objective-current` skill workflows remain unaccounted for across the renames; re-verify in the next full sweep.

- [x] Eliminate the `/handoff:list` duplication: point the Pi command at the dedicated handoff list CLI and keep only the card renderer in TypeScript. Evidence: the handoff capability (`ts/packages/capabilities/handoff/`) owns `ji handoff list` (`src/ji/commands/list.ts` over `src/core/operations/list.ts`); the Pi surface carries typed parity metadata (`src/pi/registration.ts`, `cli: "ji handoff list"`).

- [x] Author the durable parity-review skill: diff-scoped mode flags any added/changed Pi command lacking a CLI+skill counterpart (or a recorded waiver); on-demand full-sweep mode checks and refreshes the parity table. Evidence: the workflow is the `parity-review` route of the `code-workflows` skill, documenting diff-scoped and full-sweep modes, live-evidence-first command inventory, CLI-bridge treatment, advisory finding labels, and Objective table sync with a required Semantic Update. Reference: `skills/code-workflows/references/parity-review.md`.

- [x] Add a typed Pi-host parity accounting gate. Evidence: the gate is now distributed — packages with Pi surfaces export co-located `definePiSurfaceParity` metadata (via `@ji/pi/parity/extension`, `ts/packages/hosts/pi/src/runtime/parity-extension.ts`) and carry their own fake-host parity tests using `@ji/pi/parity/testing`: `ts/packages/hosts/pi/test/parity.test.ts` plus `*-parity.test.ts` in `capabilities/flow`, `capabilities/handoff`, `capabilities/objective`, and `local/pi-tools` (thermo-council, pr-previews, backing-skill-commands, pr-feedback-watch, context-profiler, grill). Aggregation/check logic lives in `ts/packages/hosts/pi/src/parity/{registry,check}.ts`. Known coverage hole: `@ji/ccc`'s Pi surfaces (`/ccc:workspace:*`, `/ccc:claude-plan-tab`) carry no metadata and no parity test — tracked in the cmux dispatch row below.

- [x] Give `autobranch` a CLI entry point + skill, and bridge the Pi command to the same core. Evidence: the autobranch core lives in `@ji/flow` (`ts/packages/capabilities/flow/src/autobranch/`: branch-name selection, dirty/latest-commit transactions, checkpoint flow) with tests under `capabilities/flow/test/autobranch/`; `ji flow autobranch` is the CLI entry; the `sdl-flow-autobranch` skill drives it; Pi `/ji:flow:autobranch` is a CLI bridge with typed FULL metadata. Note: the previously-recorded `ccc exec autobranch` hidden-bin path is dead — the `ccc` bin's `package.json` entry points at a missing file and `ccc` is not on PATH; reachability runs entirely through `ji flow autobranch`.

- [x] Consolidate provider-specific model defaults behind the backend-neutral text-generation abstraction/config. Evidence: the model-slug seam is `ts/packages/infra/core/src/primitives/model-slug.ts` (tests: `ts/packages/infra/core/test/model-defaults.test.ts`) with a capability-kit split at `ts/packages/capability-kit/src/kit/model-slug.ts`; the canonical slug helper honors `JI_SLUG_MODEL` over the shared fast-model default; model-touching branches resolve through the seam with `JI_*` env overrides (`JI_CCC_SIDEBAR_MODEL`, `JI_CHANGES_MODEL`, `JI_CHECKPOINT_MODEL`, `JI_SUBMIT_FAILURE_MODEL`, `PI_DRAFT_MODEL`, etc.); shared prompt/skill guidance carries harness-neutral defaults with labeled OpenAI/Anthropic examples (AGENTS.md "Skill Model Examples" convention).

- [~] Give the stack-landing core a CLI entry point + skill. The CLI leg is done: the land core moved into `@ji/flow` (`ts/packages/capabilities/flow/src/land/`: preflight, landing-dispatch, isolated single-PR fast path, stack maintenance; tests under `capabilities/flow/test/land/` plus hermetic land-stack sandbox integration coverage), and `ji flow land` lands "the current PR or Graphite stack into trunk" with `--dry-run`/`--yes`/`--free` and JSON schema output; Pi `/ji:flow:land` is a CLI bridge with typed FULL metadata; the unified command covers the single-PR path, superseding the earlier skill-only single-PR plan. Remaining: no skill drives `ji flow land` — resolve via the flow skill/doctrine row below. Old `/code:land` no longer exists.

- [~] Resolve the flow-family skill/doctrine gap: `ji flow land|push|autoslot|changes|pull-trunk|regenerate-pr` have CLI entries and Pi `/ji:flow:*` bridges with typed FULL metadata, but no wrapper skills (unlike `sdl-flow-autobranch`/`-branch-latest-commit`/`-cp`/`-submit`). Either author the missing `sdl-flow-*` skills or record an explicit doctrine decision that kernel-CLI commands with typed FULL metadata are Claude/Codex-reachable without wrapper skills, and encode the decision in `parity-table.md`'s FULL rule. Notes: `ji flow push` is git-only for non-Graphite branches (its help/docs carry the Graphite-metadata caveat recorded 2026-07-01; any push skill must repeat the Graphite-tracked-PR prohibition); `ji flow autoslot` covers the former `/code:autoslot` PARTIAL row; `ji flow changes` supersedes the former `/code:changes` WAIVED verdict with a real CLI. Evidence: each `/ji:flow:*` row reaches FULL under the recorded doctrine.

- [ ] Give the cmux dispatch cores a CLI entry point + skill + typed parity metadata: a CLI with explicit inputs (plan-file path, prompt) over the `@ji/ccc` cmux modules (`ts/packages/capabilities/ccc/src/cmux/`: `dispatch-from-trunk.ts`, `dispatch-prompt.ts`, `slot-dispatch-plan.ts`, `slot-open-branch.ts`) and the shared cmux utilities (`@ji/capability-kit` `src/cmux/focused-terminal-tab.ts`, ccc's `prompt-file.ts`), with an agent-neutral launch path (not a hardcoded `pi @file`); cover `/ccc:workspace:dispatch-plan` (and `/ccc:surface:dispatch-plan`), `/ccc:workspace:dispatch-prompt`, and a thin skill for the `/ccc:workspace:open-branch` pairing. Pi keeps only session-history "latest plan" resolution over the core. Decide `/ccc:claude-plan-tab` alongside (likely WAIVED: Pi/cmux session-native tab launch) and record the verdict in the parity table. Decide whether the CLI lands as a repo-local `ji` group (the flow pattern) and whether the broken `ccc` bin (`package.json` bin → missing `./src/sdl/cli.ts`) is repaired or retired. Evidence: a Claude/Codex agent can dispatch a branch/plan into a cmux slot via skill+CLI; the `/ccc:workspace:*` rows move off NONE; the surfaces gain typed parity metadata.

- [ ] Implement parity-native command-output summaries as a shared CLI/helper, building on `@ji/clinkr` + `@ji/core` if implemented in TypeScript. Evidence: the contract names command/cwd/timeout/cancellation semantics, shell/argv invocation modes, outcome taxonomy, payload artifact log paths, bounded human and machine-readable summaries, profile-derived counts/excerpts for generic/test/lint/typecheck output where practical, no-leak guarantees, and skill guidance plus optional Pi adapter that wraps the same helper instead of becoming canonical. (No implementation evidence exists in the repo as of 2026-07-03.)

- [ ] Reconcile `parity-table.md` with two renames of drift (`@asdl`→`@sdl`→`@ji`), the capability-tree relocation, and the Flow capability landing: run a parity-review full sweep that re-verifies each row's current binary/package/skill name and re-confirms or corrects its verdict, covering the `/code:*`→`/ji:flow:*` surface renames, the `code-*`→`sdl-flow-*` skill renames, the dead `ccc exec` reachability path, the new smart-restack/stack-squash/code-workflows Pi surfaces, and the unaccounted-for former `dev-preview-url`/`objective-current` workflows. Evidence: a full-sweep Semantic Update reconciling the table against live `@ji/*` evidence.

## Parked

- [ ] Broaden the machine-checkable parity gate beyond the shipped per-package accounting tests: decide whether to add CI wiring, `@ji/ccc` Pi-surface coverage, ad hoc `.pi/extensions/*.ts` adapter coverage, and/or generated human table output from the typed metadata. The distributed gate checks accounting (missing/stale/duplicate metadata), not semantic parity correctness. (The seventh parity-rot materialization keeps this trigger live, though per-package gates have since shipped.)
- [ ] Broad automatic bash-output interceptor or framework-wide output safety net.
- [ ] Pi-only runner-subagent test-summary tool as the canonical command-summary implementation.
- [ ] LM/subagent semantic interpretation of command logs as the default summarizer.
- [ ] Migration of every validation command or package workflow to command-output summaries.
- [ ] Payload retention, garbage collection, durable archive policy, or strict numeric token-budget tests for command logs.
