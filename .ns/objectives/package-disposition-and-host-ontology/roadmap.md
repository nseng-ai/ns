# Roadmap

## Work

- [x] Draft and approve the superseding package-ontology ADR and complete destination inventory. ADR 0045 and `references/package-destination-map.md` classify all 26 baseline workspace manifests, define the 34-package target, settle Pi extraction and owner nesting, enumerate identity/dependency/release consequences, and provide the approved replacement direction for `professional-repo-curation`. The user explicitly approved the full map and ADR on 2026-07-25. The parent record was reconciled through its own tracking workflow on 2026-07-26 (`professional-repo-curation` update `2026-07-26T000342Z-adopt-disposition-ontology-direction.md`): its objective, roadmap, and always-loaded orientation now carry the disposition-ontology direction instead of the flat incubator. No package move or publication was authorized.
- [x] Design the atomic implementation stack from the approved map. `references/implementation-stack.md` is the durable design: a two-phase shape of pre-boundary trunk PRs that touch no package path or npm identity, then one Graphite boundary stack on a base cut from `master`, reviewed as ~19 PRs and landed by retargeting the top branch's PR base and squash-merging so exactly one commit reaches trunk. It settles four decisions (fold Harness Artifacts into `@nseng-ai/ns` with a new `./api`; allow Pi command-*name* constants on `@nseng-ai/branch-context/api`; write a superseding ADR for the `pi-ns-pr-feedback` CLI seam; bless adapter-to-adapter dependencies on declared curated subpaths), records eight corrections that made the raw synthesis executable, prohibits `ns flow land` for this boundary, and carries the per-branch mechanical tactics and verification list. It also fixes the execution scope: the complete filesystem reorganization runs now and the `pi-ns-*` Pi extraction is deferred. All three Open Questions are closed by it, and the approved 34-package target becomes 33 through the Harness Artifacts fold. The design row itself authorized no package move, identity change, publication, or registry write.
- [ ] Execute the coordinated package cutover. Move the complete inventory into `public/`, `incubating/`, or `internal/`; establish owner-specific nested ontologies; rename identities so every leaf matches its package; place `@nseng-ai/ns` at `public/ns/`; establish incubating `@nseng-ai/pi-runtime` at `incubating/hosts/pi/runtime/pi-runtime/`; and update all workspace consumers, package preparation, scripts, and fixtures without compatibility aliases. Scope per the approved design: stack orders 0–1, 10a–10d, 11–16, and 26–29. The Pi extraction is **not** part of this row — ns extensions keep their `src/pi/` subpackages when it lands.
- [ ] **Deferred by the approved implementation-stack design; not started.** Extract Pi integration from every ns extension that currently contains it. Remove Pi code, imports, subpackages, and entrypoints from ns extensions; create one `pi-ns-<domain>` package per retained integration under the correct `hosts/pi/extensions/` disposition; consume only curated ns extension package APIs; classify Pi-native and project-only Pi code under the approved Pi ontology. This is stack orders 3–9 and 17–25, deferred because every open design item (Flow and Handoffs curated API shapes, the `pi-runtime` export map, the parity `sourcePackage` identity model, the typed topology model, the `extension-kit/pi-types` question) blocks one of them. A landed reorganization is **not** a completed Pi separation: until this row is done, ns extensions still contain Pi subpackages, so the Completion Criterion "No ns extension contains Pi imports, Pi registration, a Pi host-surface subpackage, or Pi extension entrypoints" remains unmet. The deferral is disposition-legal — an incubating extension depending on incubating `@nseng-ai/pi-runtime` satisfies closure — so nothing lands in a rule-violating state.
- [ ] Land the package-tree contract and final enforcement with the cutover. Make `ts/packages/README.md` authoritative, add only warranted host-level READMEs (settled: none beyond that contract and the `@internal/pi-tools` inventory), enforce disposition dependency closure, scope/disposition consistency, global leaf-name identity, and duplicate-leaf rejection, then remove legacy path handling and stale terminology. Split by the deferral: the **topology** guard (stack order 27) lands with the reorganization, but the ns-extension/Pi structural rules — no `pi` subpackage, `./pi*` export, Pi peer, or Pi registration in an ns extension, and the ADR 0045 §5 `pi-ns-*` rule — are deferred with the Pi-extraction row above, as is retiring the pi-subpackage guard exemptions (order 25). This row is not complete until those structural rules are enforced.
- [ ] Complete migration reconciliation and evidence. Update CONTEXT files, package READMEs, AGENTS guidance, active Objective prose, and other live references to the final ontology; verify relevant repository checks, package builds/packs, and checkout-free smokes; record the coordinated landing in this Objective and synthesize the result into `professional-repo-curation`. No registry publication is authorized.

## Parked

- Actual npm publication or release orchestration after the repository organization and package identities are ready.
- Adding integrations for hosts other than Pi; the ontology permits each future host to define an appropriate nested structure when real code exists.
- Promoting an internal package to incubating or public without a separately approved release-intent decision.
- Compatibility aliases for old package identities or paths unless an external compatibility requirement is discovered and explicitly approved.
