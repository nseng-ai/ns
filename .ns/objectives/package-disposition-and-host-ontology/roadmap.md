# Roadmap

## Work

- [x] Draft and approve the superseding package-ontology ADR and complete destination inventory. ADR 0045 and `references/package-destination-map.md` classify all 26 baseline workspace manifests, define the 34-package target, settle Pi extraction and owner nesting, enumerate identity/dependency/release consequences, and provide the approved replacement direction for `professional-repo-curation`. The user explicitly approved the full map and ADR on 2026-07-25. The parent record was reconciled through its own tracking workflow on 2026-07-26 (`professional-repo-curation` update `2026-07-26T000342Z-adopt-disposition-ontology-direction.md`): its objective, roadmap, and always-loaded orientation now carry the disposition-ontology direction instead of the flat incubator. No package move or publication was authorized.
- [x] Design the atomic implementation stack from the approved map. `references/implementation-stack.md` is the durable design: a two-phase shape of pre-boundary trunk PRs that touch no package path or npm identity, then one Graphite boundary stack on a base cut from `master`, reviewed as ~19 PRs and landed by retargeting the top branch's PR base and squash-merging so exactly one commit reaches trunk. It settles four decisions (fold Harness Artifacts into `@nseng-ai/ns` with a new `./api`; allow Pi command-*name* constants on `@nseng-ai/branch-context/api`; write a superseding ADR for the `pi-ns-pr-feedback` CLI seam; bless adapter-to-adapter dependencies on declared curated subpaths), records eight corrections that made the raw synthesis executable, prohibits `ns flow land` for this boundary, and carries the per-branch mechanical tactics and verification list. It also fixes the execution scope: the complete filesystem reorganization runs now and the `pi-ns-*` Pi extraction is deferred. All three Open Questions are closed by it, and the approved 34-package target becomes 33 through the Harness Artifacts fold. The design row itself authorized no package move, identity change, publication, or registry write.
- [x] Execute the coordinated package cutover. **Landed on `master`** as the atomic boundary commit `1d3c65b47` ("Consolidate TypeScript Packages Under Release-Disposition Roots"), an ancestor of trunk; the tracked tree under `ts/packages/` now contains only the three disposition roots plus `README.md`, and `git ls-files` reports zero tracked files under any legacy role directory (remaining `capabilities/`, `incubator/`, etc. directories in local worktrees are ignored build/`node_modules` residue, not repository state). 25 packages sit under `public/` (7), `incubating/` (12), and `internal/` (6); no legacy role directory remains. Every leaf equals its unscoped identity with no duplicates; `@nseng-ai/ns` is at `public/ns/`; `@nseng-ai/pi-runtime` is at `incubating/hosts/pi/runtime/pi-runtime/`; `@internal/pi-editor-mods` and `@internal/review-reinvention-scanner` use the internal scope and `private: true`. Workspace discovery is narrowed to the three roots, `ns-init` and Harness Artifacts fold into `@nseng-ai/ns` behind a new `./api`, and Branch Context proved not to be a runtime edge. No compatibility aliases. The work is organized as four Graphite stack points: design, cutover preparation, product-package consolidation, and one atomic cutover boundary. Whole-tree content was preserved and measured (4984 tracked files before and after, identical blob multiset apart from changed path literals); The Pi extraction was **not** part of this row: at landing, ns extensions kept their `src/pi/` subpackages. Update `2026-07-26T041623Z-package-cutover-landed.md` recorded the pre-landing implemented state; update `2026-07-28T225725Z-cutover-and-objective-adapter-landed-on-trunk.md` records that the boundary is on trunk.
- [~] **Broad Pi separation remains incomplete; Objectives is landed, Handoffs is implemented end to end in its current PR, and Branch Context is the next separate end-to-end PR.** `@nseng-ai/pi-ns-objectives` is on `master` (commit `44612ff33`) under `ts/packages/incubating/hosts/pi/extensions/`, owns the preserved `/ns:objective:*` Pi registration/presentation and parity metadata, and consumes only the curated `@nseng-ai/objectives/api`; `@nseng-ai/objectives` has dropped its `src/pi/` files, `./pi*` exports/subpackage declaration, and Pi-runtime peer. The Handoffs extraction is implemented as one coherent current-PR boundary: its stable create/pickup names and Skill-Backed Command Registrations are owned by `@nseng-ai/handoffs/api`; the complete Pi implementation, tests, discovery, parity identity, and launch-composition subpath move to `@nseng-ai/pi-ns-handoffs`; Handoffs drops its `src/pi/`, `./pi*` exports, and Pi Runtime coupling; and Herdr consumes the declared adapter subpath pending its own extraction. Branch Context deliberately retains its current `/pi` surface until a separate PR can atomically add any required API contract, create `@nseng-ai/pi-ns-branch-context`, move the complete Pi implementation/tests/discovery/parity, migrate consumers, and remove the old surface. **Do not land API-only precursor PRs for remaining adapters:** each curated API promotion must be reviewed and landed in the same PR that removes that extension's Pi surface, because independently landable transitional ownership states are confusing. All other work remains open: `flow`, `branch-context`, and `herdr` still carry Pi subpackages/surfaces; the remaining approved `pi-ns-*` adapters and internal Pi-native extractions (`harness-session`, `model-shortcuts`, `worktree-status`) remain uncreated; and no final structural guard has landed. Complete the remaining extractions under the approved ontology, then enable the deferred guards. Neither this row nor the Objective is complete.
- [~] Land the package-tree contract and final enforcement with the cutover. **Contract and topology enforcement are landed on `master` with the cutover; the two Pi structural rules are not implemented.** `ts/packages/README.md` on trunk declares itself authoritative over any disagreeing document and states what each disposition *means*, with the identity invariants, the closure matrix as a table, and a Pi host-ownership section; no host-level README was warranted. `NS_TS_PACKAGE_DISPOSITION_TOPOLOGY` is live on trunk against the real tree and enforces disposition root, leaf-equals-unscoped-name, globally unique leaves, scope-by-disposition with `private: true` on `@internal/*`, and the closure matrix, with an unrecognized first segment a violation rather than a skip. It was mutation-tested, not merely observed passing: breaking leaf-equals-name and breaking the internal scope each produced a precise diagnostic naming the offending manifest. `NS_TS_TIER_DIRECTORY_PROJECTION` and its 17 cases are gone, and the release catalog is derived from `public/` instead of hand-maintained. Still open: the ns-extension/Pi structural rules — no `pi` subpackage, `./pi*` export, Pi peer, or Pi registration in an ns extension, and the ADR 0045 §5 `pi-ns-*` rule — are named as deliberately deferred in the guard module and land with the Pi-extraction row above, as does retiring the pi-subpackage guard exemptions (order 25). This row is not complete until those structural rules are enforced.
- [~] Complete migration reconciliation and evidence. **Reconciliation is landed with the cutover; the parent synthesis and a residue of stale references remain.** Live guidance in `docs/`, `skills/`, CONTEXT files, AGENTS guidance, and package READMEs now names the final paths and terminology; `CONTEXT.md` gained a Release disposition term retiring "incubator", "incubation zone", and "tier directory". Historical records — ADRs, prior Objective updates, dated research inventories, superseded wayfinding notes — are deliberately left as time-in-place records. `professional-repo-curation`'s always-loaded `orientation.md` was corrected in the same commit because it stated as standing rule that no package moves had landed; that is an accuracy repair, not the synthesis this row owes. Evidence: `just` green (default and style-guard lanes), integration and isolated lanes green, `pack:local` and `smoke:checkout-free` pass against the moved tree, and no registry publication. Still open, and now unblocked by the landing: synthesize the result into `professional-repo-curation` through that record's own tracking workflow, and clear the stale-reference follow-ups found during the cutover and listed in update `2026-07-26T041623Z-package-cutover-landed.md` — `sdk-reference.md:18`'s nonexistent `flow/src/shared/`, the `.ns/extensions/` references in `ts/TESTING.md:175,219` and `pr-feedback/README.md:36`, the `npm:@nseng-ai/objectives` install path documented in `objectives/README.md:20` and `ns/README.md:64` for a package that is not in the derived public set, `sdk/src/cli/index.ts:153`'s nonexistent `../cli.ts`, the `@nseng-ai/ns-init` specifier at `extension-registry.test.ts:568`, the extension-kit misattribution of `GitGateway` at `docs/conventions/consumer-gateways-and-command-shape.md:17,24`, the three wrong inventory paths in `docs/pi/README.md`, and the absence of any general clinkr guard against ambient colour environment. No registry publication is authorized.

## Parked

- Actual npm publication or release orchestration after the repository organization and package identities are ready.
- Adding integrations for hosts other than Pi; the ontology permits each future host to define an appropriate nested structure when real code exists.
- Promoting an internal package to incubating or public without a separately approved release-intent decision.
- Compatibility aliases for old package identities or paths unless an external compatibility requirement is discovered and explicitly approved.
