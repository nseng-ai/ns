# Roadmap

## Work

> **Reclassification note (re-verified at HEAD 141ac24d, post `@nseng-ai/*` rename):** remaining rows were rebaselined after the extension-architecture endgame (now closed) and the workspace rename (`@sdl/*` → `@ji/*` → `@ns/*` → `@nseng-ai/*`, `sdlcc` → `jicc` → `nscc`, `sdl-flow` → `@nseng-ai/flow`). No rows marked **Classification: neutral structural cleanup** remain open — all landed. Rows marked **capability-owned** should be routed to the owning capability/Objective or handled as package-local capability maintenance, not pulled down into neutral infra merely for dedup. Rows marked **obsolete / disposed** are retained as historical findings but are not implementation candidates here. Evidence in completed rows has been re-verified against current package homes; pre-rename Semantic Updates use retired `@sdl/*`/`@ns/*`/`sdlcc` names.

- [x] Shared `defineCli` helper (runtimeInfo derivation, version reading, IO/cwd/env defaulting, entry guard); migrate all current `cli.ts` files onto it. See `references/cli-wiring-layer.md`.
      Evidence (re-verified at HEAD): `defineCli` lives in `@nseng-ai/foundation/cli-runtime` (`ts/packages/infra/foundation/src/cli-entry.ts` no longer exists; the implementation is `ts/packages/infra/foundation/src/cli-runtime/index.ts`); all 10 first-party clinkr `cli.ts` entrypoints (address, aretro, branch-context `src/core/cli.ts`, ccc `src/ns/cli.ts`, plans, nscc, brmem, areg, packagechk, vibechk) consume it; prior validation passed. (The kernel-driven `@nseng-ai/ns` host at `ts/packages/hosts/ns-cli/src/cli.ts` uses `runCli` from `@nseng-ai/kernel/cli`, a different construction, and is out of the clinkr-`defineCli` set by design.)
- [x] Rejected: `clinkr` `execGroup(description?)` factory defaulting `name:"exec"` + `isHidden:true`; replace hand-rolled hidden-exec constructions. See `references/cli-wiring-layer.md`.
      Decision: the implementation was reverted because it added a thin wrapper around already-correct, obvious `ClinkrGroup` construction and did not delete meaningful complexity. Do not reintroduce this helper without new evidence that it prevents plausible drift or removes substantial mental load.
- [x] Unify Branch-Memory access: point `branch-context` at the in-process `@nseng-ai/brmem` `BrmemGateway`; delete the parsing half of its old `brmem-gateway.ts` and its former core `brmem-cli` dependency. See `references/branch-memory-access.md`.
      Evidence (re-verified at HEAD): `branch-context` (now `ts/packages/capabilities/branch-context/`) uses `RealGitBrmemGateway` / `BrmemGateway`; `src/brmem-gateway.ts` is absent; the old core `brmem-cli` module no longer exists anywhere (its successor is `@nseng-ai/capability-kit/kit/brmem-cli`); CLI scenario tests cover Branch Memory diagnostics.
- [x] Collapse the brmem-cli multi-candidate framework to one public `runBrmem`; fix the duplicated candidate-loop at worktree status; delete dead exports `graphqlErrorsFromJson`, `readOptionalBrmemBooleanField`. See `references/branch-memory-access.md` and `references/asdl-core.md`.
      Evidence (re-verified at HEAD): `runBrmem` is exported from `ts/packages/capability-kit/src/kit/brmem-cli.ts` (rehomed from core during the container conversion); the former public candidate-iteration names (`resolveBrmemCommandCandidates`, `runBrmemCandidate`, `runFirstAvailableBrmemCommand`) plus `readOptionalBrmemBooleanField` and `graphqlErrorsFromJson` grep to zero under `ts/packages`; the two-candidate fallback behavior lives privately inside `runBrmem`.
- [x] Compose the shared `GitGateway` inside `brmem/real-git-gateway.ts`; remove duplicated generic `currentBranch` and Git branch-ref validation primitives while keeping Branch Memory Snapshot object/ref plumbing local to brmem. See `references/cross-package-dedup.md`.
      Evidence (re-verified at HEAD): `RealGitBrmemGateway` (`ts/packages/infra/brmem/src/real-git-gateway.ts`) composes `GitGateway` from `@nseng-ai/capability-kit/git` (the gateway's current home), delegates generic branch-ref checks (`validateBranchRef`) to it, and retains brmem's Snapshot Ref encoding pre-check locally.
- [x] Disposed as obsolete: add `resolveBranchOrCurrent` to the shared core and replace per-package copies.
      Classification: obsolete / disposed. Re-probe at HEAD confirms no `resolveBranchOrCurrent` symbol exists under `ts/packages` and no clear repeated policy-free helper shape to standardize. Do not add this helper unless a future fresh count finds real duplicated neutral Git mechanics.
- [x] Export or otherwise share GitHub PR feedback leaf helpers (`ghAuthorSchema` / author normalization / numeric GitHub identity policy) from the shared feedback boundary; delete the `reviews` capability's divergent copies. See `references/cross-package-dedup.md`.
      Classification: neutral structural cleanup — landed. Evidence (re-verified at HEAD): the shared boundary now lives at `@nseng-ai/capability-kit/github/pr-feedback` (rehomed from core); `@nseng-ai/reviews` (`ts/packages/capabilities/reviews/src/gateways/github.ts`) imports `ghAuthorSchema`/`normalizeAuthor` from it and defines no local copies. Focused tests exist at `ts/packages/capability-kit/test/github/github-pr-feedback.test.ts`; original landing validation passed the full TS gate set.
- [x] Delete the vestigial core root `.` export; repoint live bare importers at explicit subpaths. See `references/asdl-core.md`.
      Classification: neutral structural cleanup — landed. Evidence (re-verified at HEAD): `ts/packages/infra/foundation/package.json` exports only curated subpaths (no `"."`); `src/index.ts` is absent; the former bare importer, now `ts/packages/hosts/pi/src/kit/sessions/harness-session.ts`, imports `truncatedSha256Digest` from `@nseng-ai/foundation/primitives`. Core has since been converted into a container package (subpath exports only).
- [x] Rebaseline and decompose `@nseng-ai/areg`'s real gateway/file-system god-file. See `references/areg.md`.
      Classification: neutral/tool-local structural cleanup — landed. Evidence (re-verified at HEAD): `ts/packages/tools/areg/src/real-gateways.ts` is a 6-line compatibility export surface; `src/gateways/` carries `project-gateway.ts`, `project-fs.ts`, `mutation-policy.ts`, and `skill-kind-classification.ts` (plus per-concern gateway modules). Completion evidence: `updates/20260629T205405Z-areg-project-gateway-filesystem-extraction.md` and `updates/20260630T114729Z-areg-policy-collapse-skill-spec-sharing.md`; canonical path classifier sharing remains explicitly deferred/non-blocking.
- [x] Resolved by Flow-owned decomposition: Flow/ccc land-stack god-file. See `references/ccc.md`.
      Classification: capability-owned (Flow) — the motivating finding no longer exists. Evidence (re-verified at HEAD): `ts/packages/capabilities/flow/src/land/stack/landing-operations.ts` is 308 lines; `performGraphiteMaintenance` lives in the dedicated `graphite-maintenance.ts`; the land-stack directory is split into ~20 focused modules (landing-plan, landing-plan-execution, landing-coordination, presentation, pre-merge-confirmation/submit, worktrees, backup-refs, …). `@nseng-ai/ccc` no longer carries `src/land-stack/landing-operations.ts`; `ts/packages/capabilities/ccc/src/ns/land.ts` re-exports from `@nseng-ai/flow/api`. Any further maintenance decomposition is Flow-owned and must preserve expected-SHA and merge→verify→cleanup ordering.
- [ ] Rebaseline Graphite topology ownership before deduping. See `references/ccc.md`.
      Classification: split / still design-sensitive, rebaselined. Evidence (re-verified at HEAD): the standalone `@sdl/graphite` package is gone; neutral Graphite mechanics (branch, metadata, stack, status, testing fakes) live in `@nseng-ai/capability-kit/graphite/*`, while Flow retains land-specific topology reads (`land/stack/graphite-topology.ts`, `stack-facts.ts`, `graphite-command-channel.ts`). Future work should extract only policy-free Graphite mechanics into `@nseng-ai/capability-kit/graphite` if a pickup proves real duplication; landing-path decisions, fork refusal wording, and land-command behavior stay Flow-owned.
- [x] `nscc` (formerly `sdlcc`): replace the hand-rolled validation tower in `stack-map-model-loader.ts` with Zod boundary schemas; delete parsed-but-unread fields if still unused. See `references/per-package-cleanups.md`.
      Classification: neutral package-local structural cleanup — landed. Evidence (re-verified at HEAD): `ts/packages/hosts/nscc/src/stack-map-model-loader.ts` parses the machine envelope, strict stack graph payload, and permissive cmux tree boundary with package-local Zod schemas; `json-fields.ts` is deleted. Cmux nested parse behavior remains permissive with focused malformed-sibling tests; malformed graph payloads keep the unavailable-stack diagnostic path. Original landing validation passed the full TS gate set.
- [ ] `@nseng-ai/retros` (the `aretro` CLI): split generic JSON accessors out of `ts/packages/capabilities/retros/src/sessions/pi-jsonl-source.ts`; unify the divergent `bashExecution` count paths if still present. See `references/per-package-cleanups.md`.
      Classification: capability-owned / Aretro-local cleanup. Evidence (re-verified at HEAD): the file exists at the capabilities home and still contains multiple `bashExecution` paths. Keep generic JSON mechanics local unless another consumer proves a neutral helper; route semantic evidence collection policy through Aretro ownership.
- [x] `@nseng-ai/kernel`: replace the reflective `MANIFEST_COMMAND_FIELDS` engine in extension discovery with a Zod schema; de-dup the `index.ts`/`index.js` dir-index block. See `references/per-package-cleanups.md`.
      Classification: neutral SDK/kernel structural cleanup — landed. Evidence (re-verified at HEAD): discovery now lives at `ts/packages/kernel/src/extensions/discovery.ts` (kernel was restructured); `MANIFEST_COMMAND_FIELDS` greps to zero under `ts/packages/kernel`; manifest command parsing uses `sdlExtensionManifestCommandSchema`/`sdlExtensionPackageManifestSchema` (Zod, from `src/sdk/`) with Zod-issue-path classification preserving diagnostic codes; directory-index discovery iterates `["index.ts", "index.js"]` in one helper preserving precedence. Focused tests at `ts/packages/kernel/test/unit/extension-discovery.test.ts`.
- [x] `@nseng-ai/packagechk`: collapse `ClaimPolicy`/`ClaimPlan` over-abstraction in `claim-command.ts` into linear claim functions sharing small helpers. See `references/per-package-cleanups.md`.
      Classification: neutral/tool-local structural cleanup — landed. Evidence (re-verified at HEAD): `ClaimPolicy` and `ClaimPlan` grep to zero under `ts/packages/tools/packagechk`; `runPypiClaimCommand`/`runNpmClaimCommand` are exported (`claim-pypi-command.ts`/`claim-npm-command.ts`) and wired directly from `cli.ts`; scenario tests plus TS gates passed at landing.
- [x] `@nseng-ai/vibechk`: refactor `workflow.ts:executeRun`; re-verify and delete any remaining dead empty-diff write, duplicate provenance/type debris, redundant casts, and `normalizeRunsFormatArgs`. See `references/per-package-cleanups.md`.
      Classification: neutral/tool-local structural cleanup — landed. Evidence (re-verified at HEAD): `normalizeRunsFormatArgs` was deliberately kept (in `src/cli.ts`) as tested compatibility behavior for the `runs --format` alias; `diff.patch` writes route through named `writeDiffArtifact`/`writeBestEffortDiffArtifact` helpers in `workflow.ts`; the unnecessary `as Record<string, string>` cast is gone from `models.ts`. Persisted bundle provenance/type fields remain compatibility surface.
- [ ] Flow submit tidy-ups (former core submit): prewrite/post-submit PR-description dedup, failure-transcript extraction, duplicate formatter cleanup, no-consumer re-export pruning, alias-file deletion. See `references/asdl-core.md`.
      Classification: capability-owned (Flow). Evidence (re-verified at HEAD): submit/PR-description orchestration lives under `ts/packages/capabilities/flow/src/submit/` (19 modules including `submit-pr-metadata-prewrite.ts`, `pr-description-orchestration.ts`, `pr-description-regenerate.ts`, `submit-failure-catalog.ts`, `submit-format.ts`). Handle as Flow maintenance unless a leaf helper is clearly neutral.
- [ ] ccc/flow small dedup: re-verify `firstNonEmptyLine`, canonical `isRecord`, gt restack/submit arg builders, and repeated `presentLandStackFailure` call sites against the current split between `@nseng-ai/ccc`, `@nseng-ai/flow`, `@nseng-ai/foundation`, and `@nseng-ai/capability-kit`. See `references/ccc.md`.
      Classification: capability-owned / split. Evidence (re-verified at HEAD): `firstNonEmptyLine` has a canonical home in `@nseng-ai/foundation` (`src/terminal/text-normalization.ts`) yet local variants persist in Flow (trunk-pull, autobranch, submit ×2), `@nseng-ai/capability-kit/src/git/index.ts`, and `@internal/pi-tools`; `isRecord` copies persist across kernel, pi, flow, and pi-tools; `presentLandStackFailure` repetition (3 Flow land files) is Flow presentation policy. Only extract policy-free helpers if pickup proves meaningful complexity deletion.

### Absorbed from `ts-cli-architecture-deepening` (subsumed)

These deepening candidates migrated from the closed `ts-cli-architecture-deepening` Objective. They use the `improve-codebase-architecture` vocabulary (module / interface / depth / seam / deletion test); the reasoning is carried across intact, not flattened.

- [ ] **Collapse slot-dispatch into one orchestration module** (`ccc`/cmux/slot dispatch) — re-verify current homes first.
      Classification: capability-owned / ccc-slot boundary. Evidence (re-verified at HEAD): ccc retains cmux slot-dispatch modules (`ts/packages/capabilities/ccc/src/cmux/`: `slot.ts`, `slot-checkout.ts`, `slot-open-branch.ts`, `dispatch-from-trunk.ts`, `sidebar.ts`). Slot is a completed capability; any remaining dispatch orchestration should be handled as Slot or ccc consumer cleanup, not neutral core work.
- [x] Disposed as obsolete: **Hide occupancy reconciliation behind the slot inventory** (`@nseng-ai/slots`) — stop leaking `SlotRecord.branch === null` pattern matching into multiple callers.
      Classification: obsolete / disposed. Evidence (re-verified at HEAD): `record.branch === null` pattern matching appears only inside `@nseng-ai/slots`'s own lifecycle operations (`src/lifecycle/gc.ts`, `operations/goto.ts`, `operations/gt/exec/*`), not in external callers; slot capability migration closed with inventory/lifecycle ownership recorded. Reopen only with fresh external-leakage evidence.
- [x] Disposed as obsolete: **Put a stack-navigator adapter over Graphite's discriminants** (`@nseng-ai/slots`/gt) — keep inside the `slot gt` boundary and use Graphite plumbing, never parsed display output.
      Classification: obsolete / absorbed by Slot. Evidence (re-verified at HEAD): slot's gt operations live at `ts/packages/capabilities/slots/src/lifecycle/operations/gt/`, and `GraphiteStackGateway` is provided by `@nseng-ai/capability-kit/graphite` with slot consuming it via `src/core/context.ts`. Future refinements belong to Slot, not this neutral Objective.
- [ ] **Pull objective-markdown rules into one validator** (`@nseng-ai/objectives`) — cross-reference Objective capability ownership instead of duplicating the validator here.
      Classification: capability-owned (Objective). Evidence (re-verified at HEAD): the Objective capability has closed, and current markdown checks live in `ts/packages/capabilities/objectives/src/core/operations/check-objective.ts` (plus `check-items.ts` and the `ns` command face `src/ns/commands/check.ts`). Further validator deepening belongs to Objective maintenance.
- [x] Disposed as obsolete / already satisfied: **Deepen Branch Memory behind an entry locator** (`@nseng-ai/brmem` / `@nseng-ai/handoffs` / `@nseng-ai/branch-context`) — next layer on the already-shipped gateway migration.
      Classification: obsolete as originally framed. Evidence (re-verified at HEAD): `@nseng-ai/brmem` exports `buildEntryLocator`, `mustEntryLocator`, and `parseEntryLocator` (`src/index.ts`); brmem operations (`put`, `delete`, `check`) produce/consume entry locators through brmem-owned ref-layout mechanics. Future compatibility gaps can be new neutral `@nseng-ai/brmem` slices, but this backlog row is no longer a clear implementation candidate.
- [ ] **Replace the shallow brmem adapter with a plan-attachment module** (`@nseng-ai/branch-context`) — a branch-context capability/domain seam; classify against ADR 0009 before placing it.
      Classification: capability-owned (Branch Context/Plans). Evidence (re-verified at HEAD): branch-context/plans child migration closed; attachment semantics live in `ts/packages/capabilities/branch-context/src/core/attach.ts` and `attached-plan.ts` with the `ns` command face at `src/ns/commands/attach.ts`. Do not move plan-attachment domain into `@nseng-ai/brmem`.

## Parked

- [x] Disposed as obsolete: migrate the two `legacyCommand`-based CLIs (`plans`, `branch-context`) off the deprecated `legacyCommand` path onto the rendered `command(...)` shape. See `references/cli-wiring-layer.md`.
      Classification: obsolete / disposed. Re-verified at HEAD: no `legacyCommand` usage under `ts/packages/capabilities/plans` or `ts/packages/capabilities/branch-context`.
