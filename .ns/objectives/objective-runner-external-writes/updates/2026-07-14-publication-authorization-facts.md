# Publication authorization facts established

## Summary

Objectives now owns strict version 1 schemas and pure policy for the parent-held publication authorization and cumulative review summary. The facts bind an autorun invocation, Objective slug, non-trunk local branch, already-existing PR identity and head ref, launch HEAD, expected remote head, and policy attestation. Rechecks classify branch, PR, head, worktree, ancestry, Runner commit, and trailer mismatches before any publisher can be called.

The summary contract carries ordered verified Runner steps, parent judgments, optional tracking commits, and bounded review-facing text. Objectives renders the complete managed section deterministically; no child-authored full PR body or policy Markdown parsing is accepted.

## Objective Impact

The second roadmap row is complete as a pure contract/core slice. Fake-driven unit tests cover invalid or cross-invocation authorization, target drift, dirty/unverified state, ancestry and Runner trailer mismatches, schema bounds, and deterministic rendering. There is still no production adapter, hidden bind/publish command, Flow mutation client, credential path, or external write.

The implementation package's focused checks and tests, formatting, lint, native TypeScript checks, style guard, and full `just` passed.

## Follow-Ups

Implement the Flow-owned two-phase publication Capability API next, preserving the host execution channel and normal non-force push semantics. Parent command composition remains deferred until that seam is proven.
