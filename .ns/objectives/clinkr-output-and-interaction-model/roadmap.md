# Roadmap

## Work

- [ ] Relocate `channel-ontology.md` from `clinkr-readme-driven-development/references/` into this Objective's `references/` and repoint the citation in that Objective's `implementation-contract-notes.md`. Both files are currently uncommitted on `clinkr-semantic-output-raw-adapters`; land the move with them.
- [ ] Grill and settle the model: two-tier membership rule, channel/representation/transport layers, the no-stream-vocabulary design test, and every term plus Avoid list. Record rejected alternatives and any structural amendments in the ontology doc and a Semantic Update.
- [ ] Run the Click prior-art session against `/Users/schrockn/code/pallets/click`: echo/secho and styling, pager, progressbar, prompt/confirm, and `CliRunner`, each read as "which channel is this, and what does Click's shape teach or falsify?" Admit other prior art only where a specific open question needs it. Capture findings as reference notes plus a Semantic Update.
- [ ] Resolve the three carried open questions as recorded decisions: streamed-durable-body placement, Elicitation vocabulary reconciliation target, and the `onOutput(stream, text)` retirement path.
- [ ] Decide the documentation-promotion strategy: which decisions (if any) become ADRs, which CONTEXT.md/README surfaces receive the vocabulary, and what implementation milestone triggers each sync given CONTEXT must trail ground truth. The selected surfaces must document the end-to-end output chain: Clinkr's invocation-scoped structured-text and raw-byte sinks with process-stream defaults for standalone execution; Foundation's sink-shape adaptation and streaming UTF-8 decoding of raw bytes for text callbacks; and Pi's capture, TUI presentation, and host-boundary terminal-control sanitization. State the ownership split directly: Clinkr owns rendering and stream selection, Foundation owns adaptation, and Pi owns presentation and renderer safety. Include the concrete failure mode in which a captured clear-screen control reaches the physical terminal, desynchronizes Pi's differential renderer from the real screen, and corrupts or misplaces later TUI updates.
- [ ] Bless the final model and hand it to `clinkr-readme-driven-development`: confirm the rename map (`ClinkrFinalPresentation` → `ClinkrResponse`, `presentFinal` → `onResponse`, private terminal adapter, Pi de-streaming) is the implementation's acceptance spec, and record the blessing as a Semantic Update. Require that Objective to select one coherent output- or interaction-heavy production workflow that uses the final Response and invocation-service model directly through a supported host; examples or a legacy conversion boundary alone are insufficient. Expect a Blocked Sentence while implementation proceeds there.
- [ ] Verify meaningful production vetting after implementation lands: inspect the selected workflow end to end for direct use of the blessed Response plus relevant Progress, Notice, or Elicitation contracts; confirm terminal or embedded-host adapter behavior as applicable; and record why the slice provides behavioral breadth and architectural depth. Do not require repository-wide migration.
- [ ] Execute the decided promotion once implementation and production vetting land: write any decided ADRs; sync the chosen Clinkr, Foundation, and Pi CONTEXT.md/README surfaces in the same change window as (never ahead of) ground truth; document the output-ownership chain and clear-screen/desynchronized-renderer failure mode; and reduce the ontology reference to a provenance pointer if fully promoted. Close this Objective before `clinkr-readme-driven-development` promotes the canonical package README.
      Evidence: promoted docs match shipped code; the ownership boundaries are unambiguous; Foundation's raw-byte adapter is documented as streaming UTF-8 rather than per-chunk decoding; Pi's sanitization is documented as an embedding-boundary renderer-safety policy rather than a Clinkr concern; `just` and relevant repo checks pass on the promoting changes.

## Parked

- Fixed comparative survey of additional CLI frameworks (oclif, clap, cliffy); prior art beyond Click enters only through a specific open question.
- Third-host generalization of the channel contracts; revisit when a concrete host beyond terminal and Pi exists. MCP is the anticipated candidate: the 2026-07-28 stateless spec plus MRTR makes the channel mapping clean, but it also imposes replay-with-answers elicitation semantics. See `references/mcp-as-third-host.md` for the mapping, the two derived design tests (MCP-derivability, replay-satisfiable elicitation), and the un-parking trigger (a concrete consumer wanting ns capabilities over MCP).
- Any new invocation service beyond Progress, Notice, and Elicitation; the streamed-durable-body ruling may propose one, which returns through the open-question decision, not this list.
