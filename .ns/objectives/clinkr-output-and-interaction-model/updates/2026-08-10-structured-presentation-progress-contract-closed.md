# Structured Presentation and Progress Contract Closed

## Summary

The corrected invocation contract is implemented and qualified. Clinkr owns structured `renderResult`/`echo` presentation; Foundation maps it to standalone terminal streams; SDK exposes exact primary `resultOutput` plus semantic command I/O and typed progress while deleting `NsExtensionApi.stdout` and `.stderr`; and Pi captures presentation per invocation with complete settled non-TTY/non-ANSI capabilities and exclusive progress ownership.

A representative integration suite composes real Clinkr, SDK, Flow, and Pi bridge behavior. It and the adjacent focused suites prove finite JSON acquisition policy, structured outcomes, exact durable result chunks, semantic interaction and headless failure, ambient-process isolation, sanitization, non-duplicated progress, completed-output compatibility, and lifecycle behavior. Full TypeScript and default validation pass; the closed Objective check and all-record edge sweep pass. The connected open Objective's individual check continues to report two pre-existing immutable updates that lack `## Follow-Ups`.

## Objective Impact

The four reopened roadmap rows are complete with evidence, durable Clinkr/Foundation/SDK/Pi/Flow documentation is synchronized to landed behavior, and the Objective closes again. This update supersedes the lifecycle judgment in `2026-08-10-objective-reopened.md`; the historical premature closure and reopening updates remain immutable.

The connected `clinkr-readme-driven-development` invocation-contract dependency is now genuinely satisfied. Its independent production-vetting, package qualification, and canonical README promotion work remains open.

## Follow-Ups

- Continue the README-driven Clinkr Objective against this closed invocation contract.
- Keep richer response/event, PTY, terminal virtualization, and additional-host directions deferred until their recorded evidence thresholds are met.
