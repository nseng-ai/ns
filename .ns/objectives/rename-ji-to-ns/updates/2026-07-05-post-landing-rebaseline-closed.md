# Post-Landing Rebaseline Completed

## Summary

The residual active-prose `ji` trail was swept after the cutover landed.

Evidence collected during this update:

- Active repo context and map prose now name the product, extension stack, package tiers, and command surfaces as `ns` / `@ns/*`.
- Active package `README.md` / `CONTEXT.md` files and first-party skill guidance that still said `ji` command face, `ji` extension, `@ji/*`, `.ji/`, `JI_*`, or `jicc` were rebaselined to `ns` equivalents.
- Open Objective records whose current guidance still described live `ji` surfaces were rebaselined, while historical rename-chain prose and immutable `updates/` records were left as provenance.
- The `checkout-free-sdl-distribution` and `ship-objectives-to-customers` records now point at `ns` / `@nseng-ai/ns` for the publish and customer install surfaces.

## Objective Impact

The final post-landing rebaseline row is complete. The Objective's substantive scope is now delivered: invocation, repo state, Pi namespace, package scope, config, host/package identifiers, owner-machine migration, and active prose have all cut over to `ns` with no compatibility surface.

Remaining `ji` hits are intentional historical or rename-context references: this Objective's own narrative, the superseded `docs/ji-naming-brief.md`, immutable updates and closed records, explicit rename-chain context, and historical/parked docs such as the removed TypeScript plans retrospective.

## Follow-Ups

- Close this Objective with the caveat that historical records remain verbatim and future work must not introduce new live `ji` surfaces.
- Follow-on Objectives such as `checkout-free-sdl-distribution`, `ship-objectives-to-customers`, and `skill-management-subsystem` should continue from the `ns` / `@nseng-ai/ns` naming baseline.
