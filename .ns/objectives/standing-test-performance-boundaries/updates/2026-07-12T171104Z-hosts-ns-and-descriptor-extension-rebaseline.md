# Hosts/ns Relocation and Descriptor Extension Rebaseline

## Summary

Trunk-style refresh of the standing Objective against repository ground truth at HEAD `c1cb8d5d3`. Three stale anchors were corrected; the durable contract is otherwise verified and unchanged.

- **Host package relocated.** `packages/hosts/ns-cli` became `packages/hosts/ns` (commit `52359d876`, "Relocate the ns CLI host package to `ts/packages/hosts/ns`"). The 2026-07-07 skills-path slice's files now live at `ts/packages/hosts/ns/test/integration/skills-path.test.ts` (six-case matrix verified present) and `ts/packages/hosts/ns/test/ns-cli.test.ts` (the `smokes skills path host wiring for an alias user-scope case` smoke verified present). The package name is still `@nseng-ai/ns`. The roadmap's rebaseline-evidence paths were corrected; lane placement survived this relocation because `integration/` stayed directly under the package `test/` root.
- **Checked-in `.ns/extensions` root removed.** Commit `8ce26550e` ("Migrate ns extension loading fully to descriptor-based exports") deleted the legacy `.ns/extensions` shim packages; extensions are now discovered from `ns.toml` declarations and package `exports["./ns-extension"]` descriptors, loaded through the kernel registry (`ts/packages/kernel/src/extensions/registry.ts`). `ls .ns/extensions` fails at HEAD. The Scope anti-pattern bullet and the roadmap's historical-path mapping no longer anchor on `.ns/extensions`. The flow model anchors survive: `nsExtension(pi, { runCli })` at `ts/packages/capabilities/flow/src/pi/ns-extension.ts`, default fake-seam test `ts/packages/capabilities/flow/test/pi/ns-extension.test.ts`, and the retained integration smoke `ts/packages/kernel/test/integration/flow-extension-registry.test.ts`, which still proves the real loader imports every checked-in flow command entry (12 entries at HEAD).
- **Shared lane globs gained a third root.** `ts/vitest.shared.ts` now generates lane globs via `testGlobsFor`: `packages/*/test/<lane>/**/*.test.ts`, `packages/*/*/test/<lane>/**/*.test.ts`, and `../.ns/reviews/*/tools/*/test/<lane>/**/*.test.ts`. The Objective's quoted two-glob set in the restructure-regression risk and the Scope lane bullet were updated to match.

Re-verified still true at HEAD: the four restored integration files from the 2026-07-04 lane restoration remain in place and the structural sweep `find ts/packages \( -path '*/test/*/integration/*.test.ts' -o -path '*/test/*/*/integration/*.test.ts' \)` returns empty; `test/isolated/` lane exists (five isolated files at HEAD), `just ts-test-isolated` and `just ts-test-integration` recipes exist, `ts/package.json` has `test:integration` and `test:isolated`, and the CI isolated job is separate; the `NS_TS_BAN_SHARED_TEST_*` guard identifiers live in `ts/packages/internal/typescript-style-guard/src/config.ts`; `ts/TESTING.md` documents the default/integration/isolated lanes, the real-Git temp-repo standard, the remediation hierarchy, and `@nseng-ai/foundation/time/testing` manual helpers; `@nseng-ai/foundation` exports `./clock`, `./timers`, and `./time/testing`; `.ns/objectives/ts-fast-test-boundaries/closed.md` exists. A scoped search found no default-lane test using `node:child_process`/`spawnSync` outside `test/integration/` and `test/isolated/`, so `orientation.md`'s temporary "what you see now" line was shrunk from "real-backend setup scattered through default tests" to the current failure mode: leaks reappear after restructures.

## Objective Impact

`objective.md`: the anti-pattern Scope bullet no longer names the removed `.ns/extensions` root; the integration-lane Scope bullet and the restructure-regression risk now cite the `testGlobsFor`-generated three-glob set. `roadmap.md`: the lane-restoration row records the 2026-07-12 re-verification; the rebaseline row's latest-evidence note maps `packages/hosts/ns-cli` to `packages/hosts/ns` and replaces the `.ns/extensions` current-equivalent mapping with the descriptor-based loader anchor. `orientation.md`: durable Direction/Getting-to lines unchanged; the temporary line was shrunk per the re-derivation rule. Thesis, Non-Goals, Completion Criteria, Definition of Progress, Runner Policy, and both Open Questions are verified and unchanged. The Objective remains open and standing — no goal-met finish line; no closure.

Provenance: objective-refresh basis target=c1cb8d5d3 from=trunk-HEAD

## Follow-Ups

- Next rebaseline should choose a fresh boundary-leak candidate; the hosts/ns relocation and extension-loader migration did not regress lane placement.
- The structural-guard open question remains undecided; the third glob root (`.ns/reviews/*/tools/*`) widens the surface such a guard would need to cover.
