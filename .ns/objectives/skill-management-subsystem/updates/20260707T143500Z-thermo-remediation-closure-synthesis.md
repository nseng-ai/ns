# harness-artifacts thermo-remediation child synthesized back into umbrella

## Summary

Synthesized the closed `harness-artifacts-thermo-remediation` Subobjective back into this umbrella. The child was edge-tracked but had no roadmap row or synthesis here — a fire-and-forget lapse this update closes.

Child outcome synthesized:

- All 5 HIGH and 10 MEDIUM findings from the 2026-07-06 thermo-nuclear review of the delivered harness-artifacts stack were remediated by a validated local Graphite stack (from `update-objective-pr-grouping-policy` through `harness-thermo/low-tail-cleanup-step-d6jQOU`), grouped into five review-shaped commits plus a small LOW tail.
- Key quality outcomes for the shared subsystem: kernel-computed `NsExtensionApi.homeDir` replaces the `?? ""` sentinels; conflict is a first-class apply outcome (error variant deleted); reconcile collisions skip-and-report with nonzero exit instead of hard-failing all of `ns update`; artifact I/O is bytes-based (binary-safe); a deep `provisionFirstPartySkill()` collapsed `skills-install.ts`/`RealSkillMaterializer` to thin adapters; the skills commands are plain preinstalled-catalog entries; AREG's dead gateway seams (GitHub, prompt, mutation-policy) and manifest-layering residue were deleted/cleaned.
- The LOW sweep closed partially implemented (`sortStrings` altitude fixed); remaining LOW candidates are explicitly parked in the child's `roadmap.md`, not silently widened.
- Each runner step reported `just` green before its commit; one staged-index mistake (PR Group 4) was recovered by a runner recovery step. No push/submit/PR mutation occurred — the local stack awaits separate human authorization to submit.

## Objective Impact

- A `[x]` roadmap row now records the child under `## Work`, keeping the umbrella's edge/row/synthesis tracking discipline intact.
- Cross-child lesson: the thermo-nuclear review-then-remediate pattern (dedicated remediation Subobjective with per-row execution policy and PR grouping) kept quality debt out of the umbrella's parked-breadth pipeline; parked breadth can now graduate on a remediated base.
- All three Subobjectives (`ns-skills-steelthread`, `npm-bundled-artifact-provisioning`, `harness-artifacts-thermo-remediation`) are now closed and synthesized. The umbrella's only remaining open Work row is the parked-breadth disposition decision.

## Follow-Ups

- Decide dispositions for the remaining parked rows (reconciliation/vocabulary sweep, marketplace/remote acquisition, update/uninstall surfaces, drift nudge, trust gating, filtering, additional artifact kinds) — follow-on slice, new Subobjective, or deliberate retirement each.
- The child's local Graphite stack still needs human-authorized submit/push; that is outside this umbrella's tracking.
