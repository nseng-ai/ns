# Harness-artifact vocabulary subobjective closed

## Summary

The `harness-artifact-vocabulary-reconciliation` Subobjective closed as completed on 2026-07-07. It delivered the bounded skill-workflow/docs/vocabulary reconciliation sweep: AREG's overlay sense is **harness overlays**, the two-channel skill-management story is documented, live residual `skillx` references were removed, and root/map context now carry the binding harness-artifact vocabulary and Avoid entries.

The Subobjective's only parked row moved here rather than being retired: push areg's remaining local logic (invocation-kind apply planning, `check` drift detection, skill find) down into `@nseng-ai/harness-artifacts` when a second consumer needs that logic at runtime, with the no-`areg-core` and areg-stays-in-`tools/` constraints preserved.

## Objective Impact

- Marks the umbrella Work row for `harness-artifact-vocabulary-reconciliation` `[x]` and synthesizes its closure evidence here.
- Adds the moved areg push-down deferred-breadth row to this umbrella's `## Parked` section so the trigger remains visible after the Subobjective closes.
- Keeps the umbrella edge to the closed Subobjective intact as historical relationship context.

## Follow-Ups

- Do not act on the moved areg push-down row until its trigger fires: a second runtime consumer needs that logic.
