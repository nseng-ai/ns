# Graduated Subobjective records created

## Summary

Created the two Subobjective records graduated by the 2026-07-07 disposition session, with mirrored edges and `[~]` tracking rows in this umbrella:

- **`harness-artifact-vocabulary-reconciliation`** — the skill-workflow/docs/vocabulary reconciliation sweep plus the bare-"artifact" collision cleanup (AREG "managed artifacts" → kind-overlays rename, `skill-conventions.md` / `skill-management`-skill two-channel positioning, residual `skillx` sweep, CONTEXT.md alignment). Doc/rename-heavy, execution-friendly Runner Policy; no provisioning behavior changes.
- **`remote-artifact-module-acquisition`** — first-party fetch path for artifact-bearing npm modules via an `ns.toml` declaration list (working name `artifact-packages`), adopting pi's debugged spec grammar and pinning semantics as the design starting point; owns version-resolution-for-fetched-modules. Design-heavy front: spec grammar, storage location, fetch mechanics, and `ns update` composition are steer-first open questions. Its Assumptions and Risks restate the umbrella's retired-trust-gating risk acceptance for re-judgment with real fetch semantics on the table, as the disposition record required.

## Objective Impact

- The "create the two graduated Subobjective records" Work row is `[x]`; both children have `[~]` rows and mirrored frontmatter edges.
- The two `[graduating]` parked rows now read `[x]` and name their child slugs.
- Umbrella closure now depends on: the two new children closing (or being explicitly parked with synthesis), the uninstall/stale-detection/rename-cleanup follow-on row, and final closure synthesis.

## Follow-Ups

- Keep both `[~]` rows current and synthesize each child's closure evidence here (fire-and-forget defense).
- The reconciliation child should settle the overlay replacement term before its mechanical rename pass; the acquisition child must resolve its recorded design questions before implementation slices run.
