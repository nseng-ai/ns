# Split the Thread into Subobjective ns-skills-steelthread; Promoted This Record to an Umbrella

## Summary

Following this morning's steelthread reshape (`updates/20260706T120909Z-reshaped-as-steelthread-objective.md`), the user directed the pattern's second shaping: the thread split into a Subobjective and this record promoted to an Umbrella Objective. The deciding factor is the parked breadth — extension-carried provisioning (+ the `ns update` hook), AREG re-platforming, the vocabulary reconciliation sweep, update/uninstall surfaces, and the remaining artifact kinds — which is substantial enough to need its own coordination, exactly the case where the patterns reference prefers the child split.

Changes:

- Created `.ns/objectives/ns-skills-steelthread/` owning exactly the thread: the two open thread rows (design the artifact model / harness path table / provision plan / install manifest; implement and validate `ns skills list/path/install`), the thread-validation Closure Gate, and the steelthread failure-mode risks. Its `## Parked` is deliberately empty — deferred breadth stays here.
- Rewrote this record as the Umbrella: thesis holds the reusable-subsystem ambition; scope is coordination — durable decisions (vocabulary, reconcile orientation, the `@nseng-ai/harness-artifacts` boundary), child tracking via edges and `[~]` rows, parked-breadth graduation, and synthesis. Completion criteria are now children-closed-or-parked plus synthesis, per the Umbrella pattern; the fire-and-forget umbrella is a named risk.
- Roadmap: the two thread rows were replaced by one `[~]` Subobjective row for `ns-skills-steelthread` plus a disposition row for parked breadth after the thread validates; the completed vocabulary row and all parked rows stay here.
- Edges: added the mirrored `skill-management-subsystem` ↔ `ns-skills-steelthread` edge, and moved the `ship-objectives-to-customers` edge to the child (counterpart frontmatter updated), since what that Objective consumes is exactly the thread's `ns skills` surface.

Historical `updates/` in this record remain the immutable decision provenance for both records; the child's scope prose references them by filename.

## Objective Impact

- This record no longer closes on thread validation; it closes when Subobjectives are closed or explicitly parked and synthesized, and each parked row has an explicit disposition.
- Thread execution tracking moves entirely to `ns-skills-steelthread`; duplicating it here is now a named non-goal.
- This record's direct edge count is 1 (the child); customer-shipping consumption is now expressed through the child's edge.

## Follow-Ups

- Keep the `[~]` child row current as the thread progresses (defends against the fire-and-forget umbrella failure mode).
- On child closure, synthesize its evidence here and run the parked-breadth disposition row.
