# Shaped as an Autoobjective

## Summary

The user directed that this record take the Autoobjective pattern. It now carries durable execution policy per the `objective` skill's execution-policy reference:

- `## Definition of Progress`: keepable progress advances an open row as one coherent slice — decision dossiers for grilling rows, the throwaway prototype, documentation, or post-crystallization implementation slices — while preserving the settled direction (constrained sessions, `runTurn()` primitive, composition over subclasses, fail-explicitly) and the PRESERVE rows of the consumer-inventory migration-invariants table. Not keepable: non-goal crossings (optional-bag interfaces, persistent sessions, sandbox coupling, MCP/tool registries), premature changes to live consumer behavior or exports, prototype code presented as production capability, or broken validation.
- `## Runner Policy`: the current phase is design-heavy, so runner steps prepare decisions and evidence rather than settle them. Direct execution covers research/probe work, dossiers, the throwaway prototype, and documentation; post-crystallization, implementation rows inside whatever packages the placement decision names. Steer/ask triggers on settling any grilling or Fog decision, changing live consumer behavior, or widening the exec-channel contract. Local per-slice commits on the working branch, clean tree per step, `just` plus touched-package tests; no push/PR/publish, no `draftWithFastText` deletion or exports edits before the migration decision, no harness invocations beyond read-only local probes.
- Row-level `Policy:`/`Evidence:` prose on all five open roadmap rows: both grilling rows and the placement row are dossier-runnable but human-settled; the prototype row is directly executable once unblocked with stated evidence; the crystallization row is parent tracking work through `objective-update`, not a runner step.

The autonomy-designed minimum is satisfied: durable goal (thesis), Definition of Progress, load-bearing assumptions and risks (already present, research-annotated), and runner boundaries with escalation guidance.

## Objective Impact

- Future `objective-next` sessions may proactively offer confirmed execution, and the record is a valid `objective-autorun` target with parent checkpoints between committed slices.
- No scope, completion-criteria, thesis, or edge changes. In the current design phase an autorun will mostly produce dossiers and (once the grilling rows settle) the prototype; the main autonomous surface arrives when the crystallization row replaces the Frontier with implementation slices.

## Follow-Ups

- First runner slice available now: the decision dossier for the domain-model grilling row, built from the two research artifacts.
