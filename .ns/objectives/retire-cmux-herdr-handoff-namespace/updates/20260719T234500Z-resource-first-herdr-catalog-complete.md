# Resource-First Herdr Catalog Complete

## Summary

The exact eleven-command resource-first Herdr Pi catalog is implemented. Ten base commands register under `space` and `tab`, while `tab:handoff` remains conditional on the curated Handoffs integration. The interim generic `handoff`/`objective` namespaces and `tab:plan-dispatch` alias are absent from live registration and exports.

Explicit `tab:new` and `tab:goal` workflows now validate Herdr-injected caller identity before model or destination work. Refreshed-trunk Saved Plan dispatch composes shared Graphite trunk preparation with Branch Context's canonical collision and race-revalidation policy, creates at the exact refreshed SHA under the explicit trunk parent, and provides a non-mutating dry-run preview.

## Objective Impact

All substantive roadmap rows are complete. Live contexts and Pi/Herdr documentation describe current Herdr topology, dedicated deleted-cmux operating guides are removed, and historical records remain unchanged. The stale docs-site extension catalog remains an explicitly gated follow-up outside this slice.

Validation passed focused Herdr, Branch Context, Capability Kit, Foundation, and areg suites; TypeScript format, lint, typecheck, style guard, integration and isolated lanes; Markdown formatting; `git diff --check`; and the repository `just` entrypoint.

Objective closure is not recorded because the immutable legacy update `20260719T181812Z-reference-based-herdr-handoff-launch.md` lacks three headings now required by the per-record Objective checker. Rewriting that historical update would violate update immutability.

## Follow-Ups

- Provide an authorized Objective-check compatibility mechanism for immutable legacy Semantic Updates before closing this Objective.
- Reconcile `docs-site/lib/extensions-catalog.ts` only when the gated docs-site launch slice authorizes edits.
