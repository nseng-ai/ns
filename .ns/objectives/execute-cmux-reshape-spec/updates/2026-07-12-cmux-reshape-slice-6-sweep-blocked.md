# Cmux Reshape Slice 6 Sweep Blocked

## Summary

Slice 6 completed its six enumerated glossary, agent-guidance, kit-comment, and
wayfinding edits, but the required final word-boundary sweep found 89
`ccc`/`CCC` lines across 35 files after excluding the currently defined
immutable-history directories. Many stale live claims and paths occur outside
Slice 6's ratified file set, so the runner stopped rather than widening scope.

The inventory also contains deliberate non-stale occurrences: newly required
Avoid terms, the legacy-prefix guard, synthetic text-wrapping fixtures,
migration guard tests, and historical research references. These make the
current gate wording broader than a simple zero-hit check.

## Objective Impact

Slice 6 remains incomplete and its six in-scope edits are left uncommitted on
`cmux-reshape/glossary-and-docs`. Root `just` passed, but the final stale-term
completion evidence cannot be claimed. The Objective is blocked pending human
re-ratification of either a broader cleanup set, more precise deliberate-hit
exclusions, or both.

## Follow-Ups

Classify the 35-file inventory into stale live claims that must be migrated and
deliberate historical/guard/fixture hits that should be explicitly accounted
or excluded. Re-ratify that classification before recovering Slice 6; do not
discard the six completed in-scope edits.
