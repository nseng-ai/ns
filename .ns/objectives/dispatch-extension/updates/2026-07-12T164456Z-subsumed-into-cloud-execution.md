# Subsumed into cloud-execution

## Summary

Closed by subsumption into the new consolidated cloud workstream
`cloud-execution` (user decision, 2026-07-12) so the cloud effort has one
roadmap instead of this record plus the `docs/wayfinding/ns-cloud-capabilities/`
map (also retired). Active tracking for dispatch — the capability package,
`ns dispatch plan|prompt`, the execution-target seam, credentials, and the
cloud steel thread — now lives in `cloud-execution`. Two dispositions made at
consolidation: the cloud infrastructure decision this record owned resolved
to Vercel Sandbox + AI SDK `HarnessAgent` (pi adapter first, Claude Code
second, Eve as potential seam consumer rather than chassis), and the
`--target cmux` local leg was parked rather than sequenced.

## Objective Impact

This record is closed; its roadmap rows are superseded by
`cloud-execution/roadmap.md`. Historical updates here remain immutable
provenance.

## Follow-Ups

- None here; follow-ups live in `cloud-execution`.
