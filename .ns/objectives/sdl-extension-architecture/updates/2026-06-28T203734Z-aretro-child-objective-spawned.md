# Aretro child Objective spawned

## Summary

Created the child Objective `aretro-capability-extension` for the parent architecture Objective's Phase 2 per-capability migration row.

The child frames Aretro as the deterministic branch-retrospective evidence capability. Its first work is to inventory the current `@sdl/aretro` package, standalone `aretro exec ...` command usage, docs, tests, and `branch-retro` integration; then decide the supported command-face strategy and whether a curated `@sdl/aretro/api` Capability API is needed.

## Objective Impact

This advances parent Phase 2 row 4 by selecting and spawning Aretro as a remaining capability migration/disposition child. Aretro is not yet complete; it now has its own durable Objective record for command-face/API disposition, gateway-injected core alignment, docs/context refresh, and final parent closeout.

The parent roadmap now records Aretro as spawned so future `objective-next` runs should not treat it as unselected hidden work.

## Follow-Ups

- Run `objective-next` on `aretro-capability-extension` to start with the current-boundary inventory.
- Keep the Aretro evidence-vs-judgment boundary intact; do not add semantic retrospective diagnosis to the CLI as part of capability migration.
- Continue parent Phase 2 after Aretro by completing Aretro, choosing/dispositioning Roaster, rebaselining `ccc` clean-consumer work, and eventually deleting `@sdl/domain-primitives-transitional`.
