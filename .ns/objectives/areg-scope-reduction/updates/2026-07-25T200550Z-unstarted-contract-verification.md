# Unstarted Contract Verification

## Summary

The reduced-tool contract remains accurate and implementation has not started. The package and binary are still `@nseng-ai/areg` / `areg`; `skill find`, broad `check`/`doctor` behavior, all five invocation kinds including `unlisted`, standalone `setup-dprint` leaves, whole-project discovery guidance, and the flat Objective skill-family source layout remain present. No supported upstream `skills` CLI pin was found.

This refresh also establishes the required `updates/` inventory that was absent from the newly created record.

Provenance: objective-refresh basis target=5d52b257cc380143528f8353e3712e3cf63152fe from=trunk-HEAD

## Objective Impact

No roadmap row advances. The first row remains the correct starting point: revalidate current harness evidence, inventory consumers, settle the retained kinds/interface, and choose the replacement name before implementation churn.

## Follow-Ups

- Begin with the consumer and harness evidence inventory.
- Do not infer progress from the existing separation between `npx skills`, `ns skills`, and areg; the standalone tool still owns the generic surfaces this Objective removes.
