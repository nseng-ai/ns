# Roadmap

## Work

- [ ] Settle retained product contract and replacement name from current consumer and harness evidence.
      Inventory every `areg` command and first-party caller. Classify each behavior as invocation policy, `npx skills` responsibility, `ns skills` responsibility, or deletion. Revalidate Claude Code, Codex, and Pi exposure mechanics. Decide which of `normal`, `ambient-only`, `invoke-only`, and `command-backed` remain necessary. Choose replacement name and explicit-path command interface before implementation.

- [ ] Pin and contract-test adopted Agent Skills toolchain behavior.
      Establish one supported `skills` CLI version or range across development guidance and CI. Prove nested catalog discovery, leaf-name selection, flat `.agents`/`.claude` installation, and lockfile/link behavior ns relies upon without duplicating upstream implementation.

- [ ] Reduce and rename standalone tool around skill-to-harness policy.
      Cut over package, binary, docs, configuration, and first-party callers without compatibility shims. Delete generic discovery and installation-health behavior, including `skill find` and generic `check`/`doctor` responsibilities. Retain only explicit-path application, inferred policy presentation, cross-harness overlay reconciliation, and ns/Pi replacement-command verification justified by first row.

- [ ] Retire `unlisted` by embedding router-only leaves in parent skills.
      Move `setup-dprint` and `setup-dprint-gh-ci` instructions into `project-setup` or its `references/`, remove standalone skill records and harness overlays, and delete `unlisted` invocation-kind implementation and policy documentation. Check for other router-only standalone leaves. Apply same parent-embedding rule when evidenced.

- [ ] Prove reduced boundary through nested Objective skill family.
      Organize Objective family under category folder while preserving leaf skill names. Verify selection and flat installation through pinned `skills` CLI. Update package publication and `ns skills` provisioning metadata where needed. Demonstrate renamed policy tool applies and audits intended Claude Code, Codex, and Pi exposure without owning discovery or installation.

- [ ] Reconcile first-party guidance and remove obsolete ownership claims.
      Update skill conventions, skill-management procedures, harness-invocation research, package/CLI documentation, and tests to describe three-channel boundary consistently. Remove instructions that rewrite upstream lockfile semantics, hand-maintain custom installation topology as universal law, or direct users to retired `areg` surfaces.

## Parked

- [ ] Group remaining first-party skills under stable category folders after Objective-family steelthread proves model.
- [ ] Extract generally reusable skill-policy core if second independent consumer demonstrates real seam beyond ns's Claude Code, Codex, and Pi policy bundle.
- [ ] Add support for another coding harness when real consumer requires exposure semantics not covered by retained policy vocabulary.
