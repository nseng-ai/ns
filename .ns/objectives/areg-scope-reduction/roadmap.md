# Roadmap

## Work

- [ ] Settle the retained product contract and replacement name from current consumer and harness evidence.
      Inventory every `areg` command and first-party caller; classify each behavior as invocation policy, `npx skills` responsibility, `ns skills` responsibility, or deletion. Revalidate Claude Code, Codex, and Pi exposure mechanics; decide which of `normal`, `ambient-only`, `invoke-only`, and `command-backed` remain necessary. Choose the replacement name and explicit-path command interface before implementation.

- [ ] Pin and contract-test the adopted Agent Skills toolchain behavior.
      Establish one supported `skills` CLI version or range across development guidance and CI. Prove nested catalog discovery, leaf-name selection, flat `.agents`/`.claude` installation, and the lockfile/link behavior ns actually relies upon without duplicating the upstream implementation.

- [ ] Reduce and rename the standalone tool around skill-to-harness policy.
      Cut over the package, binary, docs, configuration, and first-party callers without compatibility shims. Delete generic discovery and installation-health behavior, including `skill find` and generic `check`/`doctor` responsibilities. Retain only explicit-path application, inferred policy presentation, cross-harness overlay reconciliation, and ns/Pi replacement-command verification justified by the first row.

- [ ] Retire `unlisted` by embedding router-only leaves in their parent skills.
      Move `setup-dprint` and `setup-dprint-gh-ci` instructions into `project-setup` or its `references/`, remove their standalone skill records and harness overlays, and delete the `unlisted` invocation-kind implementation and policy documentation. Check for any other router-only standalone leaves and apply the same parent-embedding rule when evidenced.

- [ ] Prove the reduced boundary through the nested Objective skill family.
      Organize the Objective family under a category folder while preserving leaf skill names. Verify selection and flat installation through the pinned `skills` CLI, update package publication and `ns skills` provisioning metadata where needed, and demonstrate that the renamed policy tool applies and audits the intended Claude Code, Codex, and Pi exposure without owning discovery or installation.

- [ ] Reconcile first-party guidance and remove obsolete ownership claims.
      Update skill conventions, skill-management procedures, harness-invocation research, package/CLI documentation, and tests so they consistently describe the three-channel boundary. Remove instructions that rewrite upstream lockfile semantics, hand-maintain custom installation topology as universal law, or direct users to retired `areg` surfaces.

## Parked

- [ ] Group the remaining first-party skills under stable category folders after the Objective-family steelthread proves the model.
- [ ] Extract a generally reusable skill-policy core if a second independent consumer demonstrates a real seam beyond ns's Claude Code, Codex, and Pi policy bundle.
- [ ] Add support for another coding harness when a real consumer requires exposure semantics not covered by the retained policy vocabulary.
