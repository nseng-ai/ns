# ADR 0025 records the zero-kind mirrored edge design

## Summary

The decision-record roadmap row is complete: `docs/adr/0025-zero-kind-mirrored-objective-edges.md`
(local branch `adr-zero-kind-objective-edges`, verified runner-step commit) records the decided
design — Record Frontmatter carrying exactly `blocked` and `edges`, undirected kind-less mirrored
edges with required per-side annotations, presence-is-state Blocked Sentence as a sub-state of
open, structural-only linter in `sdl objective check`, and skill-owned mutation — plus the three
rejected alternatives (typed edge taxonomy, formal `to:`/`from:` directionality, single-sided
storage) and the linter-enforced-symmetry argument that makes mirrored storage safe.

## Objective Impact

First roadmap row done; the ADR transcribes decisions already made in this record, so no design
changed. The two open spellings (repo-wide sweep CLI form, blocked STATUS glyph) remain open in
both the ADR and this record, to be resolved with recorded defaults by the linter and
list-rendering slices. The Record Frontmatter parsing row is now the earliest open row and stays
the enabling slice for all implementation rows.

## Follow-Ups

- ADR number 0025 was verified free against `docs/adr/` (duplicates exist at 0012/0016/0022);
  keep the number if unrelated ADRs land before this branch does.
