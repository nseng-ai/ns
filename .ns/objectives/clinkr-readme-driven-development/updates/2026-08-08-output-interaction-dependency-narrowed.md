# Output and Interaction Dependency Narrowed

## Summary

The connected `clinkr-output-and-interaction-model` Objective has been rebaselined from a comprehensive channel ontology to the minimum invocation surface required by current hosts: finite JSON request input, invocation-scoped output, semantic confirmation/selection, and explicit Pi embedding behavior. Its former mandatory Response vocabulary, additional output-heavy workflow, Click survey, and broad documentation-promotion program are no longer completion gates.

## Objective Impact

This Objective still depends on `clinkr-output-and-interaction-model` closing before canonical README promotion. The implementation and acceptance contract is narrower: the modern Clinkr and Pi path must remove general stdin virtualization from shared structured command interfaces, use the finite JSON-specific input contract, keep output and rendering capabilities invocation-scoped, and adapt interaction through host semantics. Brmem and the substantial Objectives subtree remain the bounded production-vetting set; no additional output-heavy workflow is required solely to prove a speculative ontology.

## Follow-Ups

- Keep the README draft and fixtures synchronized with the finite JSON request-input contract as implementation lands.
- Ensure the modern SDK/Pi path conforms to the reduced host contract without legacy conversion inside the vetted slice.
- Wait for the connected Objective's documentation sync and closure before canonical README promotion.
