# Roadmap

## Work

- [x] Settle the cold-audience Clinkr product contract: filesystem-first command structure, immediate-child lazy discovery, schema-derived argv plus stdin-only `--input-json`, app-owned execution/completion, explicit outcomes and command renderers, invocation-owned context, narrow raw execution, and runtime discovery without manifests or production codegen.
- [x] Audit implementation, tests, exports, packaging, and representative callers; run the broad steelthread through Clinkr, Foundation, SDK composition, Flow, Objectives, and Brmem; preserve its public-contract changes and implementation lessons while rejecting its parallel transitional architecture as the production design.
- [x] Settle the remaining API and architecture decisions: `requiresContext: true` as the contextful discriminant; one typed `resultSchema` with fixed untyped diagnostic outcomes; exact `success | negative | failure | usage-error` status vocabulary; one narrow scoped composition builder; disjoint source ownership; `defineRawCommand(...)`; specialized subpath-only APIs; one private topology and traversal; and a dependency-ordered rebuild stack.
- [x] Approve `references/README-draft.md` as the cold-audience prose contract, remove provisional promises, fix the Node.js floor at `>=24.12.0`, make `md` the sole Markdown CLI token while retaining `renderMarkdown`, record completion support maturity, and specify structured stdin JSON behavior. The now-merged executable-fixture stack supplies 14 synchronized TypeScript fixtures, primary argv/JSON execution, and a cold-process smoke on trunk.
- [x] Land and review the initial Clinkr implementation stack. PRs #3948, #3951, #3952, #3953, #3949, and #3950 merged dependency-order onto `master`; their substantive CI lanes passed. The review accepted the command/outcome contract, bounded root-command filesystem runtime, honest partial README fixture evidence, structured-runtime hardening, and raw dispatch as foundations—not the final clean cut. Before broader acceptance, reconcile the duplicate builder narrative, remove `src/app/` dependencies on legacy exit/confirmation code, enforce semantic metadata validation in the topology owner, and delete the legacy raw model at the tail cutover.
- [x] Build the canonical private recursively lazy topology and source-composition owner. Landed on `master` as `26d2651c7` (private recursive `ClinkrTopology` + filesystem source, parent-gated lazy opening, shared transactional promise caching) and `34221cfde` (scoped-callback programmatic source with per-source and cross-source collision diagnostics). Filesystem, programmatic, and mixed sources open immediate-child scopes under one-owner subtree semantics; exact modules and definitions, semantic metadata, canonical names and aliases, absolute directories, conditional root reservations, transactional publication, source labels, and all sibling collision classes are qualified. The modern surface remains quarantined at `/app`, with topology, decoding, provenance, cache, publication, and invalidation ownership private. The runtime still dispatches only the root default command — nested dispatch, groups, aliases, and completion consumption belong to the next row.
- [x] Complete the single `ClinkrApp` runtime over that topology. One navigator owns recursive execution, help, schema, and app-owned completion; parsing, help, and completion share command-surface planning; selected loading is transactional; and raw-tail ownership remains explicit. Direct `/app` confirmation tests now qualify non-interactive, confirmed, declined, aborted, default-no, and strict-fake behavior. `ClinkrApp` deliberately exposes no speculative structured in-process execution seam; SDK/Objectives must prove the required host contract. Export-surface evidence plus LM code-review tripwires keep modern app ownership independent of the quarantined legacy closure; consumer migration, old group/raw/testing deletion, package-root cutover, package qualification, and README promotion remain later rows.
- [x] Reconcile Foundation and prove Brmem as the first production consumer. Foundation now exposes a separate durable `defineClinkrAppCli` lifecycle while legacy `defineCli` remains unchanged for later callers. Brmem uses the modern contextful filesystem app with route-local `src/cli/`, hidden `exec`, command-local contracts/presentation, schema-owned field annotations, narrowed context, fake-driven scenarios, structural inventory/eager-import guards, and packed inventory plus execution evidence. Its old `src/operations/`, legacy Clinkr imports, mutable group construction, and confirmation gate are deleted.
- [x] Rebuild SDK/host composition over the same private topology and traversal, then prove Objectives as the real-host consumer. The SDK now inventories separately labelled filesystem and programmatic sources without precedence, mounts disjoint source-owned subtrees into one contextful `ClinkrApp`, and calls only app-owned `run()`/completion. Modern descriptors carry filesystem roots or definitions; migrated extensions own noun subtrees; and each host extension subtree has one owner. Objectives supplies the recursive hidden `objective/exec` tree, with a 37-case real-host acceptance suite proving help/schema/outcomes, context adaptation, malformed-neighbor isolation, completion, and selected import laziness. No public selector or topology API was added.
- [ ] Complete the bounded production-vetting set rather than migrating every caller. Brmem already supplies the complete standalone filesystem CLI. Port a substantial coherent Objectives subtree so its production handlers return the final documented outcomes directly through the real `ns` host without legacy exits, per-exit rendering, old confirmation gates, or legacy-to-modern conversion inside the slice. Through `clinkr-output-and-interaction-model`, verify that the modern embedded path uses finite JSON request input rather than general stdin virtualization, invocation-scoped output, semantic interaction, and explicit Pi host adaptation. Preserve behavior tests and judge sufficiency by behavioral breadth and architectural depth, not package or file count.
- [~] Keep the quarantined legacy architecture outside the documented and vetted modern path. The qualified exit/envelope, failure, emit/format, and JSON Schema closure is isolated behind `/legacy`; the per-symbol ownership and eventual deletion prerequisites remain recorded in `references/legacy-api-deletion-inventory.md`. Repository-wide migration and deletion of mutable `ClinkrGroup`, old raw/completion/testing models, legacy Foundation construction, confirmation gates, obsolete exports, and `/legacy` are explicitly not closure gates for this Objective. Split or continue that cleanup separately; this row completes when the canonical README and every selected production-vetting slice avoid those owners.
- [ ] Qualify packed Clinkr and representative consumers, intact command directories, the truthful supported modern entrypoint and public subpath boundaries, and shell-completion instructions.
- [~] At the tip of the implementation stack, complete README testing to the strongest truthful level each example permits. The stack beginning with PR #3980 adds public-boundary evidence for recursive `contacts find`, group discovery, aliases, app-owned completion, and contextful `contacts list` fixtures without root-command substitutes. Preserve exact fence synchronization and compilation; add the remaining strongest truthful direct or public-boundary evidence from `updates/2026-07-28-readme-executable-fixture-gate-complete.md`, retaining explicit deferrals where an intentionally abbreviated fence cannot itself execute.
- [~] Consume and help implement the reduced `clinkr-output-and-interaction-model`, then wait for its documentation sync and closure. The modern Clinkr and Pi host path must conform to its finite-JSON input, higher-level invocation presentation, semantic-interaction, structured-progress, and Pi embedding contract end to end; closure of that Objective is a hard prerequisite for README promotion here. The connected Objective was reopened after review showed that invocation-local stdout/stderr capture was only a safety increment, not the final structured presentation and progress abstraction.
- [ ] Promote the approved draft to the canonical package README and replace the Objective draft with a provenance pointer. The README must accurately teach the supported modern entrypoint; package-root cutover and wholesale legacy deletion are not prerequisites if `/app` remains the truthful stable surface.
- [ ] Return the steelthread/rebuild gate lesson to `foundation-readme-driven-pass`, including the rule to rebaseline when a vertical slice validates the product interface but exposes central parallel architecture, then close this Subobjective.

## Parked

- Opaque Commander subtree mounting until a concrete caller requires a framework-specific adapter.
- Generated manifests, filesystem codegen, and single-file bundle support; use the justified programmatic seam or a later dedicated adapter instead of weakening the filesystem contract.
- Per-command context derivation beyond one homogeneous contextful tree until a concrete caller justifies another model.
- Unrelated Clinkr utilities or downstream CLI redesign discovered during the rebuild; split them after discussion.
