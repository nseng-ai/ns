# Roadmap

## Work

- [x] Settle the cold-audience Clinkr story through the README-driven-development grilling loop. The audience lens and structural progression are settled: document for a cold external TypeScript adopter to improve ns's design; lead with a web-framework-like, direct filesystem hierarchy whose groups expose one cheap complete definition and whose commands expose cheap typed metadata plus a selected-only definition; then explain outcomes, rendering, context, testing, and compact optional paths. Runtime filesystem discovery lowers into one immutable app/builder runtime without manifests or production codegen. Builders remain the approved lower seam and public advanced escape hatch, documented separately rather than taught through callbacks in the main README. The exact filesystem bootstrap helper remains provisional. Preserve app-owned execution/completion, context-free ergonomics, and the homogeneous tree-context model for now; per-command context is only a possible future enhancement.
- [x] Audit package exports, implementation, tests, and representative callers against the emerging README. `references/contract-audit.md` records the evidence bar, current package and caller evidence, migration impact, accidental complexity, and proposed disposition for all ten confirmed mismatches. It adds material findings for context-free signatures, bodyless success semantics, positional metadata spelling, the tested `md` format alias, Node engine metadata, and shell-completion installation verification. The audit found representative callers current against the implemented API but necessarily dependent on several surfaces superseded by the provisional contract. The naming questions are settled: retain positional metadata spelling `position`, and retain and document `md` as an alias for `markdown`; every contract-supporting refactor remains discussion-gated.
- [ ] Discuss every contract-supporting refactoring proposal and disputed mismatch disposition with the user. Filesystem-first authoring, runtime discovery without codegen, and the foundational app/builder/lazy-route lower seam are approved and bounded by `references/decision-record.md`; the outcome-schema, runtime-validation, command-level-rendering, and SDK-policy migration cluster is approved. The broad discussion remains open for raw mounting, completion-error policy, `ClinkrFailure` removal, and the exact filesystem bootstrap API. Positional metadata retains `position`, and `md` remains a documented alias for `markdown`. Update the README with settled public-interface and observable-behavior decisions; park unrelated redesign with rationale.
- [ ] Reconcile approved implementation and caller mismatches to the settled contract as a hard clean cut: establish the immutable app/builder runtime seam and unified four-status outcome model, implement the filesystem adapter over it, migrate Clinkr internals/tests and the public common path, then Foundation; redesign SDK/catalog routing and command adaptation so typed status data feeds Clinkr-owned validation and command-level renderers; migrate remaining CLIs/testing; and finally delete per-exit render overrides, SDK duplicate success validation/render synthesis, obsolete group execution, automatic aliases, and duplicated SDK pre-routing before README promotion. Preserve transactional selected-route loading, per-app success caching/retry, and fresh Foundation apps. Reviewable internal commits or a stack are acceptable, but do not ship a compatibility layer, generated manifest, production codegen, or two routing implementations.
- [ ] Verify the reconciled contract against targeted package tests, relevant caller evidence, package/type checks, and appropriate repository validation. Record evidence under the reconciliation or promotion outcome rather than as a standalone validation task.
- [ ] Promote the settled draft to `ts/packages/infra/clinkr/README.md`, replace `references/README-draft.md` with a provenance pointer, and confirm the durable package README remains the sole canonical user contract.
- [ ] Return Clinkr's gate-calibration lessons and concrete process amendments to `foundation-readme-driven-pass`, then close this Subobjective when its mismatches are resolved or explicitly parked.

## Parked

- Unrelated Clinkr redesign or cleanup discovered during the audit; split it into separate tracked work after user discussion.
