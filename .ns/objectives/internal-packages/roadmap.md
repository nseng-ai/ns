# Roadmap

## Work

- [x] Charter the internal space: rename `ts/packages/local/` → `ts/packages/internal/`, rewrite its section in `ts/packages/README.md` (tested repo-internal tooling; promotion ladder `.ns/*` → `internal/*` → platform; explicit `internal/` vs `tools/` distinction — enforced no-outside-runtime-dependents and never published vs. standalone and potentially shippable), and update all `packages/local` references.
      Evidence: the `local/`→`internal/` rename and reference sweep landed in the prior PR of this stack; the `ts/packages/README.md` charter (promotion ladder, `internal/` vs `tools/`, dependency semantics) was rewritten in this PR.
- [x] Add the internal rung to `docs/conventions/platform-and-consumer.md`: consumer-side tested tooling, with explicit promotion paths in (from `.ns/*`) and out (to platform). Report the taxonomy change to the repo-ontology objective per its drift rules.
      Evidence: third rung added to `docs/conventions/platform-and-consumer.md` (promotion paths in/out, `NS_TS_INTERNAL_SPACE_ADMISSION` boundary, cross-ref to the `ts/packages/README.md` charter); taxonomy drift reported to repo-ontology via `.ns/objectives/repo-ontology/updates/2026-07-04T183916Z-internal-packages-rung.md`.
- [x] Carry the boundary rule through the rename: rename `NS_TS_LOCAL_SPACE_ADMISSION` and the `LOCAL_SPACE_DIR`/scope constants in the style guard to the internal space, extend only if a gap appears, and document the dependency semantics (runtime dependencies on `internal/*` banned from outside; `devDependencies` and test consumption allowed).
      Evidence: the existing failing-case and real-repo conformance tests updated and passing under the new names. Dependency semantics documented in the `ts/packages/README.md` charter in this PR.
- [x] Audit the repo for internal-package candidates: `ts/packages/*` packages that are repo-internal (e.g. `@ns/vibechk` in `ts/packages/tools/`), machinery embedded in test-support trees, justfile-recipe scripts, `.ns/*` workspace tools (`../.ns/reviews/*/tools/*`), and Pi extensions. Deliverable: a per-candidate promote/keep/park report recorded under this objective.
      Evidence: per-candidate promote/keep/park report at `.ns/objectives/internal-packages/references/internal-package-audit.md` (5 candidate groups, dated 2026-07-04). Result: 1 promote (`typescript-style-guard`, in-flight next PR), 31 keep (3 `tools/*`, 10 `test/support` trees, 18 `.pi/extensions` shims), 6 park. All 3 `tools/*` packages confirmed correctly placed (generic/shippable); strongest new promotion candidates are the untested topology-report `.mjs` suite and embedded `.pi/extensions` logic (`objective-autorun.ts` self-declares its promotion path).
- [x] First resident: extract the subpackage conformance machinery out of `ts/packages/infra/core/test/support/typescript-style-guard/` into `ts/packages/internal/`, with the style guard consuming it from there as a dev/test dependency.
      Evidence: all 14 guard modules moved to `@internal/typescript-style-guard` (tier `internal-tool`, flat per-module `exports`, no subpackages); `@ns/core` consumes it as a `devDependency` and the suite imports the package specifiers. `just ts-test-typescript-style-guard` green (108 tests), including the real-repo admission/tier/subpackage/exports conformance checks that now also admit the new package; full `just ts-test` green (4094 tests).

## Parked

- [ ] Ghost-directory guard: ~26 untracked node_modules-only remnant directories sit under `ts/packages/` (e.g. top-level `sdl/`, `flow/`, `vibechk/`, `local-pi-tools/`, `packagechk/`). Disk cleanup is per-worktree and cannot land as repo work; if wanted, add a style-guard rule that flags package-less directories instead.
- [ ] Migrations of audit candidates beyond the first resident — sequence from the audit report once dispositions are accepted.
- [ ] Promote the architecture-topology-report script suite (`skills/architecture-topology-report/scripts/*.mjs`, ~100 KB untested `.mjs` behind `just topology`) into a tested `packages/internal/*` package. Strongest concrete promotion candidate from the audit after the first resident; sequence behind `typescript-style-guard`. Evidence basis: `.ns/objectives/internal-packages/references/internal-package-audit.md` (Group 3).
- [ ] De-embed package-grade `.pi/extensions/*` logic into `@internal/pi-tools` subpackages, leaving thin loader shims like the existing 18: `objective-autorun.ts` (447 lines; already self-declares this path in its header), `just-fix.ts` (128 lines), and `home-directory-guard.ts` (199 lines). Extraction earns the logic Vitest coverage. Evidence basis: `.ns/objectives/internal-packages/references/internal-package-audit.md` (Group 5).
