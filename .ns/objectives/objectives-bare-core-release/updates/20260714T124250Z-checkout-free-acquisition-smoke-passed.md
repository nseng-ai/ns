# Checkout-Free Acquisition Smoke Passed

## Summary

A vanilla foreign git repository installed registry-served `@nseng-ai/ns@0.1.3` and confirmed that the bare core did not initially expose `ns objective`. After `ns init --harness claude-code` and `ns extension install npm:@nseng-ai/objectives@0.1.3`, activation provisioned all ten declared Objective skills under `.claude/skills/` and `ns objective list` succeeded.

The smoke used the foreign repository's installed registry artifacts without an ns checkout or `ts/node_modules` dependency.

## Objective Impact

The decisive checkout-free release gate passes. Together with the earlier coordinated publication and strict tarball verification, this satisfies the Objective's completion criteria and clears its registry-artifact dependency for the customer-shipment umbrella and Claude Code onboarding steelthread.

The onboarding steelthread's bare-core release blocker is resolved; its separate publishable documentation launch gate remains.

## Follow-Ups

- Continue `objectives-claude-onboarding-steelthread` with docs-verbatim, fresh-session Claude Code create → next → update → close verification once its documentation gate clears.
- Synthesize the released `0.1.3` and acquisition evidence into `ship-objectives-to-customers` tracking.
- Keep general release automation, self-update, fleet updates, and non-npm channels parked outside this completed Objective.
