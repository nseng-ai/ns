# Orientation — Flow Deepening Round 2 / Land Domain Extraction

Direction: Flow land execution runs on the Land Domain Core
(`flow/src/land/`, four-gateway `LandContext`); the Flow Land Compatibility
Boundary stays a single preflight crossing (Flow Stack Preflight Adapter).
Getting to: one operation-shaped Graphite command channel, `SubmitGateway`
returning domain results, per-failure catalogs where one failure is one edit
site — all landed and merged to `master`.

What you see now: everything above is trunk reality (migration slices,
round-trip retirement, channel, `--force`, catalogs all merged 2026-07-02;
`plan-mapping.ts` gone, no `runRaw`/`LandPlanForFlow`/`preloadedShape`).
Post-landing renames apply: package `@nseng-ai/flow`, CLI `ns flow …`,
`land-stack/` is now `land/stack/`. Only the Parked presentation row
(review #5) remains — an owner promote/re-scope/drop decision gates closure.
Avoid: adding wrappers, mirror types, or mappers at the compatibility
boundary the retirement deleted; designing tests against a scripted channel
adapter — scripted `pi.exec` is the canonical land test seam.
