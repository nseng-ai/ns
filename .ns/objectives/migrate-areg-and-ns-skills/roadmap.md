# Roadmap

## Work

- [x] Whole-repo capability inventory and disposition audit — catalog every capability in `/Users/schrockn/code/nonslop`, not just `areg`: `areg` modules/templates/tests; all 21 nonslop `ns-*` skills; existing asdl-tools vendored `ns-*` copies; missing skills; duplicate/renamed skill names; lockfile entries; README/AGENTS/CLAUDE instructions; `docs/skill-standards.md`; sync/cleanup scripts; GitHub workflows/actions; just/dprint/pyproject/release recipes; agent configuration; local development integrations such as `local.just`; checkout-local `.agents`/`.claude` symlinked capabilities such as twerk links; empty/skeletal package directories such as `packages/nonslop-dev`; and every live `nonslop`/`nseng-ai/nonslop` reference. Evidence: `nonslop-capability-inventory.md` now records final dispositions for every discovered item and names the migration/rewrite/fold/retire/ignore path before files move.
- [x] Port `areg` as a standalone workspace package — create `packages/areg`, move/adapt source and tests, wire root workspace metadata, keep the `areg` script, and remove the root dev dependency on `nonslop`. Evidence: branch `port-areg-standalone-workspace-package` adds `packages/areg`, root workspace/source/dev/lint/test wiring, and `uv.lock` replacement of the registry `nonslop` package with workspace `areg`; `areg` unit/scenario/gateway tests exercise `create-project`, `check`, `update-skills`, and generic `exec skillx` behavior with fake gateways; targeted checks, `just areg-check`, and full `just` passed.
- [x] Promote and unprefix the catalog to first-party local skills — create canonical `skills/<name>/` directories for the retained promoted skills, reconcile differing existing copies, create `.agents`/`.claude` symlinks, update `skills-lock.json` to local sources, and delete the distribution-boundary shortcuts `nsx` and `ns-install`. Evidence: the retained 19 promoted skills now live under flat semantic names in `skills/<name>/`; `.agents/skills/<name>` symlinks to `../../skills/<name>`; `.claude/skills/<name>` symlinks to `../../.agents/skills/<name>`; `skills-lock.json` records local `skills/<name>` sources; deleted shortcuts have no skill, symlink, lockfile, or `areg exec nsx` surface; `dignified-python` and `python-fake-driven-testing` reconciliation choices are reflected in the migrated files.
- [x] Repoint distribution and command references — rewrite `areg` defaults, generated templates, skill tooling, just recipes, docs, and tests from `nonslop`/`nseng-ai/nonslop`/`uvx nonslop` to `areg` and `dagster-io/asdl-tools`. Evidence: generated projects install only `skill-management` and `skillx` from `dagster-io/asdl-tools`; root workspace metadata no longer depends on `nonslop`; `nonslop-check` became `areg-check`; promoted skill prose and allowed-tools use `areg`; `skills-lock.json` uses local sources; repo docs/tests were rewritten; the stale root `refresh-nonslop` recipe was replaced with `refresh-skills` calling `areg update-skills`.
- [x] Prove nonslop deletion readiness — demonstrate that normal repo development and skill invocation no longer require `/Users/schrockn/code/nonslop`, the old package, or the old GitHub source. Evidence: `uv run areg check`, targeted `areg exec skillx` parse, `uv run pytest packages/areg/tests -q`, `just dprint-check`, full `just`, local-skill structure checks, and a live-reference search excluding Objective history all passed/found no live dependency path to the old nonslop checkout/repo.

## Parked

- Actual deletion of `/Users/schrockn/code/nonslop`, deletion/archive of `nseng-ai/nonslop`, or removal of external project artifacts — perform only after an explicit future request.
- Mounting `areg` under the top-level `asdl` CLI — revisit only if the standalone package boundary becomes a burden.
- Backward-compatible `nonslop` package aliases or `uvx nonslop` command support — intentionally out of scope for these private in-development projects.
