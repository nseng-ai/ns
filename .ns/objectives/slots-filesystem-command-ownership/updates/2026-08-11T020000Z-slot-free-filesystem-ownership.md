# Slot Free Filesystem Ownership

## Summary

Runner checkpoint `939f7322ff1019e287d08d5435d5e62ccc5ffa92` moved the typed `slot free` command assembly into its filesystem route and removed free from the central registry. Free scenarios now use the production filesystem harness.

## Objective Impact

The dedicated Phase 1 `slot free` ownership row is complete. Narrow route-neutral harness and temporary outcome-adapter changes preserve pull-request fakes, interactive input, and legacy negative human rendering. Focused Slot scenarios and typecheck, all 354 Slot tests, style-guard checks, full `just` validation, and `git diff --check` passed; no other command or Phase 2 outcome was migrated.

## Follow-Ups

- Migrate `slot foreach` command ownership as the next focused Runner step.
- Keep the central registry and legacy command face for commands not yet migrated.
- Preserve the temporary legacy-outcome adapter until Phase 2.
