# Roadmap

## Work

- [x] **Slice 1 — Unify failure and concept vocabulary; sever the submit-prose edge.** Move `LandingExecutionFailure` and `NotifyLevel` into core `types.ts`, move constructors/facts into `results.ts`, and initially retain `stack/errors.ts` as a re-export shim. Remove duplicate worktree, landed-PR, and retained-cleanup types from `stack/types.ts` while retaining only the transitional `LandedPr` alias. Add `isLikelyInProgressGitOperation` to failed local-branch deletion results; the real adapter computes it with the existing heuristic and maintenance branches on the typed flag. Extend fake-contract coverage and lock failure facts for every variant.

- [x] **Slice 2 — Invert the matrix-rendering leak in the merge loop.** Add `execution/host-seams.ts` with `LandExecutionProgress` and a null implementation. Replace matrix/command-stream dependencies in landing operations with that seam, preserving the exact active → operation → failed/done transitions and skipped-restack behavior. Build the Flow implementation from the existing command stream, context, and matrix; plan recalculation resets rows from the recalculated plan.

- [x] **Slice 3 — Extract pure core vocabulary modules.** Create `graphite-operations.ts` for Graphite operation formatting/parsing and the backup recovery hint, and `worktree-paths.ts` for managed-slot/path/conflict helpers. Repoint stack imports without changing call sites, export gateway-compatible worktree conflict detection from preflight, and remove the duplicate managed-slot helper. This is a pure move with behavior and text unchanged.

- [x] **Slice 4 — Migrate post-landing cleanup.** Move cleanup decisions into `execution/post-landing-cleanup.ts` over `LandContext`, progress, plain option data, and the confirmation gateway. Keep prompt text in `land-presentation.ts`, adapt through `confirmLandStackAction`, and guarantee status clearing with the same current `try/finally` behavior. Add fake deletion-result and slot-free/worktree-mutation knobs with paired adapter protocol tests; cover free-slot failure, retained/failed deletion text, residual checkout, and confirmation payloads.

- [x] **Slice 5 — Migrate the isolated fast path.** Move isolated execution and `isIsolatedFastPath(StackSnapshot)` into `execution/isolated-landing.ts`, returning typed outcomes. Keep success/dry-run notification rendering Flow-side and byte-identical; thread cleanup confirmation through the host bag at the same evaluation point. Make the GitHub fake auto-transition merged PRs with an override for post-merge facts, pair it with adapter protocol coverage, replace the bespoke recording gateway in tests, and pin exit-code / failure-level mapping.

- [x] **Slice 6 — Move maintenance out of `stack/`.** Move Graphite maintenance and its plan to `execution/maintenance.ts` and `execution/maintenance-plan.ts`; move merge-loop state vocabulary to `execution/merge-loop.ts` for transitional stack consumers. Add per-branch refresh failure/checkout-conflict injection and typed operation failure display/result injection, each with paired adapter protocol tests. Cover SHA-moved halt/warn behavior and recovery hints, refresh deferral with no subsequent mutation, unexpected children, retained/failed deletion including mid-rebase wording, and warning aggregation. Do not convert `graphiteRefreshFailure` to typed branching in this slice.

- [x] **Slice 7a — Migrate pre-merge preparation.** Move slot freeing, required-update submission/recheck, and residual failure construction into `execution/pre-merge.ts`; relocate prompt-detail builders to `land-presentation.ts` while failure-message builders remain core. Implement the execution-specific clean-repo recheck over `git.workingTreeStatus` with the existing stack wording byte-for-byte, reconcile operation-label tables explicitly, and verify worktree-conflict classification including realpath normalization. Add fake-driven tests for slot-free success/residual/failure, restack-submit-recheck, and non-interactive refusal before any mutation.

- [x] **Slice 7b — Migrate the merge loop.** Move `prepareMergeLoopState` and `runMergeLoop` into `execution/merge-loop.ts`, then thin Flow composition and delete migrated landing/pre-merge stack modules. Add direct tests for multi-branch gateway call order and accumulation, snapshot-once behavior, snapshot failure before mutation, strict-gate failure after partial landing, mid-loop squash rejection, verification failure/load failure, and maintenance skip-versus-halt. Treat 7a and 7b as separate sub-PRs; after both, run `just ts-test-integration` in addition to the per-slice gates.

- [x] **Slice 8 — Delete shims and finish the vocabulary sweep.** Delete `stack/errors.ts`; rename stack failure/result aliases and constructors to the canonical core vocabulary; rename adapter `toLandingFailure` to `normalizeAdapterFailure` with a comment documenting legitimate inbound contract normalization; remove result adapters case-by-case only when emitted shapes remain identical. Delete dead pi-based conflict detection and orphaned code, then run stale-symbol and stale-import sweeps.

- [x] **Slice 9 — Rewire `executeLanding` to own execution end-to-end.** Add `execution/execute.ts` to compose main-confirmation policy, pre-merge, and merge-loop execution. Make execute mode in `calculateLandingOutcome` populate real phases, landed chunks, and cleanup; remove the `merge execution remains in Flow` skip; initially add an optional host with safe refusing defaults, superseded by Slice 10's required-host contract. Preserve dispatch's upfront confirmation, routing, and cleanup-decision ordering exactly; replace mutation out-parameters with returned values while adapting Flow. Add fake end-to-end scenarios for linear landing, submit-required updates, slot conflicts, maintenance warnings, and cleanup, plus one gateway-sequence cross-check against a permanent transcript scenario. Run `just ts-test-integration` and an entrypoint-level sandbox/dry-run behavioral check.

- [x] **Slice 10 — Replace approval booleans with confirmation-gateway provenance.** Add explicit prompted/upfront provenance to approved decisions, a Flow adapter decorator over an immutable approved-kind set, and a pure flags/upfront-result/cleanup-preview mapper. Make canonical confirmation calls unconditional wherever policy requires confirmation; keep dry-run, preserve, force-cleanup, and no-target as policy branches. Collapse Flow pass-through options, require an explicit execution host and discover/prepared source, validate prepared-source compatibility and scope representability, and remove the temporary approval vocabulary.

- [x] **Slice 11 — Make canonical execution the sole trunk/no-PR routing authority.** Add a typed completion disposition for ordinary stack execution, cleanup-only completion, and informational nothing-to-land completion. Move the trunk/no-landing-branches decision entirely into canonical execution, delete Flow's cleanup-only/no-op routing wrapper, retain cleanup preview only for upfront presentation and isolated glue, and preserve exact no-op text, notification level, cleanup-only presentation, merge-call absence, and completed exit semantics.

- [x] **Slice 12 — Make phases observational through typed execution facts.** Add `failedPhase` to failed execution results, derive failure phase records from that source, replace partial-success presentation scans with typed failure/landing/cleanup facts, and record submit preparation and prepared-versus-discovered shape work from observed execution. Inline the identity-only pre-merge Graphite failure helper while preserving failure phase audit entries and post-cleanup partial-success output ordering.

- [ ] **Slice 13 — Unify merge-loop observations and initialization.** Give success and failure one `MergeLoopObservations` value, remove optional prepared merge-state injection, run one production state-preparation path, name and test the current last-defined descendant-maintenance aggregation policy, iterate directly over landing branches, and centralize snapshots so initial and partial failures preserve the same observed facts as success.

- [ ] **Slice 14 — Centralize operation-in-progress vocabulary.** Move the shared operation label table into a small core vocabulary module consumed by preflight and the execution recheck. Keep each caller's intentionally distinct sentence framing, retain direct and end-to-end wording assertions, extend import-direction coverage if needed, and finish the duplicate-label/stale-symbol sweep before closure evidence.

Each slice also extends `test/unit/land-import-direction.test.ts` as modules migrate: migrated modules cannot reference `LandStackExtensionAPI`, command execution, or pi loaders; `execution/` cannot import `stack/`; only adapter-family code may execute subprocesses. Every row is complete only with its recorded validation gates, paired adapter protocol coverage for every new fake knob, and no behavioral or fixture drift in the Objective's permanent transcript invariants. Remediation slices may update test harness wiring only when an intentionally removed API shape makes that unavoidable; such an exception must be called out explicitly in evidence. Record that evidence in Objective tracking before advancing.

## Parked

- [ ] Typed-variant branching for `graphiteRefreshFailure`.
- [ ] Rename `stack/` to `adapters/` after migration boundaries settle.
- [ ] Lift isolated-target support into core `LandingRequest`; `isolated-pull-request` remains not implemented here.
- [ ] Prune transcript scenarios only after later performance measurement work confirms the permanent measurement set; no pruning belongs to this Objective.
- [ ] Generalize telemetry or extract phase streaming under their separate initiatives.
