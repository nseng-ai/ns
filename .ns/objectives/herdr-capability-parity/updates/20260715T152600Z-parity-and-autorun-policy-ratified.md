# Parity and Autorun Policy Ratified

## Summary

The cmux feature walkthrough settled the complete Herdr parity scope and the rejected cmux surfaces. The Objective is now shaped as an autoobjective with three ordered, independently reviewable local Graphite branches: parity contract and cmux retirements; Herdr package foundation and objective metadata; then the related dispatch and branch-opening workflows.

Autonomous execution may implement and commit each fixed slice locally. It must use `@nseng-ai/herdr`, the `herdr` command noun, mirrored selected command suffixes, explicit Herdr IDs, ns-owned slot orchestration, and CLI-first integration. Full `just` is required before every runner commit. Pushes, PR submission or mutation, deployment, publishing, and all other external writes remain prohibited.

## Objective Impact

The initial discovery and applicability decisions are resolved in durable Objective scope and roadmap prose. Claude plan tab, session summary, and branch-state summary have no Herdr parity and are selected for removal from cmux in the first PR. Objective summary remains in cmux and gains Herdr-native parity; prompt, trunk, plan, and branch-opening workflows gain adapted Herdr parity.

The prior ambiguity around implementation breadth, package naming, stack shape, and runner permissions is retired. Contradictions in the installed Herdr contract, fixed public-surface changes, architecture-boundary changes, stack-boundary movement, or inability to make full validation green are explicit escalation triggers.

## Follow-Ups

- Execute the three roadmap rows in order through Objective Runner steps.
- Stop after preparing the validated local Graphite stack for human review and separately authorized submission.
