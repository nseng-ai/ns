Model note: Generated by OpenAI ChatGPT coding assistant in Pi on 2026-06-15.

Review result:

Review complete. No edits made.

Validation run:

- `uv run ruff check packages/areg` ✅
- `uv run ty check packages/areg` ✅
- `uv run pytest packages/areg` ✅ — 287 passed

Findings:

1. **High — `areg init` can apply stale file plans after `npx skills add` mutates the project**
   - `packages/areg/src/areg/init_project.py:447-466`
   - The init plan is computed before `npx skills add`, then applied after the external command mutates the same repo. If `npx skills add` creates/updates `AGENTS.md`, `CLAUDE.md`, `asdl.toml`, or `.claude/settings.local.json`, the stale plan can overwrite those post-install changes.
   - Fix: either apply local files before `npx`, or re-read/recompute plans after `npx` and before writing.

2. **Medium — standalone CLI misses repo convention for `--version` and `-h`**
   - `packages/areg/src/areg/cli.py:18`
   - Current behavior:
     - `uv run areg --version` → exits 2
     - `uv run areg -h` → exits 2
   - Repo CLI convention says standalone CLI scenario tests should cover `--version` and `-h`; `areg` only tests `--help`.
   - Fix: add Click version option and `context_settings={"help_option_names": ["-h", "--help"]}` plus scenario tests.

3. **Medium — `areg check` local-exclude logic breaks in linked worktrees**
   - `packages/areg/src/areg/check/context.py:11`
   - It reads `project_dir/.git/info/exclude`, but in linked worktrees `.git` is a file pointing elsewhere. This repo itself has that shape. Result: local-only excluded skills can be falsely reported as orphans.
   - Fix: resolve the actual git dir via `git rev-parse --git-dir` / git gateway, or use `git check-ignore`.

4. **Low — exclude parsing only supports one exact pattern shape**
   - `packages/areg/src/areg/check/context.py:14-22`
   - Patterns like `.agents/skills/foo/`, `.agents/skills/*`, or negated gitignore patterns won’t map to skill names correctly.
   - Fix: either document the exact supported pattern or delegate matching to git.

5. **Low — mutable dataclasses where immutable would fit repo style**
   - `packages/areg/src/areg/context.py:13`
   - `packages/areg/src/areg/check/models.py:50`
   - Repo guidance prefers frozen dataclasses. `AregContext` looks like immutable wiring and could be `frozen=True`; `CheckResult` could store a tuple unless mutation is intentional.

6. **Low — dead/default skill repo constant**
   - `packages/areg/src/areg/skillx.py:20`
   - `_DEFAULT_SKILL_REPO` is unused. Either wire it into parsing/default behavior or remove it.
